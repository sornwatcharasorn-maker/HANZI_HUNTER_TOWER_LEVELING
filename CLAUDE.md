# HANZI HUNTER — TOWER LEVELING

เกมฝึกคำศัพท์จีนระดับ A1 (อ้างอิง 《汉语教程》) ธีมหอคอย/ฮันเตอร์แนว Solo Leveling
ผู้ใช้จริงคือนักเรียนบนมือถือ ครูเป็น GM คุมผ่านแผงในเกม

## กฎเหล็กข้อเดียว: ไฟล์เดียวจบ

ทั้งเกมอยู่ใน **`hanzi_hunter_tower_v3_1_intro.html`** ไฟล์เดียว (ชื่อไฟล์ยังเป็น `v3_1`
แต่เนื้อในคือ **v5.2 · CLOUD SYNC** — อย่าเปลี่ยนชื่อไฟล์ ลิงก์ที่แจกนักเรียนไปแล้วจะพัง
ส่วนป้ายรุ่นบนหน้าจอเกม `#gVersion` ยังเขียน `v4.1 · SYSTEM` ค้างไว้ตั้งแต่ v4.1 ไม่มีใครขยับ)

ห้ามแตกไฟล์เป็น `.css` / `.js` / โฟลเดอร์ `assets/` เด็ดขาด เพราะไฟล์นี้ถูกส่งต่อทาง
LINE / ดาวน์โหลดไปเปิดออฟไลน์ ถ้าอ้างไฟล์ภายนอกจะพังทันที

**ผลที่ตามมา — รูปทุกรูปต้องเป็น data URI**
ห้ามเขียน `<img src="วาป.png">` แม้ไฟล์นั้นจะอยู่ใน repo ก็ตาม
ขั้นตอนที่ใช้: ย่อด้วย Pillow → WebP → base64 → ฝังเป็นค่าคงที่ใน JS

| ค่าคงที่ | ใช้กับ | ขนาดที่ใช้จริง |
|---|---|---|
| `CHAR_IMG_SRC` | มาสคอตหน้า intro/login | 640×961 WebP (~110KB) |
| `SKILLS[].img` | ไอคอนสกิล 4 ตัว | 96px WebP q80 (~2–4KB/รูป) |
| `SN_WARP_ART` | ภาพประตูวาป | 350×190 WebP q82 (~18KB) |
| `TR_ART` | ภาพหอคอยในเรดาร์ (v4.5) | 800×993 WebP q82 (~105KB) |
| `CD_CARDS[].img` | ภาพประกอบการ์ด 17 ใบ (v4.7) | 300px WebP q80 (~21–29KB/รูป · รวม ~557KB) |
| `ITEMS[].img` | ไอคอนไอเทมใช้งาน 7 ตัว (Item Asset Patch) | 160×160 WebP q82 (~3–10KB/รูป · รวม ~39KB) |

**ต้นฉบับที่ยังอยู่ใน repo ตอนนี้** — รากrepo มี .jpg 22 ไฟล์ (สกิล 4 + ประตูวาป + การ์ด 17 ใบ)
และ `assets/items/` มีต้นฉบับไอคอนไอเทมอีก 7 ไฟล์ · `TOWER.png` ยังอยู่ (9.8MB)
ชุด .jpg มาแทนชุด `.png` เดิม (2048–2816px, ~8–10MB/ไฟล์) ที่ถูกลบทิ้งไปตั้งแต่
Asset Replacement Patch (ส.ค. 2026) แล้วเจ้าของ repo ทยอยอัปโหลดต้นฉบับกลับเข้ามาทีหลัง

ตัวเกมไม่ได้อ่านไฟล์พวกนี้ตอนรัน ถ้าจะเปลี่ยนรูปต้องย่อแล้วฝังใหม่เสมอ
**ภาพที่ฝังไว้แล้วไม่ได้ผูกกับต้นฉบับ** — ต่อให้ลบต้นฉบับทิ้งอีกรอบ การ์ด · หอคอย · มาสคอต ·
ไอเทม ก็ยังขึ้นจอครบเหมือนเดิม แต่ถ้าจะ *ฝังใหม่* รูปไหนที่ต้นฉบับหายไป ต้องอัปโหลดกลับเข้ามาก่อน
(ของเก่ายังอยู่ในประวัติ git ดึงกลับได้ด้วย `git show <commit>:<ไฟล์>`)

ไฟล์รวมตอนนี้ **~2.02MB — ทะลุเพดาน ~2MB ที่ตั้งไว้ไปราว 17KB แล้ว** เพราะชั้น v5.2 · CLOUD SYNC
เป็นตรรกะล้วน 48KB (ไม่มีรูป ไม่มีเสียง) **ห้ามเพิ่มรูปอีกจนกว่าจะหาที่คืนได้**
ทางคืนที่ว่างที่คุ้มที่สุดคือ **ฝังภาพการ์ด 17 ใบใหม่ที่คุณภาพต่ำลง** (`python3 embed_card_art.py`
ชุดปัจจุบันกิน ~557KB ที่ q80 — ลงมา q70 ประหยัดได้ราว 60-80KB โดยตาแทบไม่เห็นความต่างบนกรอบ 300px)
(เสียงเอฟเฟกต์ทั้ง 20 รายการของ v5.1 สังเคราะห์ด้วย Web Audio ไม่ได้กินพื้นที่ตรงนี้เลย
ส่วนไฟล์เพลง BGM ของ v5.1 ยิ่งเสียบไม่ได้แล้วตอนนี้ ต้องคืนที่ก่อนอย่างเดียว)

**สคริปต์ฝังภาพมี 4 ตัว แยกตามชุดของมัน** — `embed_icon_art.py` (สกิล 4 + ประตูวาป) ·
`embed_tower_art.py` (หอคอยของ v4.5) · `embed_card_art.py` (การ์ด 17 ใบของ v4.7) ·
`embed_item_art.py` (ไอเทมใช้งาน 7 ตัวของ v4.0)
ทุกตัวย่อ → WebP → base64 → เขียนกลับลงค่าคงที่ให้เอง และหยุดให้ถ้าไฟล์รวมจะทะลุเพดาน 2MB

`embed_icon_art.py` ใช้กติกาชื่อไฟล์เดียวกับการ์ดคือ **ชื่อไฟล์ = ชื่อภาษาไทยของสกิลเป๊ะ ๆ**
แต่ไม่ต้องเพิ่มฟิลด์ `image` ให้ `SKILLS` เพราะชื่อสกิลมีชื่อไฟล์อยู่ในตัวแล้ว —
สคริปต์อ่านฟิลด์ `name` แล้วตัดคำนำหน้าข้างหน้า `:` ทิ้ง (`'ทลายอักษร: ดาบโลหิต'` → `ดาบโลหิต.jpg`)
สารบัญจึงอยู่ที่เดียวคือตัวเกม มีแค่ภาพประตูวาปที่ไม่มีชื่อในเกมให้อ่าน จึงประกาศไว้ที่ `WARP_STEM`

```
python3 embed_icon_art.py            # ฝังทุกรูปที่หาไฟล์เจอ
python3 embed_icon_art.py eye warp   # ฝังเฉพาะบางรูป
```

ไอคอนสกิลฝังที่ 96px เพราะแสดงจริงแค่ 26×26 CSS px (`.g-skill-icon img`) — คมพอถึง DPR 3.7
**ต้นฉบับไม่ใช่สี่เหลี่ยมจัตุรัสเป๊ะ** (350×346 ถึง 350×362) และ CSS บังคับ `width/height` เท่ากัน
ภาพจึงถูกยืดราว 1–3% ซึ่งมองไม่ออก **อย่าไป crop ให้เป็นจัตุรัส** จะกินกรอบลายรูนของภาพหายไป
ส่วนประตูวาปกว้าง 350px เท่าต้นฉบับ (ของเดิม 720px ละเอียดกว่านี้ แต่ต้นฉบับ .png ถูกลบไปแล้ว)
แสดงจริงราว 308px บนจอ 390 จึงยังคมพอดูบนจอปกติ แต่จะนุ่มขึ้นเล็กน้อยบนจอ retina

## โครงสร้างโค้ด — 15 ชั้น

ไฟล์เดียวแต่แบ่งเป็นสิบห้าชั้นชัดเจน **อย่าเอาโค้ดสิบห้าชั้นนี้มาปนกัน**
แต่ละชั้นบนลบทิ้งทั้งก้อนได้โดยชั้นล่างยังทำงานปกติ

### ชั้นล่าง — v4.0 · TOWER (แกนเกม)

ระบบหลักทั้งหมด: หอคอย 20 ชั้น × 4 มอนสเตอร์, โซน/เทียร์คำศัพท์, แรงค์, ฉายา,
ค่าสถานะ, กระเป๋า/ไอเทม, สกิล/MP, หลอดเวลา, วิเคราะห์, กระดานผู้นำ, แผง GM

ค่าคงที่สำคัญ (แก้บาลานซ์ที่นี่):

```
FLOOR_MAX 20 · MONSTERS_PER_FLOOR 4 · QUESTION_MS 15000 · BOSS_QUESTION_MS 12000
BAG_SLOTS 10 · SELL_RATE 0.75 · SCAN_COST 80 · FREEZE_MS 5000 (⏳ ต่อเวลา · v4.7.9.1)
MP_BASE 100 · MP_PER_CORRECT 5 · MP_STREAK_BONUS 15
ZONES  1: ชั้น 1-6 เกทอักขระเดี่ยว · 2: ชั้น 7-13 เกทอักขระคู่ · 3: ชั้น 14-20 เกทอักขระซ้อน
TIERS  A อักขระเดี่ยว · B อักขระคู่ · C อักขระซ้อน  (แบ่งด้วย cjkLen ไม่ใช่ลำดับใน VOCAB)
SKILLS eye 35MP/cd1 · blade 55MP/cd2 · frost 65MP/cd3 · ward 85MP/cd5
```

จุดยึด DOM ที่ใช้บ่อย: แถบสกิล `#gSkills` (ปุ่มคือ `.g-skill` + `.g-skill-icon`
`.g-skill-name` `.g-skill-mp`), แผงวาป `#gWarp`, ชั้นแจ้งเตือน `#snLayer`,
หน้าต่างบังคับกดยืนยัน `#snGate`, แผงระบบ `#snPanel`

`.g-skill` เป็น `flex-direction:column` อยู่แล้ว ปุ่มจึงเรียงไอคอน/ชื่อ/มานาลงล่างเอง
ถ้าสร้างการ์ดใหม่เองอย่าลืมว่า `<span>` เปล่า ๆ เป็น inline ต้องจัด layout ให้ด้วย

**ไอคอนไอเทมใช้งาน — ภาพจริง 7 ตัว (Item Asset Patch)**

`ITEMS` มีสองฟิลด์ที่คู่กันและห้ามสลับหน้าที่กัน **กติกาเดียวกับ `CD_CARDS` ของ v4.7 เป๊ะ**

| ฟิลด์ | คืออะไร | ใครอ่าน |
|---|---|---|
| `image` | ที่อยู่ไฟล์ต้นฉบับ `assets/items/<ชื่อไอเทมภาษาไทย>.jpg` | `embed_item_art.py` เท่านั้น |
| `img` | data URI (WebP base64) ที่ฝังจริง | `itemArt()` → `itemIconHtml()` |

**ชื่อไฟล์คือชื่อไอเทมภาษาไทยเป๊ะ ๆ** (ตรงกับฟิลด์ `name`) สารบัญจึงอยู่ที่เดียวคือตัวเกม
นามสกุลไม่ใช่ตัวชี้ขาด — `resolve()` ลองทั้ง `.jpg/.jpeg/.png/.webp` และลองทั้ง
`assets/items/` `assets/cards/` กับรากrepo (เจ้าของ repo อัปโหลดผ่านเว็บ GitHub ไฟล์จึงมาโผล่ที่รากได้)

**ไอคอนโผล่บนจอ 3 จุด และทั้งสามจุดเรียกตัวประกอบเดียวกันคือ `itemIconHtml(it, cls)`**

| จุด | DOM | คลาส | ขนาดกรอบ |
|---|---|---|---|
| ช่องกระเป๋า = ปุ่มกดใช้ด่วนระหว่างต่อสู้ | `#gBagGrid` | `.g-slot-ico` | 28×28 (ในช่องสูง 36) |
| แถบรายละเอียดใต้กระเป๋า | `#gTipName` | `.g-tip-ico` | 20×20 |
| แถวสินค้าในร้านค้าประจำชั้น | `#gShopBody` | `.g-shop-icon` | 44×44 (จอ ≤380px เหลือ 38) |

**ถ้าจะเพิ่มจุดที่ 4 ให้เรียก `itemIconHtml()` อย่าเขียน `<img>` เองซ้ำ** —
`img` ว่าง = ตกกลับไปใช้อีโมจิ `icon` ทั้งตัว ไม่มีคลาส `has-art` ไม่มี `<img>`
ไม่มีรูปแตกและไม่มีคำขอเน็ตออกไปข้างนอก ฝังทีละตัวก็ได้ ไม่ต้องครบ 7 พร้อมกัน

`renderTip()` เปลี่ยนจาก `textContent` มาเป็น `innerHTML` แล้ว **ชื่อไอเทมจึงต้องผ่าน `esc()` เสมอ**

**โครงราคาร้านค้า + เอฟเฟกต์ไอเทม (Ultra-Hardcore Economy · Patch v4.7.9.1)**

เลิกใช้ "ตัวคูณจากราคาเดิม" ของ v4.7.4 แล้ว — ชุดนี้ตั้งราคาเป็นขั้นบันไดตามระดับของไอเทมตรง ๆ
เพื่อให้ร้านค้าเป็น **Gold Sink** จริงจัง กันเงินเฟ้อปลายเกม

| ซื้อ | ขายคืน | ไอเทม | เอฟเฟกต์ |
|---|---|---|---|
| 150 | 112 | 🧪 ยาฟื้นพลัง | ฟื้น HP ทันที 50 (คงเดิม) |
| 160 | 120 | 🎁 กล่องสุ่มปริศนา | เรตโหด 70/22/8 (ดูตารางถัดไป) |
| 200 | 150 | ⏳ หยุดเวลา | ต่อเวลาข้อปัจจุบัน **5 วิ** (เดิม 10) |
| 300 | 225 | ⚡ ยาเพิ่มสมาธิ | EXP ×2 ต่อเนื่อง **5 ข้อ** (เดิม 10) |
| 350 | 262 | 🔍 แว่นขยายสัจธรรม | ใบ้พินอิน + อักษรแรกคำแปล เฉพาะข้อที่กดใช้ |
| 500 | 375 | 📜 ม้วนคัมภีร์ | ตัดตัวเลือกผิด **1 ข้อ** เหลือ 3 (เดิมตัด 2 เหลือ 2) |
| 950 | 712 | 🛡️ ประกันภัยฮันเตอร์ | ชุบชีวิตทันที ไม่ร่วงกลับต้นโซน (คงเดิม) |

ตัวเลขเอฟเฟกต์ทั้งสามตัวที่ปรับอยู่ที่ค่าคงที่ **`FREEZE_MS` 5000 · `FOCUS_QUESTIONS` 5 ·
`SCROLL_CUT` 1** เหนือ `ITEMS` และ **ข้อความ `effect`/`how` บนหน้าร้านถูกประกอบจากค่าคงที่พวกนี้**
(ไม่ได้พิมพ์เลขทับไว้) จึงไม่มีทางที่ตัวเลขบนจอกับตัวเลขที่ทำงานจริงจะเพี้ยนกัน — **แก้ที่เดียว**
· `SKILL_FREEZE_MS` ของสกิล ❄️ ยังเป็น 10000 **แยกก้อนกัน อย่าเผลอรวม**

**`sellPrice()` เปลี่ยนจาก `Math.round` เป็น `Math.floor`** เพื่อให้ตรงตารางเป๊ะ
(150→112 · 350→262 · 950→712 ไม่ใช่ 113/263/713) `SELL_RATE` ยังเป็น 0.75 เท่าเดิม

**🎁 กล่องสุ่มปริศนา — เรต Ultra-Hardcore 70/22/8**

| ระดับ | โอกาส | ได้อะไร |
|---|---|---|
| 🥉 เกลือ | 70% | ทองคืน 30-80 (ขาดทุนเฉลี่ย 66% ของราคากล่อง) |
| 🥈 คุ้มค่า | 22% | ครึ่ง-ครึ่ง: ทอง 150-220 **หรือ** 🧪 / ⏳ 1 ชิ้น |
| 🥇 แจ็คพอต | 8% | ครึ่ง-ครึ่ง: ทอง 350-500 **หรือ** 🔍 / 📜 / 🛡️ 1 ชิ้น |

EV ≈ 108 ต่อราคา 160 = **house edge 32%** (ชุด v4.7.4 อยู่ที่ 17.7%) กว้างขึ้นเกือบเท่าตัว
โดยตั้งใจ — กล่องสุ่มต้องเป็น "การพนัน" ไม่ใช่ทางลัดปั๊มไอเทม/ทองที่คุ้มกว่าซื้อตรงจากร้าน
แต่หางแจ็คพอตยังจ่ายถึง 🛡️ (มูลค่า 950) จึงยังมีเหตุผลให้เสี่ยง

เรตทั้งชุดอยู่ที่ค่าคงที่ `MB_SALT_P` `MB_FAIR_P` `MB_SALT_G` `MB_FAIR_G` `MB_JACK_G`
`MB_FAIR_IT` `MB_JACK_IT` · **ประกาศไว้เหนือ `ITEMS` โดยจำเป็น ไม่ใช่เพื่อความสวยงาม**
เพราะข้อความ `effect` ของกล่องอ่านค่าไปโชว์ ถ้าย้ายไปไว้ข้าง `rollMystery()` จะติด TDZ
ของ `const` แล้วพังตอนโหลดหน้าทั้งเกม (`ReferenceError` ก่อน DOM จะขึ้นเลย)

**`SN_PRICEY` ถูกดันจาก 250 เป็น 500** หน้าต่างยืนยันตอนกดซื้อจึงกลับมาหายากตามเจตนาเดิม —
เตือนแค่ 2 ตัวคือ 📜 ม้วนคัมภีร์ (500) · 🛡️ ประกันภัยฮันเตอร์ (950) ส่วน ⏳ ⚡ 🔍 ซื้อได้ทันที
(ชุด v4.7.4 เคยเตือนถึง 5 ตัวเพราะเกณฑ์ค้างที่ 250 ขณะที่ราคาขยับขึ้น)

**ไอเทมช่วยตอบใช้ได้ข้อละ 1 ชิ้น (Exclusive Item Cooldown v4.7.4 · ยังใช้กติกาเดิม)**

`helper:true` ใน `ITEMS` = ไอเทมสายช่วยตอบคำถาม ตอนนี้มี 2 ตัวคือ 📜 ม้วนคัมภีร์ · 🔍 แว่นขยายสัจธรรม
กดตัวใดตัวหนึ่งแล้วอีกตัวถูกล็อกทันทีจนกว่าจะขึ้นข้อใหม่ · 🧪 ⏳ ⚡ 🎁 🛡️ ยังกดร่วมกับตัวอื่นได้ตามปกติ

| ชิ้นส่วน | หน้าที่ |
|---|---|
| `G.helperUsed` | ธงของ "ข้อที่กำลังตอบอยู่" ไม่ใช่ของบัญชี — ไม่ต้องเซฟ |
| `startQuestionTimer()` | จุดเดียวที่ล้างธง (= ขึ้นข้อใหม่จริง ๆ) |
| `helperLocked(key)` | ตัวอ่านกลาง ทั้ง 3 จุดบนจอถามที่นี่ที่เดียว |
| `useItem()` | ตัวบล็อกจริง — วางไว้ที่ฟังก์ชันกลาง ไม่ใช่ที่ปุ่ม จึงครอบทุกทางเข้า |

**ธงถูกปั๊มหลังของถูกหักออกจากกระเป๋าแล้วเท่านั้น** (บรรทัดท้าย `useItem` ก่อน `BAG_SEL = -1`)
เพราะ `useItem` มีทางออกก่อนหักของหลายทาง (ตัวเลือกเหลือ 2 · ยังไม่มีคำถาม) กดแล้วไม่ได้ผล
ต้องไม่ถูกกินสิทธิ์ · **`extendQuestionTimer` / `freezeQuestionTimer` ไม่ผ่าน `startQuestionTimer`**
จึงไม่คืนสิทธิ์ให้ฟรี (ต่อเวลาแล้วใช้ได้อีกชิ้น = ช่องโหว่ที่กติกานี้ตั้งใจปิด)

บนจอ: ช่องกระเป๋าที่ถูกล็อกได้คลาส `.g-slot.spent` (หรี่) · ปุ่ม ▶ ใช้ กลายเป็น `⛔ ข้อนี้ใช้ไปแล้ว`
และ `disabled` · กดช่องนั้นซ้ำยังได้ข้อความบอกเหตุผลเสมอ (กติกาเดียวกับไอเทมที่ยังไม่ปลดล็อกแรงค์)

**ทุกกรอบตรึงขนาดเป็น px ตายตัวและเตี้ยกว่าของที่อยู่ข้าง ๆ อยู่แล้ว เลย์เอาต์เดิมจึงไม่ขยับ**
วัดจริงหลังฝังภาพ: ช่องกระเป๋ายังสูง 36px เท่าเดิม · แถวร้านค้าสูง 87px เท่าเดิม
(ความสูงแถวถูกกำหนดโดยปุ่มซื้อ/ขายสองใบที่ซ้อนกัน ไม่ใช่ไอคอน) · ไม่ล้นแนวนอนทุกความกว้าง
**ถ้าจะขยายกรอบต้องวัดใหม่ทั้งชุด**

**ต้นฉบับ 2 เรื่องที่สคริปต์ต้องซ่อมให้ก่อนฝัง — อย่าถอดออก**

1. **ลายตารางหมากรุกติดมาในภาพ 3 ตัว** (`ยาฟื้นพลัง` ลายสีอ่อนเห็นชัดมาก ·
   `แว่นขยายสัจธรรม` `กล่องสุ่มปริศนา` ลายสีเข้ม) เพราะเป็น PNG พื้นโปร่งที่ถูกแบนเป็น JPG มาแล้ว
   `strip_checker()` ไล่ระบายจากขอบเข้ามา (flood fill) แล้วถมด้วยสีพื้นเกม —
   **ไม่ใช้การกวาดทั้งภาพ** เพราะส่วนที่เป็นตัวไอเทมซึ่งบังเอิญเป็นเทาระดับเดียวกันจะโดนลบตามไปด้วย
   ภาพพื้นดำสนิทอีก 4 ตัวไม่เข้าเงื่อนไข จะถูกปล่อยผ่านโดยไม่แตะเลย
2. **สัดส่วนต้นฉบับต่างกันสุดขั้ว 0.50 ถึง 1.84** แต่กรอบไอคอนบนจอเป็นจัตุรัสหมด
   `pad_square()` ถมให้เป็นจัตุรัสด้วย**สีพื้นของภาพเอง**ตั้งแต่ตอนฝัง `object-fit:cover`
   จึงไม่ครอบอะไรทิ้งเลยและรอยต่อมองไม่เห็น ถ้าเอาต้นฉบับดิบยัดกรอบจัตุรัสตรง ๆ
   ตัวที่ผอมสูงอย่าง `ยาเพิ่มสมาธิ` (198×400) จะเหลือแต่ท่อนกลางขวดจนดูไม่ออกว่าเป็นอะไร

```
python3 embed_item_art.py                  # ฝังทุกตัวที่หาไฟล์เจอ
python3 embed_item_art.py potion mystery   # ฝังเฉพาะบางตัว
python3 embed_item_art.py --clear          # ถอดภาพทุกตัวกลับไปใช้อีโมจิ
```

**ข้อความแจ้งเตือน/บรรทัดผลลัพธ์ยังใช้อีโมจิ `icon` ตามเดิมทั้งหมด** (เช่น
`itemMsg(it.icon + ' ' + it.name + ' ทำงานอัตโนมัติ…')`) — จงใจ เพราะเป็นข้อความล้วน
ไม่ใช่ DOM ที่จะยัด `<img>` เข้าไปได้ และ toast ของชั้น v4.1 ก็ escape HTML อยู่แล้ว

**ตัวเลือกคำตอบ — ต้องล้างข้อความก่อนแสดงเสมอ**

`THAI_MEANINGS` ผูกคำจีนไว้ในวงเล็บกันสับสนตอนทำข้อมูลอยู่ 24 รายการ เช่น `"ภาษาฝรั่งเศส(法文)"`
· `"สอง(二)"` · `"เล็กน้อย, นิดหน่อย(有(一)点儿)"` (วงเล็บซ้อน) ถ้าเอาข้อความดิบขึ้นปุ่มตัวเลือก
= เฉลยคำจีนให้ผู้เล่นเห็นก่อนตอบ

`cleanMeaning()` / `meaningOf(id)` วางอยู่ต่อท้ายก้อน `THAI_MEANINGS` ทำหน้าที่ล้าง:
ลบวงเล็บที่ "มีอักษรจีนอยู่ข้างใน" จากชั้นในสุดออกมาทีละชั้น (จึงกิน `((一)点儿)` ได้)
→ กวาดอักษรจีนที่หลงอยู่นอกวงเล็บ → ตัดเครื่องหมายวรรคตอนค้างหัว/ท้าย → ถ้าล้างแล้วว่าง
ให้คืนข้อความเดิม (ปุ่มเปล่ากดไม่ได้) ผลลัพธ์แคชไว้ที่ `MEANING_CLEAN`

**ห้ามอ่าน `THAI_MEANINGS[id]` ตรง ๆ ไปแสดงผล — ให้เรียก `meaningOf(id)` เสมอ**
ผู้เรียกตอนนี้: `getMonster._build()` (เฉลย + ตัวลวง + `choiceMeta`) และ `weakWordsOf()`

ตัวเลือกถูกระบุด้วย "ข้อความ" ล้วน ๆ (`m.choices` เป็นอาร์เรย์สตริง · `m.answer` · `m.revealed`
· คีย์ของ `choiceMeta`) และ `removeDistractors()` / `reshuffleChoices()` ก็ filter ด้วยข้อความ
→ ห้ามมีตัวเลือกซ้ำกันเด็ดขาด (ดูกับดักข้อ 7)

### ชั้นบน — v4.1 · SYSTEM (แจ้งเตือนสีฟ้า 25 รายการ)

อยู่ท้ายไฟล์ เริ่มที่ `const SN_BOOK` ทุกอย่างขึ้นต้นด้วย `sn` / `SN_`

**ชั้นนี้ไม่แก้โค้ด v4.0 เลยแม้แต่บรรทัดเดียว — ใช้วิธีห่อฟังก์ชันแทน**

```js
const _snClearFloor = clearFloor;
clearFloor = function (...a) {
  const before = /* อ่านสถานะก่อน */;
  const r = _snClearFloor.apply(this, a);   // ให้ v4.0 ทำงานตามปกติ
  /* เทียบ before/after แล้วยิงแจ้งเตือน */
  return r;
};
```

ฟังก์ชันที่ถูกห่อไว้: `addItem` `buyItem` `sellItem` `openShop` `useSkill` `renderSkills`
`resolveAnswer` `nextMonster` `clearFloor` `openWarp` `warpGo` `openAnalytics` `renderWarp`

**ทำไมต้องห่อ:** v4.0 เป็นระบบที่บาลานซ์มาแล้วและยาวมาก การแทรกโค้ดแจ้งเตือนลงไปตรง ๆ
ทำให้ merge งานรุ่นถัดไปลำบากและเสี่ยงพังตรรกะเดิม การห่อทำให้ลบชั้น SYSTEM ทิ้งทั้งก้อน
แล้วเกมยังเล่นได้ปกติ

**ถ้าจะเพิ่มแจ้งเตือนใหม่** → เพิ่ม entry ใน `SYS_NOTIF` แล้วห่อฟังก์ชัน v4.0 ที่เกี่ยวข้อง
อย่าไปเติม `sysNotify()` ลงกลางฟังก์ชัน v4.0

`SYS_NOTIF` มี 25 entry พอดี (แท็ก `01/25`–`25/25` ครบ ไม่ซ้ำ) แต่ละ entry รับได้แค่ 6 คีย์:

| คีย์ | ความหมาย |
|---|---|
| `tag` | ป้ายมุมขวา เช่น `'BATTLE 07/25'` — เลขนี้คือสารบัญ อย่าให้ซ้ำ |
| `tone` | `''` ฟ้าปกติ · `good` เขียว · `warn` เหลือง · `danger` แดง |
| `title(d)` | หัวข้อ (template string) |
| `body(d)` | เนื้อความ — `\n` ขึ้นบรรทัดใหม่, `<b>` `<i>` ใช้ได้ นอกนั้นถูก escape โดย `snMarkup` |
| `gate` | `true` = หน้าต่างบังคับกดยืนยัน (`#snGate`) แทน toast ลอย |
| `btn` | ข้อความบนปุ่มยืนยัน (ใช้คู่กับ `gate`) |

**ไม่มีกลไกแทรก HTML ในตัว def** ถ้าต้องการภาพประกอบให้แทรกจาก wrapper ของฟังก์ชันนั้น
(ดูวิธีที่ `renderWarp` ทำกับ `snWarpArt()`)

### ชั้นที่สาม — v4.2 · QUEST BOARD (กระดานภารกิจ 20 เควส)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น SYSTEM เริ่มที่ `const Q_KEYS` ทุกอย่างขึ้นต้นด้วย `q` / `Q_`

**ชั้นนี้ไม่แก้โค้ด v4.0 และ v4.1 เลย — ใช้วิธีห่อฟังก์ชันแบบเดียวกับชั้น SYSTEM**

ฟังก์ชันที่ถูกห่อเพิ่ม: `migrateAccount` `startGame` `saveProgress` `resolveAnswer`
`onMonsterDefeated` `clearFloor` `useSkill` `renderStats`

โครงสร้าง 20 เควสและบาลานซ์ที่ล็อกไว้ (แก้ตัวเลขที่ `Q_DEFS`):

| หมวด | จำนวน | รอบ | ทอง/เควส | รวม | ของแถม |
|---|---|---|---|---|---|
| รายวัน `D1-D7` | 7 | รีเซ็ตเที่ยงคืน | 10-25 | 115/วัน | — |
| รายสัปดาห์ `W1-W6` | 6 | รีเซ็ตเช้าวันจันทร์ | 60-100 | 485/สัปดาห์ | ไอเทม 1 ชิ้นทุกเควส |
| ระยะยาว `L1-L7` | 7 | ครั้งเดียวจบตลอดชีพ | 150-500 | — | ไอเทม + ฉายาปลายเกม |

เทียบค่าครองชีพ: เคลียร์ชั้นปกติ +150 ทอง · ชั้นบอส +500 ทอง · ไอเทมแพงสุด 950 (ราคาชุด v4.7.9.1)
เควสรายวันทั้งชุดยังได้น้อยกว่าเคลียร์หอคอย 1 ชั้น จึงเป็นรายได้เสริม ไม่ใช่ทางลัดแทนการไต่หอคอย

**กันเงินเฟ้อ 2 ชั้น — อย่าไปรื้อ**

1. ทองรางวัลเควส **ไม่** บวกเข้า `G.totalGoldEarned` (บวกเข้า `G.gold` อย่างเดียว)
   ไม่งั้นผู้เล่นจะเอาทองจากเควสมาปั๊มเงื่อนไขเควส 19 (สะสมทองตลอดชีพ 3,000) วนเป็นงูกินหาง
2. เควสระยะยาวเก็บสถานะที่ `quest.done.L` การรีเซ็ตรายวัน/รายสัปดาห์ไม่แตะคีย์นี้

`Q_NOTIF` มีสารบัญของตัวเอง `QUEST 01/06`–`06/06` **แยกจาก `SYS_NOTIF` ที่ตรึงไว้ 25 entry**
ยืมแค่กลไกโชว์ (`snShowToast` / `snShowGate`) มาใช้ ถ้าจะเพิ่มแจ้งเตือนของเควสให้เพิ่มใน `Q_NOTIF`
แล้วขยับตัวหารเป็น `/07` ทั้งชุด อย่าไปกินเลขของ `SYS_NOTIF`

**สถานะเควสเก็บที่ `account.quest`** โครงสร้าง:

```
{ day:'YYYY-MM-DD', week:'W YYYY-MM-DD (วันจันทร์)',
  d:{...ตัวนับรายวัน}, w:{...ตัวนับรายสัปดาห์},
  done:{ d:[], w:[], L:[] } }
```

`qEnsureQ()` ทำหน้าที่ทั้งซ่อมข้อมูลเพี้ยนและรีเซ็ตรอบที่หมดอายุ ต้อง idempotent เสมอ
เพราะถูกเรียกทั้งจาก `migrateAccount` (ทุกครั้งที่อ่าน store) และจาก `qSync()` ระหว่างเล่น

ฉายา 🐲 **ปรมาจารย์ฮันเตอร์** (`grandmaster`) ถูก `TITLES.push()` เข้าไปจากชั้นนี้
ไม่ได้แก้อาร์เรย์ `TITLES` เดิม — `check` ของมันอ่าน `s.quest.done.L` จึงต้องทนกับ `s.quest`
ที่ยังไม่มี (บัญชีเก่าตอน migrate รอบแรก) ให้ได้

### ชั้นที่สี่ — v4.3 · GM CONTROL (เครื่องมือหลังบ้านครู 6 ระบบ)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น QUEST BOARD เริ่มที่ `const GC_EKEY` ทุกอย่างขึ้นต้นด้วย `gc` / `GC_`

**ชั้นนี้ไม่แก้โค้ด v4.0 / v4.1 / v4.2 เลย — ห่อฟังก์ชันแบบเดียวกับสองชั้นก่อนหน้า**

ฟังก์ชันที่ถูกห่อเพิ่ม: `migrateAccount` `startGame` `saveProgress` `exitGame` `openGm` `closeGm`
`gmRender` `gmExportCsv` `gmGift` `gmEditLevel` `gmResetPw` `gmFreeze` `gmResetProgress`

| ระบบ | จุดเข้า | เก็บข้อมูลที่ |
|---|---|---|
| กลุ่มห้องเรียน | `#gcRoom` + ปุ่ม 🏫 จัดกลุ่มยกชุด | `account.classroom` (ฟิลด์เดิมของ v4.0) |
| ประวัติกิจกรรม | ปุ่ม 📋 แฟ้มฮันเตอร์ ในคอลัมน์ชื่อ | `account.gcLog` `sessions` `loginCount` `playMs` |
| เสกไอเทมรายบุคคล | แท็บ 🎒 คลังไอเทม | `account.items` (เคารพเพดาน `BAG_SLOTS`) |
| เติม HP/MP | ปุ่ม 💊 ในตาราง + แท็บ 💊 สถานะ | `account.hp` `mp` `shield` |
| กู้คืนฉุกเฉิน | แท็บ 🚑 กู้คืน + ปุ่ม 🚑 บนหัวแผง | `account.floor` `floorProgress` `skillCd` + DOM |
| Error Log | ปุ่ม 🐞 Error Log บนหัวแผง | `localStorage['yao_errlog']` (ระดับเครื่อง) |

**ฟิลด์ใหม่ในบัญชี** — `gcv` `gcLog[]` `sessions[]` `loginCount` `lastLogin` `playMs`
`gcEnsure()` เติมให้ทั้งหมดและต้อง idempotent (ถูกเรียกทุกครั้งที่อ่าน store ผ่าน `migrateAccount`)

`gcv` คือตรารุ่น ใช้แยก "บัญชีที่เกิดก่อนชั้นนี้" ออกจาก "บัญชีที่เพิ่งสมัคร" — ดูกับดักข้อ 8

**Error Log ไม่ได้เก็บภาพจริง** เพราะกฎไฟล์เดียวจบห้ามพึ่ง lib ภายนอกอย่าง html2canvas
`gcSnapText()` เก็บเป็นข้อความแทน: อยู่หน้าไหน · หน้าต่างไหนเปิดค้าง · ชั้น/HP/MP · `G.locked` ·
คำที่ค้างอยู่ · ขนาดจอ ซึ่งพอวิเคราะห์ "จอค้างตรงไหน" ได้จริง

**ปุ่มของชั้นนี้อยู่ในคอลัมน์ชื่อ ไม่ใช่คอลัมน์ ⚙️ จัดการ** — ดูกับดักข้อ 9

### ชั้นที่ห้า — v4.4 · CODEX (คลังอักขระ + โบนัสสะสมแบบมีเพดาน)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น GM CONTROL เริ่มที่ `const CX_VER` ทุกอย่างขึ้นต้นด้วย `cx` / `CX_`

**ชั้นนี้ไม่แก้โค้ด v4.0 / v4.1 / v4.2 / v4.3 เลย — ห่อฟังก์ชันแบบเดียวกับสามชั้นก่อนหน้า**

ฟังก์ชันที่ถูกห่อเพิ่ม: `maxMpOf` `goldMul` `migrateAccount` `startGame` `saveProgress`
`resolveAnswer` `renderStatus`

**"คำที่สะสมได้" ไม่ได้เก็บเป็นฟิลด์ใหม่ — อ่านจาก `wordStats` ของ v4.0 ตรง ๆ**

```js
cxHas(src, id)  →  (ws[id].seen - ws[id].wrong) > 0     /* เคยตอบถูกอย่างน้อย 1 ครั้ง */
```

`recordWord()` บวก `seen` ทุกครั้งที่ตอบจบ (ถูก/ผิด/หมดเวลา) และบวก `wrong` เฉพาะตอนพลาด
`seen - wrong` จึงคือจำนวนครั้งที่ตอบถูก ผลพลอยได้สองอย่าง: บัญชีเก่าได้คลังย้อนหลังทันที
โดยไม่ต้อง migrate ข้อมูล และปั๊มไม่ได้เพราะต้องตอบถูกจริง

`account.codex` เก็บแค่ของประกอบ ไม่ใช่ตัวคลัง: `{ ms: ขั้นสูงสุดที่แจ้งเตือนไปแล้ว, first: {id:timestamp} }`
`a.cxv` คือตรารุ่นสำหรับ backfill ครั้งเดียว (กฎเดียวกับ `gcv` ของ v4.3 — ดูกับดักข้อ 8)

**ขั้นบันไดและเพดาน — แก้ตัวเลขที่ `CX_MILES` เท่านั้น**

| ขั้น | คำ | รางวัล | ขั้น | คำ | รางวัล |
|---|---|---|---|---|---|
| I | 10 | 🔮 MP +2 | VII | 140 | 🔮 MP +5 |
| II | 25 | 🪙 ทอง +1% | VIII | 175 | 🪙 ทอง +2% |
| III | 40 | 🔮 MP +3 | IX | 210 | 🔮 MP +6 |
| IV | 60 | 🪙 ทอง +1% | X | 250 | 🪙 ทอง +2% |
| V | 85 | 🔮 MP +4 | XI | 290 | 🔮 MP +10 |
| VI | 110 | 🪙 ทอง +2% | XII | 329 | 🪙 ทอง +2% + ฉายา 🏛️ |

ผลรวม MP = `CX_MP_CAP` 30 พอดี · ผลรวมทอง = `CX_GOLD_CAP` 10% พอดี
**ถ้าแก้ `CX_MILES` ต้องให้ผลรวมเท่าเพดานเป๊ะ** ไม่งั้นขั้นท้าย ๆ จะกลายเป็นขั้นเปล่าที่ไม่ให้อะไร
(โค้ดหนีบด้วย `Math.min` อยู่แล้วจึงไม่พัง แต่จะหลอกผู้เล่น)

กันเงินเฟ้อ: +10% เทียบเท่า LUK 20 หน่วย และต้องเก็บครบทั้งเล่มถึงจะถึงเพดาน
ตัวนับคือ "จำนวนคำที่เคยตอบถูก" ไม่ใช่ทอง จึงเอาทองมาเร่งตัวเองไม่ได้ (ไม่เป็นงูกินหาง)

**โบนัสถูกใส่ที่จุดเดียว คือ wrapper ของ `maxMpOf` / `goldMul`** ทุกที่ที่เรียกสองฟังก์ชันนี้
(หลอด MP · แผงวาป · แผง GM · การให้ทองตอนตอบถูก · แถว LUK ในแผงสถานะ) จึงได้ค่าที่รวมโบนัสแล้ว
โดยไม่ต้องไปไล่แก้ทีละจุด — **ถ้าจะเพิ่มโบนัสถาวรอันใหม่ ให้ห่อฟังก์ชันสูตร อย่าไปบวกตามจุดเรียก**

`CX_NOTIF` มีสารบัญของตัวเอง `CODEX 01/04`–`04/04` **แยกจาก `SYS_NOTIF` (ตรึง 25) และ `Q_NOTIF` (ตรึง 6)**

`CX_LORE` คือประวัติความเป็นมารายอักขระ 335 รายการ (ครอบคลุมครบทั้ง 328 ตัวที่ VOCAB ใช้จริง)
`CX_WORD` คือบันทึกระดับ "คำ" 71 รายการ สำหรับคำที่ความหมายรวมไม่ได้มาจากการบวกอักขระตรง ๆ
`cxLoreBlocks(entry)` ประกอบสองก้อนนี้เข้าด้วยกันเอง — คำหลายอักขระจะได้บล็อกของอักขระย่อยทีละตัว

**เพิ่มคำใน `VOCAB` แล้วต้องเติม `CX_LORE` ให้อักขระใหม่ด้วย** ไม่งั้นคำนั้นจะตกไปใช้ข้อความสำรอง
(`CX_TOTAL` อ่านจาก `VOCAB.length` เอง แต่ `CX_MILES` ขั้นสุดท้ายผูกกับ `CX_TOTAL` อยู่แล้วจึงขยับตาม)

### ชั้นที่หก — v4.5 · TOWER RADAR (เรดาร์ดันเจี้ยนสามมิติ)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น CODEX เริ่มที่ `const TR_VER` ทุกอย่างขึ้นต้นด้วย `tr` / `TR_`

**ชั้นนี้ไม่แก้โค้ด v4.0 / v4.1 / v4.2 / v4.3 / v4.4 เลย — ห่อฟังก์ชันแบบเดียวกับสี่ชั้นก่อนหน้า**

ฟังก์ชันที่ถูกห่อเพิ่ม: `migrateAccount` `startGame` `saveProgress` `resolveAnswer` `renderFloor`

แถบชั้นแบน `#gFloorBar` ของ v4.0 **ถูกซ่อน (`display:none`) ไม่ได้ถูกลบ** — wrapper ของ `renderFloor`
ซ่อนมันแล้ววาดแผง `#trPanel` แทน ข้อมูลชุดเดียวกัน (ชั้น/โซน/ความคืบหน้า) ย้ายไปอยู่บนหัวแถบเรดาร์
ถ้า `trPaint()` โยน error ตัว wrapper จะ **คืน `#gFloorBar` กลับมาโชว์เอง** แล้วเขียนลง Error Log
ของ v4.3 (`gcErrPush`) เกมจึงไม่มีทางเหลือหน้าจอที่ไม่รู้ว่าตัวเองอยู่ชั้นไหน

#### ภาพหอคอย — SVG ล้วน ไม่มีไลบรารี

| ส่วน | ทำอะไร |
|---|---|
| `trTowerSvg()` | ประกอบภาพทั้งหมด **ครั้งเดียวตอนติดตั้ง** (โครงหอคอยเป็นของนิ่ง) |
| `trPaint()` | เปลี่ยนแค่กล้อง · อวตาร · คลาสสถานะรายชั้น · ข้อความหัวแถบ — เรียกได้ถี่ ๆ ไม่แพง |
| `trCam(floor)` | คืน `{k, tx, ty}` ของกล้อง — ย่อรอบจุด (0,0) **จึงต้องเลื่อน x กลับเองเสมอ** |
| `trBreakthrough()` | วงแหวนขยาย + ลำแสงวูบตอนขึ้นชั้นใหม่ ล้าง `#trFx` ก่อนยิงทุกครั้ง |
| `trTipRender(f)` | แผงข้อมูลย่อยรายชั้น (ชี้เมาส์ / แตะ) |

ค่าคงที่เรขาคณิต (แก้ที่นี่ที่เดียว): `TR_CX 150 · TR_BASE 700 · TR_FH 34 · TR_W0 96 · TR_TAPER 2.0
· TR_RATIO 0.34 · TR_ROOF 50` · กล้อง `TR_VH_C 190 (ปกติ) · TR_VH_O 430 (กดดูทั้งหอคอย) · TR_K_C 0.78`

**ยุคสมัย 5 ช่วง อยู่ที่ `TR_ERAS` ช่วงละ 4 ชั้นพอดี** แต่ละ entry มี `line` (เส้นโครง) `glow`
(เส้นเรืองแสง/ชั้นปัจจุบัน) `accent` (เกราะอวตาร) `sky0`/`sky1` (ไล่โทนฉากหลัง)

| ยุค | ชั้น | โทน |
|---|---|---|
| I | 1-4 | อิฐโบราณ · ดินทราย |
| II | 5-8 | หินชนวน · ตะไคร่เวทมนตร์ |
| III | 9-12 | หินแกรนิตเงิน · แสงคบเพลิง |
| IV | 13-16 | น้ำเงินเข้ม · ออร่าอาร์เคน |
| V | 17-20 | คริสตัล · ออโรร่านีออน |

**สีถูกส่งผ่าน CSS custom property จุดเดียว** — กลุ่มรายชั้นตั้ง `--l/--gl/--ac` ไว้ในแอตทริบิวต์ `style`
และ `trPaint()` ตั้ง `--tr-line/--tr-glow/--tr-accent` บน `#trPanel` เกราะอวตาร ออร่า และหัวแถบ
จึงเปลี่ยนสีตามยุคพร้อมกันหมดโดยไม่ต้องไล่แก้ทีละโหนด **ถ้าจะเพิ่มของที่ต้องเปลี่ยนสีตามยุค
ให้ผูกกับตัวแปรพวกนี้ อย่าฮาร์ดโค้ดสี**

#### ช่องเสียบภาพหอคอยเรนเดอร์ (`TR_ART`)

ชั้นนี้รองรับสองโหมด สลับด้วยค่าเดียวคือ `TR_ART`

| `TR_ART` | ผลลัพธ์ |
|---|---|
| `''` | วาดหอคอยเส้นเรืองแสงเองตามสูตรเรขาคณิต `TR_BASE/TR_FH/TR_W0/...` (โหมดสำรอง) |
| data URI | ใช้ภาพเป็นพื้นหลังแทนโครงเส้น แล้วอ่านพิกัดชั้นจาก `TR_ART_Z` — **โหมดที่ใช้อยู่ตอนนี้** |

ภาพที่ฝังอยู่มาจาก `TOWER.png` (1856×2304) ย่อเหลือ 800×993 → WebP q82 ≈ 105KB (base64 140KB)
**ตัวไฟล์ `TOWER.png` ถูกลบออกจาก repo แล้ว** (Asset Replacement Patch · หนัก 9.8MB)
ภาพที่ฝังใน `TR_ART` ยังอยู่ครบและเกมไม่กระทบ แต่ถ้าจะฝังใหม่/วัด `TR_ART_Z` ใหม่
ต้องอัปโหลด `TOWER.png` กลับเข้ามาก่อน (หรือดึงจากประวัติ git)
**ไม่มีตัวละครวาดติดมา** เพราะอวตารพิกเซลเป็นตัวเดียวที่ควรปรากฏ ถ้าเปลี่ยนภาพใหม่แล้วมีคนวาดติดมา
จะกลายเป็นตัวละครสองตัวซ้อนกัน (เคยเจอมาแล้วกับภาพรุ่นก่อน แก้ด้วยการถมพิกเซลไม่สวยเลย)

**สัดส่วนภาพเป็นตัวตัดสินว่าจะเหลือขอบดำข้างหรือไม่ ไม่ใช่ความละเอียด** — กรอบเรดาร์เป็นแนวนอน
(กว้าง 264–448px × สูง 190px) ภาพหอคอยเป็นแนวตั้ง ยิ่งภาพสูงเรียวยิ่งเหลือขอบมาก วัดจริงแล้วได้:

| สูง ÷ กว้าง | จอ 320 | จอ 360 | จอ 390 | จอ 430 |
|---|---|---|---|---|
| 3.31 | 11px | 15px | 30px | 50px |
| 1.66 | 11px | 15px | 30px | 50px |
| 1.33 (3:4) | เต็ม | เต็ม | เต็ม | 16px |
| **1.24 (ที่ใช้อยู่)** | เต็ม | เต็ม | เต็ม | เต็ม |

ถ้าจะเจนภาพใหม่ให้สั่ง **สัดส่วน 4:5 หรือ 5:6 แนวตั้ง** (สูง ÷ กว้าง ราว 1.20–1.25)
และ**ให้ 5 โซนสูงเท่า ๆ กัน โซนละ ~18% ของความสูงภาพ** ไม่งั้นโซนที่เตี้ยกว่าเพื่อนจะมีชั้น
กองทับกันจนต้องยืดพิกัดชดเชย (เจอมาแล้วทั้งสองภาพก่อนหน้า)

`trY()` / `trW()` เป็นตัวสลับโหมด **ทุกอย่างที่เหลือ (กล้อง · อวตาร · พื้นที่แตะ · วงแหวนทะลุชั้น ·
แผงข้อมูลย่อย) อ่านผ่านสองฟังก์ชันนี้อยู่แล้ว จึงไม่ต้องแก้อะไรเพิ่มตอนสลับโหมด**

**เปลี่ยนภาพใหม่ = ต้องวัด `TR_ART_Z` ใหม่ทั้งชุด** ค่าปัจจุบันผูกกับการจัดกรอบของภาพนี้เท่านั้น
(ภาพร่างรุ่นก่อนสัดส่วน 3.31:1 ใช้ตัวเลขคนละชุดโดยสิ้นเชิง) และ `TR_ART_PAD0`/`TR_ART_PAD1`
คือระยะเผื่อใต้ชั้น 1 / เหนือชั้น 20 ที่ยังเป็นตัวหอคอยอยู่ (วงรูนที่ฐาน · หลังคาคริสตัลกับยอดแหลม)
ถ้าตั้งแคบไป โหมดดูทั้งหอคอยจะตัดยอดหรือตัดฐานทิ้ง

`TR_ART_Z` มี 5 รายการเรียงจากโซน I (ล่างสุดของภาพ) ขึ้นไปโซน V ต่อรายการมี 4 ค่า
เป็น**สัดส่วน 0-1** ของขนาดภาพ: `y0`/`y1` = พื้นที่ยืนของชั้นล่างสุด/บนสุดในโซนนั้น
(`y0` มากกว่า `y1` เพราะ 0 คือขอบบน) · `w0`/`w1` = ครึ่งความกว้างของตัวหอคอยตรงระดับนั้น
ชั้นทั้ง 4 ในโซนถูกไล่ระดับเชิงเส้นระหว่าง `y0 → y1` อวตารจึงปีนอยู่ในกล่องสถาปัตยกรรมของโซนนั้นพอดี

**เตรียมภาพด้วย `tools/embed_tower_art.py`** — ย่อเหลือกว้างไม่เกิน 800px (ไม่ขยายเกินต้นฉบับ) → WebP q82 → base64
→ เขียน `TR_ART` กับ `TR_ART_AR` ให้เอง พร้อมหยุดให้ถ้าไฟล์รวมจะทะลุเพดาน 2MB
และพ่นภาพ `tools/tower_art_ruler.png` ที่ขีดเส้นบอกความสูงทุก 5% ไว้ให้วัด `TR_ART_Z`
(ตัวเลขชุดนี้วัดจากภาพเอา สคริปต์เดาแทนไม่ได้)

ต่อให้สัดส่วนพอดีแล้ว จอกว้าง ๆ (แท็บเล็ต) ก็ยังเหลือขอบอยู่ดี — โหมดภาพจึงปูภาพเดิมอีกใบแบบ `slice`
+ เบลอไว้เป็นฉากหลังเต็มกรอบ (`#trArtBg`) **วางไว้นอกกลุ่มกล้อง** จึงไม่ไถลตามการปีน
ถ้าเปลี่ยนความสูงกรอบต้องอัปเดต `height` ของมันด้วย (ทำอยู่แล้วใน `trPaint()`)

**กล่องฉากหลังต้องกว้างเกิน viewBox เสมอ** (`TR_ART_BG_W` = 640 เทียบ viewBox 300)
เพราะ `preserveAspectRatio="meet"` ทำให้กรอบจริงกว้างกว่า viewBox ได้ ถ้าตั้งไว้ 300 เท่ากันพอดี
จอกว้างจะเห็นพื้นแผงโล่ง ๆ สองข้างแทนเนบิวลา (เคยพลาดมาแล้ว วัดได้ 74px บนจอ 768)

#### ข้อมูลที่ชั้นนี้เพิ่ม

`account.trFloors` — `{ '7': { n: ตอบไปกี่ข้อ, ok: ถูกกี่ข้อ, ids: [id คำล่าสุด สูงสุด 10] } }`
ใช้เติมแผงข้อมูลย่อยรายชั้น ส่วนเครื่องหมาย ✓/✗ รายคำอ่านจาก `wordStats[id].recent` ของ v4.0 ตรง ๆ

`a.trv` คือตรารุ่น (กฎเดียวกับ `gcv` / `cxv` — ดูกับดักข้อ 8) แต่เคสนี้ **ไม่มีอะไรให้ backfill**
เพราะ v4.0 ไม่เคยเก็บว่าชั้นไหนเจอคำอะไร บัญชีเก่าจึงได้ `{}` แล้วเริ่มจดจากนี้ไป — อย่าไปเดา
ย้อนหลังจาก `maxFloor` เด็ดขาด มันจะกลายเป็นตัวเลขปลอมที่นักเรียนแยกไม่ออกจากของจริง

`trEnsure()` ซ่อมข้อมูลเพี้ยน (ชั้นนอกช่วง 1-20 · `ok > n` · id ที่ไม่มีใน VOCAB) และต้อง idempotent
เพราะถูกเรียกทุกครั้งที่อ่าน store ผ่าน `migrateAccount` · `trSaveFloors()` เขียนลง store เอง
เพราะ `saveProgress` ของ v4.0 ไม่รู้จักฟิลด์นี้

**โหมดฝึกจุดอ่อนไม่จดบันทึก** (ไม่ผูกกับชั้น) และเรดาร์ถูกซ่อนทั้งแผงในโหมดนั้น

### ชั้นที่เจ็ด — v4.6 · ABYSS PROTOCOL (กิมมิคและความท้าทายในการไต่หอคอย)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น TOWER RADAR เริ่มที่ `const AB_VER` ทุกอย่างขึ้นต้นด้วย `ab` / `AB_`

**ชั้นนี้ไม่แก้โค้ด v4.0 / v4.1 / v4.2 / v4.3 / v4.4 / v4.5 เลย — ห่อฟังก์ชันแบบเดียวกับห้าชั้นก่อนหน้า**

ฟังก์ชันที่ถูกห่อเพิ่ม: `migrateAccount` `startGame` `saveProgress` `exitGame` `resolveAnswer`
`nextMonster` `clearFloor` `renderMonsterHp` `renderStats` `renderStatus` `useItem` `useSkill`
`startQuestionTimer` `questionMs` `goldMul` `trPaint`

สี่ระบบในชั้นเดียว ทุกระบบใช้ข้อมูลก้อนเดียวกันคือ `account.ab`

#### 1) TOWER QUAKE — สตรีคสะเทือนหอคอย

ผูกกับ `G.streak` ของ v4.0 ตรง ๆ ไม่ได้นับเอง ขั้นบันไดอยู่ที่ `AB_QUAKE`

| สตรีค | ขั้น | ผลทันที | ออโรร่าเสริมทอง |
|---|---|---|---|
| 3 | I · แรงสั่นขั้นต้น | 🔮 MP +8 | ×1.30 อีก 3 ข้อ |
| 5 | II · ออโรร่าพุ่งทะยาน | — | ×1.50 อีก 4 ข้อ |
| ทุก ๆ 10 (10,20,30…) | III · หอคอยสะเทือน | 🔮 MP +15 | ×1.80 อีก 5 ข้อ |

`abShake(lv)` สั่น **เฉพาะแผง `#trPanel`** ไม่สั่นทั้งจอ — `transform` บน `#gameScreen`
จะดันเลย์เอาต์ล้นแนวนอนจนผิดกติกา `document.body.scrollWidth <= window.innerWidth`
ส่วน `abAurora(lv)` ยิงลำแสงเข้า `#abFx` ซึ่งอยู่ **นอกกลุ่มกล้อง** จึงพุ่งเต็มกรอบเสมอ
และไม่ชนกับ `#trFx` ที่ `trBreakthrough()` ล้างทิ้งทุกครั้งที่ขึ้นชั้นใหม่

กันเงินเฟ้อ: ขั้นสูงสุดให้ทองเสริมรวมราว 80 ทองต่อสตรีค 10 ข้อ = ครึ่งหนึ่งของเคลียร์ชั้นธรรมดา (150)
ทองก้อนนี้ **บวกเข้า `G.totalGoldEarned` ด้วย** (ต่างจากทองรางวัลเควสของ v4.2)
เพราะมันมาจากการตอบถูกในหอคอยจริง ไม่ได้วนกลับไปปั๊มเงื่อนไขของตัวเอง

#### 2) SEAL GATES — ประตูผนึกหน้าบอสชั้น 5/10/15/20

**เกราะผนึกคือ HP ชั้นนอกที่บวกทับ HP บอสเดิม** จึงไม่ต้องแตะสูตร `monsterHpFor` ของ v4.0 เลย
`abSealSetup()` (ห่อ `nextMonster`) บวก `hits × hunterAtk()` เข้าทั้ง `monsterHp` และ `monsterMaxHp`
แล้ว `abSealLeft()` คืนค่า `monsterHp - AB_SEAL.base` เป็นเกราะที่เหลือ

| ชั้น | ระลอกเกราะ (`AB_SEAL_HITS`) |
|---|---|
| 5 · 10 | 2 |
| 15 · 20 | 3 |

- ตอบถูกขณะสตรีค ≥ 3 → ดาเมจต่อเกราะ +60% · สตรีค ≥ 6 → +120% (`AB_PREC_1/2`)
- ตอบผิดขณะเกราะยังตั้งอยู่ → **ผนึกซ่อมตัวเอง** +34% ของเกราะเต็ม (`AB_SEAL_REBIND`) และเสียตรา 💠 ไร้รอย
- กดใช้ไอเทมจากกระเป๋า 1 ชิ้น → ทลายเกราะ 30% (`AB_ITEM_STRIP`) นับเฉพาะตอนของในกระเป๋าลดลงจริง
  เพราะ `useItem` ของ v4.0 มีทางออกก่อนหักของหลายทาง (HP เต็ม · ยังไม่มีนาฬิกา · ตัวเลือกเหลือ 2)
- ทลายสำเร็จได้ 💎 12 เศษคริสตัล

**ดาเมจพิเศษของความแม่นยำต้องหนีบไม่ให้ทะลุลงไปกิน HP จริงของบอส** (`Math.max(AB_SEAL.base, …)`)
ไม่งั้นบอสอาจตายหลังจากที่ v4.0 ตรวจ `monsterHp <= 0` ไปแล้วในทิกเดียวกัน → ชั้นค้างไม่ไปไหนต่อ
ส่วนดาเมจปกติของ v4.0 ปล่อยให้ทะลุได้ตามเดิม (เกราะแตกแล้วดาเมจส่วนเกินลงบอสต่อ ถูกต้องแล้ว)

ฉายาระดับตำนานสองใบ ถูก `TITLES.push()` เข้าไปจากชั้นนี้:
🔓 **ผู้ทลายผนึก** (`sealbreaker`, ครบ 4 บาน) · 💠 **ผนึกไร้รอย** (`unbroken`, ทลาย 1 บานโดยไม่พลาดเลย)
`check` ของทั้งคู่ต้องทนกับ `s.ab` ที่ยังไม่มีได้ (บัญชีเก่าตอน migrate รอบแรก)

**ผนึกไม่โผล่ในโหมดฝึกจุดอ่อน** และไม่โผล่บนชั้นที่ไม่ใช่ชั้นบอส

#### 3) FOG OF WAR + DANGER ALERT

หมอกเป็นแถบ SVG รายชั้นใน `#abFog` ที่ **อยู่ในกลุ่มกล้อง `#trCam`** จึงเลื่อนตามการปีนหอคอย
`abFogPaint()` (ห่อ `trPaint`) ใส่/ถอดคลาส `.clear` ตาม `f <= maxFloor` แล้วปล่อยให้ CSS
ทรานซิชัน `opacity` 1.2 วิ เป็นคนเปิดม่านให้เอง — **อ่านพิกัดผ่าน `trY()` ของ v4.5 เท่านั้น**
จึงย้ายตามโหมดภาพ/โหมดเส้นเองอัตโนมัติโดยไม่ต้องแก้อะไร

สัญญาณอันตราย: `#abDanger` เป็น overlay `position:fixed` + `pointer-events:none` + `z-index:120`
(อยู่ใต้โมดัลของ v4.0 ที่ 200 ขึ้นไป จึงไม่บังหน้าต่างใด และไม่ขวางการกดปุ่ม)
เกณฑ์อยู่ที่ `abDangerLevel()`: HP ≤ 40% หรือตอบผิดติดกัน 3 ข้อ = เหลือง · HP ≤ 25% = แดงกระพริบ

#### 4) ABYSS MODE + SYSTEM CORE

โหมดเหวลึกย่อเวลาต่อข้อเหลือ 62% (`AB_ABYSS_MUL` · พื้นขั้นต่ำ `AB_ABYSS_MIN` 4 วินาที)
ตอบถูกได้ 💎 เศษคริสตัล: พื้นฐาน 1 · ตอบไวกว่า 3.5 วิ +1 · ชั้นบอส +2

**`AB_LIVE` คือสวิตช์ของ "ข้อที่กำลังตอบอยู่" ไม่ใช่ของบัญชี** ถูกล็อกค่าไว้ตอน `startQuestionTimer`
ถ้าอ่านสถานะสด ๆ ใน `questionMs()` การกดสลับโหมดกลางข้อจะทำให้หลอดเวลาที่วิ่งอยู่กับ `limitMs`
ที่ `resolveAnswer` ใช้ตัดเวลาเป็นคนละค่ากันทันที

แกนกลางระบบ 3 เส้น เส้นละ 6 ขั้น (`AB_CORES` · `AB_CORE_COST` = `[8,14,22,32,46,64]` รวมเส้นละ 186):

| แกน | ผลต่อขั้น | เพดาน | ใส่ที่ไหน |
|---|---|---|---|
| 🔮 แกนมานา | ร่ายสกิลแล้วคืน MP +5% | 30% | wrapper ของ `useSkill` (วัดส่วนต่าง `G.mp` ก่อน/หลัง) |
| ⏱️ แกนเวลา | เวลาต่อข้อ +0.4 วิ | +2.4 วิ | wrapper ของ `questionMs` (บวกก่อนคูณตัวหารของเหวลึก) |
| 🪙 แกนโชคลาภ | อัตราทอง +1% | +6% | wrapper ของ `goldMul` |

**สองแกนหลังทำที่ wrapper ของสูตร ไม่ไล่แก้ตามจุดเรียก** ตามกฎเดียวกับ v4.4
(เพดานทอง +6% ของแกนนี้ **แยกก้อนกับ** เพดาน +10% ของคลังอักขระ — รวมกันได้ ไม่ใช่ตัวเดียวกัน)

กันเงินเฟ้อ: 💎 ซื้อด้วยทองไม่ได้และขายเป็นทองไม่ได้ ได้จาก "โหมดที่ยากขึ้น" กับ "การทลายผนึก" เท่านั้น
แกนกลางระบบจึงไม่เป็นงูกินหางกับค่าครองชีพในหอคอย

`AB_NOTIF` มีสารบัญของตัวเอง `ABYSS 01/09`–`09/09` **แยกจาก `SYS_NOTIF` (ตรึง 25) ·
`Q_NOTIF` (ตรึง 6) · `CX_NOTIF` (ตรึง 4) · `CD_NOTIF` (ตรึง 9)** ยืมแค่กลไกโชว์ (`snShowToast` / `snShowGate`) มาใช้

**แจ้งเตือนของชั้นนี้เป็น toast ล้วน ไม่มี gate สักอัน** — จงใจ เพราะทุกจุดที่ยิงอยู่ระหว่าง
นาฬิกาต่อข้อเดินอยู่ หน้าต่างบังคับกดยืนยันจะบังตัวเลือกจนนักเรียนหมดเวลาโดยไม่ได้ทำอะไรผิด
(ขั้น QUAKE I ไม่ยิง toast เลย เขียนลงบรรทัดผลลัพธ์อย่างเดียว เพราะยิงบ่อยมากในการเล่นจริง — กับดักข้อ 1)

#### ข้อมูลที่ชั้นนี้เพิ่ม

```
account.ab = {
  shards: 0,                                   /* 💎 เศษคริสตัลเหวลึก */
  core:   { mp:0, time:0, gold:0 },            /* ขั้นแกนกลางระบบ 0-6 */
  seals:  { '5':{broken,flawless}, '10':…, '15':…, '20':… },
  abyss:  false                                /* สวิตช์โหมดเหวลึกที่จำไว้ข้ามรอบ */
}
```

`a.abv` คือตรารุ่น (กฎเดียวกับ `gcv` / `cxv` / `trv` — ดูกับดักข้อ 8) และเคสนี้
**ไม่มีอะไรให้ backfill** — v4.0-v4.5 ไม่เคยเก็บว่าใครทลายผนึกบานไหน การเดาย้อนหลังจาก
`bossClears` จะแจกฉายาระดับตำนานฟรีให้บัญชีเก่า บัญชีเก่าจึงเริ่มนับจากศูนย์แล้วไปทลายเอาในรอบถัดไป
(หอคอยวนกลับชั้น 1 หลังจบชั้น 20 อยู่แล้ว จึงไม่มีใครถูกตัดสิทธิ์ถาวร)

`abEnsure()` ซ่อมข้อมูลเพี้ยน (เศษติดลบ · ขั้นเกินเพดาน · คีย์แกนที่ไม่มีจริง · ชั้นผนึกที่ไม่มีจริง)
และต้อง idempotent เพราะถูกเรียกทุกครั้งที่อ่าน store ผ่าน `migrateAccount`
· `abSave()` เขียนลง store เอง เพราะ `saveProgress` ของ v4.0 ไม่รู้จักฟิลด์นี้

### ชั้นที่แปด — v4.7 · CARD DRAFTING (การ์ดเวทมนตร์ประจำช่วงชั้น แนว Rogue-lite)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น ABYSS PROTOCOL เริ่มที่ `const CD_VER` ทุกอย่างขึ้นต้นด้วย `cd` / `CD_`

**ชั้นนี้ไม่แก้โค้ด v4.0 / v4.1 / v4.2 / v4.3 / v4.4 / v4.5 / v4.6 เลย — ห่อฟังก์ชันแบบเดียวกับหกชั้นก่อนหน้า**

ฟังก์ชันที่ถูกห่อเพิ่ม: `migrateAccount` `startGame` `saveProgress` `exitGame` `nextMonster`
`startQuestionTimer` `questionMs` `goldMul` `useItem` `useSkill` `onHunterDown` `clearFloor`
`resolveAnswer` `renderStats` `renderStatus` `gcUnlockScreen`

กติกา (**patch ZONE MILESTONE** — ของเดิมจั่วทุกชั้น ยกเลิกไปแล้ว):
**จั่วการ์ดเฉพาะตอนก้าวเข้า "ช่วงชั้นใหม่" เท่านั้น** แล้วการ์ดใบนั้นอยู่กับผู้เล่นตลอดช่วงชั้นนั้น
ไม่มีการ์ดสะสมข้ามช่วง จึงไม่มีทางกองบัฟจนเกมพัง

#### ช่วงชั้น (Zone Band) — หน่วยอายุของการ์ด

ช่วงละ `CD_BAND_SIZE` (5) ชั้น **จบลงที่ด่านบอสประจำช่วงพอดี** เพราะ `isBossFloor` ของ v4.0
คือทุก 5 ชั้นอยู่แล้ว จึงไม่ต้องนิยามชั้นบอสใหม่

| ช่วง | ชั้น | รอยต่อ (เปิดหน้าต่างจั่ว) | ด่านบอสที่ปิดฉากการ์ด |
|---|---|---|---|
| 0 | 1-5 | ชั้น 1 | ชั้น 5 |
| 1 | 6-10 | ชั้น 6 | ชั้น 10 |
| 2 | 11-15 | ชั้น 11 | ชั้น 15 |
| 3 | 16-20 | ชั้น 16 | ชั้น 20 |

ตัวช่วยทั้งชุดคำนวณจาก `FLOOR_MAX` เอง ไม่มีเลขชั้นฮาร์ดโค้ดไว้ที่ไหน:
`cdBandOf(floor)` · `cdBandFrom(band)` · `cdBandTo(band)` · `cdBandText(floor)` ·
`cdIsMilestone(floor)` (1/6/11/16) · `cdIsBandBoss(floor)` (5/10/15/20) · `cdNextDraftFloor(floor)`

**หอคอยวนกลับชั้น 1 หลังจบชั้น 20** `cdNextDraftFloor(20)` จึงคืน 1 ไม่ใช่ 21

#### จุดดักมีจุดเดียว คือ `nextMonster` → `cdCheckZoneMilestone()`

ทุกเส้นทางที่พาผู้เล่นไปยืนบนชั้นใหม่ (เคลียร์ชั้น · ประตูวาปหลังบอส · ร่วงเพราะ HP หมด ·
รูเล็ตข้ามชั้น · ตาชั่งดึงลง · GM ย้ายชั้น · ออกจากโหมดฝึก) **ไหลมาลงที่ `nextMonster` ทั้งหมด**
wrapper จึงยังเช็กแค่ที่นี่ที่เดียว แต่ตัวตัดสินย้ายไปอยู่ที่ `cdCheckZoneMilestone()` แล้ว
(Zone Milestone Event — จุดลงทะเบียนเหตุการณ์เพียงจุดเดียวของระบบจั่ว)

```js
nextMonster = function () {
  if (cdCheckZoneMilestone()) return;   /* รอยต่อช่วงชั้นเท่านั้น — ยังไม่ให้ v4.0 ทำงาน */
  return _cdNextMonster.apply(this, arguments);
};
```

`cdNeedDraft()` เหลือทางเข้าแค่สองทาง **และทั้งคู่คือรอยต่อของช่วงชั้นทั้งสิ้น**

1. ยืนอยู่คนละช่วงกับช่วงที่จั่วไว้ (`CD_BAND !== cdBandOf(G.floor)`) → ก้าวข้ามรอยต่อมาจริง ๆ
   ครอบทั้งการเคลียร์บอสขึ้นช่วงใหม่ · ถูกดึงข้ามช่วง · และการล็อกอินกลับเข้ามา
2. ยืนอยู่ชั้นแรกของช่วง (1/6/11/16) โดยมือเปล่า → ร่วงกลับต้นช่วงแล้วเริ่มช่วงนั้นใหม่

**ระหว่างกลางช่วงชั้นจะไม่มีหน้าต่างจั่วเด้งเด็ดขาด ต่อให้การ์ดสลายไปก่อนก็ตาม**
เลือกการ์ดเสร็จแล้ว `cdPick()` เรียก `nextMonster()` อีกรอบ คราวนี้ `cdNeedDraft()` เป็น false
จึงไหลลงชั้นล่างตามปกติ — **หน้าต่างจั่วจึงเปิดตอนที่ยังไม่มีนาฬิกาเดินเสมอ** (กับดักข้อ 18)

**การ์ดจะ "มีผล" ก็ต่อเมื่อ `CD_BAND === cdBandOf(G.floor)` เท่านั้น** (`cdActive()`)
เช็กช่วงชั้นสดทุกครั้งแทนการไล่ล้างตามจุด → ทุกทางที่พาข้ามรอยต่อทำให้การ์ดหมดอายุเองอัตโนมัติ
ส่วนการเดินขึ้นชั้นถัดไป **ในช่วงเดียวกัน** การ์ดยังอยู่ครบตามกติกาใหม่

**`CD_BAND` กับ `CD_CARD` แยกกันโดยตั้งใจ** — `CD_BAND` คือ "ตราช่วงชั้นที่จั่วไปแล้ว"
`cdClear()` ล้างแค่การ์ด (`CD_CARD` / `CD_ST`) **ไม่แตะตรา** หน้าต่างจั่วจึงไม่เด้งซ้ำกลางช่วง
ตอนการ์ดสลายก่อนเวลา (ร่วงเพราะ HP หมด · ⚖️ ตาชั่ง · 🎲 รูเล็ต · 🚑 กู้คืนของ GM)
มีแต่ `cdZoneCleanup()` เท่านั้นที่ปลดตรา (`CD_BAND = -1`)

#### Zone Cleanup Routine — `cdZoneCleanup()`

เรียกจาก wrapper ของ `clearFloor` **เฉพาะตอน `cdIsBandBoss(ชั้นที่เพิ่งเคลียร์)` เป็นจริง**
(เคลียร์ด่านบอสประจำช่วงสำเร็จ = จบช่วงชั้น) ล้างคืนค่าเริ่มต้นทั้งชุด: การ์ด · `CD_ST`
(ตัวคูณ · Flag ห้ามใช้ไอเทม/ห้ามฟื้น) · โทเคน 🧘 · ตราช่วงชั้น · คลาส UI (`cd-on` `cd-blind` `cd-noitem`)
เคลียร์ชั้นกลางช่วงจะเรียกแค่ `cdPaintUi()` การ์ดอยู่ต่อ

**รางวัลของการ์ดยังจ่าย "ทุกชั้นที่เคลียร์ได้" เหมือนเดิม** ค่าครองชีพจึงเท่ารุ่นก่อนเป๊ะ —
รุ่นก่อนจั่วชั้นละใบจ่ายชั้นละครั้ง · ตอนนี้จั่วช่วงละใบแล้วจ่ายทุกชั้นในช่วง ผลรวมเท่ากัน
ต่างกันแค่ผู้เล่นเลือกน้อยลงแต่ต้องรับผลของใบเดียวให้ครบทั้งช่วง **อย่าเปลี่ยนไปจ่ายก้อนเดียวตอนจบช่วง**
ไม่งั้นสายแลกเปลี่ยนจะเสียเปรียบสายประคองเกมทันที (ของเสียมีตลอด 5 ชั้น แต่ของดีได้ครั้งเดียว)

#### กฎแบ่งบาลานซ์: ผลต่อเนื่อง vs โควตารายชั้น — `cdFloorRollover()`

**อายุการ์ดยาวขึ้น 5 เท่า แต่ "พลังต่อชั้น" ต้องเท่ารุ่นจั่วรายชั้นเป๊ะ** patch นี้ตั้งใจเปลี่ยนแค่
*ตอนไหนได้เลือก* ไม่ได้เปลี่ยน *เลือกแล้วแรงแค่ไหน* จึงแบ่งผลของการ์ดเป็นสองประเภท

| ประเภท | ตัวอย่าง | อายุ |
|---|---|---|
| ผลต่อเนื่อง | ตัวคูณเวลา/ทอง/ดาเมจ · Flag ห้ามใช้ไอเทม/ห้ามฟื้น · พลังสะสมของ 🩸 | ตลอดช่วง |
| โควตารายชั้น | โล่ 🛡️ 1 ใบ · สถานะไร้ที่ติของ ⚡/💀 · ค่าผ่านทาง HP ของ 🩸 · ตัวนับ `hit`/`miss` | รีเซ็ตทุกชั้น |

`cdFloorRollover()` ถูกเรียกจาก wrapper ของ `clearFloor` **เฉพาะตอนเคลียร์ชั้นกลางช่วง**
(จบช่วงใช้ `cdZoneCleanup()` แทน) และต้องมา **หลัง** `cdFloorReward()` เสมอ
เพราะรางวัลอ่าน `st.perfect` / `st.hit` ของชั้นที่เพิ่งจบ

**ถ้าไม่มีตัวนี้จะพังสามใบพร้อมกัน** — ⚡/💀 กลายเป็นใบตาย (ไร้ที่ติ 5 ชั้นติดเป็นไปไม่ได้สำหรับ A1
ทั้งที่ยังรับดาเมจ ×3 ครบทุกชั้น) · 🛡️ เหลือโล่ใบเดียวต่อ 5 ชั้น · 🩸 จ่าย 15 HP ครั้งเดียว
แล้วถือ +120% ยาว ๆ กลายเป็นใบฟรีของช่องกลางที่ไม่มีของเสียเลย

🩸 เป็นใบเดียวที่ผลสะสม (`st.atk`) ข้ามชั้นได้ **เพราะจ่ายค่าผ่านทางทุกชั้นแล้ว** —
ถ้าจะแก้ให้ล้าง `st.atk` รายชั้น ต้องเอาค่าผ่านทางรายชั้นออกด้วย ไม่งั้นเก็บเงินซ้ำแต่ไม่ได้ของ

`CD_BOOT` ปิดการจั่วไว้ตลอดช่วงที่ `startGame` ของ v4.0 ยังทำงาน (มันเรียก `nextMonster` เองข้างใน)
แล้ว wrapper ค่อยจั่วให้หลังอ่านค่าจาก store ครบ · `CD_SKIP` คือชั้นที่ถูกสั่งข้ามการจั่ว
ใช้เป็นทางออกฉุกเฉินของปุ่ม 🚑 กู้คืนในแผง GM (ไม่งั้น `gcUnlockScreen` จะเรียก `nextMonster`
แล้วเปิดหน้าต่างเดิมกลับมาเป็นวงจรปิด) — ปุ่มนี้ปั๊ม `CD_BAND` ของชั้นปัจจุบันไว้ด้วย
ไม่งั้นชั้นถัดไปในช่วงเดียวกันจะเด้งหน้าต่างเดิมกลับมาอีก

#### การ์ดต้องรอดข้ามการล็อกอิน — `account.cd.cur`

ช่วงชั้นหนึ่งกินเวลาหลายสิบนาที นักเรียนปิดเกมกลางช่วงเป็นเรื่องปกติ **จึงเซฟการ์ดที่ถืออยู่ลง store**

```
account.cd.cur = { band: 0-3, id: 'edge' | null, st: {...CD_ST} } | null
```

- `id` เป็นชื่อการ์ด → กลับมาได้ใบเดิมพร้อมสถานะรายชั้นครบ (`ward` `atk` `perfect` `hit` `miss`)
- **`id: null` แต่ `band` ยังอยู่ = "จั่วช่วงนี้ไปแล้วแต่การ์ดสลาย"** ต้องจำไว้ด้วย
  ไม่งั้นตั้งใจร่วง/กด 🎲 แล้วล็อกอินใหม่จะได้จั่วซ้ำกลางช่วง (รีโรลฟรี)
- `cur` เป็น **null** = ยังไม่ได้จั่วช่วงไหนเลย

`cdCurSnapshot()` สร้างภาพนิ่งจากตัวแปรสด · `cdRestore(cur)` คืนกลับเข้ามือ
**ต้องเรียกก่อน `cdNeedDraft()` ใน wrapper ของ `startGame`** ไม่งั้นจะจั่วใบใหม่ทับใบที่ยังไม่หมดอายุ
· `cdEnsure()` ล้างข้อมูลเพี้ยนของ `cur` ด้วย (ชื่อการ์ดที่ไม่มีจริง · ช่วงนอกช่วง 0-3 · `atk` เกินเพดาน)

**`cur` เป็นสถานะสด ไม่ใช่ยอดสะสม** `cdSave()` จึงเขียนทับตรง ๆ ไม่ใช้ `Math.max` แบบตัวนับตัวอื่น
(การ์ดสลายแล้วต้องกลายเป็น `id: null` จริง ๆ ไม่ใช่ค้างใบเดิมไว้)

**ลำดับใน wrapper ของ `exitGame` สำคัญมาก** — `cdSave()` → **`CD_READY = false`** → `cdReset()`
→ ค่อยเรียกของเดิม เพราะ `exitGame` ของ v4.0 เรียก `saveProgress()` ต่อข้างใน
ถ้าปิดสวิตช์ทีหลัง `cdSave()` รอบนั้นจะเขียน `cur` ทับเป็น `null` (เพราะ `cdReset()` ล้าง `CD_BAND` ไปแล้ว)
= การ์ดหายทุกครั้งที่ออกจากเกม ซึ่งเป็นอาการเดียวกับกับดักข้อ 16 แต่คนละฟังก์ชัน

#### คลังการ์ด 17 ใบ — สุ่มช่องละ 1 ใบจากสายของช่องนั้น

| ช่อง | สาย | ใบ | id |
|---|---|---|---|
| 1 (ซ้าย) | ประคองเกม | 5 | `ward` `focus` `mana` `dew` `merchant` |
| 2 (กลาง) | แลกเปลี่ยน | 5 | `shackle` `blood` `chrono` `seal` `edge` |
| 3 (ขวา) | วัดใจ | 7 | `curse` `exec` `scales` `vow` `sand` `roulette` `inferno` |

ค่าบาลานซ์ทุกตัวอยู่เป็นค่าคงที่ก้อนเดียวเหนือ `CD_CARDS` (`CD_SHACKLE_CUT` `CD_BLOOD_STEP`
`CD_EXEC_LOSS` …) **แก้ตัวเลขที่นั่นที่เดียว** ข้อความบนหน้าการ์ดประกอบจากค่าคงที่พวกนี้เอง
จึงไม่มีทางที่ตัวเลขบนการ์ดกับตัวเลขที่ทำงานจริงจะไม่ตรงกัน

| ใบ | ผลดี | ผลเสีย |
|---|---|---|
| 🛡️ `ward` | ตอบผิดครั้งแรก **ของแต่ละชั้น** ไม่เสีย HP + สตรีคไม่ขาด | โล่แตกแล้วต้องรอเคลียร์ชั้นถึงก่อใหม่ |
| 🧘 `focus` | แช่แข็ง 5 วิแรกของทุกข้อ | ไม่ได้ต่อเวลา |
| 🌀 `mana` | ตอบถูกคืน MP 5% ของหลอด | — |
| 🌿 `dew` | สตรีคครบทุก 3 ข้อ ฟื้น HP 10% | สตรีคขาดต้องเริ่มใหม่ |
| 🪙 `merchant` | ทองจากการตอบถูก ×1.25 | — |
| ⛓️ `shackle` | ทุกชั้นที่เคลียร์ในช่วง ได้ทอง ×2 | เวลาต่อข้อ −30% ทั้งช่วง |
| 🩸 `blood` | ตอบถูกสะสมพลังโจมตี +12%/ข้อ (เพดาน +120% · สะสมข้ามชั้นในช่วง) | จ่าย 15 HP **ทุกครั้งที่เข้าชั้นใหม่** |
| 👁️‍🗨️ `chrono` | ทุกชั้นที่เคลียร์ในช่วง ได้ EXP ×2 | ซ่อนนาฬิกาทั้งช่วง (เวลายังเดิน) |
| 🚫 `seal` | ทุกชั้นที่เคลียร์ในช่วง ได้ไอเทมฟรี 1 ชิ้น | ห้ามใช้ไอเทมทั้งช่วง · โอกาสคริต −10 จุด% |
| 🗡️ `edge` | ตอบถูกลงดาเมจ ×2 | ตอบผิดรับดาเมจ ×1.5 |
| ⚡ `curse` | ชั้นไหนไร้ที่ติ = ไอเทมสุ่มพิเศษของชั้นนั้น (นับใหม่ทุกชั้น) | ตอบผิดรับดาเมจ ×3 ตลอดช่วง |
| 🗡️ `exec` | ตอบถูกใน 3 วิ = สังหารทันที | ตอบผิดเสีย HP รวม 50% ของหลอด |
| ⚖️ `scales` | ตอบถูกฟื้น HP เต็ม + 💎 3 | ตอบผิดข้อเดียว ร่วงต้นโซนทันที |
| 💀 `vow` | ชั้นไหนไร้ที่ติ = ทอง/EXP ×5 ของชั้นนั้น + ฉายาลับ | เริ่มช่วงด้วย HP 1 · ห้ามฟื้นทุกทางตลอดช่วง |
| ⏳ `sand` | ตอบถูกได้ 💎 2 + ทอง ×2 | เวลา 3 วิ/ข้อ · พลาดเสีย HP 80% |
| 🎲 `roulette` | สุ่มได้ 💎 4 หรือทะลุขึ้นชั้นถัดไป | หรือเสีย HP 50% (โอกาสเท่ากันสามหน้า) |
| 🔥 `inferno` | ตอบถูกลงดาเมจ ×3 · ชั้นบอสเผาเกราะผนึกทิ้ง 50% ตอนเริ่มต่อสู้ | ตอบผิดเสีย HP ×3 |

**สองใบที่ถูกปรับใน Balance Patch v4.7.4 — แก้ตัวเลขที่ `CD_INF_SEAL` / `CD_SEAL_CRIT`**

- 🔥 `inferno` เดิม "ตอบถูกแล้วเผาเกราะผนึกทั้งบานทันที" ทำให้ด่านบอสประจำช่วงกลายเป็นด่านเปล่า
  ตอนนี้เผา `CD_INF_SEAL` (50%) **ครั้งเดียวตอนบอสปรากฏตัว** ผ่าน `cdInfernoSealStrike()`
  ซึ่งยิงจาก wrapper ของ `nextMonster` **หลัง** `_cdNextMonster` ทำงานจบ (ต้องรอ `abSealSetup()`
  ของ v4.6 สร้างเกราะก่อน ยิงก่อนจะไม่มีอะไรให้เผา) · ปั๊มธง `AB_SEAL.infernoBurned` กันเผาซ้ำ
  · ที่เหลือทลายผ่านทางเดิมทั้งหมด ตอบผิดแล้วผนึกยังซ่อมตัวเอง (`AB_SEAL_REBIND`) ตามกติกา v4.6
- 🚫 `seal` เพิ่มผลลบกันสายดองไอเทม: หักโอกาสคริติคอล `CD_SEAL_CRIT` (10) **จุดเปอร์เซ็นต์**
  ใส่ที่ wrapper ของ `critChance` จุดเดียวตามกฎของ v4.4/v4.6 (หนีบไม่ให้ต่ำกว่า 0)
  ทั้งการสุ่มคริตตอนตอบถูกและแถว AGI ในแผงสถานะจึงเห็นค่าที่หักแล้วตรงกันโดยไม่ต้องไล่แก้

**ทองของ `shackle` / `vow` บวกเข้า `G.totalGoldEarned` ด้วย** (กฎเดียวกับออโรร่าของ v4.6)
เพราะมาจากการไต่หอคอยจริง ไม่ได้วนกลับไปปั๊มเงื่อนไขของตัวเองแบบทองรางวัลเควสของ v4.2
· 💎 ใช้ก้อนเดียวกับชั้น v4.6 (`account.ab.shards`) ไม่สร้างสกุลเงินใหม่ให้ต้องบาลานซ์อีกอัน
· 🎲 ชั้นที่ถูกข้าม **ไม่ได้ทองและ EXP รางวัลเคลียร์ชั้น** ตัวการข้ามคือรางวัลของมันเอง

💀 ยังโหดที่สุดอยู่ดีเพราะ **HP 1 ตลอดช่วง** — พลาดข้อเดียวคือร่วง (ประกันภัยก็ถูกบล็อก)
คนที่ยังยืนอยู่จึงแทบจะไร้ที่ติโดยอัตโนมัติ การรีเซ็ต `perfect` รายชั้นเลยแทบไม่ช่วยใบนี้
แต่ช่วย ⚡ เต็ม ๆ ซึ่งเป็นใบที่รอดจากความผิดพลาดได้ **ถ้าจะผ่อนอีก ให้ผ่อนที่ค่าคงที่
`CD_VOW_MUL` / `CD_BLOOD_HP` / `CD_CURSE_TAKE` อย่าไปยืดอายุโควตารายชั้นให้ยาวทั้งช่วง**

#### ผลของการ์ดถูกใส่ผ่านตัวกลาง 4 ตัวเท่านั้น

`cdHitMonster` (ดาเมจใส่มอนสเตอร์) · `cdExecute` (สังหารทันที) · `cdHurt` (ดาเมจใส่ฮันเตอร์)
· `cdHeal` (ฟื้น HP · เคารพ Flag ห้ามฟื้น) — **ห้ามบวก/ลบ HP ตรง ๆ ในโค้ดของการ์ด** ดูกับดักข้อ 19

ทุกใบคูณจาก **ส่วนต่างจริง** ที่ v4.0 ทำไปแล้ว (ดาเมจที่ลงไปแล้ว · HP ที่เสียไปแล้ว)
ไม่ได้คำนวณสูตร ATK/VIT/คริตซ้ำเอง จึงไม่พังถ้าสูตรพวกนั้นถูกแก้ทีหลัง

โบนัสอัตราส่วนใส่ที่ **wrapper ของสูตร** ตามกฎเดียวกับ v4.4/v4.6:
`questionMs` (⛓️ ⏳) · `goldMul` (🪙 ⏳) — ไม่ไล่แก้ตามจุดเรียก

#### ข้อกำหนดกันบั๊ค 8 ข้อ อยู่ตรงไหนบ้าง

| ข้อ | เรื่อง | ทำที่ไหน |
|---|---|---|
| 1 | ซ่อนนาฬิกาด้วย CSS ห้ามหยุด Timer | คลาส `#gameScreen.cd-blind` เท่านั้น · ไม่มีบรรทัดไหนแตะ `QUESTION_TIMER` |
| 2 | ห้ามเขียนทับค่าเวลาฐาน | wrapper `questionMs` คำนวณจากค่าที่ชั้นล่างคืนมาทุกครั้ง |
| 3 | หัก HP ต้อง `Math.max(1, …)` | `cdPick()` ตอนจ่ายค่า 🩸 / 💀 |
| 4 | Flag ห้ามใช้ไอเทม/ห้ามฟื้น | บล็อกที่ `useItem` `useSkill` `onHunterDown` (ตัวฟังก์ชันกลาง ไม่ใช่ปุ่ม UI) |
| 5 | ย้ายชั้นกลางคันต้อง Abort ข้อปัจจุบันก่อน | `cdAbortQuestion()` ถูกเรียกก่อนเสมอใน `cdDropZone` / `cdSkipFloor` |
| 6 | `questionStart` ต้องรีเซ็ตทุกข้อ | v4.0 ทำใน `startQuestionTimer` อยู่แล้ว · 🧘 เลื่อนจุดเริ่มนับอีกทีตอนน้ำแข็งละลาย |
| 7 | โล่ต้องเป็น Mutex ใบเดียว | `CD_ST.ward = false` ถูกเซ็ต **บรรทัดแรก** ก่อนทำอย่างอื่น |
| 8 | ปัดเลข + ล้างสถานะตอนจบช่วงชั้น | `Math.round()` ทุกจุด · `cdZoneCleanup()` ใน wrapper ของ `clearFloor` (เฉพาะชั้นบอสประจำช่วง) ถอดคลาส UI คืนครบ |

#### หน้าจอ

หน้าต่างจั่วคือ `#cdDraft` (ใช้โครง `.g-modal` ของ v4.0) **ไม่มีปุ่มปิดโดยตั้งใจ**
ทางออกฉุกเฉินคือปุ่ม 🚑 กู้คืนในแผง GM · `.g-modal-inner` ของหน้าต่างนี้ใช้ `max-height:92vh`
(โมดัลอื่นใช้ 84vh) เพราะเป็นการเลือกบังคับ ไม่มีอะไรอื่นให้ดูพร้อมกัน

**การ์ดสามใบต้องพยายามให้เห็นครบในจอเดียว** วัดจริงแล้ว (ชุดการ์ดยาวสุด `focus`/`blood`/`sand`
สูง 844): จอ 360 / 375 / 390 / 412 / 430 พอดีเป๊ะ (เกิน 0px) · จอ 320 ล้น 14px
จึงมีป้าย `#cdMore` "▼ เลื่อนลงดูการ์ดให้ครบ 3 ใบ" โผล่ให้

**วัดความสูงต้องเปิดหน้าเพจใหม่ต่อหนึ่งความกว้าง** ถ้าใช้ `setViewportSize()` ไล่วัดในหน้าเดียว
คลาส `cd-scroll` ของจอก่อนหน้าจะค้าง แล้วป้าย `#cdMore` ที่โผล่อยู่บวกความสูงทบเข้าไปอีก 9-10px
(ตัวเลขจะดูเหมือนจอกว้างล้นมากกว่าจอแคบ ซึ่งเป็นไปไม่ได้)
`cdScrollHint()` **วัดจาก `scrollHeight` ของจริงทุกครั้ง ไม่ใช้ media query เดา**
เพราะความสูงการ์ดขึ้นกับความยาวข้อความของใบที่สุ่มได้ ถ้าเพิ่มบรรทัดในการ์ดต้องวัดใหม่

แบดจ์ `#cdBadge` เป็นลูกของ `#gameScreen` (จึงถูกซ่อนเองตอนออกจากเกท) วางแบบ `position:fixed`
มุมซ้ายล่าง และ `#gameScreen.cd-on` ตั้ง `padding-bottom:56px` เผื่อที่ให้มันเสมอ
**แถวปุ่มล่างจึงไม่โดนทับ** — ถ้าเปลี่ยนความสูงแบดจ์ต้องขยับ padding ตามด้วย
แตะแบดจ์แล้วโชว์รายละเอียดการ์ดเป็น **toast ไม่ใช่ `snOpenPanel`** เพราะแบดจ์กดได้ตอนนาฬิกาเดิน

แบดจ์มีสถานะที่สาม `.cd-badge.spent` (เทา · `disabled`) สำหรับ **"มือเปล่ากลางช่วงชั้น"**
คือจั่วของช่วงนี้ไปแล้วแต่การ์ดสลายก่อนเวลา บรรทัดล่างบอกชั้นที่จะได้จั่วใบใหม่ (`cdNextDraftFloor`)
**อย่าตัดทิ้ง** ไม่งั้นนักเรียนจะเข้าใจว่าระบบจั่วพัง แล้วครูจะโดนถาม

#### ภาพประกอบการ์ด (Asset Mapping v4.7.3)

การ์ดแต่ละใบมี **สองฟิลด์ที่คู่กันและห้ามสลับหน้าที่กัน**

| ฟิลด์ | คืออะไร | ใครอ่าน |
|---|---|---|
| `image` | ที่อยู่ไฟล์ต้นฉบับ `assets/cards/<ชื่อการ์ดภาษาไทย>.jpg` | `embed_card_art.py` เท่านั้น |
| `img` | data URI (WebP base64) ที่ฝังจริง | `cdArt()` → หน้าต่างจั่ว |

**ชื่อไฟล์คือชื่อการ์ดภาษาไทยเป๊ะ ๆ** (ตรงกับฟิลด์ `name`) นามสกุลไม่ใช่ตัวชี้ขาด —
`resolve()` ของสคริปต์ลองทั้ง `.jpg/.jpeg/.png/.webp` และลองทั้ง `assets/cards/` กับรากrepo
เพราะเจ้าของ repo อัปโหลดผ่านเว็บ GitHub ไฟล์จึงมาโผล่ที่รากได้ ย้ายเข้าโฟลเดอร์แล้วก็ยังหาเจอ

**ตัวเกมไม่เคยอ่าน `image` ตอนรัน** — กฎไฟล์เดียวจบห้ามอ้าง path ภายนอก (กติกาเดียวกับ
`.jpg` ต้นฉบับของสกิล/ประตูวาปที่วางไว้รากrepo) `image` มีหน้าที่เดียวคือเป็น **สารบัญ**
ให้สคริปต์ฝังภาพรู้ว่าการ์ดใบไหนใช้ไฟล์ไหน สารบัญจึงอยู่ที่เดียวไม่ต้องซิงก์สองที่

**ไฟล์ต้นฉบับการ์ดทั้ง 17 ใบเคยถูกลบออกจาก repo** (Asset Replacement Patch · ส.ค. 2026)
**แล้วถูกอัปโหลดกลับเข้ามาที่รากrepo ทีหลัง — ตอนนี้อยู่ครบทั้ง 17 ไฟล์**
ภาพที่ฝังในฟิลด์ `img` ไม่ได้ผูกกับต้นฉบับอยู่แล้ว ต่อให้หายอีกรอบการ์ดก็ยังขึ้นจอเหมือนเดิม —
**ฟิลด์ `image` ยังต้องอยู่** เพราะมันคือสารบัญที่บอกว่าใบไหนต้องใช้ไฟล์ชื่ออะไร
ถ้าจะฝังภาพการ์ดใหม่ ให้วางไฟล์ตามชื่อในสารบัญแล้วค่อยรันสคริปต์
(`resolve()` หาให้ทั้งที่ `assets/cards/` และรากrepo อยู่แล้ว)

`img` ว่าง = ยังไม่ได้ฝังภาพใบนั้น การ์ดตกกลับไปใช้อีโมจิ `card.icon` เหมือนรุ่นก่อนทั้งใบ
**ไม่มีรูปแตกและไม่มีคำขอเน็ตออกไปข้างนอก** — ฝังทีละใบก็ได้ ไม่ต้องครบ 17 ใบพร้อมกัน

**เตรียมภาพด้วย `embed_card_art.py`** (วางไฟล์ตามชื่อในฟิลด์ `image` แล้วสั่งรัน)
ย่อกว้างไม่เกิน 300px → WebP q80 → base64 → เขียนกลับลงฟิลด์ `img` ให้เอง
ไล่ลดคุณภาพตามบันได `LADDER` อัตโนมัติถ้าไฟล์รวมจะทะลุ 2MB · `--clear` ถอดภาพออกทั้งชุด
· ใส่ id ท้ายคำสั่งเพื่อฝังเฉพาะบางใบ · วัดจริงแล้วครบ 17 ใบกินพื้นที่ราว 557KB
(ไฟล์รวมตอนนี้ ~1.69MB ถ้าถอดการ์ดออกทั้งชุดจะเหลือ ~1.14MB)

**ไฟล์ต้นฉบับเป็น "หน้าการ์ดเต็มใบ" ไม่ใช่ภาพประกอบลอย ๆ** (350×~470 · 3:4 แนวตั้ง ·
มีกรอบลายรูนของตัวเอง) จึงต้องใช้ `object-fit:contain` ชิดขวา **ห้ามใช้ `cover`**
ไม่งั้นจะเหลือแต่แถบกลางภาพจนดูไม่ออกว่าเป็นการ์ด (เจอมาแล้วรอบแรก)

**ภาพถูกวาง 2 จุด และทั้งคู่ไม่กินความสูงของการ์ดเพิ่ม** เพราะหน้าต่างนี้ตึงมาก (ดูหัวข้อถัดไป)

- `.cd-art` หน้าการ์ดเต็มใบชิดขวา `position:absolute` จึงไม่ดันเลย์เอาต์เลยแม้แต่พิกเซลเดียว
- `.cd-ico.art` ย่อเป็นตราทรงการ์ดแนวตั้ง **สูงเท่ากล่องอีโมจิเดิมเป๊ะ**
  (23×30px · จอ ≤400px 17×23px เท่าอีโมจิ 23px พอดี)

**ขอบซ้ายของภาพต้องจางหายไปเอง ห้ามเอาแผ่นทึบมาถมทับ** — พื้นการ์ดเป็นเกรเดียนต์คนละสี
ในแต่ละสาย (`t1`/`t2`/`t3`) แผ่นทึบสีเดียวจึงขึ้นเป็น**เส้นคาดตั้ง**ให้เห็นชัดตรงรอยต่อ (เจอมาแล้ว)
ตอนนี้ใช้ `mask-image` ไล่ระดับกับตัวภาพ + ม่าน `.cd-art-veil` ที่เริ่มจากอัลฟา **0 พอดี**
ตรงขอบซ้าย · ใส่ทั้ง `-webkit-mask-image` และแบบไม่มี prefix (กับดักข้อ 15) เบราว์เซอร์ที่ไม่รู้จัก
จะได้ภาพจาง ๆ ไม่มี mask ซึ่งยังดูดีอยู่ ไม่ใช่จอพัง

วัดจริงบนชุดการ์ดยาวสุด (`focus`/`blood`/`sand`) หลังฝังภาพครบ: ความสูงเกินกรอบ
จอ 320 = 14px · จอ 360 / 375 / 390 / 412 / 430 / 768 = 0px ทุกจอ —
**เท่ากับรุ่นก่อนฝังภาพเป๊ะทุกความกว้าง** เพราะตราภาพถูกตั้งให้สูงเท่ากล่องอีโมจิเดิมพอดี
**ถ้าจะขยายตราต้องวัดใหม่ทั้งชุด**

#### ข้อมูลที่ชั้นนี้เพิ่ม

```
account.cd = {
  drafts:  0,             /* จั่วไปแล้วกี่ครั้ง */
  vow:     0,             /* 💀 พันธสัญญาโลหิตทมิฬสำเร็จกี่ครั้ง (เงื่อนไขฉายาลับ) */
  picks:   { <id>: n },   /* เลือกใบไหนไปกี่ครั้ง */
  cleared: { <id>: n },   /* ผ่านชั้นด้วยใบไหนไปกี่ครั้ง */
  cur:     { band, id, st } | null   /* การ์ดที่ถืออยู่ตอนนี้ — ต้องรอดข้ามการล็อกอิน */
}
```

`a.cdv` คือตรารุ่น (กฎเดียวกับ `gcv` / `cxv` / `trv` / `abv` — ดูกับดักข้อ 8) และเคสนี้
**ไม่มีอะไรให้ backfill** เพราะ v4.0-v4.6 ไม่เคยมีระบบการ์ด บัญชีเก่าเริ่มนับจากศูนย์ทุกคน

`cdEnsure()` ต้อง idempotent เพราะถูกเรียกทุกครั้งที่อ่าน store ผ่าน `migrateAccount`
· `cdSave()` เขียนลง store เอง เพราะ `saveProgress` ของ v4.0 ไม่รู้จักฟิลด์นี้ และมีสวิตช์
`CD_READY` แบบเดียวกับ `AB_READY` ของ v4.6 (กับดักข้อ 16) · `cdOf()` เป็น **read-only**
คืน `null` ถ้ายังไม่มี ห้ามสร้างก้อนเปล่าให้เอง

ฉายาลับ 💀 **ผู้สาบานโลหิต** (`bloodvow`) ถูก `TITLES.push()` เข้าไปจากชั้นนี้
`check` ต้องทนกับ `s.cd` ที่ยังไม่มีได้ (บัญชีเก่าตอน migrate รอบแรก)

`CD_NOTIF` มีสารบัญของตัวเอง `CARD 01/09`–`09/09` (ใบที่ 9 คือ `CD_INFERNO` ของแพตช์ v4.7.4)
**แยกจาก `SYS_NOTIF` (ตรึง 25) ·
`Q_NOTIF` (ตรึง 6) · `CX_NOTIF` (ตรึง 4) · `AB_NOTIF` (ตรึง 9)** และเป็น **toast ล้วนทั้งชุด**

**โหมดฝึกจุดอ่อนไม่จั่วการ์ดและการ์ดไม่มีผล** (`cdActive()` คืน `null` ทันทีที่ `practiceMode`)

**💀 พันธสัญญาโลหิตทมิฬห้ามฟื้นพลัง "ทุกทาง" รวมจังหวะเคลียร์ชั้นด้วย** — พออยู่ยาวทั้งช่วงชั้น
ผู้เล่นจะเดินผ่านจุดที่ v4.0 ฟื้น HP ให้ถึง 4 ครั้ง (INT ฟื้นตอนผ่านชั้น + `levelUpCheck()`
ที่เซ็ต `hp = maxHp` ตอนเลเวลอัป ซึ่งรางวัล EXP ของการ์ดเองก็เรียกซ้ำอีกรอบ)
wrapper ของ `clearFloor` จึงหนีบ `g.hp` กลับเป็นค่าก่อนเคลียร์ **หลังจ่ายรางวัลเสร็จแล้วเสมอ**
หนีบก่อนจ่ายจะโดนเลเวลอัปของรางวัลฟื้นกลับให้เต็มหลอด (รุ่นจั่วรายชั้นไม่เคยเจอเพราะการ์ดหมดอายุพร้อมชั้น)

### ชั้นที่เก้า — v4.8 · HARDCORE DAMAGE (สเกลดาเมจตามช่วงชั้น + เพดานความเสียหาย)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น CARD DRAFTING เริ่มที่ `const HD_VER` ทุกอย่างขึ้นต้นด้วย `hd` / `HD_`

**ชั้นนี้ไม่แก้โค้ด v4.0-v4.7 เลย — ห่อฟังก์ชันแบบเดียวกับเจ็ดชั้นก่อนหน้า**
ฟังก์ชันที่ถูกห่อเพิ่มมีแค่ **สองตัว**: `wrongDamage` `resolveAnswer`

#### ตารางดาเมจ — ค่าคงที่ก้อนเดียวเหนือทุกอย่าง

ดาเมจฐานเลิกเป็นค่าคงที่ `15` ของ v4.0 แล้ว มาไล่ระดับตามช่วงชั้นแทน (ช่วงละ `HD_BAND_SIZE` = 5 ชั้น
คำนวณจาก `FLOOR_MAX / HD_BASE.length` เอง **ไม่มีเลขชั้นฮาร์ดโค้ดไว้ที่ไหน**)

| ช่วง | ชั้น | ตอบผิด | หมดเวลา ×2 | บอส ×2.5 | บอส + หมดเวลา |
|---|---|---|---|---|---|
| 1 | 1-5 | 20 | 40 | 50 (ชั้น 5) | 100 |
| 2 | 6-10 | 35 | 70 | 87 (ชั้น 10) | 175 |
| 3 | 11-15 | 45 | 90 | 112 (ชั้น 15) | 225 |
| 4 | 16-20 | 55 | 110 | 137 (ชั้น 20) | 275 |

ค่าคงที่: `HD_BASE [20,35,45,55]` · `HD_BOSS_MUL 2.5` · `HD_TIME_MUL 2`
· เพดาน `HD_CAP_NORMAL 120` · `HD_CAP_BOSS 150`

**`hdDamage()` ปัดลงครั้งเดียวตอนท้าย ไม่ปัดระหว่างทาง** — บอสชั้น 10 คือ `35 × 2.5 = 87.5 → 87`
แต่บอสชั้น 10 ที่หมดเวลาคือ `35 × 2.5 × 2 = 175` **ไม่ใช่ `87 × 2 = 174`**
ถ้าเผลอปัดตรงตัวคูณบอสก่อน ตัวเลขจะเพี้ยนไป 1 หน่วยทั้งสามช่วงบน

**VIT ไม่หั่นดาเมจอีกแล้ว** (สูตรเดิม `15 × (1 - vit/300)` ถูกแทนที่ทั้งก้อน) — สเปกของแพตช์นี้
ระบุลำดับคำนวณไว้ว่า `ฐาน × หมดเวลา × บอส × ตัวคูณการ์ด` ซึ่งไม่มี VIT อยู่ในสูตร
VIT ยังมีผลผ่าน `maxHpFor()` (+2 HP ต่อหน่วย) เหมือนเดิม **ถ้าจะเอาการหั่นดาเมจกลับมา
ให้คูณที่ `hdDamage()` จุดเดียว อย่าไปแก้ `wrongDamage` ของ v4.0**

แถว VIT ในแผงสถานะเรียก `wrongDamage()` อยู่แล้ว จึงโชว์ตัวเลขของชั้นปัจจุบันให้เองโดยไม่ต้องแก้

#### `HD_TIMEOUT` — ธงของ "ข้อที่กำลังตัดสินอยู่" ไม่ใช่ของบัญชี

v4.0 เรียก `wrongDamage()` เองข้างใน `resolveAnswer` จึงส่ง "หมดเวลาหรือเปล่า" เป็นพารามิเตอร์ไม่ได้
wrapper ของ `resolveAnswer` (ตัวนอกสุด) จึงปั๊มธงไว้ก่อนแล้ว **คืนค่าเดิมใน `finally` เสมอ**
กฎเดียวกับ `AB_LIVE` ของ v4.6 · ไม่ต้องเซฟ

**ตัวคูณของหมดเวลาต้องอยู่ "ก่อน" การ์ดของ v4.7 มาคูณต่อ** ตามลำดับในสเปก จึงต้องทำผ่านธง
ให้ v4.0 หัก HP เป็นยอดที่คูณแล้ว **ห้ามหักเพิ่มทีหลังใน wrapper** ไม่งั้นตัวคูณการ์ด
(🗡️ ×1.5 · ⚡ ×3 · 🔥 ×3) จะไปเกาะแค่ยอดก่อนคูณเวลา

#### เพดานความเสียหายสุทธิต่อข้อ — `hdApplyCap()`

นับ **"HP ที่หายไปจริงตลอดการตัดสินข้อนั้น"** ทั้งก้อน (`hp ก่อนเข้า wrapper − hp หลังทุกชั้นทำงานจบ`)
แล้วคืนส่วนเกินกลับ จึงครอบทั้งตัวคูณของการ์ดและใบที่หัก HP เป็นเปอร์เซ็นต์ของหลอด
(🗡️ ตัดสินเทพเจ้า 50% · ⏳ ทรายมรณะ 80% · 🎲 รูเล็ต 50%) โดยไม่ต้องไล่แก้ทีละใบ

ผลข้างเคียงที่ตั้งใจ: **บอสหมดเวลาชั้น 10/15/20 (175/225/275) ลงเอยที่ 150 ทุกใบ**
และสามใบเปอร์เซ็นต์ข้างบนอ่อนลงมากในชั้นสูง ๆ ซึ่งคือเจตนาของสเปก ("ให้การ์ดสายความเสี่ยง
ยังใช้งานได้จริง") **ถ้าจะให้บอสหมดเวลาแรงเต็มตาราง ให้ดัน `HD_CAP_BOSS` ขึ้น อย่าไปถอดเพดาน**

**เพดานทำงานหลังทุกชั้นตัดสินผลไปแล้ว จึงต้องรับผิดชอบคิวเทิร์นเอง (กับดักข้อ 19)**
ถ้าการคืน HP ทำให้รอดจากที่ v4.0 สั่ง `onHunterDown` ไว้แล้ว ต้องสั่งคิวใหม่ทับ —
และมีเคสพิเศษหนึ่งใบที่**ห้ามลืม**

| การ์ด | ทำไมพิเศษ |
|---|---|
| ⚖️ `scales` | v4.7 ดึงลงต้นโซนเฉพาะตอน `hpAfter > 0` ถ้า HP หมดมันปล่อยให้ `onHunterDown` พาร่วงแทน · พอเพดานกันไม่ให้ตาย **จะไม่มีใครลงโทษเลย** `hdApplyCap()` จึงเรียก `cdDropZone()` เองในเคสนี้ |

การ์ดอื่น (🗡️ `exec` · ⏳ `sand` · 🎲 `roulette` · ⚡ `curse` · 🔥 `inferno`) ใช้คิวปกติ
คือ `scheduleTurn(ไปข้อถัดไป, 1600)` เพราะผลของมันจบในตัวไปแล้ว

**เพดานไม่แตะเกราะ `G.shield`** — เกราะกินดาเมจทั้งก้อนก่อน HP จะขยับ `lost` จึงเป็น 0 แล้วออกทันที
· **โหมดฝึกจุดอ่อนออกตั้งแต่บรรทัดแรก** (ไม่เสีย HP อยู่แล้ว)

#### ชั้นนี้ไม่เพิ่มฟิลด์ในบัญชีเลย

ไม่มี `hdEnsure()` ไม่มี `hdSave()` ไม่มีตรารุ่น `hdv` — ทุกอย่างคำนวณสดจาก `G.floor` ตอนตัดสินข้อ
บัญชีเก่าจึงได้ตารางใหม่ทันทีที่เปิดเกม โดยไม่ต้อง migrate อะไรเลย
ข้อความบนจอมีจุดเดียวคือบรรทัดผลลัพธ์ `#gFeedback` (`🧱 เพดานความเสียหาย …`) **ไม่มี toast ไม่มี gate**
ตามกฎกับดักข้อ 1 และข้อ 18 เพราะจุดที่ยิงอยู่ระหว่างนาฬิกาต่อข้อเดินอยู่

### ชั้นที่สิบ — v4.8.1 · ANTI-CHEAT PAUSE (หยุดเวลากันโกง + ปิดบังโจทย์ + ล็อกหน้าจอเล่น)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น HARDCORE DAMAGE เริ่มที่ `const AC_VER` ทุกอย่างขึ้นต้นด้วย `ac` / `AC_`

**ชั้นนี้ไม่แก้โค้ด v4.0-v4.8 เลย — ห่อฟังก์ชันแบบเดียวกับแปดชั้นก่อนหน้า**

ฟังก์ชันที่ถูกห่อเพิ่ม: `startQuestionTimer` `clearQuestionTimer` `freezeQuestionTimer`
`extendQuestionTimer` `answer` `startGame` `exitGame`

สามเรื่องในชั้นเดียว ทุกเรื่องวิ่งผ่านสวิตช์เดียวคือ `acPaused()`

#### 1) หยุดนาฬิกาต่อข้อทุกครั้งที่จอถูกบัง

| ต้นเหตุ | ตัวแปร | ปลดยังไง | ม่าน `#acVeil` |
|---|---|---|---|
| มีหน้าต่างระบบเปิดค้าง | **ไม่มีตัวแปร** — อ่าน DOM สดทุกครั้ง | ปิดหน้าต่างนั้น (ทุกบานที่เปิดอยู่) | ไม่ขึ้น (หน้าต่างบังอยู่แล้ว) |
| กดปุ่ม ⏸️ พัก เอง | `AC_MANUAL` | กดปุ่มซ้ำ หรือกด ▶ เล่นต่อ บนม่าน | ขึ้น |
| สลับแท็บ / สลับแอป / พับจอ | `AC_FOCUS` | **กด ▶ เล่นต่อ เท่านั้น** | ขึ้น |
| เลื่อนจอหนีโซนคำถาม-คำตอบ | **ไม่มีตัวแปร** — วัดพิกัดสดด้วย `acQaVisible()` | เลื่อนกลับไปที่ตัวเลือก (เดินต่อเอง) | **ห้ามขึ้น** |

**"มีหน้าต่างเปิดค้าง" จงใจไม่เก็บเป็นตัวแปร** — `acOverlayOpen()` ยิง `querySelector(AC_OVL_SEL)`
สดทุกครั้ง จึงไม่มีทางค้างไม่ตรงกับของจริงตอนหน้าต่างถูกปิดด้วยเส้นทางที่ชั้นนี้ไม่รู้จัก
(เช่น `gcUnlockScreen` ของ v4.3 ที่กวาดปิดทุกบานรวดเดียว หรือ `cdPick` ที่ถอด `.active`
ทีหลังผ่าน `setTimeout` 620ms)

`AC_OVL_SEL` กวาดหน้าต่างของทุกชั้นด้วยตัวเลือกเดียว:
`.g-modal.active` (ครอบ `#gShop` `#gStatus` `#gBoard` `#gAnalytics` `#gModal` `#gLock`
`#qBoard` `#cxBoard` `#abCore` `#cdDraft`) · `.sn-gate.active` (`#snGate` `#snPanel`)
· `#gmPanel` `#gmModal` `#gcModal`
**ถ้าชั้นใหม่สร้างหน้าต่างที่ไม่ได้ใช้คลาส `.g-modal` / `.sn-gate` ต้องเติมชื่อลง `AC_OVL_SEL`**
ไม่งั้นนาฬิกาจะเดินอยู่หลังหน้าต่างบานนั้น

**AC_FOCUS ปลดอัตโนมัติไม่ได้โดยตั้งใจ** — ถ้าคืนให้เองตอนสลับกลับมา นักเรียนจะเปิดหน้า
ค้นคำแปลไว้ข้าง ๆ แล้วสลับกลับมาตอบทันทีโดยไม่เสียเวลาสักวินาที ต้องบังคับให้กดปุ่มก่อนเสมอ
**ส่วน "เลื่อนหนี" ปลดเองได้** เพราะระหว่างนั้นโจทย์ถูกเบลอไว้ ไม่มีอะไรให้แอบอ่านตั้งแต่แรก
กลับมาที่ตัวเลือกเมื่อไหร่ก็เดินต่อทันที ไม่ต้องกดปุ่มให้เสียจังหวะ

#### 1.1) เลื่อนจอหนีโซนคำถาม-คำตอบ = หยุดเวลาให้เงียบ ๆ

นักเรียนเลื่อนลงไปดูกระเป๋า · ร้านค้า · แถบสกิล · กระดานภารกิจ ระหว่างข้อ = เวลาไม่ควรเดิน
`acQaVisible()` วัดสองอย่างพร้อมกัน **ต้องผ่านทั้งคู่ถึงนับว่ายังอยู่กับโจทย์**

1. เห็นแถวตัวเลือก `#gChoices` อย่างน้อย `AC_QA_MIN` (60%) ของความสูง — วัดในกรอบที่เริ่มจาก
   **ขอบล่างของการ์ดโจทย์ที่ติดหนึบ** ไม่ใช่ขอบบนจอ เพราะการ์ดบังพื้นที่ตรงนั้นอยู่จริง
2. เห็นตัวอักษรจีน `#gWord` ด้วย — **สำคัญเฉพาะจอเตี้ยที่ปิด sticky ไว้** ถ้าไม่เช็กข้อนี้
   ผู้เล่นจะเลื่อนจนโจทย์หลุดจอแต่ยังเห็นตัวเลือก แล้วเวลาเดินอยู่โดยไม่มีโจทย์ให้อ่าน

**ห้ามขึ้นม่าน `#acVeil` สำหรับเหตุผลนี้เด็ดขาด** — เหตุผลเดียวที่เลื่อนลงมาคือจะไปดู/กดของ
ที่อยู่ข้างล่าง ถ้าม่านขึ้นมาบังก็เท่ากับพังฟีเจอร์ตัวเอง ตอนนี้ยัง**ซื้อของ · ใช้ไอเทม ·
เปิดแผงอื่นได้ครบตามปกติ** มีแค่นาฬิกาที่หยุดกับโจทย์ที่ถูกเบลอ
(`acPaintBtn()` ก็ไม่นับเหตุผลนี้ ปุ่ม ⏸️ จึงไม่ขึ้น "เล่นต่อ" ทั้งที่กดแล้วไม่มีอะไรเกิดขึ้น)

ตัวบอกสถานะคือคลาส `ac-scroll` บน `#gameScreen` ซึ่งเปลี่ยนข้อความป้าย `::after` บนการ์ดโจทย์
เป็น `⏸️ หยุดเวลาให้แล้ว — เลื่อนกลับไปที่ตัวเลือกเพื่อเล่นต่อ` **ไม่ได้สร้าง DOM ใหม่เลย**
(การ์ดโจทย์ติดหนึบอยู่บนสุดจึงเห็นเสมอ · หลอดเวลาก็ขึ้น `⏸️ 12.4 วิ` ให้อยู่แล้ว)

**`acFocusQa()` ดึงจอกลับมาที่โจทย์ทุกครั้งที่ขึ้นข้อใหม่** ไม่งั้นผู้เล่นที่ค้างอยู่ล่างจอจะเจอ
ข้อใหม่เริ่มมาในสภาพ "หยุดอยู่" ทันที ซึ่งงงกว่าเดิม · **ต้องคิดจากตำแหน่งที่การ์ดจะไปติด
ไม่ใช่ตำแหน่งที่การ์ดอยู่ตอนนั้น** (`ขอบบนกล่องเลื่อน + ความสูงการ์ด`) เพราะตอนเรียกฟังก์ชันนี้
ผู้เล่นมักอยู่บนสุดและการ์ดยังไม่ติดขอบ ถ้าเอาขอบล่างปัจจุบันมาคิดจะได้ระยะเลื่อนแค่ ~16px
แล้วตัวเลือกยังอยู่ใต้ขอบจออยู่ดี (เจอมาแล้ว ผ่านเฉพาะจอ 320 กับ 768 ที่บังเอิญเข้าเงื่อนไขอื่น)
· และ**ต้องกระโดดทันที ห้ามใช้ scroll แบบนุ่ม** เพราะระหว่างเลื่อน ตัวเลือกจะหลุดจอชั่วขณะ
แล้วระบบจะหยุด-เล่นต่อสลับกันให้เห็นเป็นอาการกระตุก

เลื่อนขึ้นไปบนสุด (ดูเรดาร์หอคอย/หลอด HP) ก็ยัง**หยุดอยู่**เหมือนกัน เพราะการ์ดสถานะสูง 545px
ดันตัวเลือกพ้นจอไป — เป็นพฤติกรรมที่ตั้งใจ ไม่ใช่บั๊ค

**สถานะนาฬิกาที่ถือค้างไว้เป็นของ "ข้อที่กำลังตอบอยู่" ไม่ใช่ของบัญชี** (กฎเดียวกับ `AB_LIVE`
ของ v4.6 · `HD_TIMEOUT` ของ v4.8) จึงไม่ต้องเซฟ: `AC_HELD` `AC_LEFT` `AC_FRZ` `AC_AT` `AC_FRZ_ENDS`

| ชิ้นส่วน | หน้าที่ |
|---|---|
| `acPauseTimer()` | เคลียร์ `QUESTION_TIMER` / `QUESTION_TICK` / `FREEZE_TIMER` แล้วจดเวลาที่เหลือ |
| `acResumeTimer()` | คืนค่าผ่าน `resumeQuestionTimer()` ของ v4.0 (หรือ `freezeQuestionTimer()` ถ้าค้างตอนแช่แข็ง) |
| `acSync(force)` | ตัวประสานกลาง — เทียบสถานะที่ควรเป็นกับที่เป็นอยู่ แล้วลงมือครั้งเดียว |
| `acKick(muts)` | ตัวรับจาก MutationObserver · กรอง + หน่วงด้วย rAF |

**`acPauseTimer` เคลียร์ handle ตรง ๆ ห้ามเรียก `clearQuestionTimer()`** เพราะ wrapper ของชั้นนี้
ดัก `clearQuestionTimer` ไว้เพื่อทิ้งสถานะค้าง (= ข้อจบจริงแล้ว) ถ้าเรียกมันจะลบเวลาที่เพิ่งจดไป

**❄️ สกิลแช่แข็งต้องหยุดตามไปด้วย** — `freezeQuestionTimer` ของ v4.0 เก็บเวลาจบไว้ในตัวแปร
ท้องถิ่น อ่านจากข้างนอกไม่ได้ wrapper ของชั้นนี้จึงจด `AC_FRZ_ENDS` ไว้เอง
ตอนเล่นต่อจะตั้ง `QUESTION_ENDS = now + AC_LEFT` **ก่อน** เรียก `freezeQuestionTimer(AC_FRZ)`
เพราะตอนนั้น `FREEZE_TIMER` เป็น `null` แล้ว มันจึงอ่านเวลาที่เหลือจาก `QUESTION_ENDS` ไม่ใช่ `FROZEN_LEFT`

**เวลาที่หยุดต้องถูกคืนให้ `G.questionStart` ด้วย** (`questionStart += เวลาที่หยุด`)
ไม่งั้นทุกอย่างที่วัดความไวจะเพี้ยน — 🗡️ ตัดสินเทพเจ้า (3 วิ) · 💎 เศษคริสตัลตอบไว (3.5 วิ)
· `recentTimes` ที่ใช้คำนวณ AGI

**ข้อยกเว้นที่ตั้งใจ: หยุดแค่ "นาฬิกาต่อข้อ" เท่านั้น** — บัฟที่นับเป็นจำนวนข้อไม่ถูกแตะเลย
(⚡ ยาเพิ่มสมาธิ = `G.focusLeft` · 🧘 การ์ดสมาธิแน่วแน่) และ `G.helperUsed` ก็ไม่ถูกคืนสิทธิ์
เพราะการคืนสิทธิ์อยู่ที่ `startQuestionTimer` ที่เดียวตามกติกา v4.7.4 ซึ่งการหยุด/เล่นต่อไม่ผ่าน

#### 2) หน้ากากปิดโจทย์

ระหว่างหยุด `#gameScreen` ได้คลาส `ac-mask` แล้ว CSS เบลอ **เฉพาะโหนดที่เป็นเนื้อโจทย์**
คือ `#gWord` `#gPinyin` `#gQuestion` `#gChoices` `#gFeedback` (+ `pointer-events:none` กันกดตัวเลือก)
และการ์ดโจทย์ได้ป้าย `🔒 โจทย์ถูกปิดบังระหว่างหยุดเกม` ทับกลางใบ

**ห้ามเบลอทั้ง `#gameScreen`** — `filter` บนตัวแม่สร้าง containing block ใหม่ แล้วแบดจ์การ์ด
`#cdBadge` ของ v4.7 ที่เป็น `position:fixed` จะหลุดตำแหน่งทันที

ม่าน `#acVeil` (มีปุ่ม ▶ เล่นต่อ) โผล่เฉพาะตอน **ไม่มีหน้าต่างระบบเปิดอยู่** เพราะหน้าต่างระบบ
เป็น `position:fixed` เต็มจอ z-index 200 ขึ้นไป บังโจทย์ให้อยู่แล้ว (และของข้างหลังก็ยังถูก `ac-mask` เบลอซ้ำ)
· z-index ของม่านคือ **190** = เหนือ `#abDanger` (120) กับ `#gVfx` (150) แต่ใต้ `.g-modal` (200)

**ม่านมีหน้าที่แค่ "หรี่จอ + กันกด" เท่านั้น — ห้ามใส่ `backdrop-filter` เด็ดขาด**
พื้นเป็น `rgba(6,10,16,.5)` โปร่ง ๆ ล้วน การเบลอเป็นหน้าที่ของ `ac-mask` ซึ่งเบลอเฉพาะ
โจทย์กับตัวเลือก ร่างแรกใส่ `backdrop-filter:blur(18px)` ไว้ที่ม่านด้วย ผลคือ**ทั้งหน้าจอพร่าไปหมด**
— หลอด HP/MP · เรดาร์หอคอย · แถบสกิล · กระเป๋า ซึ่งไม่มีอะไรเฉลยคำตอบเลยสักอย่าง
แถมยังกินแรงเครื่องนักเรียนฟรี ๆ เพราะ `backdrop-filter` ต้องรีคอมโพสิตทั้งหน้าทุกเฟรม
(พื้นโปร่งล้วนยังทำงานได้ทุกเบราว์เซอร์ ไม่ต้องมี `@supports` มาช่วยแล้ว — กับดักข้อ 15)

`answer()` ถูกห่อให้ no-op ระหว่างหยุดอีกชั้น เผื่อมีทางเรียกที่ไม่ได้ผ่านการกดปุ่ม

#### 3) ล็อกหน้าจอเล่น (Viewport Lock)

`body.game-mode` กลายเป็น `height:100dvh` + `overflow:hidden` และ `#gameScreen.active` กลายเป็น
**กล่องเลื่อนของตัวเอง** (`overflow-y:auto`) — body จึงไม่เลื่อนอีกเลย แถบที่อยู่ของเบราว์เซอร์มือถือ
ไม่ยุบ-ยืดจนโจทย์กระโดดหนีนิ้ว · ประกาศ `100vh` ไว้ก่อนแล้วทับด้วย `100dvh` เบราว์เซอร์เก่ายังใช้ได้

การ์ดโจทย์ได้คลาส `ac-battle` แล้ว `position:sticky; top:0` **เลื่อนดูกระเป๋า/ปุ่มล่างยังไง
โจทย์กับหลอดเวลาก็ไม่หลุดจอ** (วัดจริงแล้วตัวเลือกครบ 4 ปุ่มยังเห็นพร้อมโจทย์ทุกความกว้าง)

**`padding-top` ของกล่องเลื่อนต้องเป็น 0** แล้วใส่ที่ว่างหัวจอเป็น `<div class="ac-space">`
ในสายเลื่อนแทน เพราะ sticky ใช้ `top:0` = ขอบบนของ scrollport ถ้ามี padding-top เนื้อหาจะไหล
ผ่านช่องนั้นให้เห็นเป็นแถบ

**จอสูงไม่ถึง 620px ปิด sticky อัตโนมัติ** (`@media (max-height:620px)`) เพราะการ์ดโจทย์สูง ~341px
ถ้าตรึงไว้บนจอเตี้ยจะไม่เหลือที่ให้ตัวเลือกเลย — คืนเป็น `position:relative` **ไม่ใช่ `static`**
เพราะป้าย `::after` ของหน้ากากยึดกล่องนี้เป็นหลัก

`#gameScreen.active` เขียน `padding` ทั้งชุดทับ จึงต้องเขียน `#gameScreen.active.cd-on`
(ความจำเพาะ 0,3,0) คืนที่เผื่อแบดจ์การ์ดของ v4.7 ให้ด้วย ไม่งั้นแบดจ์จะทับแถวปุ่มล่าง

หน้าต่างระบบทุกบานได้อนิเมชัน `acDrop` (แทรกลงมาจากด้านบน) ผ่าน `.g-modal.active > .g-modal-inner`
ความจำเพาะ 0,3,0 **จึงยังแพ้ `#cdDraft.cd-out .g-modal-inner` (1,2,0) ของ v4.7**
อนิเมชันสลายการ์ดตอนเลือกเสร็จจึงทำงานเหมือนเดิม

#### ชั้นนี้ไม่เพิ่มฟิลด์ในบัญชีเลย

ไม่มี `acEnsure()` ไม่มี `acSave()` ไม่มีตรารุ่น `acv` — ทุกอย่างเป็นสถานะของข้อที่กำลังตอบ
บัญชีเก่าจึงได้ระบบนี้ทันทีที่เปิดเกม โดยไม่ต้อง migrate อะไรเลย
และ **ไม่มี toast ไม่มี gate** สักรายการ (กับดักข้อ 1 และข้อ 18) — สื่อสารผ่านม่าน `#acVeil`
กับตัวหนังสือบนหลอดเวลาเท่านั้น


### ชั้นที่สิบเอ็ด — v4.9.1 · NOTIFY CONTROL (แถบปลอดภัย + การ์ดใหญ่ยิงสด + สวิตช์ 3 ระดับ)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น ANTI-CHEAT PAUSE เริ่มที่ `const NC_VER` ทุกอย่างขึ้นต้นด้วย `nc` / `NC_`

**ชั้นนี้ไม่แก้โค้ด v4.0-v4.8.1 เลย — ห่อฟังก์ชันแบบเดียวกับเก้าชั้นก่อนหน้า**

ฟังก์ชันที่ถูกห่อเพิ่ม: `snShowToast` `snCheckMpReady` `startQuestionTimer` `clearQuestionTimer`
`renderStats` `renderStatus` `onHunterDown` `startGame` `exitGame`

**จุดกรองมีจุดเดียวคือ `snShowToast`** — toast ของ *ทุกชั้น* (`sysNotify` ของ v4.1 · `qNotify`
ของ v4.2 · `cxNotify` ของ v4.4 · `abNotify` ของ v4.6 · `cdNotify` ของ v4.7) ไหลมาลงที่นี่หมด
จึงไม่ต้องไล่ห่อทีละตัว · **หน้าต่างบังคับกดยืนยัน (`snShowGate`) ไม่ถูกแตะเลย** ทุกโหมดยังเห็นครบ

#### 1) แถบปลอดภัยหัวจอ (Safe Zone)

จองแถบสูง `NC_BAND` (42px) ไว้ที่ขอบบนสุดตลอดเวลาที่อยู่ในโหมดเล่น แล้ว **ดันกล่องเลื่อนของ
หน้าเล่นทั้งใบลงมาให้เริ่มต่ำกว่าแถบ พร้อมหักความสูงคืนเท่ากัน**

```
#gameScreen.active { margin-top: var(--nc-band); height: calc(100% - var(--nc-band)) }
```

ของข้างในจึงถูกตัดขอบที่ขอบบนกล่อง **ไม่มีทางโผล่ขึ้นมาในแถบได้เลยไม่ว่าเลื่อนไปตรงไหน**
`--nc-band` ถูกตั้งที่ `#gameScreen` จุดเดียวโดย `ncApplyBand()` โหมด 🔕 ตั้งเป็น 0
แล้วทั้งชุดยุบหายไปเอง คืนพื้นที่ให้เกม

**สองทางที่ลองแล้วไม่ปิดสนิท อย่าย้อนกลับไปใช้** — ดูกับดักข้อ 23

จองแบบถาวรตลอดโหมดเล่น ไม่ใช่จองเฉพาะตอนมี toast เพราะการยืด-หดตามการมาของ toast
จะดันเนื้อหาทั้งหน้ากระโดดทุกครั้งที่มีแจ้งเตือน

#### 2) การ์ดใหญ่เต็มใบ ยิงสดทันที (Patch v4.9.1)

**ร่าง v4.9 เคยบีบเป็น Mini-Banner บรรทัดเดียว 34px — ยกเลิกไปแล้วทั้งหมด**
ตอนนี้ใช้ **โครง DOM เดียวกับ toast ของ v4.1 เป๊ะ** (`.sn` + `.sn-head` + `.sn-title` + `.sn-body`)
จึงได้รายละเอียดครบทุกอย่างที่ `SYS_NOTIF` ทั้ง 25 รายการเขียนไว้อยู่แล้ว — หัวข้อ SYSTEM ·
รหัสอ้างอิงเต็ม (`ANALYTICS 24/25`) · อักษรจีน · พินอิน · คำแปล · หมวดคำ · ตำราอ้างอิง
《汉语教程》 A1 · สถิติการปราบ **ไม่ต้องแก้ตัว `def` เลยสักรายการ** คลาสที่เพิ่มคือ `.nc-card`

| ค่าคงที่ | ค่า | ความหมาย |
|---|---|---|
| `NC_MAX` | 1 | Single Active Replace — บนจอมีได้ครั้งละ 1 ใบ ใบใหม่ล้างใบเก่าทันที |
| `NC_LIFE` | 2200 | อายุการ์ดทั่วไป (สเปก v4.9.1 = 2.0-2.5 วิ) |
| `NC_LIFE_CRIT` | 2500 | หมวดวิกฤตอยู่นานสุดของช่วง |
| `NC_FIT_GAP` | 6 | ระยะเว้นระหว่างขอบล่างการ์ดกับหัวโซนโจทย์ |
| `NC_FIT_MIN` | 72 | หนีบไม่ต่ำกว่านี้ — ต้องเหลือหัวข้อ + รหัสอ้างอิงให้อ่านออก |

**ลบใบเก่าแบบซิงโครนัสเท่านั้น ห้ามเรียก `snDismiss` ในลูป** (กับดักข้อ 1 — กติกาเดียวกับ v4.1)

**กติกา Single Active Replace ใช้ทั้งในและนอกหน้าเล่นเกม** ไม่มีการ์ดกองซ้อนอีกเลย

#### 2.1) Fit Guard — หนีบความสูงให้จบก่อนถึงโซนโจทย์

การ์ดใหญ่ยาวไม่เท่ากันตามความยาวข้อความของแต่ละ `def` **วัดจริงแล้ว 48 ใบจาก 51 ใบ
(ทุกชั้นรวมกัน) จบเหนือหัว `#gWord` พอดีอยู่แล้ว** เหลือ 3 ใบที่ยาวสุด (`CD_PICK_2` ·
`CD_PICK_3` · `CD_DROP`) ล้นไปคร่อมหัวอักษรจีนราว 10px

`ncFitCard(el)` วัดขอบบนของ `gWord`/`gPinyin`/`gQuestion`/`gChoices` **สด ๆ ทุกครั้ง
ไม่ใช้ media query เดา** แล้วตั้ง `max-height` ให้การ์ดจบก่อนถึงตรงนั้น ส่วนที่ถูกตัดจางหาย
ด้วย `mask-image` (ใส่ทั้งแบบมี `-webkit-` และไม่มี ตามกับดักข้อ 15)

**ต้องล้าง `maxHeight` กับคลาส `nc-clip` ทิ้งก่อนวัดเสมอ** ไม่งั้นจะได้ความสูงที่หนีบไว้
รอบก่อนแทนความสูงธรรมชาติ แล้วการ์ดจะหดลงเรื่อย ๆ ทุกครั้งที่คำนวณใหม่

`ncRefit()` คำนวณใหม่ตอนเลื่อนจอ/หมุนจอ (หน่วงด้วย rAF · ผูกที่ `#gameScreen` ไม่ใช่ `window`
เพราะ Viewport Lock ของ v4.8.1) เพราะการ์ดอยู่ 2.0-2.5 วิ ผู้เล่นเลื่อนจอระหว่างนั้นได้
โดยเฉพาะจอเตี้ยที่ปิด sticky ไว้ **ไม่มีการ์ดอยู่ = จบตั้งแต่บรรทัดแรก แทบไม่มีต้นทุน**

**ห้ามดันขนาดตัวหนังสือขึ้นจากของ v4.1** — ร่างแรกขยับหัวเรื่องเป็น 14px และเนื้อความเป็น
12.5px/1.6 ผลคือใบที่ยาวที่สุดสูงจาก ~160px เป็น 178px แล้วขอบล่างไปคร่อมหัวอักษรจีนที่ y=161
ถ้าจะขยับต้องวัดใหม่ทั้ง 51 ใบเทียบกับขอบบนของ `#gWord`

**ที่เหลืออยู่จริง (ยอมรับไว้โดยตั้งใจ):** จอเตี้ยกว่า 620px ที่ v4.8.1 ปิด sticky ไว้
ยังเหลือราว 5 ตำแหน่งเลื่อนจาก ~60 ที่การ์ดคร่อมโจทย์ เพราะพื้นที่เหนือโจทย์แคบกว่า
`NC_FIT_MIN` — เลือกให้การ์ดอ่านออกดีกว่าหนีบจนเหลือแถบเปล่า และแตะการ์ดเพื่อปิดได้ทันทีอยู่แล้ว

#### 3) Real-Time Immediate Display + สวิตช์คัดกรอง

**Patch v4.9.1 ยกเลิกการกักคิวทั้งหมด (Disable Silence Queue)** — ทุกเหตุการณ์ยิงการ์ดขึ้น
ทันทีไม่ว่านาฬิกาต่อข้อจะเดินอยู่หรือไม่ ผู้เล่นจึงประเมินสถานการณ์ต่อสู้ได้สด ๆ
**ที่กันจอรกคือกติกา Single Active Replace ไม่ใช่การหน่วงเวลา** · `NC_Q` `NC_PUMP` `ncBusy()`
`ncQueue()` `ncFlush()` ถูกถอดออกหมดแล้ว อย่าเอากลับมา

หมวดวิกฤตอยู่ที่ `NC_CRIT` **เทียบด้วยตัว `def` ตรง ๆ ไม่ใช่ชื่อคีย์** จึงครอบแจ้งเตือน
ของทุกชั้นได้โดยไม่ต้องไปเติมคีย์ที่ 7 ให้ `SYS_NOTIF` (ซึ่งตรึงไว้ 6 คีย์)
ตอนนี้เหลือหน้าที่เดียวคือ **เป็นตัวคัดของโหมด ⚠️ เฉพาะวิกฤต** (ไม่ได้ใช้กักคิวอีกแล้ว)

| ชั้นเจ้าของ | คีย์ | ทำไมวิกฤต |
|---|---|---|
| v4.1 | `BTL_COMBO5` | 🔥 คอมโบ 5 ข้อรวด |
| v4.1 | `SKL_READY_WARD` | 🛡️ สกิลชุบชีวิตพร้อมใช้ |
| v4.1 | `SKL_NO_MP` · `GATE_NO_SKIP` | ตอบสนองการกดปุ่มโดยตรง — ถ้ากักไว้จะดูเหมือนปุ่มเสีย |
| v4.6 | `AB_QUAKE` | 🔥 สตรีคสะเทือนหอคอย |
| v4.6 | `AB_DANGER` | 🔴 HP ต่ำวิกฤต |
| v4.9 | ทั้ง 3 รายการของ `NC_NOTIF` | เวลาใกล้หมด · เกราะ/ชุบชีวิต · สกิลพร้อมรวบ |

**หมวด "HP ต่ำวิกฤต" ไม่ต้องสร้างใหม่ — `AB_DANGER` ของ v4.6 ทำอยู่แล้ว** ชั้นนี้แค่ติดป้ายว่าวิกฤต

#### 4) Notification Batching — สกิลพร้อมใช้

`snCheckMpReady` ถูกห่อโดย **ดัก `sysNotify` ชั่วคราวระหว่างเรียกของเดิม** จึงไม่ต้องคัดลอก
ตรรกะ edge-trigger (`SN_MP_SEEN`) ออกมาเขียนซ้ำ — ของเดิมยังเป็นเจ้าของสถานะเหมือนเดิม

- เพิ่งพร้อม **1 ตัว** → ปล่อย `SKL_READY_*` ใบเดิม **คงดรรชนี 17-20/25 ไว้ครบ**
- เพิ่งพร้อม **≥2 ตัว** → รวบเป็น `NC_MPBATCH` ใบเดียว `⚡ สกิลพร้อมใช้งาน [ดาบโลหิต / เนตรพินอิน]`
  (ชื่อเวทเอาส่วนหลัง `:` ของ `SKILLS[].name` เพราะสั้นพอสำหรับแบนเนอร์บรรทัดเดียว)

#### 5) สวิตช์ 3 ระดับ

| โหมด | ปุ่ม | ผล |
|---|---|---|
| `all` | 🔔 | เห็นทั้ง toast ทั่วไปและวิกฤต (ค่าเริ่มต้น) |
| `crit` | ⚠️ | ซ่อน toast ทั่วไปทั้งหมด เหลือเฉพาะหมวดวิกฤต |
| `off` | 🔕 | ซ่อน toast 100% · แถบปลอดภัยยุบเป็น 0 · **Gate ยังบังคับขึ้นตามปกติ** |

เก็บที่ `localStorage['yao_notify']` **ระดับเครื่อง ไม่ผูกบัญชี** เพราะเป็นค่าการแสดงผล
ไม่ใช่ความคืบหน้าของผู้เล่น (กติกาเดียวกับ `yao_errlog` ของ v4.3)

ทางเข้ามีสองที่: ปุ่มกระดิ่ง `#ncBell` ในแถบหัวหน้าจอเล่น (`.g-topbar` ถัดจากปุ่มเสียง 🔊)
กดวนโหมดทีละระดับ · และแถวตัวเลือกเต็มรูปแบบท้ายแผงสถานะ (`#gStatusBody .nc-set`)

`renderStatus` เขียน `innerHTML` ทับทั้งก้อนทุกครั้ง แต่ยัง **เช็กก่อนแทรกเสมอ** ตามกับดักข้อ 2

#### สารบัญของชั้นนี้ — NOTIFY 01/03-03/03

**แยกจาก `SYS_NOTIF` (ตรึง 25) · `Q_NOTIF` (6) · `CX_NOTIF` (4) · `AB_NOTIF` (9) · `CD_NOTIF` (9)**
ยืมแค่กลไกโชว์มาใช้ ไม่ไปกินเลขของใคร และเป็น **toast ล้วนทั้งชุด** ไม่มี gate (กับดักข้อ 18)

| แท็บ | คีย์ | ยิงจากไหน |
|---|---|---|
| 01/03 | `NC_TIMELOW` | ⏱️ เหลือเวลา 3 วินาทีสุดท้าย |
| 02/03 | `NC_GUARD` | 🛡️ ประกันภัยฮันเตอร์ชุบชีวิต / เกราะเพิ่งตั้งขึ้น |
| 03/03 | `NC_MPBATCH` | ⚡ สกิลพร้อมใช้พร้อมกันหลายตัว |

**เตือนเวลาใกล้หมดไม่แตะ `QUESTION_TICK` ของ v4.0 เลย** — `ncLowTick()` ตั้งนาฬิกาของตัวเอง
แบบ **แก้ค่าเองทุกครั้งที่ตื่น** (อ่าน `QUESTION_ENDS` สดแล้วตั้งรอบใหม่) จึงทนต่อการต่อเวลา ⏳
การแช่แข็ง ❄️ และการหยุดของ v4.8.1 ได้ทั้งหมด · `NC_LOW_FIRED` กันยิงซ้ำในข้อเดียวกัน
และถูกล้างที่ `startQuestionTimer` / `clearQuestionTimer`

`NC_SHIELD_SEEN` เป็น edge-trigger ของเกราะ (กับดักข้อ 3) · `NC_BOOT` ปิดการแจ้งเตือนไว้ตลอด
ช่วงที่ `startGame` ของ v4.0 ยังทำงาน (มันเรียก `renderStats` เองข้างใน) ไม่งั้นบัญชีที่เซฟเกราะ
ค้างไว้จะเจอ "เกราะเพิ่งตั้งขึ้น" เด้งทุกครั้งที่ล็อกอิน — กติกาเดียวกับ `CD_BOOT` ของ v4.7

#### ชั้นนี้ไม่เพิ่มฟิลด์ในบัญชีเลย

ไม่มี `ncEnsure()` ไม่มี `ncSave()` ไม่มีตรารุ่น `ncv` — โหมดอยู่ที่ `localStorage` ระดับเครื่อง
ส่วนคิวและธงเตือนเวลาเป็นสถานะของรอบการเล่น/ของข้อที่กำลังตอบ บัญชีเก่าจึงได้ระบบนี้ทันที
ที่เปิดเกมโดยไม่ต้อง migrate อะไรเลย

### ชั้นที่สิบสอง — v4.9.2 · DUAL WARP GATE (ประตูวาปก่อนบอส + หลังบอส)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น NOTIFY CONTROL เริ่มที่ `const DW_VER` ทุกอย่างขึ้นต้นด้วย `dw` / `DW_`

**ชั้นนี้ไม่แก้โค้ด v4.0-v4.9.1 เลย — ห่อฟังก์ชันแบบเดียวกับสิบเอ็ดชั้นก่อนหน้า**

ฟังก์ชันที่ถูกห่อเพิ่ม: `clearFloor` `openWarp` `renderWarp` `warpGo` `startGame` `exitGame`

**ชื่อในคำสั่งคือ "Patch v4.8.2" แต่ป้ายชั้นเป็น v4.9.2** เพราะต้องวางท้ายไฟล์เพื่อห่อทับ v4.9.1 ได้
เลขชั้นเดินตามลำดับการห่อจริงเสมอ ไม่ใช่ตามเลขในคำสั่ง

#### สองประตู · ทั้งคู่ใช้แผง `#gWarp` ใบเดิมของ v4.0

| ประตู | `kind` | เปิดตอน | ยืนอยู่ชั้น | ปุ่มหลัก |
|---|---|---|---|---|
| ก่อนบอส (ใหม่ทั้งหมด) | `preboss` | เคลียร์ชั้น 4 / 9 / 14 / 19 | 5 / 10 / 15 / 20 | ⚔️ เข้าสู่ห้องบอส ชั้น N |
| หลังบอส (ต่อยอดของเดิม) | `floor` | ปราบบอสชั้น 5 / 10 / 15 / 20 | 6 / 11 / 16 / 1 | 🌀 ข้ามสู่โซนถัดไป — ชั้น N |
| ตั้งหลักใหม่ (ของ v4.0 เดิม) | `down` | HP หมด / ประกันภัยทำงาน | ต้นโซน | ⚔️ ถัดไป ▶ (ไม่แตะ) |

**ประตูหลังบอสมีอยู่แล้วใน v4.0** (`clearFloor` สั่ง `openWarp('floor')` ไว้เอง) ชั้นนี้จึงไม่สร้าง
ประตูใหม่ทับ แต่เติมของสามอย่างที่สเปกขอลงบนประตูเดิม — กล่องสรุปรางวัล · ปุ่มร้านค้า · ข้อความปุ่ม
ส่วนประตูก่อนบอสเป็นของใหม่ทั้งหมด เพราะ v4.0 ไหลจากชั้น 4 เข้าชั้น 5 ทันทีโดยไม่มีจุดพัก

**ชั้นก่อนบอสคำนวณเอาเอง ไม่มีเลข 4/9/14/19 ฮาร์ดโค้ดไว้ที่ไหน** —
`dwIsPreBossFloor(f)` = `f < FLOOR_MAX && isBossFloor(f + 1)` ถ้าคาบบอสของ v4.0 เปลี่ยน ชั้นนี้ขยับตามเอง

#### จุดดักมีจุดเดียว คือ wrapper ของ `clearFloor`

```js
if (boss)                            DW_WON = { …ยอดรางวัลที่เพิ่งจ่าย… };
else if (dwIsPreBossFloor(cleared))  scheduleTurn(function () { openWarp(DW_PRE); }, DW_WAIT);
```

v4.0 สั่ง `scheduleTurn(nextMonster, 1500)` ค้างไว้แล้วตอนเคลียร์ชั้นธรรมดา ชั้นนี้จึง**สั่งคิวใหม่ทับ**
(`scheduleTurn` เคลียร์ `TURN_TIMER` เดิมให้อยู่แล้ว จึงทับได้สะอาด) ตามกฎกับดักข้อ 19
· `DW_WAIT` ตั้งไว้ 1500 เท่ากับของ v4.0 เป๊ะ จังหวะจึงไม่เปลี่ยน

**ทางเข้าชั้นบอสที่ไม่ผ่าน `clearFloor` จะไม่มีประตูก่อนบอสมาคั่น** — 🎲 รูเล็ตแห่งความพินาศ
ที่ทะลุจากชั้น 4 ขึ้นชั้น 5 (`cdSkipFloor` ของ v4.7) และการย้ายชั้นด้วยแผง GM ตั้งใจให้เป็นแบบนั้น
เพราะ "ข้ามไปเลย" คือรางวัลของการ์ดใบนั้นเอง ถ้าเอาประตูไปคั่นก็เท่ากับหักรางวัลทิ้ง

**ไม่มี wrapper ตัวไหนแตะ `nextMonster`** — กดปุ่มบนประตูแล้ว `warpGo()` ของ v4.0 เรียก
`nextMonster()` ให้เองตามเดิม เกราะผนึกของ v4.6 (`abSealSetup`) และหน้าต่างจั่วการ์ดของ v4.7
(`cdCheckZoneMilestone` ที่ชั้น 6/11/16) จึงยังทำงานหลังประตูตามลำดับเดิมทุกประการ

#### **ไม่มีทองหรือ EXP ก้อนใหม่ถูกแจกจากชั้นนี้**

"แจก Gold และ EXP Bonus จากบอส" ทำโดย**ยกยอดที่ `clearFloor` ของ v4.0 จ่ายไปแล้วขึ้นมาโชว์**
(ทองบอส 500 · EXP 250 · คูณด้วยการ์ด ⛓️ 💀 👁️‍🗨️ ของ v4.7 ถ้าถืออยู่) ไม่ใช่จ่ายซ้ำอีกก้อน
ถ้าจ่ายเพิ่มจะพังกติกากันเงินเฟ้อที่ v4.2 / v4.6 / v4.7 ล็อกไว้ทั้งชุด

`DW_WON` ถูกจดใน wrapper ของ `clearFloor` **หลังทุกชั้นทำงานจบ** เพราะตัวคูณของการ์ดจ่ายทีหลัง
· ยอด EXP ใช้ `dwExpGained()` ไม่ใช่ส่วนต่าง `G.exp` ดิบ ๆ เพราะ `levelUpCheck` ของ v4.0
หัก `maxExp` ออกทุกครั้งที่ขึ้นเลเวล (ได้ EXP เยอะแล้วส่วนต่างติดลบได้) จึงบวกเพดานของทุกขั้น
ที่ข้ามไปคืนเข้ามาโดยไล่เพดานด้วย `expForLevel` เหมือนที่ v4.0 ทำ

`DW_WON` เป็นสถานะของ **"ประตูที่กำลังเปิดอยู่"** ไม่ใช่ยอดสะสมของบัญชี — ไม่ต้องเซฟ
(กฎเดียวกับ `AB_LIVE` ของ v4.6 · `HD_TIMEOUT` ของ v4.8 · `AC_HELD` ของ v4.8.1)
ถูกล้างที่ `warpGo` ตอนประตูปิดจริง ไม่งั้นประตู `down` ที่เปิดทีหลังจะหยิบยอดของบอสตัวก่อนมาโชว์ซ้ำ

#### กฎหยุดเวลา (Timer Rule)

`openWarp` ของ v4.0 เรียก `clearQuestionTimer()` อยู่แล้ว wrapper ของชั้นนี้ยืนยันซ้ำ**หลัง**
ทุกชั้นทำงานจบ (idempotent ทั้งของ v4.0 · wrapper ของ v4.8.1 ที่ทิ้ง `AC_HELD` · v4.9 ที่ปลดธงเตือนเวลา)

**ต้องเช็ก `G.warpOpen` ก่อนเสมอ** — โหมดฝึกจุดอ่อนไม่เปิดประตู แต่ `openWarp` จะเด้งไป
`nextMonster()` ซึ่ง **เริ่มนาฬิกาข้อใหม่** ถ้าล้างโดยไม่เช็ก จะดับนาฬิกาที่เพิ่งตั้งทิ้ง

เปิด-ปิดร้านค้าจากบนประตูก็ยังหยุดอยู่ เพราะ `acQaVisible()` ของ v4.8.1 คืน `true` ตอน `#gChoices`
สูง 0 (ประตูซ่อนตัวเลือกไว้) จึงไม่มีใครสั่ง `acResumeTimer()` และ `AC_HELD` ก็ถูกทิ้งไปแล้ว

**ตอนกดเข้าห้องบอสจะมีหน้าต่างบังคับกดยืนยันของ v4.1/v4.6 เด้งมาคั่นหนึ่งใบ** (พฤติกรรมเดิม
ของเกมตั้งแต่ก่อน patch นี้) ระหว่างนั้น v4.8.1 หยุดนาฬิกาไว้ให้อย่างถูกต้อง กดยืนยันแล้วเดินต่อทันที
— เทสต์ต้องเรียก `snGateConfirm()` ก่อนวัด ไม่งั้นเคสจะตกด้วยเหตุผลผิด

#### `dwFocusGate()` / `dwRefocusQa()` — ตำแหน่งเลื่อนจอ (ส่วนที่ยากที่สุดของชั้นนี้)

**แผงวาปสูงเกินช่องที่เห็นได้บนมือถือ และเป็นแบบนั้นมาตั้งแต่ก่อน patch นี้แล้ว** —
วัดจริงบนจอ 390×844: แผง **633px** (ภาพประตู 169 · หัวข้อ 31 · แถวสถานะ 280 · ปุ่มร้านค้า 55
· ปุ่มไป 60 · padding 26) เทียบช่องว่างใต้การ์ดสถานะที่ติดหนึบ (สูง 341) เหลือแค่ **443px**
ปุ่ม "เข้าสู่ห้องบอส" จึงไปตกที่ y≈960 คือ **นอกจอ** ถ้าผู้เล่นค้างอยู่ตำแหน่งเลื่อนเดิม

พอใส่ไม่หมดต้องเลือกว่าจะตัดหัวหรือตัดท้าย → **ตัดหัวเสมอ** เพราะท้ายคือปุ่มที่ต้องกด
ตัดท้ายแล้วผู้เล่นจะเห็นแผงสวย ๆ ที่ไม่มีปุ่ม แล้วนึกว่าเกมค้าง · **กล่องรางวัลจึงอยู่เหนือปุ่ม
ไม่ใช่ใต้หัวข้อ** เพราะของที่อยู่ใกล้ปุ่มที่สุดคือของที่ผู้เล่นเห็นแน่นอน
· **ต้องกระโดดทันที ห้ามใช้ scroll แบบนุ่ม** (บทเรียนเดียวกับ `acFocusQa` ของ v4.8.1)
· ใช้กับประตูทุก `kind` รวม `down` เพราะเป็นแผงใบเดียวกัน

**สองกับดักที่เสียเวลาไปมากที่สุดของ patch นี้ — อย่าเดินซ้ำ**

1. **วัดครั้งเดียวตอน `openWarp` คืนค่าไม่พอ** ความสูงหน้ายังขยับต่ออีกหลังจากนั้น
   (แถบเกราะผนึกของ v4.6 · เรดาร์ของ v4.5 · การ์ดสถานะที่ยืดตัวในห้องบอส) วัดจริงแล้วของ
   เหนือแผงงอกอีก ~153px รอบแรกจึงเลื่อนได้แค่ 18px จาก 177px ที่ต้องใช้ → ต้องมีรอบตาม
   (2 rAF · 260ms · 700ms)
2. **เงื่อนไขของรอบตามต้องเป็น "ปุ่มยังไม่อยู่ในจอ" (`dwGoInView()`) ไม่ใช่ "ตำแหน่งเลื่อน
   ยังเท่าที่เราตั้งไว้"** ร่างแรกจำ `scrollTop` ไว้แล้วยกเลิกรอบตามถ้าค่าเปลี่ยน ใช้ไม่ได้เลย
   เพราะ **scroll anchoring ของเบราว์เซอร์ขยับ `scrollTop` ให้เองทุกครั้งที่ของเหนือจอโตขึ้น**
   ซึ่งคือสิ่งที่เรากำลังรออยู่พอดี รอบตามจึงถูกยกเลิกทุกครั้งและปุ่มยังตกนอกจอเหมือนเดิม
   (เช็กที่ปุ่มตรง ๆ ยังได้ผลพลอยได้ว่าถ้าผู้เล่นเลื่อนเองจนเห็นปุ่มแล้ว ระบบจะไม่ไปแย่งนิ้วเขา)

**`dwRefocusQa()` — ขากลับก็ต้องพากลับ** ชั้นนี้เป็นคนย้ายตำแหน่งเลื่อนไปเอง จึงต้องเป็นคน
พากลับมาเอง ไม่ใช่หน้าที่ของ v4.8.1 · `acFocusQa()` ถูกเรียกไปแล้วครั้งหนึ่งใน `startQuestionTimer`
แต่ตอนนั้นความสูงหน้ายังไม่นิ่ง (แผง 633px เพิ่งถูกซ่อน · แถบเกราะผนึกเพิ่งงอก) ระยะที่คำนวณ
คลาดไปราว 112px แล้ว **จอค้างในสภาพ "⏸️ หยุดเวลาให้แล้ว — เลื่อนกลับไปที่ตัวเลือก" ถาวร
โดยไม่มี error ให้เห็นเลย** (อาการเดียวกับกับดักข้อ 23) จึงเรียก `acFocusQa()` + `acSync(true)`
ซ้ำใน wrapper ของ `warpGo` ที่ 2 rAF และ 260ms · ทั้งคู่ไม่ทำอะไรเลยถ้าจออยู่ถูกที่แล้ว

#### DOM ที่ชั้นนี้เติมลงในแผงเดิม — `dwEnsureDom()` ต้อง idempotent

`renderWarp` ถูกเรียกทุกครั้งที่เปิดประตู **ต้องกันแทรกซ้ำเอง** (กับดักข้อ 2) ไม่งั้นกล่องรางวัล
กับปุ่มร้านค้าจะงอกเพิ่มทุกครั้งที่ปราบบอส

| ชิ้น | id | วางไว้ที่ | โผล่ตอน |
|---|---|---|---|
| กล่องสรุปรางวัลบอส | `#dwReward` | `beforebegin` ของ `#gWarpBody` | `floor` เท่านั้น |
| ปุ่มร้านค้า | `#dwShop` | `beforebegin` ของ `.g-warp-go` | `preboss` + `floor` |

ลำดับบนจอหลังแทรก: ภาพประตูของ v4.1 → วงแหวน → หัวข้อ → กล่องรางวัล → แถวสถานะ → ปุ่มร้านค้า → ปุ่มไป

**แถวเตือนดาเมจห้องบอสต่อท้าย `#gWarpBody` ตรง ๆ ได้** เพราะ `renderWarp` ของ v4.0 เขียนทับ
ด้วย `innerHTML` ทุกครั้ง จึงไม่สะสมซ้ำ · ตัวเลขอ่านสดจาก `hdDamage()` ของ v4.8
**จึงไม่มีทางที่เลขบนประตูกับเลขที่โดนจริงจะเพี้ยนกัน** (กติกาเดียวกับข้อความราคาร้านค้าของ v4.7.9.1)

**ปุ่มร้านค้าไม่ได้ปลดล็อกแรงค์ให้** — `openShop()` ของ v4.0 ยังล็อกที่ D-RANK (ชั้น 6) ตามเดิม
บอสตัวแรก (ชั้น 5) ของรอบแรกจึงยังซื้อของไม่ได้ กดแล้วได้หน้าต่างอธิบายเงื่อนไขเหมือนปุ่ม SHOP
ในแถวปุ่มล่าง **ถ้าจะให้ซื้อได้ตั้งแต่บอสตัวแรกต้องไปขยับ `FEATURE_RANK` ซึ่งเป็นบาลานซ์ของ v4.0**

#### ชั้นนี้ไม่เพิ่มฟิลด์ในบัญชีเลย

ไม่มี `dwEnsure()` ไม่มี `dwSave()` ไม่มีตรารุ่น `dwv` — `DW_WON` เป็นสถานะของประตูที่เปิดอยู่
บัญชีเก่าจึงได้ทั้งสองประตูทันทีที่เปิดเกม โดยไม่ต้อง migrate อะไรเลย
และ **ไม่มีสารบัญแจ้งเตือนของตัวเอง** ไม่ได้เพิ่ม toast หรือ gate ใบใหม่สักใบ — ตัวแผงประตู
สื่อสารครบอยู่แล้ว และ `GATE_FORCE_NEXT` ของ v4.1 ก็ยิงให้ทุกประตูอยู่แล้วตามเดิม


### ชั้นที่สิบสาม — v4.9.3 · TRIPLE QUEST ENGINE (เควสต์ 3 สายหลัก)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น DUAL WARP GATE เริ่มที่ `const TQ_VER` ทุกอย่างขึ้นต้นด้วย `tq` / `TQ_`

**ชั้นนี้ไม่แก้โค้ด v4.0-v4.9.2 เลย — ห่อฟังก์ชันแบบเดียวกับสิบสองชั้นก่อนหน้า**

ฟังก์ชันที่ถูกห่อเพิ่ม: `migrateAccount` `startGame` `exitGame` `saveProgress` `resolveAnswer`
`onMonsterDefeated` `clearFloor` `useSkill` `buyItem` `useItem` `openWarp`
`qRenderBoard` `qOpenBoard` `qRenderBadge`

**คนละก้อนกับ 20 เควสของ v4.2 โดยสิ้นเชิง** — คนละข้อมูล (`account.tq` ไม่ใช่ `account.quest`)
คนละสารบัญแจ้งเตือน คนละตัวนับ **ลบชั้นนี้ทิ้งทั้งก้อนแล้วกระดาน 20 เควสเดิมยังทำงานครบ**
ที่ใช้ร่วมกันมีสองอย่างคือ **แผง `#qBoard` ใบเดิม** (จึงไม่ต้องเติมชื่อลง `AC_OVL_SEL` ของ v4.8.1)
กับ **สไตล์ `.q-row` ชุดเดิม** ของ v4.2

#### เควส 9 รายการ 3 สาย — แก้ตัวเลขที่ค่าคงที่เหนือ `TQ_DEFS`

| สาย | id | เควส | เงื่อนไข | รางวัล |
|---|---|---|---|---|
| 1 · ประจำวัน | `T1` | 🎯 สายฟิต | ตอบถูกสะสม 15 ข้อ | 🪙 200 |
| 1 · ประจำวัน | `T2` | ⚡ สายร่าย | ร่ายสกิล 2 ครั้ง **ขณะต่อสู้จริง** | 🔮 30 MP |
| 1 · ประจำวัน | `T3` | 🛒 สายช็อป | ซื้อไอเทมจากร้าน 1 ครั้ง | ⭐ 100 EXP |
| 2 · ฮันเตอร์ | `T4` | 📚 ล้างคลังศัพท์ | ตอบถูกในคำที่เคยผิด ≥3 ครั้ง สะสม 5 ข้อ | 🪙 250 · ⭐ 150 |
| 2 · ฮันเตอร์ | `T5` | 🎯 แม่นยำสูง | ความแม่นยำประจำรอบ > 80% (ขั้นต่ำ 20 ข้อ) | 🧪 ยาฟื้นพลัง 1 |
| 2 · ฮันเตอร์ | `T6` | 📖 เซียน A1 | ปราบมอนสเตอร์ด้วยคำ A1 ครบ 10 ตัว | 🪙 300 |
| 3 · หอคอย | `T7` | 👑 ล้มยักษ์ | ปราบบอส S-RANK โดยตอบถูกครบ 6 รอบ | 🪙 500 · การ์ดหายาก 1 ใบ |
| 3 · หอคอย | `T8` | 🛡️ สายไร้รอยขีดข่วน | ผ่านชั้นบอส 5/10/15 โดยไม่ใช้ 🧪 เลย | 🪙 1,000 |
| 3 · หอคอย | `T9` | 🔥 สตรีคโหด | คอมโบ 10 ข้อรวด (สะสมข้ามชั้นในโซนได้) | 🛡️ ประกันภัยฮันเตอร์ 1 |

สาย 1 กับสาย 2 **รีเซ็ตเที่ยงคืน** (ใช้ `qDayKey()` ของ v4.2 เป็นตัวตัดสินรอบวันตัวเดียวกัน
ไม่สร้างนาฬิกาชุดที่สองมาให้เพี้ยนกันเอง) · สาย 3 **ทำครั้งเดียวจบตลอดชีพ ไม่รีเซ็ต**

**ข้อความ `cond` ของทุกเควสประกอบจากค่าคงที่เอง** (`TQ_FIT_GOAL` `TQ_ACC_PCT` `TQ_SBOSS_ROUND` …)
จึงไม่มีทางที่เลขบนกระดานกับเลขที่ทำงานจริงจะเพี้ยนกัน — กติกาเดียวกับข้อความราคาร้านค้าของ v4.7.9.1
· **ชั้นบอสของ `T8` คำนวณจาก `FLOOR_MAX` กับ `RANKS` เอง** (`tqScarFloors()` = ชั้นบอสที่ยังไม่ถึง
S-RANK) ไม่มีเลข 5/10/15 ฮาร์ดโค้ดไว้ที่ไหน · `goal` กับ `cond` ของ `T8` ถูกเติมตอนติดตั้ง

#### สองกฎกันฟาร์มที่สเปกสั่งไว้

**1 · Daily Unlocking Requirement — สาย 1 เท่านั้น**
`lock:true` ใน `TQ_DEFS` = ต้องเคลียร์หอคอยจบอย่างน้อย 1 ชั้นในวันนั้นก่อน ตัวนับถึงจะเริ่มเดิน
**บล็อกที่ `tqAdd()` / `tqSet()` ไม่ใช่ที่จุดเรียก** จึงครอบทุกทางเข้าโดยไม่ต้องไล่เช็กทีละ wrapper
· ธง `t.unlocked` ถูกปั๊มใน wrapper ของ `clearFloor` แล้วรีเซ็ตพร้อมรอบวัน
· **สาย 2 ไม่มีธง `lock`** เพราะสเปกล็อกไว้เฉพาะ "เควสต์ประจำวัน" — สายฮันเตอร์เป็นการทบทวน
ที่ควรนับได้ตั้งแต่ข้อแรก

**2 · Anti-Farm Skill Logic — `tqInBattle()`**
สาย ⚡ นับเฉพาะการร่ายที่เกิดขณะอยู่ใน Battle State จริง: มีมอนสเตอร์อยู่ตรงหน้า · ไม่ใช่โหมดฝึก
· ไม่ได้ยืนอยู่ที่ประตูวาป · หน้าต่างจั่วการ์ดไม่ได้เปิด · **ไม่มีหน้าต่างระบบบานไหนเปิดค้าง**
(ยืม `acOverlayOpen()` ของ v4.8.1 ซึ่งกวาดหน้าต่างของทุกชั้นด้วยตัวเลือกเดียวอยู่แล้ว)
· และนาฬิกาต่อข้อต้องมีอยู่จริง — เดินอยู่ · ถูกแช่แข็ง ❄️ · หรือถูก v4.8.1 หยุดค้างไว้
**สถานะถูกอ่านก่อนเรียกของเดิมเสมอ** เพราะ `useSkill` อาจเปลี่ยนสภาพจอระหว่างทาง
· นับเฉพาะครั้งที่ร่ายติดจริง (ดูจาก MP ที่ถูกหักไป) กติกาเดียวกับ v4.2

#### รางวัลจ่ายอัตโนมัติ — ไม่มีปุ่ม "รับรางวัล" ในทางปกติ

ต่างจาก v4.2 ที่ต้องกดรับ — สเปกเขียนข้อความ toast ไว้ว่า *"รับรางวัล +1,000 Gold"*
คือประกาศว่าได้ของแล้ว `tqComplete()` จึงจ่ายทันทีที่เงื่อนไขครบ

**ทางเดียวที่ปุ่ม 🎁 รับรางวัล โผล่คือ "ของแถมไม่มีที่เก็บ"** — เควสถูกพักไว้ที่ `t.pend`
(ยังไม่นับว่าสำเร็จ) แล้วโชว์ปุ่มบนกระดานให้กดใหม่หลังเคลียร์ช่องกระเป๋า **รางวัลไม่หาย**

`tqPay()` เป็นจุดจ่ายจุดเดียว และเคารพกติกาของชั้นล่างครบ:

- **ทองรางวัลเควสไม่บวกเข้า `G.totalGoldEarned`** — กฎกันเงินเฟ้อข้อ 1 ของ v4.2
  (ไม่งั้นเอาทองจากเควสไปปั๊มเงื่อนไขเควส `L6` สะสมทอง 3,000 วนเป็นงูกินหาง)
- **MP หนีบด้วย `maxMpOf(g)`** จึงได้โบนัสของ v4.4/v4.6 ไปด้วยโดยไม่ต้องเขียนสูตรซ้ำ (กับดักข้อ 12)
- **EXP อาจดันเลเวลอัป ซึ่ง `levelUpCheck()` ของ v4.0 เซ็ต `hp = maxHp` ให้ทันที** —
  ถ้ากำลังถือ 💀 พันธสัญญาโลหิตทมิฬ ต้องหนีบ `hp` กลับเป็นค่าก่อนจ่าย (อ่าน `cdSt().noHeal`)
  ท่าเดียวกับที่ wrapper ของ `clearFloor` ใน v4.7 ทำ

**การ์ดหายากของ `T7` ถูกใส่มือตรง ๆ ผ่านโมเดลสถานะของ v4.7 ทุกประการ** (`CD_CARD` / `CD_BAND`
/ `CD_ST` เหมือน `cdPick()` เป๊ะ) จึงหมดอายุเองตอนจบช่วงชั้นผ่าน `cdZoneCleanup()` และ
**ไม่ทำให้หน้าต่างจั่วเด้งซ้ำกลางช่วงเพราะปั๊ม `CD_BAND` ไว้ด้วย**
· `TQ_RARE_POOL` คัดเฉพาะใบที่เป็นคุณล้วน ๆ จากสายประคองเกม/แลกเปลี่ยน (`ward` `focus` `mana`
`dew` `merchant` `chrono` `edge`) **ไม่หยิบสายวัดใจ** เพราะของรางวัลที่บังคับให้ HP เหลือ 1
ไม่ใช่รางวัล · การ์ดใบเดิมที่ถืออยู่ถูกแทนที่ (บอกไว้ในตัว toast แล้ว)

**บาลานซ์:** สเกลรางวัลชุดนี้มาจากคำสั่งโดยตรง (200-1,000) ซึ่ง **สูงกว่ากรอบเดิมของ v4.2 มาก**
(เควสรายวันทั้งชุดของ v4.2 = 115 ทอง/วัน น้อยกว่าเคลียร์ 1 ชั้น) — เพดานรายวันของชั้นนี้คือ
750 ทอง + ไอเทม ≈ 5 ชั้นปกติ **ถ้าจะรัดเข็มขัดให้ลดที่ค่าคงที่ `TQ_RW_*` อย่างเดียวพอ**
ไม่ต้องแตะตรรกะ เพราะข้อความบนกระดานอ่านจากค่าพวกนั้นอยู่แล้ว

#### Silence Mode Constraint — กัก toast เฉพาะของชั้นนี้

สเปกสั่งให้ toast ของสายเควสเด้งเฉพาะ "ช่วงจบข้อ/สรุปผล" เพื่อไม่ให้บังโจทย์ · พินอิน · คำแปล
· ตัวเลือก A-D ระหว่างนับเวลาตอบ

**นี่ไม่ใช่การเอา Silence Queue ที่ v4.9.1 ถอดทิ้งกลับมา** — `NC_Q` `NC_PUMP` `ncBusy()`
`ncQueue()` `ncFlush()` ยังไม่มีอยู่และอย่าเอากลับมา · แจ้งเตือน**ของชั้นอื่นทุกใบยังยิงสดเหมือนเดิม**
ตัวกักอยู่ที่ `tqNotify()` ของชั้นนี้ชั้นเดียว และมีผลเฉพาะ def ที่ติดธง `hold`

| ชิ้นส่วน | หน้าที่ |
|---|---|
| `tqBusy()` | "มีข้อค้างอยู่ไหม" — `QUESTION_TIMER` · `FREEZE_TIMER` · `AC_HELD` ของ v4.8.1 |
| `TQ_HOLD` | คิวที่กักไว้ — เป็นของรอบการเล่น ไม่ต้องเซฟ |
| `tqFlush()` | ปล่อยคิว เรียกจาก `resolveAnswer` · `onMonsterDefeated` · `clearFloor` · `openWarp` |

**ห้ามผูก `tqFlush` เข้ากับ `clearQuestionTimer`** ทั้งที่ดูเป็นจุด "จบข้อ" ที่ตรงที่สุด —
`startQuestionTimer()` ของ v4.0 เรียกมันเป็นบรรทัดแรก การ์ดที่ค้างอยู่จะถูกปล่อยพอดีจังหวะ
ที่ข้อใหม่ขึ้นจอ ซึ่งคือสิ่งที่กติกานี้ตั้งใจกันไว้

**คิวหลายใบต้องรวมเป็นใบเดียวก่อนยิง** เพราะ v4.9.1 ตั้ง `NC_MAX = 1` (ใบใหม่ล้างใบเก่าทันที)
ยิงรัวจะเหลือให้เห็นแค่ใบสุดท้าย — `tqFlush()` รวมใบ `DONE` ทั้งหมดเป็น `DONE_MANY`
ส่วนใบอื่นยุบตามคีย์ (เก็บใบล่าสุดของแต่ละคีย์)

#### สารบัญของชั้นนี้ — TRACK 01/05-05/05

**แยกจาก `SYS_NOTIF` (ตรึง 25) · `Q_NOTIF` (6) · `CX_NOTIF` (4) · `AB_NOTIF` (9) · `CD_NOTIF` (9)
· `NC_NOTIF` (3)** ยืมแค่กลไกโชว์ `snShowToast` มาใช้ และเป็น **toast ล้วนทั้งชุด ไม่มี gate**
(กับดักข้อ 18 — ทุกจุดที่ยิงอาจอยู่ระหว่างนาฬิกาต่อข้อเดิน)

| แท็บ | คีย์ | ยิงตอนไหน |
|---|---|---|
| 01/05 | `DONE` | เควสสำเร็จ 1 รายการ (ข้อความตามสเปก `✨ เควสต์สำเร็จ!: … — รับรางวัล …`) |
| 02/05 | `DONE_MANY` | สำเร็จพร้อมกันหลายรายการ |
| 03/05 | `UNLOCK` | เคลียร์ชั้นแรกของวัน = ปลดล็อกสาย 1 |
| 04/05 | `BAGFULL` | ของแถมไม่มีที่เก็บ รางวัลถูกพักไว้ |
| 05/05 | `CLAIM` | กดรับของที่พักไว้สำเร็จ |

#### ข้อมูลที่ชั้นนี้เพิ่ม

```
account.tq = {
  day: 'YYYY-MM-DD',                       /* รอบวัน — ใช้ qDayKey() ของ v4.2 */
  d: { ans, cast, shop, weak, acc, a1 },   /* ตัวนับรายวันของสาย 1 + สาย 2 */
  unlocked: false,                         /* เคลียร์ครบ 1 ชั้นของวันนี้แล้วหรือยัง */
  done: { d: [], L: [] },                  /* จ่ายรางวัลไปแล้ว */
  pend: [],                                /* สำเร็จแล้วแต่ของแถมยังไม่มีที่เก็บ */
  L: { sboss: 0, nopot: [], streak: 0 }    /* ความคืบหน้าสาย 3 (ตลอดชีพ) */
}
```

**ไม่มีตรารุ่น `tqv`** เพราะ **ไม่มีอะไรให้ backfill** — v4.0-v4.9.2 ไม่เคยเก็บว่าใครร่ายสกิล
ขณะต่อสู้กี่ครั้งหรือผ่านบอสโดยไม่ใช้ยาบานไหน การเดาย้อนหลังจาก `bossClears` จะแจกรางวัล
1,000 ทองฟรีให้บัญชีเก่า บัญชีเก่าจึงเริ่มนับจากศูนย์แล้วไปทำเอาในรอบถัดไป

`tqEnsure()` ซ่อมข้อมูลเพี้ยน (ตัวนับติดลบ/ไม่ใช่ตัวเลข · id ซ้ำหรือผิดหมวด · `sboss` เกิน 1
· ชั้นใน `nopot` ที่ไม่ใช่ชั้นบอสของเควสนี้) และต้อง idempotent เพราะถูกเรียกทุกครั้งที่อ่าน store
ผ่าน `migrateAccount` · `tqSave()` เขียนลง store เอง เพราะ `saveProgress` ของ v4.0 ไม่รู้จักฟิลด์นี้
และมีสวิตช์ `TQ_READY` แบบเดียวกับ `AB_READY` / `CD_READY` (กับดักข้อ 16) · `tqOf()` เป็น
**read-only** คืน `null` ถ้ายังไม่มี

**สถานะที่ผูกกับ "ชั้นที่ยืนอยู่" ไม่ใช่บัญชี จึงไม่เซฟ** — `TQ_HITS`/`TQ_HIT_F` (ตอบถูกกี่รอบ
ในห้องบอส ใช้กับ 👑) · `TQ_POT_F` (ชั้นที่เพิ่งกดยาฟื้นพลัง ใช้กับ 🛡️) · `TQ_SES` (ความแม่นยำ
ประจำรอบ ใช้กับ 🎯) ทั้งสามตัวถูกล้างใน `tqSync()` เมื่อก้าวพ้นชั้นนั้น/เข้าเกมใหม่
กฎเดียวกับ `AB_LIVE` ของ v4.6 · `HD_TIMEOUT` ของ v4.8 · `AC_HELD` ของ v4.8.1

**`clearFloor` ต้องอ่านค่าก่อนให้ของเดิมทำงานเสมอ** เพราะ `clearFloor` ของ v4.0 ดัน `G.floor`
ขึ้นไปแล้วตอนจบ — ทั้งเลขชั้นที่เพิ่งเคลียร์ · จำนวนรอบที่ตอบถูก · ธงยาฟื้นพลัง ถูก snapshot
ไว้ก่อนบรรทัด `apply` ทั้งหมด

**`tqSeedSeen()` ตีตราเฉพาะเควสที่จ่ายไปแล้วหรือพักรอที่ว่างอยู่** ห้ามตีตราเควสที่เงื่อนไขครบ
แต่ยังไม่ได้รับของ (เช่นปิดแท็บกลางคัน) ไม่งั้นมันจะค้างอยู่แบบนั้นตลอดกาล — ตัวจ่ายจะเก็บตกให้เอง
ในการตอบข้อถัดไป **นี่ต่างจาก `qSeedSeen()` ของ v4.2 โดยตั้งใจ** เพราะที่นั่นมีปุ่มกดรับเป็นทางออก

**โหมดฝึกจุดอ่อนไม่นับความคืบหน้าเลยสักตัว** (ทุก wrapper เช็ก `practiceMode` ก่อน)

#### ผลข้างเคียงที่ต้องรู้เวลาเขียนเทสต์

**รางวัลเควสเข้ากระเป๋าในสายเรียกเดียวกับ `clearFloor`** ส่วนต่าง `G.gold` คร่อม `clearFloor()`
จึง **ไม่ใช่ "ทองที่บอสจ่าย" อีกต่อไป** — กล่องรางวัลของ v4.9.2 (`DW_WON`) ยังรายงานเฉพาะยอด
ของบอสอย่างถูกต้อง เพราะ wrapper ของ v4.9.2 วัดเสร็จก่อนชั้นนี้จะจ่าย
เทสต์ที่วัดส่วนต่างทั้งก้อนต้องปิดเควสสาย 3 ก่อน (`test_dual_warp_gate.js` บล็อก 4 ทำแบบนั้น)



### ชั้นที่สิบสี่ — v5.1 · DYNAMIC AUDIO ENGINE (Sound Manager + BGM Architecture Ready)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น TRIPLE QUEST ENGINE เริ่มที่ `const AU_VER` ทุกอย่างขึ้นต้นด้วย `au` / `AU_`

**ชั้นนี้ไม่แก้โค้ด v4.0-v4.9.3 เลย — ห่อฟังก์ชันแบบเดียวกับสิบสามชั้นก่อนหน้า**

ฟังก์ชันที่ถูกห่อเพิ่ม: `toggleMute` `startGame` `exitGame` `resolveAnswer` `renderMonsterHp`
`useItem` `useSkill` `levelUpCheck` `snShowToast` `onHunterDown` `clearFloor` `renderStats`
`startQuestionTimer` `clearQuestionTimer` `nextMonster` `openWarp` `warpGo` `cdPick`
`cdInfernoSealStrike` `acSync`

**เสียงทุกเสียงสังเคราะห์ด้วย Web Audio ไม่มีไฟล์เสียงแนบสักไฟล์** — กฎไฟล์เดียวจบห้ามอ้าง
path ภายนอก และการฝังเสียงเป็น data URI จะทะลุเพดาน 2MB ทันที (ไฟล์รวมเหลือที่ว่างราว 30KB)
ทั้งชั้นนี้จึงเพิ่มขนาดไฟล์แค่ ~44KB ซึ่งเป็นตัวโค้ดล้วน ๆ

#### กราฟเสียง — สองช่องสัญญาณผ่าน master เดียว

```
AU_CTX
 └─ AU_MASTER ──┬── AU_SFX_BUS ── ช่องเสียงในพูล 16 ช่อง (Audio Pooling)
                └── AU_BGM_BUS ── MediaElementSource ของเพลงประกอบ
```

**สวิตช์ 🔊 หรี่ที่ `AU_MASTER` จุดเดียว จึงปิดครบทั้งสองช่องพร้อมกันในคลิกเดียว**
ระดับเสียงแยกช่องอยู่ที่ `AU_VOL_MASTER 0.90 · AU_VOL_SFX 0.85 · AU_VOL_BGM 0.42` — แก้ที่นี่ที่เดียว

`auCtx()` สร้างกราฟทั้งชุดแบบ lazy ครั้งเดียว แล้ว **ยกให้ `chime()` ของ v4.0 ใช้ context ตัวเดียวกัน**
(เติมค่าให้ตัวแปร `AUDIO_CTX` ที่มันเช็กอยู่แล้ว ไม่ได้แก้โค้ดของมัน) ไม่งั้นจะมี AudioContext
สองตัวแย่งฮาร์ดแวร์เสียงบนมือถือ · เสียงกริ๊งของ v4.0 ยังเช็ก `G.muted` ตัวเดียวตามเดิม
**ชั้นนี้จึงต้องซิงก์ `G.muted` ให้ตรงกับสวิตช์เสมอ** ไม่งั้นจะปิดเสียงแล้วยังได้ยินกริ๊งตอนแช่แข็ง

#### Audio Pooling — ช่องเสียงหมุนเวียน 16 ช่อง

Web Audio บังคับว่า `OscillatorNode` / `BufferSourceNode` ใช้ได้ครั้งเดียว สร้างใหม่ทุกครั้งเสมอ
**ของที่พูลได้จริงคือ `GainNode` ของซองเสียง** — `AU_VOICES` ถือไว้ 16 ตัวต่อค้างกับ `AU_SFX_BUS`
ตั้งแต่ตอนสร้าง context แล้วหมุนเวียนใช้ ไม่ต้องต่อ/ตัดสายใหม่ทุกครั้งที่กดปุ่ม

- เต็มพูลแล้วจะ **แย่งช่อง** ตัวที่ใกล้จบที่สุด (voice stealing) ไม่ใช่ปล่อยให้กราฟบานปลาย
- `slot.gen` คือรุ่นของการยืมช่อง **จำเป็น ไม่ใช่ของประดับ** — เสียงตัวเก่าที่ถูกแย่งช่องไปจะจบ
  ทีหลังแล้วเรียก `auFree()` ถ้าไม่เทียบรุ่นไว้ มันจะไปปลดช่องที่เสียงตัวใหม่กำลังใช้อยู่
- `AU_LAST[key]` เป็น throttle รายคีย์ (ค่าเริ่มต้น `AU_GAP` 45ms · แต่ละเสียงตั้ง `gap` เองได้)
  กดปุ่มย้ำ ๆ จึงไม่ได้เสียงซ้อนกันจนแตก

วัดจริงแล้วยิงรัว 300 ครั้งโดยปิด throttle: ไม่มี error · ช่องที่ใช้พร้อมกันสูงสุดไม่เกิน 16 · พูลไม่งอกโหนดเพิ่ม

#### คลังเสียง — `AU_SFX_DEF` 20 รายการ ใช้รหัสตามสเปกเป็นชื่อคีย์ตรง ๆ

หนึ่งเสียงคือรายการ "พาร์เชียล" ที่เล่นพร้อมกัน หนึ่งพาร์เชียลกินหนึ่งช่องของพูล

| คีย์ | ความหมาย |
|---|---|
| `w` | รูปคลื่น `sine` / `square` / `saw` / `tri` / `noise` |
| `f` · `f2` | ความถี่เริ่ม · ความถี่ปลาย (ไล่ระดับตลอดช่วง) |
| `t` · `d` · `g` · `a` | หน่วงจากจุดเริ่ม · ความยาว · ความดังยอด · ระยะขาขึ้น (วินาที) |
| `lp` · `lp2` · `hp` · `q` | โลว์พาสเริ่ม-ปลาย · ไฮพาส · ความชันฟิลเตอร์ |

**สารบัญเสียงอยู่ที่ก้อนนี้ก้อนเดียว** เพราะชื่อคีย์คือรหัสในสเปก (`SFX_HIT_CRIT` ฯลฯ)
ไม่ต้องมีตารางแปลงชื่อให้ต้องซิงก์สองที่ · `noise` ใช้บัฟเฟอร์ขาวความยาว 0.5 วิ ที่สร้างครั้งเดียว
แล้ววนซ้ำ (`AU_NOISE`)

#### Trigger Mapping — ยิงเสียงตอน "เกิดผลจริง" ไม่ใช่ตอนกดปุ่ม

| หมวด | รหัสเสียง | ยิงจากไหน |
|---|---|---|
| BATTLE | `SFX_HIT_CORRECT` · `SFX_HIT_CRIT` | wrapper ของ `resolveAnswer` |
| BATTLE | `SFX_TAKE_DAMAGE` | ส่วนต่าง `G.hp` คร่อม `resolveAnswer` (เกราะรับไว้ = ไม่มีเสียง) |
| CARDS | `SFX_CARD_FIRE` | เลือก 🔥 · ตอบถูกขณะถือ 🔥 · `cdInfernoSealStrike()` เผาผนึกสำเร็จ |
| CARDS | `SFX_CARD_LIGHTNING` | เลือก ⚡ · เสีย HP ขณะถือ ⚡ |
| CARDS | `SFX_CARD_DRAW` | เลือกการ์ดใบอื่น |
| ITEMS | `SFX_ITEM_POTION` · `SFX_ITEM_USE` | ของถูกหักจากกระเป๋าจริงใน `useItem` |
| SKILLS | `SFX_SKILL_CAST` | MP ถูกหักจริงใน `useSkill` |
| ALERTS | `SFX_LOW_HP_LOOP` | ลูปตาม `abDangerLevel()` ของ v4.6 ระดับ 2 (แดงวิกฤต) |
| ALERTS | `SFX_TIME_WARNING_3S` | ลูป 3 วินาทีสุดท้ายของข้อ |
| SYSTEM | `SFX_QUEST_COMPLETE` | toast สีฟ้าโทน `good` ที่ **ขึ้นจอจริง** |
| SYSTEM | `SFX_SYSTEM_ALERT` · `SFX_SYSTEM_TOAST` | toast โทน `warn`/`danger` · โทนปกติ |
| SYSTEM | `SFX_LEVEL_UP` | `G.level` ขยับใน `levelUpCheck` |
| SYSTEM | `SFX_CLICK` | ลิสเนอร์คลิกระดับเอกสารตัวเดียว (`button, .g-choice, .cd-card, .g-slot, .nc-opt, .tr-hit`) |
| FLOOR | `SFX_FLOOR_CLEAR` · `SFX_BOSS_CLEAR` | `clearFloor(false/true)` |
| FLOOR | `SFX_GUARD` · `SFX_HUNTER_DOWN` | `onHunterDown` — ดูว่า 🛡️ ประกันภัยถูกกินไปหรือเปล่า |

**ห้ามยิงเสียงตั้งแต่ตอนเข้าฟังก์ชัน** — `useItem` / `useSkill` / `resolveAnswer` มีทางออกก่อน
หักของ/หัก MP/ลด HP หลายทาง (HP เต็ม · ยังไม่มีนาฬิกา · ตัวเลือกเหลือ 2 · MP ไม่พอ · เกราะรับไว้)
ทุกจุดจึงวัด **ส่วนต่างจริง** ก่อน/หลังเรียกของเดิม กติกาเดียวกับที่ v4.6 ใช้กับ `AB_ITEM_STRIP`

**เสียง toast ต้องเช็กว่า "ขึ้นจอจริง" ไม่ใช่ว่าถูกเรียก** — wrapper ของ v4.9.1 ปัดตกทั้งใบตามโหมด
🔕 / ⚠️ ที่ผู้เล่นตั้งไว้ ชั้นนี้จึงเทียบ `#snLayer` `lastElementChild` ก่อน/หลัง ถ้าไม่มีโหนดใหม่ = เงียบ

**คริติคอลอ่านจาก `renderMonsterHp` ไม่ใช่จาก `resolveAnswer`** — v4.0 ตัดสินคริตในตัวแปรท้องถิ่น
ที่อ่านจากข้างนอกไม่ได้ จุดสังเกตที่มีอยู่จริงคือตอนมันเรียก `renderMonsterHp()` ซึ่งตอนนั้น
`#gFeedback` เพิ่งถูกเขียนว่า `⚡ CRITICAL!` และ **ยังไม่ถูกข้อความ "อสูรอักขระถูกปราบ" เขียนทับ**
จึงอ่านได้ทั้งเคสล้มและไม่ล้ม · ธง `AU_LIVE` กันไม่ให้ไปอ่านตอน `renderMonsterHp` ถูกเรียกจากที่อื่น
และถูกคืนค่าใน `finally` เสมอ (กฎเดียวกับ `HD_TIMEOUT` ของ v4.8)

#### Autoplay Safeguard

เบราว์เซอร์มือถือสร้าง AudioContext มาในสถานะ `suspended` จนกว่าจะมี user gesture
`auUnlock()` ถูกผูกกับ `pointerdown` / `touchstart` / `keydown` / `click` ที่ระดับเอกสารแบบ
**capture** จึงได้ยินก่อนเสมอแม้ปุ่มนั้นจะ `stopPropagation`

**ไม่ถอด listener ทิ้งหลังปลดล็อกครั้งแรก** เพราะ iOS สั่ง suspend ซ้ำได้ตอนสลับแอป —
ตัวเช็กอ่าน `ctx.state` แล้วออกทันทีถ้าปกติดีอยู่แล้ว จึงแทบไม่มีต้นทุน
· มี `visibilitychange` เรียกซ้ำอีกทางตอนกลับเข้าแท็บ

#### Control Constraint — Auto-Pause ของ v4.8.1

ข้อกำหนดข้อ 4 ของสเปก: เกมเข้าสถานะหยุดเมื่อไหร่ **เสียงแจ้งเตือนแบบลูปต้องเงียบ และ BGM ต้องหรี่/พักทันที**

**เกาะไปกับ wrapper ของ `acSync` ตัวเดียว ไม่ไล่ดักเหตุการณ์ทีละทาง** — v4.8.1 รวมทุกต้นเหตุ
(กด ⏸️ · สลับแท็บ · เปิดหน้าต่างระบบบานไหนก็ได้ · เลื่อนจอหนีโซนคำถาม) ไว้ที่ `acPaused()` อยู่แล้ว
`auPauseSync()` จึงเทียบสถานะกับรอบก่อนแล้วลงมือครั้งเดียวตอนข้ามรอยต่อ

| ชิ้นส่วน | หน้าที่ |
|---|---|
| `auPauseSync()` | ตัวประสาน — เข้าหยุด: `auLoopsOff()` + `auBgmDuck(true)` · ออก: ต่อกลับให้ครบ |
| `auDangerSync()` | เปิด/ปิดลูป HP ต่ำ — เรียกจาก wrapper ของ `renderStats` (ทุกทางที่ HP ขยับจบที่นี่) |
| `auTimeTick()` | ตัวเฝ้าเวลา 3 วินาทีสุดท้าย — ตั้งนาฬิกาตัวเองแบบแก้ค่าใหม่ทุกครั้งที่ตื่น |

**ตัวลูปเองต้องเช็ก `auPaused()` ซ้ำทุกครั้งที่ตื่นด้วย ไม่พึ่งแต่ตัวสั่งหยุดจากข้างนอก** —
สถานะหยุดเกิดได้โดยไม่ข้ามรอยต่อ (เช่นหยุดค้างอยู่ก่อนแล้วเปิดหน้าต่างซ้อนอีกบาน)
ตอนนั้น `auPauseSync()` จะไม่ทำอะไรเลยเพราะสถานะไม่เปลี่ยน แต่ต้องไม่มีเสียงหลุดออกมาอยู่ดี

`auTimeTick()` ลอกท่ามาจาก `ncLowTick()` ของ v4.9.1 ตรง ๆ — **ไม่แตะ `QUESTION_TICK` ของ v4.0 เลย**
จึงทนต่อการต่อเวลา ⏳ การแช่แข็ง ❄️ และการหยุดของ v4.8.1 ได้ทั้งหมด
· ติดอาวุธที่ `startQuestionTimer` ปลดที่ `clearQuestionTimer`
· **กด 🔇 ก็ต้องดับลูปทั้งสองตัวด้วย** (`auSetMute` เรียก `auLoopsOff()`) ไม่ใช่แค่หรี่ master

#### BGM Infrastructure — โครงพร้อม รอเสียบไฟล์เพลง

`AU_BGM_SRC` มี 4 ช่องตามสเปก **ตอนนี้ว่างทั้งหมด** วันไหนมีเพลงจริงให้เติม data URI
ลงตารางนี้ตารางเดียวก็ดังทันที ไม่ต้องแก้ตรรกะสักบรรทัด

| สถานะ | เล่นตอนไหน | วนซ้ำ |
|---|---|---|
| `BGM_TITLE` | นอกหน้าเล่นเกม (หน้าแรก / เกท) | ✓ |
| `BGM_BATTLE_NORMAL` | ชั้นมอนสเตอร์ทั่วไป 1-4 · 6-9 · 11-14 · 16-19 | ✓ |
| `BGM_BATTLE_BOSS` | ชั้นบอส 5 · 10 · 15 · 20 | ✓ |
| `BGM_VICTORY` | ยืนที่ประตูวาปหลังปราบบอส (อ่าน `DW_WON` ของ v4.9.2) | ✗ |

`auBgmWant()` **คำนวณสดจากหน้าจอตอนนั้นทุกครั้ง ไม่เก็บธงซ้อน** — `auBgmSync()` เปลี่ยนเพลง
ก็ต่อเมื่อสถานะที่ควรเป็นไม่ตรงกับที่เล่นอยู่ จึงเรียกถี่ ๆ ได้ฟรี (ผูกไว้ที่ `nextMonster`
`clearFloor` `openWarp` `warpGo` `startGame`) · ไม่มีเลขชั้นฮาร์ดโค้ด อ่าน `isBossFloor()` ของ v4.0

`auBgmFade(to, ms)` ไล่ระดับที่ `AU_BGM_BUS.gain` จุดเดียว · `AU_FADE_IN 900 · AU_FADE_OUT 700
· AU_FADE_DUCK 260` · `auBgmDuck(on)` คือการพักชั่วคราวตอนเกมหยุด (จำสถานะไว้แล้วต่อกลับ)
ต่างจาก `auBgmStop()` ที่ทิ้งทั้งก้อน

**ตอนที่ยังไม่มีเพลง `auBgmPlay()` คืน `false` แต่ยังจำสถานะไว้ใน `AU_BGM_STATE`** —
ตั้งใจให้เป็นแบบนั้น เพื่อให้ระบบสถานะทั้งชุดทดสอบได้จริงตั้งแต่วันนี้ ไม่ต้องรอไฟล์เพลง

#### สวิตช์ 🔊 — ปุ่มเดิมของ v4.0 ที่แถบหัวหน้าจอเล่น

**ไม่ได้ย้ายปุ่มและไม่ได้สร้างปุ่มใหม่** — ปุ่ม `#gMute` ใน `.g-topbar` (ถัดจากปุ่มกระดิ่ง `#ncBell`
ของ v4.9.1) ยังเรียก `toggleMute()` ของ v4.0 ตามเดิม ชั้นนี้ห่อมันไว้อีกทีเพื่อ

1. หรี่ `AU_MASTER` → ปิดครบทั้ง SFX และ BGM ในคลิกเดียว
2. เขียนค่าลง `localStorage['yao_audio']` (ระดับเครื่อง)
3. ดับลูปแจ้งเตือน + พัก BGM ทันที

**ค่าในเครื่องเป็นตัวตัดสินเหนือ `a.muted` ของบัญชี** — wrapper ของ `startGame` เขียน
`G.muted = AU_MUTE` ทับหลังของเดิมทำงานจบ เพราะลำโพงเป็นของเครื่องไม่ใช่ของคน
(กติกาเดียวกับโหมดแจ้งเตือน `yao_notify` ของ v4.9.1) · `a.muted` ยังถูกเซฟตามเดิม ไม่ได้ถูกถอดทิ้ง

`auToggleMute()` เป็นทางเข้าสาธารณะสำหรับโค้ดอื่น/เทสต์ ที่ทำงานได้แม้ยังไม่ได้เข้าเกม

#### ชั้นนี้ไม่เพิ่มฟิลด์ในบัญชีเลย

ไม่มี `auEnsure()` ไม่มี `auSave()` ไม่มีตรารุ่น `auv` และ **ไม่ได้ห่อ `migrateAccount` ด้วยซ้ำ** —
ค่าเสียงอยู่ที่ `localStorage` ระดับเครื่อง ส่วนที่เหลือเป็นสถานะของรอบการเล่น/ของข้อที่กำลังตอบ
(`AU_LIVE` `AU_CRIT` `AU_TM_T` `AU_HP_T` `AU_PAUSED` `AU_BGM_STATE`) บัญชีเก่าจึงได้ระบบนี้ทันที
ที่เปิดเกม โดยไม่ต้อง migrate อะไรเลย

**และไม่มีสารบัญแจ้งเตือนของตัวเอง** ไม่ได้เพิ่ม toast หรือ gate ใบใหม่สักใบ ไม่ได้เพิ่ม DOM สักโหนด
(นอกจากแก้ `title` ของปุ่ม `#gMute` ให้ตรงกับหน้าที่ใหม่) เลย์เอาต์จึงไม่ขยับแม้แต่พิกเซลเดียว

**เสียงพังห้ามลามไปทำเกมพัง** — ทุกจุดหุ้ม `try/catch` แล้วเขียนลง Error Log ของ v4.3 ผ่าน `auErr()`
เครื่องที่ไม่มี Web Audio เลย (`AudioContext` ไม่มีจริง) จะได้เกมเงียบ ๆ ที่เล่นได้ครบทุกอย่าง


### ชั้นบนสุด — v5.2 · CLOUD SYNC (Online Save & Data Sync Engine)

อยู่ท้ายสุดของไฟล์ ต่อจากชั้น DYNAMIC AUDIO ENGINE เริ่มที่ `const CS_VER` ทุกอย่างขึ้นต้นด้วย `cs` / `CS_`

**ชั้นนี้ไม่แก้โค้ด v4.0-v5.1 เลย — ห่อฟังก์ชันแบบเดียวกับสิบสี่ชั้นก่อนหน้า**

ฟังก์ชันที่ถูกห่อเพิ่ม: `saveStore` `migrateAccount` `clearFloor` `openWarp` `buyItem` `levelUpCheck`
`startGame` `exitGame` `handleSubmit` `enterGate` `openBoard` `renderBoard` `gmRender`

**ชื่อในคำสั่งคือ "ชั้น v5.0" แต่ป้ายชั้นเป็น v5.2** เพราะต้องวางท้ายไฟล์เพื่อห่อทับ v5.1 ได้
เลขชั้นเดินตามลำดับการห่อจริงเสมอ ไม่ใช่ตามเลขในคำสั่ง (กติกาเดียวกับ v4.9.2 ที่คำสั่งเรียกว่า v4.8.2)

#### สามข้อจำกัดที่ตัดสินรูปร่างของชั้นนี้ทั้งหมด — อ่านก่อนแก้

1. **กฎไฟล์เดียวจบห้ามโหลด SDK จาก CDN** Firebase JS SDK และ supabase-js ใช้ไม่ได้ทั้งคู่
   ชั้นนี้จึงคุยกับคลาวด์ด้วย `fetch` + **REST ล้วน** — Supabase ใช้ PostgREST · Firebase ใช้ RTDB REST
   **ห้ามเปลี่ยนไปใช้ SDK แม้จะเขียนง่ายกว่ามาก** ไฟล์ที่แจกทาง LINE จะพังทันทีตอนเปิดออฟไลน์
2. **Google Auth ทำไม่ได้ด้วยเหตุผลเดียวกัน** — Google Identity Services ต้องโหลดสคริปต์จาก
   `accounts.google.com` และ OAuth redirect ก็ทำงานไม่ได้บนหน้าที่เปิดจาก `file://`
   ตัวตนจึงยังเป็น **รหัสฮันเตอร์ + รหัสผ่านลับ** ชุดเดิมของ v4.0 แต่ย้ายไปตรวจกับคลาวด์ก่อน
   แล้วเสริม **Auto Silent Relogin** (จำเซสชันไว้ในเครื่อง) ให้แทนความสะดวกที่ Google Auth จะให้
3. **"100% Online" ถูกทำเป็น Cloud-First ไม่ใช่ Online-Only** ตัวเกมยังเล่นได้เต็มรูปแบบตอนเน็ตหลุด
   (นักเรียนเปิดจากไฟล์ที่ดาวน์โหลดไว้ · เน็ตมือถือหลุดกลางคาบ) แล้วซิงก์ตามให้เองเมื่อสัญญาณกลับมา
   ถ้าครูต้องการออนไลน์บังคับจริง ๆ มีสวิตช์ `strict` ให้ติ๊ก — ต่อคลาวด์ไม่ได้ = ล็อกอินไม่ได้
   **ค่าเริ่มต้นคือปิด** เพราะห้ามให้นักเรียนที่เน็ตไม่ดีเข้าเรียนไม่ได้

#### คอนฟิกไม่ได้ฝังในไฟล์ — ครูกรอกเองที่แผง GM

`repo นี้เป็น public` การฝัง URL + API key ลงซอร์สคือการแจกกุญแจฐานข้อมูลให้ทุกคน
คอนฟิกจึงอยู่ที่ `localStorage['yao_cloud']` **ระดับเครื่อง** และกรอกผ่านปุ่ม ☁️ คลาวด์ บนหัวแผง GM

```
{ provider: '' | 'supabase' | 'firebase', url, key, table: 'hunters', auto: true, strict: false }
```

**`provider` ว่าง = ทั้งชั้นนี้เงียบสนิท** (`CS_ON` เป็น false) ไม่มีคำขอออกเน็ตแม้แต่ครั้งเดียว
ชิปสถานะถูกซ่อน เกมทำงานเหมือนก่อนมีชั้นนี้ทุกประการ — **ไฟล์ที่แจกนักเรียนออกจากมือไปในสภาพนี้เสมอ**

โครงตารางฝั่ง Supabase (คำสั่งเดียวกันนี้โชว์อยู่ท้ายแผง GM ให้ก๊อปได้):

```sql
create table hunters (usr text primary key, room text, name text, pwh text,
                      rev int8, ts int8, dev text, ver text, doc jsonb, an jsonb);
```

**คอลัมน์ชื่อ `usr` ไม่ใช่ `user`** เพราะ `user` เป็นคำสงวนของ Postgres
· ฝั่ง Firebase ใช้โหนด `/<table>/<รหัสฮันเตอร์>` โครงเดียวกัน ไม่ต้องตั้ง `.indexOn`
เพราะการดึงทั้งห้องอ่านทั้งโหนดมากรองในเครื่อง (ห้องเรียนเดียวข้อมูลไม่ใหญ่)

#### หนึ่งแถว = หนึ่งฮันเตอร์ · `doc` กู้คืน · `an` ให้ครูอ่าน

| ฟิลด์ | คืออะไร |
|---|---|
| `doc` | บัญชีทั้งก้อนของ v4.0-v4.9.3 ไว้กู้คืนแบบเป๊ะ — **ตัด `pw` ออกเสมอ** |
| `pwh` | แฮชรหัสผ่าน (`csPwh`) ใช้ตรวจตัวตนอย่างเดียว |
| `an` | ข้อมูลวิเคราะห์แบบแบน สำหรับ GM / กระดานผู้นำ อ่านได้โดยไม่ต้องแกะ `doc` |
| `rev` `ts` `dev` | เลขรุ่น · เวลาที่เขียน · รหัสเครื่องที่เขียน (ใช้ตัดสินตอนข้อมูลชนกัน) |

`an` ครอบสามหมวดที่สเปกสั่งไว้ครบ: **โปรไฟล์/ความคืบหน้า** (`lv` `exp` `hp` `mhp` `mp` `gold`
`tgold` `floor` `mfloor` `zone` `boss` `tower` `rank` `srank`) · **เควสและกระเป๋า**
(`quest` ของ v4.2 · `tquest` ของ v4.9.3 · `items` · `card` · `drafts` · `shards` · `core`) ·
**สถิติการเรียนรู้** (`corr` `wrong` `acc` `agi` `fast` `words` + `weak` = คำที่ตอบผิด ≥2 ครั้ง
สูงสุด `CS_WEAK_MAX` 12 คำ แนบอักษรจีนกับพินอินจาก `VOCAB` ไปด้วย เรียงจากผิดมากสุดก่อน)

**ห้ามส่ง `pw` ขึ้นคลาวด์เด็ดขาด** `csDoc()` กรองออกให้แล้วที่จุดเดียว ตอนกู้คืนกลับมา
`csAdopt()` เติม `pw` จากรหัสที่ผู้เล่นเพิ่งพิมพ์ (หรือของเดิมในเครื่อง) ผลพลอยได้คือแถวของเพื่อน
ร่วมห้องที่ครูดึงลงมาจะไม่มี `pw` ใครล็อกอินบัญชีคนอื่นจากเครื่องครูไม่ได้

#### จุดดักมีจุดเดียวคือ `saveStore` — ครอบ Auto-Save Trigger ทุกข้อในสเปก

ทุกการเขียนบัญชีของทุกชั้น (`saveProgress` ของ v4.0 · `qSave` · `trSaveFloors` · `abSave` ·
`cdSave` · `tqSave` · เครื่องมือทุกตัวในแผง GM) ไหลมาลงที่ `saveStore` หมด
**ถ้าจะเพิ่มจุดเซฟใหม่ในอนาคต ไม่ต้องมาแตะชั้นนี้เลย** ขอแค่เขียนผ่าน `saveStore` ตามกติกาเดิม

| จังหวะ | ทำอะไร |
|---|---|
| ตอบจบข้อ (`saveProgress`) | `csTouch(false)` → หน่วง `CS_DEBOUNCE` 6 วิ แล้วยุบเป็นการส่งครั้งเดียว |
| เคลียร์ชั้น · ประตูวาป · ซื้อของ · อัปเลเวล | `csTouch(true)` → ส่งทันที |
| ออกจากเกม · สลับแอป (`visibilitychange`) · ปิดแท็บ (`pagehide`) | เก็บลงกล่องส่งออกแล้วส่งทันที |
| สัญญาณกลับมา (`online`) | ล้างตัวนับ backoff แล้วดันคิวเอง |

**หน่วง 6 วินาทีไม่ใช่ของประดับ** — บัญชีหนึ่งก้อนมี `wordStats` ครบ 329 คำ ถ้ายิงทุกข้อ
นักเรียนหนึ่งห้องจะกินโควตาฐานข้อมูลฟรีหมดภายในคาบเดียว · จุดสำคัญยังส่งทันทีอยู่ ข้อมูลจึงไม่หาย

#### กล่องส่งออก — เน็ตหลุดต้องไม่ทำข้อมูลหาย

`csStage(user)` เก็บ **ภาพนิ่ง** ของบัญชีลง `localStorage['yao_cloud_out']` **ก่อนยิงเสมอ**
ปิดแท็บกลางทางหรือเน็ตตัดกลางคำขอก็ยังอยู่ครบ รอบหน้าที่เปิดเกมจะถูกส่งต่อให้เอง
· กล่องเก็บ **รายการเดียวต่อคน** (เขียนทับ) จึงไม่มีทางบวมแม้ออฟไลน์ทั้งวัน
· ส่งไม่สำเร็จ → `csRetry()` ไล่ระยะ `CS_BACKOFF` `[4, 8, 16, 32, 60]` วินาที

**`csStage` เขียน `SKEY` ตรง ๆ ไม่ผ่าน `saveStore`** เพราะ `saveStore` ถูกห่อไว้แล้ว
เรียกมันจะวนกลับมาหาตัวเอง — ต้องเขียนเองเพื่อจด `cs.rev` ที่เพิ่งเดินหน้าไป

#### ข้อมูลชนกัน — Timestamp-based Server Wins

| ฝั่ง | วิธี |
|---|---|
| Supabase | `PATCH ?usr=eq.X&ts=lt.<ของเรา>` = **compare-and-set จริง** แถวบนคลาวด์ใหม่กว่า → ไม่มีแถวไหนถูกแก้เลย |
| Firebase | REST ไม่มี CAS จึง `GET` มาเทียบก่อน `PUT` เหลือช่องว่างเสี้ยววินาที (ยอมรับไว้ · ถ้าต้องกันแน่นจริงต้องใช้ Supabase) |

เจอว่าคลาวด์ใหม่กว่า → `csConflict()` **สำรองก้อนของเครื่องนี้ลง `yao_cloud_bak` ก่อนเสมอ**
แล้วเด้ง gate `CLOUD 05/05` · กดยืนยันแล้วลำดับคือ `exitGame()` → `csAdopt()` → `startGame()`

**ลำดับนี้สลับไม่ได้** ถ้า adopt ก่อน `exitGame` ตัว `saveProgress()` ที่ `exitGame` เรียกข้างใน
จะเอา `G` ก้อนเก่าเขียนทับก้อนที่เพิ่งดึงมาทันที (อาการเดียวกับกับดักข้อ 16 แต่คนละทาง)
· ระหว่างสลับก้อนต้องปั๊ม `CS_HALT = true` ไม่งั้น `saveStore` ระหว่างทางจะยิงคิวส่งก้อนเก่าขึ้นไปอีก
และ wrapper ของ `exitGame` จะไปล้างเซสชันทิ้งทั้งที่แค่รีสตาร์ตให้

**ที่ต้องยึดฝั่งเซิร์ฟเวอร์เพราะกันปั๊มของ** — ถ้าให้เครื่องที่ล็อกอินทีหลังชนะเสมอ นักเรียนจะเปิด
สองเครื่อง เล่นแยกกัน แล้วเลือกส่งก้อนที่ได้ทอง/การ์ดเยอะกว่าขึ้นไป

#### ล็อกอิน + Auto Silent Relogin

`handleSubmit` ถูกห่อให้ไปถาม `csGetRow(id)` ก่อน **แล้วค่อยเรียกของเดิม** ตัว v4.0 ไม่ถูกแก้เลย

- **เข้าสู่ระบบ** — `pwh` ไม่ตรง = ปฏิเสธ (รหัสบนคลาวด์เป็นใหญ่) · `doc.frozen` = ถูก GM ระงับจากอีกเครื่อง
  · แถวคลาวด์ใหม่กว่าหรือเท่ากับของในเครื่อง → ดึงลงมาใช้ (สำรองของเดิมไว้ก่อน)
- **สมัคร** — รหัสฮันเตอร์ที่มีบนคลาวด์แล้ว = สมัครทับไม่ได้ (กันสองคนใช้รหัสเดียวกันคนละเครื่อง)
- **ต่อคลาวด์ไม่ได้** — ไม่ติ๊ก `strict` → เล่นด้วยข้อมูลในเครื่องไปก่อน + toast `CLOUD 02/05`

`csSilent()` ทำงาน 300ms หลังโหลดหน้า อ่านเซสชันจาก `localStorage['yao_cloud_ses']`
(อายุ `CS_SES_MS` 14 วัน) แล้วพาเข้าหอคอยเลยโดยไม่ต้องพิมพ์อะไร

**`CS_GATE` ยกเลิกล็อกอินเงียบทันทีที่ผู้เล่นกดเข้าเกทเอง** — ไม่งั้นคำขอที่ค้างอยู่จะกลับมาลาก
เขาเข้าบัญชีเก่ากลางคันขณะกำลังพิมพ์รหัสของอีกบัญชี (นักเรียนใช้เครื่องร่วมกันในห้องคอมเป็นเรื่องปกติ)
· กด 🚪 ออกจากเกท = ตั้งใจเลิกใช้เครื่องนี้ → `exitGame` ล้างเซสชันให้ (ยกเว้นตอน `CS_HALT`)

#### ห้องควบคุม GM + กระดานผู้นำ = ข้อมูลสด โดยไม่เขียน UI ใหม่สักบรรทัด

`csRoomPull(room)` ดึงทั้งห้องลงมาแล้ว **เขียนลง store ของเครื่องนี้เลย** ตาราง GM ของ v4.3 ·
กระดานผู้นำของ v4.0 · Export CSV จึงเห็นข้อมูลชุดเดียวกันทันทีโดยไม่ต้องแก้อะไร

- **ข้ามบัญชีที่กำลังเล่นอยู่บนเครื่องนี้เสมอ** และข้ามคนที่ก้อนในเครื่องใหม่กว่าบนคลาวด์
- แคช `CS_ROOM_TTL` 20 วิ · เปิดกระดานผู้นำ/เข้าเกมจะสั่งดึงให้เอง · แผง GM มีปุ่มดึงทันที
- แผงคลาวด์ของครู (`#csGm`) มีตารางของตัวเองที่โชว์ **คำที่ผิดซ้ำรายคน** ซึ่งตาราง GM เดิมไม่มี

`#csGm` ใช้โครง `.g-modal` ของ v4.0 **จึงถูก `AC_OVL_SEL` ของ v4.8.1 นับเป็นหน้าต่างระบบเอง**
ไม่ต้องไปเติมชื่อลงในตัวเลือกนั้น (ซึ่งเป็น `const` แก้ไม่ได้อยู่แล้ว)
· ปุ่ม ☁️ คลาวด์ วางบน **หัวแผง GM** ไม่ใช่คอลัมน์ ⚙️ จัดการ (กับดักข้อ 9)

#### สารบัญแจ้งเตือน — CLOUD 01/05-05/05

**แยกจาก `SYS_NOTIF` (ตรึง 25) · `Q_NOTIF` (6) · `CX_NOTIF` (4) · `AB_NOTIF` (9) ·
`CD_NOTIF` (9) · `NC_NOTIF` (3) · `TQ_NOTIF` (5)** ยืมแค่กลไกโชว์มาใช้

| แท็บ | คีย์ | ยิงตอนไหน |
|---|---|---|
| 01/05 | `PULLED` | ดึงข้อมูลจากคลาวด์มาใช้ตอนล็อกอิน |
| 02/05 | `OFFLINE` | ต่อคลาวด์ไม่ได้ตอนล็อกอิน (ไม่ได้บังคับออนไลน์) |
| 03/05 | `SYNCED` | กดปุ่มซิงก์เดี๋ยวนี้แล้วสำเร็จ |
| 04/05 | `RELOGIN` | ล็อกอินเงียบสำเร็จ |
| 05/05 | `CONFLICT` | **ใบเดียวที่เป็น gate** |

**`CONFLICT` เป็น gate ได้เพราะมันต้องหยุดเกมเพื่อสลับก้อนข้อมูลจริง ๆ อยู่แล้ว**
(และ v4.8.1 หยุดนาฬิกาให้ตอนหน้าต่างระบบเปิด) ที่เหลือเป็น toast ล้วนตามกับดักข้อ 18

#### ชิปสถานะ ☁️ บนแถบหัวหน้าจอเล่น

วางถัดจากปุ่มกระดิ่งของ v4.9.1 · 5 สถานะ: `off` ซ่อน · `ok` ☁️ · `sync` 🔄 · `wait` 📡 · `err` ⚠️
มีตัวเลขจำนวนที่ค้างส่งต่อท้าย · แตะแล้วเปิดแผงสถานะ (`snOpenPanel`) ที่มีปุ่ม **ซิงก์เดี๋ยวนี้**
กับ **เลิกจำเครื่องนี้** — ปุ่มหลังจำเป็นสำหรับเครื่องที่ใช้ร่วมกันในห้องคอม

#### ฟิลด์เดียวที่ชั้นนี้เพิ่มในบัญชี

```
account.cs = { rev, ts, dev }     /* เลขรุ่น · เวลาที่ส่งขึ้นไปล่าสุด · เครื่องที่ส่ง */
```

**ไม่มีตรารุ่น `csv`** เพราะไม่มีอะไรให้ backfill — บัญชีเก่าได้ `{rev:0, ts:0}` แล้วเริ่มนับจากนั้น
ซึ่งแปลว่า "ยังไม่เคยขึ้นคลาวด์" พอดี · `migrateAccount` ถูกห่อให้เติมให้และต้อง idempotent
· ที่เหลือทั้งหมด (`CS_SRV` `CS_REV` `CS_DIRTY` `CS_HALT` `CS_GATE` `CS_ROOM`) เป็นสถานะของ
รอบการเล่น ไม่ต้องเซฟ · คอนฟิก/เซสชัน/กล่องส่งออกอยู่ที่ `localStorage` ระดับเครื่อง

**คลาวด์พังห้ามลามไปทำเกมพัง** — ทุก wrapper หุ้ม `try/catch` แล้วเขียนลง Error Log ของ v4.3
ผ่าน `csErr()` เครื่องที่ไม่มี `fetch` หรือ `AbortController` จะได้เกมออฟไลน์ที่เล่นได้ครบทุกอย่าง

#### สามเรื่องที่ยังไม่ได้ทำ และเหตุผล

1. **Realtime แบบ push (WebSocket / Firestore listener)** — ตอนนี้เป็น pull ตามจังหวะ + แคช 20 วิ
   ห้องควบคุมจึงเห็นข้อมูล "สดในระดับ 20 วินาที" ไม่ใช่วินาทีต่อวินาที
   ถ้าจะทำจริงต้องใช้ Supabase Realtime ซึ่งเป็น WebSocket + ต้องมี SDK หรือเขียน protocol เอง
2. **Google Auth** — เหตุผลอยู่ในข้อจำกัดข้อ 2 ข้างบน
3. **`pwh` ไม่ใช่แฮชระดับ cryptographic** (`csHash` เป็น djb2) และ anon key อยู่ในเครื่องครู
   ระดับความปลอดภัยเท่ากับ `TEACHER_PIN` ที่ฝังในซอร์ส คือ **กันเด็กกดเล่น ไม่ได้กันคนตั้งใจ**
   ถ้าต้องการของจริงต้องมี Supabase Auth + RLS ผูก `auth.uid()` ซึ่งต้องใช้ SDK/redirect flow


## กับดักที่เคยทำพังมาแล้ว

### 1. คิว toast วนไม่รู้จบ → หน้าเกมค้างสนิท

`snShowToast` ตัดตัวเก่าเมื่อ toast เกิน 3 ใบ **ห้ามใช้ `snDismiss` ในลูปนั้น** เพราะ
`snDismiss` ลบโหนดหลังอนิเมชัน 300ms → `layer.children.length` ไม่ลดในลูป → วนไม่จบ
เธรดหลักค้าง ทั้งหน้าตาย

ต้อง `layer.removeChild(oldest)` แบบซิงโครนัสเท่านั้น

อาการตอนเจอ: เคลียร์ชั้นแล้วจอค้าง เพราะ 3 แจ้งเตือน (ปลดล็อกคำ + สรุปผล + กดถัดไป)
มาพร้อมกัน แล้วใบที่ 4 ทำให้วนลูป — **เจอง่ายมากในการเล่นจริง**

### 2. ฟังก์ชันที่ห่อแล้ว "แทรก DOM" ต้องกันแทรกซ้ำเอง

`renderWarp` ถูกเรียกทุกครั้งที่เปิดแผงวาป ถ้าห่อแล้วแทรก HTML ตรง ๆ ภาพจะซ้อนกันเพิ่มขึ้น
เรื่อย ๆ ทุกครั้งที่เปิด ต้องเช็กก่อนเสมอ:

```js
if (w && !w.querySelector('.wp-art')) w.insertAdjacentHTML('afterbegin', snWarpArt());
```

ใช้กฎเดียวกันกับทุก wrapper ที่ยัด element ใหม่เข้าไปในแผงที่ถูก render ซ้ำได้

### 3. แจ้งเตือน "MP พร้อมใช้" ต้องเป็น edge-trigger

`snCheckMpReady()` ใช้ `SN_MP_SEEN[key]` จำว่าเคยแจ้งไปแล้ว แล้วรีเซ็ตเป็น `false`
เมื่อมานาตกต่ำกว่าค่าใช้ ถ้าลืมส่วนนี้ ทุกครั้งที่ MP ขยับจะยิงแจ้งเตือนซ้ำรัว
และตอนเข้าเกม (MP เต็ม) จะเด้งพร้อมกันทั้ง 4 สกิล

### 4. `enterGate()` มี setTimeout 360ms

มันตั้งเวลาไว้โชว์กล่องล็อกอิน ถ้าสคริปต์เรียก `enterGate()` แล้วสมัคร/ล็อกอินทันทีในทิกเดียวกัน
timeout จะยิงทีหลังแล้วดัน `.login-box` กลับมาโชว์ทับจอเกม — ต้องรอ ~600ms ก่อนเสมอ

### 5. เงื่อนไขเควสต้องเช็กว่า "แรงค์ต่ำสุดทำได้จริงไหม"

ฟีเจอร์หลายอย่างล็อกตามแรงค์ใน `FEATURE_RANK` — เควส**รายวัน**ที่อิงฟีเจอร์ล็อกสูงจะกลายเป็นเควสตาย

เคสจริง: D7 เคยออกแบบเป็น "ตอบถูกในโหมดฝึกจุดอ่อน 8 ข้อ" แต่โหมดฝึกเข้าได้จากหน้า Analytics
ซึ่งล็อกที่ **S-RANK (ชั้น 20)** → นักเรียนต้องพิชิตหอคอยจบก่อนถึงจะเริ่มทำเควสรายวันข้อนี้ได้
เปลี่ยนมานับ "แก้มือคำที่เคยตอบผิด" (`redeem`) ซึ่งวัดเป้าหมายการเรียนอันเดียวกันแต่ทำได้ตั้งแต่ชั้น 1

ตารางที่ต้องเปิดดูก่อนตั้งเงื่อนไขเควส: `titles` C-RANK (ชั้น 11) · `shop`/`statPoints` D-RANK (ชั้น 6)
· `scroll` C · `freeze` B · `analytics` S (ชั้น 20)

ผลพลอยได้: ฉายารางวัลเควส 20 ปลดล็อกได้ทันทีที่กดรับ แต่จะ**สวม**ได้ต่อเมื่อถึง C-RANK
(กฎเดิมของ `equipTitle` ใน v4.0) — ตั้งใจให้เป็นแบบนี้ ไม่ต้องไปแก้

### 6. อย่าใช้ Read อ่านไฟล์ทั้งไฟล์

มีบรรทัดยาวหลายแสนตัวอักษร (base64 ของมาสคอต) ให้ใช้ `grep -n` หาเลขบรรทัดก่อน
แล้ว `sed -n 'A,Bp'` อ่านเฉพาะช่วง หรือกรอง `| grep -v base64`

การแก้ไฟล์ให้ใช้สคริปต์ Python แทนที่สตริงแบบตรงตัว พร้อม assert ว่าเจอ anchor
เป๊ะ 1 ครั้ง (ถ้าเจอ 0 หรือ >1 ให้ล้มทันที อย่าเดา)

### 7. ล้างข้อความตัวเลือกแล้ว ต้องกันตัวเลือกซ้ำพร้อมกันเสมอ

พอตัด `(法文)` `(法语)` ทิ้ง คำแปลไทย **11 คู่** จะเหลือข้อความเดียวกันเป๊ะ:

    1/2 ภาษาอาหรับ · 33/34 ภาษาเยอรมัน · 48/49 ภาษารัสเซีย · 50/124 สอง
    52/53 ภาษาฝรั่งเศส · 72/73 ภาษาเกาหลี · 75/298 ภาษาจีน · 165/225 ท่าน
    187/189 ภาษาญี่ปุ่น · 231/232 ภาษาสเปน · 271/272 ภาษาอังกฤษ

ตัวกรองเดิมใน `getMonster._build()` เทียบข้อความ **ดิบ** จึงปล่อยตัวลวงที่อ่านเหมือนเฉลยเป๊ะ
ออกมาได้ ตอนนี้ใช้ตาราง `taken` กันซ้ำจากข้อความ **ที่ล้างแล้ว** ทั้งเฉลยและตัวลวง แล้วไล่เก็บ
ตัวลวงจากลิสต์ที่สับแล้วจนครบ 3 ตัว (ไม่ใช่ `.slice(0,3)` แบบเดิมที่กันซ้ำไม่ได้)

ไม่ใช่แค่เรื่อง UX — `removeDistractors()` `reshuffleChoices()` `choicePinyin()` ใช้ข้อความ
เป็นตัวระบุตัวเลือก ถ้าซ้ำ ตัดตัวลวงทีเดียวจะหายสองใบ และ `choiceMeta` จะชี้ผิดคำ

**ไฮไลต์ปุ่มเฉลยตอนตอบผิด** ใน `resolveAnswer` เทียบด้วย `b.dataset.choice` ไม่ใช่ `b.textContent`
เพราะปุ่มที่ [เนตรพินอิน] เปิดไว้จะมี `<span class="g-choice-py">👁️ 多少 · duōshao</span>` ต่อท้าย
ทำให้ `textContent` ไม่เท่ากับ `m.answer` (บั๊กเดิม: ปุ่มเขียวไม่ขึ้นถ้าเผอิญเปิดพินอินข้อที่ถูกไว้)
กฎเดียวกันนี้ใช้กับทุกโค้ดที่ต้องหา "ปุ่มไหนคือตัวเลือกไหน" — อย่าอ่าน `textContent`

### 8. เดา "เคยเข้าเล่นมาแล้ว" จาก `lastActive` ไม่ได้ — บัญชีที่เพิ่งสมัครก็มี

`blankAccount()` ตั้ง `lastActive: Date.now()` ตั้งแต่วินาทีที่สมัคร และมันวิ่งผ่าน `migrateAccount()`
ทันที ดังนั้น "บัญชีเก่าที่เคยเล่นมาก่อน" กับ "บัญชีที่เพิ่งเกิดเมื่อ 1 มิลลิวินาทีที่แล้ว" มี `lastActive`
เหมือนกันเป๊ะ แยกไม่ออก

รอบแรกที่เขียน `gcEnsure()` ใช้ `a.loginCount = a.lastLogin ? 1 : 0` โดยเดา `lastLogin` จาก `lastActive`
→ บัญชีใหม่ถูกนับว่าเข้าเกทไปแล้ว 1 ครั้งตั้งแต่ยังไม่เริ่มเล่น แล้ว wrapper ของ `startGame` บวกซ้ำ
กลายเป็น **สมัครเสร็จปุ๊บ loginCount = 2 และมี 2 session ห่างกัน 83 มิลลิวินาที**

ทางแก้: ตีตรารุ่นด้วย `a.gcv` แล้ว backfill **ครั้งเดียว** ตอนเจอบัญชีที่ยังไม่มีตรา และใช้
"เคยตอบข้อไหนหรือยัง" (`correct + wrong > 0`) เป็นเกณฑ์แทน `lastActive`

กฎทั่วไป: **ถ้าจะ backfill ข้อมูลย้อนหลัง ให้ตีตรารุ่นไว้เสมอ** อย่าเดาจากฟิลด์ที่บัญชีใหม่ก็มี

### 9. คอลัมน์ ⚙️ จัดการ ในตาราง GM เต็มแล้ว อย่ายัดปุ่มเพิ่ม

`.gm-table` เป็น `width:100%` + `min-width:760px` และคอลัมน์สุดท้ายได้พื้นที่ "เท่าที่เหลือ"
ซึ่งพอดีกับปุ่มไอคอน 6 ปุ่มเดิมแบบพอดิบพอดี

ยัดปุ่มที่ 7-8 เข้าไปแล้วเกิด 2 อาการพร้อมกัน: ปุ่มเรียงเป็นแถวตั้งยาวจนแถวสูงเป็นสองเท่า
และถ้าแก้ด้วยการตั้ง `min-width` ให้คอลัมน์ ตารางจะกว้างเกินกรอบ → ปุ่มใหม่ไปโผล่นอกจอ
ต้องเลื่อนแนวนอนถึงจะเห็น (แย่กว่าเดิม เพราะเป็นคอลัมน์สุดท้าย คนไม่รู้ว่าเลื่อนได้)

ทางแก้ที่ใช้: วางปุ่มของชั้นใหม่ไว้ใน **คอลัมน์ชื่อฮันเตอร์** ซึ่งชิดซ้ายและกว้าง ใส่ข้อความกำกับได้
(`📋 แฟ้มฮันเตอร์` / `💊 เติม HP/MP`) วัดแล้ว `table.scrollWidth` เท่าเดิมเป๊ะกับก่อนแก้

### 10. GM แก้ข้อมูลของคนที่กำลังเล่นอยู่ ต้องดัน `G` ตามด้วย

`syncFromStore()` ของ v4.0 ดันกลับเข้า `G` แค่ ทอง/เลเวล/EXP/HP เท่านั้น — **ไอเทม MP โล่ พิกัดชั้น
คูลดาวน์ ไม่ได้ดัน** ถ้าเสกไอเทมหรือเติม MP ให้คนที่เปิดเกมค้างอยู่บนเครื่องเดียวกัน
`saveProgress()` รอบถัดไปจะเขียนค่าเก่าจาก `G` ทับทันที ของที่เสกให้หายเกลี้ยงเหมือนไม่เคยกด

`gcLive(user)` ทำหน้าที่นี้: เช็ก `CURRENT_USER === user` ก่อน แล้วดันทุกฟิลด์ที่แก้เข้า `G`
พร้อมเรียก `renderFloor/renderItems/renderSkills/renderStats` ให้จอขยับตาม
**ทุกเครื่องมือที่เขียน store ของชั้น GM ต้องเรียก `gcLive()` ปิดท้ายเสมอ**

### 11. ช่องว่างในแถวปุ่มล่างมีช่องเดียว และ `:last-child` เป็นตัวกำหนดปุ่มเต็มแถว

`.g-actions` เป็นกริดสองคอลัมน์ + `.g-actions .g-btn:last-child { grid-column: 1 / -1 }`
ปุ่มในนั้นมาจากสองที่: 5 ปุ่มใน HTML และ 📜 QUESTS ที่ v4.2 ยัดเข้า `afterbegin`
รวมเป็นเลขคี่ จึงเหลือช่องว่าง 1 ช่องที่มุมขวาล่าง (ขวาของ SHOP · ใต้ LEADERBOARD)

v4.4 เติมช่องนั้นด้วย 📖 CODEX โดยหาปุ่มที่ `onclick` มีคำว่า `openShop` แล้ว `insertAdjacentHTML('afterend')`
**ห้ามใช้ `appendChild` / `beforeend`** เพราะปุ่มใหม่จะกลายเป็น `:last-child` แทน
👁️ SYSTEM SCAN แล้วสลับกันกินเต็มแถว — ผังพังทั้งบล็อกทันที

v4.6 เพิ่ม ⚙️ SYSTEM CORE กับ 🕳️ ABYSS **พร้อมกันสองใบ** โดยแทรก `beforebegin` ที่ปุ่ม
👁️ SYSTEM SCAN (หาจาก `onclick` ที่มีคำว่า `showHint`) — แทรกไว้ "ก่อน" ตัวสุดท้ายเสมอ
SYSTEM SCAN จึงยังเป็น `:last-child` ที่กินเต็มแถวเหมือนเดิม

ตอนนี้ปุ่มครบ 9 = 4 แถวคู่ + SYSTEM SCAN เต็มแถว ลงตัวพอดี **ถ้าจะเพิ่มปุ่มที่ 10 ต้องเพิ่มเป็นคู่**
ไม่งั้นช่องว่างจะกลับมาอีก

### 12. `migrateAccount` หนีบ `a.mp` ด้วยสูตรตรง ๆ ไม่ได้เรียก `maxMpOf`

บรรทัด `a.mp = Math.min(a.mp, MP_BASE + (hasRankSrc(a,'D') ? 20 : 0));` เขียนสูตรซ้ำไว้เอง
**โบนัส MP อันใหม่ที่ห่อไว้ที่ `maxMpOf` จะถูกตัดทิ้งทุกครั้งที่อ่าน store**
(อาการ: มานาที่เกิน 100/120 หายทุกครั้งที่รีเฟรชหรือสลับหน้าจอ หาสาเหตุยากมากเพราะไม่มี error)

v4.4 แก้ด้วยการห่อ `migrateAccount` — จำ `a.mp` ดิบไว้ก่อน ให้ของเดิมทำงานจนจบ แล้วหนีบใหม่ด้วย
`Math.min(rawMp, maxMpOf(r))` ที่รวมโบนัสแล้ว **ถ้าเพิ่มโบนัส MP อีกในอนาคต ให้ไปต่อท้าย wrapper ตัวนี้**

### 13. ลำดับการวาดชั้นในภาพไอโซเมตริกคือทุกอย่าง — วาดผิดลำดับได้ "กรวย" ไม่ใช่ "ตึก"

รอบแรกวาดพื้นทั้ง 20 ชั้นไล่จากบนลงล่างรวดเดียว แล้วค่อยแปะผนัง ผลที่ได้คือขนมเปียกปูน 20 ใบ
ซ้อนกันเป็นรูปกรวย มองไม่ออกเลยว่าเป็นตึก เพราะสองเรื่องนี้พร้อมกัน:

1. **ครึ่งความสูงของขนมเปียกปูนมากกว่าระยะห่างระหว่างชั้น** (เดิม 49.7 vs 30) พื้นชั้นบนจึงยื่นลงมา
   คลุมพื้นชั้นล่างเกือบทั้งใบ — ต้องคุม `TR_W0 * TR_RATIO` ให้ใกล้เคียง `TR_FH` (ตอนนี้ 32.6 vs 34)
2. **ไม่มีอะไรทึบมาบัง** ทุกชั้นเห็นทะลุถึงกันหมด

ลำดับที่ถูกคือ **ไล่จากชั้นล่างขึ้นบน และในแต่ละรอบวาด "พื้นชั้น" ก่อน แล้วค่อยวาด "ผนังเหนือพื้นชั้นนั้น"**
ผนังจะบังครึ่งหลังของพื้นชั้นล่าง และพื้นชั้นถัดไปมาวางทับหัวผนังพอดี ได้ตึกตันที่มีขอบเรืองแสง
ผนังแต่ละท่อนมี 3 ชิ้น: `.tr-solid` (ทึบกันมองทะลุ) + `.tr-face.lit` (ด้านรับแสง) + `.tr-face.dim` (ด้านเงา)

**เครื่องหมายบนชั้น (ซุ้มประตูบอส · จุดคืบหน้า) ต้องวาดเป็นชั้นแยกทับหลังตัวตึกทั้งหมด**
ถ้าปล่อยไว้ในกลุ่มของชั้นตัวเอง ซุ้มประตูจะโดนผนังของชั้นถัดไปบังหายไปครึ่งตัว

### 14. บนมือถือ การแตะยิง `mouseover` ตามด้วย `click` ในทิกเดียวกัน

แผงข้อมูลย่อยของเรดาร์เปิดด้วย `mouseover` (เดสก์ท็อป) และ `click` (มือถือ) ถ้าเขียน click เป็น
"แตะชั้นเดิมซ้ำ = ปิด" ตรง ๆ มือถือจะเปิดแล้วปิดทันทีในการแตะครั้งเดียว = ดูเหมือนกดไม่ติด

ทางแก้: จำเวลาที่เปิดไว้ที่ `TR_TIP_T` แล้ว click จะปิดก็ต่อเมื่อผ่านไปเกิน 400ms
กฎเดียวกันนี้ใช้กับทุก UI ที่ผูกทั้ง hover และ tap เข้ากับสวิตช์ตัวเดียวกัน

### 15. CSS สมัยใหม่บางตัวใช้ไม่ได้ — นักเรียนเปิดจากเบราว์เซอร์อะไรก็ไม่รู้

ร่างแรกของชั้น v4.5 ใช้ `color-mix(in srgb, var(--l) 12%, transparent)` ซึ่งต้อง Chrome 111+
เครื่องนักเรียนที่อัปเดตไม่ทันจะได้หอคอยไร้สีทั้งอัน โดยไม่มี error ให้เห็นเลย

ของที่เลี่ยงแล้วในชั้นนี้ และควรเลี่ยงต่อไป:

- `color-mix()` → ใช้ `fill:var(--l)` คู่กับ `fill-opacity` แทน (SVG แยกสองค่านี้ออกจากกันอยู่แล้ว)
- อนิเมต `r` ของ `<circle>` ด้วย CSS (Safari ไม่รองรับ) → อนิเมต `opacity` แทน
- `transform: scale()` บนโหนด SVG **ต้องใส่ `transform-box:fill-box` คู่กับ `transform-origin` เสมอ**
  ไม่งั้นจุดหมุนจะเป็นมุมบนซ้ายของ viewBox ไม่ใช่ตัวโหนดเอง (วงแหวนจะกระเด็นออกนอกจอ)

`transform` ที่เซ็ตด้วย `setAttribute` ทรานซิชันด้วย CSS ได้ (presentation attribute แมปเป็น CSS
property) — กล้องกับอวตารใช้ท่านี้ ส่วน `viewBox` **ทรานซิชันไม่ได้** จึงต้องเปลี่ยนเป็นค่าใหม่ทันที
แล้วปล่อยให้ความสูงของกรอบ `.tr-view` เป็นตัวเล่นอนิเมชันแทน

**ถ้าเปลี่ยนความสูง `.tr-view` ผ่าน media query ต้องแก้ `TR_VH_C` ให้ตรงกันด้วย**
ไม่งั้น `preserveAspectRatio="meet"` จะย่อหอคอยลงจนเล็กและเหลือขอบดำบนล่าง

### 16. ตัวอ่านสถานะของชั้นบนห้าม "สร้างค่าเปล่า" ให้ตัวเอง — `saveProgress` ระหว่าง `startGame` จะเขียนทับของจริง

รอบแรกที่เขียนชั้น v4.6 ตัวช่วย `abOf()` เขียนไว้ว่า "ถ้ายังไม่มี `g.ab` ให้ `abEnsure(g)` สร้างให้เลย"
ดูไม่มีพิษภัย แต่ทำให้ **เศษคริสตัล ขั้นแกนกลางระบบ และผนึกที่ทลายไปแล้วหายเกลี้ยงทุกครั้งที่ล็อกอินใหม่**

ลำดับที่ทำให้พัง — ทุกชั้นห่อ `startGame` ไว้หมด โค้ดหลังของแต่ละชั้นจึงวิ่งไล่จากในออกนอก
(v4.0 → v4.2 → v4.3 → v4.4 → v4.5 → v4.6) และ **v4.4 เรียก `renderStats()` ในโค้ดหลังของมัน**

1. v4.0 สร้าง `G` ก้อนใหม่ที่ยังไม่มี `G.ab`
2. v4.4 เรียก `renderStats()` → wrapper ของ v4.6 เรียก `abSyncButtons()` → `abOf()` → สร้าง `g.ab` เป็นศูนย์
3. `renderStats()` รอบถัดไปในสายเดียวกันเรียก `saveProgress()` → `abSave()` เขียนศูนย์ทับ localStorage
4. wrapper ของ v4.6 เพิ่งได้อ่านค่าจริงจาก store ทีหลัง — แต่ของจริงถูกทับไปแล้ว

แก้สองชั้นพร้อมกัน และควรใช้เป็นแบบเดียวกันนี้ทุกครั้งที่ทำชั้นใหม่:

- **ตัวอ่าน (`abOf`) เป็น read-only เท่านั้น** คืน `null` ถ้ายังไม่มี ผู้เรียกทุกตัวต้องทนกับ `null` ได้
- **มีสวิตช์ `AB_READY`** ปิดไว้ตอนเข้า `startGame` เปิดหลังอ่านค่าจาก store เข้ามาครบแล้ว
  `abSave()` ไม่เขียนอะไรเลยถ้าสวิตช์ยังปิด

อาการตอนเจอ: ทุกอย่างถูกต้องหมดจนกระทั่ง `exitGame()` แล้วล็อกอินกลับเข้ามา ค่าเป็นศูนย์หมด
ไล่หาจากอาการอย่างเดียวยากมาก เพราะ `saveProgress` ตอน "ออก" ทำงานถูกต้องทุกประการ
**ตัวที่เขียนทับคือตอน "เข้า"** — ถ้าเจออาการทำนองนี้ ให้ดักที่ `saveStore` แล้วพิมพ์ stack ออกมาดูเลย

### 17. แถบหมอกที่ซ้อนเหลื่อมกันจะขึ้นเป็นริ้วขวาง

ร่างแรกของหมอกสงครามให้แถบรายชั้นซ้อนทับเพื่อนบ้าน 72% ตั้งใจให้ขอบกลืนกัน
ผลคือพื้นที่ทับได้อัลฟาสองชั้น แล้วขึ้นเป็นแถบเข้ม-อ่อนสลับกันเห็นชัดบนภาพหอคอย

ทางที่ใช้: ให้แถบต่อกันพอดีที่กึ่งกลางระหว่างชั้น (0.5 ไม่ใช่ 0.72) + สีทึบเรียบ `fill-opacity`
เดียวทั้งแถบ + ปุยหมอกเรเดียลวางทับเป็นเท็กซ์เจอร์ ได้หมอกเนียนสม่ำเสมอและเส้นแบ่ง
"ไปถึงแค่ไหนแล้ว" คมชัดพอดี — **อย่าแก้ด้วย `feGaussianBlur`** เบลอ SVG 20 ก้อนพร้อมกัน
หนักเกินไปสำหรับมือถือนักเรียน

และแถบของชั้นบนสุดต้องเผื่อขึ้นไปเยอะ ๆ (ตอนนี้ `top -= 900`) เพราะยอดหอคอย —
หลังคาคริสตัล ยอดแหลม ลำแสง — อยู่สูงกว่าพิกัดชั้น 20 ไปอีกมาก ถ้าเผื่อน้อยไปยอดจะโผล่พ้นหมอก
ทั้งที่ยังไปไม่ถึง (กลุ่มนี้อยู่ในกล้องซึ่ง SVG ตัดขอบให้อยู่แล้ว ยืดเผื่อได้ฟรี)

### 18. อย่ายิงหน้าต่างบังคับกดยืนยัน (`#snGate`) ระหว่างนาฬิกาต่อข้อเดินอยู่

`snShowGate` เปิดโมดัลทับจอ แต่ **ไม่ได้หยุดนาฬิกาของ v4.0** นักเรียนจะอ่านแจ้งเตือนอยู่แล้วโดน
ตัดสินว่าหมดเวลาไปเฉย ๆ ทั้งที่ไม่ได้ทำอะไรผิด

จุดที่ปลอดภัยสำหรับ gate มีแค่ที่ที่ v4.0 หยุดเกมไว้ให้แล้วจริง ๆ คือ **ประตูวาปหลังบอส**
(`openWarp` เรียก `clearQuestionTimer()` และตั้ง `G.locked = true`) — ที่อื่นให้ใช้ toast
ชั้น v4.6 จึงเป็น toast ล้วนทั้ง 9 รายการ ส่วนแจ้งเตือนที่ยิงถี่มาก (สตรีค 3 ข้อ)
ไม่ยิง toast เลย เขียนลงบรรทัดผลลัพธ์ `#gFeedback` อย่างเดียว (กับดักข้อ 1 เรื่องคิว toast ล้น)

### 19. ผลของการ์ดเกิด "หลัง" v4.0 ตัดสินผลของข้อไปแล้ว — ต้องสั่งคิวเทิร์นใหม่เอง

ทุก wrapper ของชั้น v4.7 ทำงานหลัง `resolveAnswer` ของ v4.0-v4.6 จบแล้ว ซึ่งตอนนั้น v4.0
**ตัดสินไปแล้ว** ว่ามอนสเตอร์ตายหรือยัง (`monsterHp <= 0`) และฮันเตอร์ตายหรือยัง (`hp <= 0`)
แล้วสั่ง `scheduleTurn()` ค้างไว้เรียบร้อย ดาเมจที่การ์ดเติมทีหลังจึงไม่มีใครเห็น

อาการสองแบบที่เจอ:

- 🗡️ ดาบสองคมฟันจนมอนสเตอร์ HP เหลือ 0 แต่ v4.0 สั่ง "ไปข้อถัดไป" ไว้แล้ว
  → มอนสเตอร์ยืนรอที่ HP 0 อีกหนึ่งข้อเต็ม ๆ ก่อนจะยอมตาย
- 🛡️ โล่พิพากษาคืน HP ให้หลังจาก v4.0 สั่ง `onHunterDown` ไปแล้ว
  → ฮันเตอร์ร่วงชั้นทั้งที่ HP เต็ม

ทางแก้: ทุกตัวกลางรับ "ค่าตอนชั้นล่างทำงานจบ" เข้ามาเทียบ แล้วสั่งคิวใหม่ทับเอง
(`scheduleTurn` เคลียร์ `TURN_TIMER` เดิมให้อยู่แล้ว จึงทับได้สะอาด)

```js
if (mhpAfter > 0 && g.monsterHp <= 0) cdQueueDefeat();          /* เพิ่งฆ่ามันตายเดี๋ยวนี้ */
if (hpAfter  > 0 && g.hp        <= 0) scheduleTurn(onHunterDown, 900);
if (hpAfter <= 0 && ฟื้นคืนแล้ว) scheduleTurn(ไปข้อถัดไป, 1600);   /* ยกเลิกการร่วง */
```

**กฎทั่วไป: ชั้นบนที่แก้ HP หลังจากชั้นล่างตัดสินผลไปแล้ว ต้องรับผิดชอบคิวเทิร์นเองเสมอ**
ห้ามบวก/ลบ HP ตรง ๆ ในโค้ดของการ์ด ให้ผ่าน `cdHitMonster` / `cdHurt` / `cdHeal` / `cdExecute`
ซึ่งจัดการเรื่องนี้ให้ครบแล้วที่เดียว

และดาเมจพิเศษต้องหนีบไม่ให้ทะลุเกราะผนึกของ v4.6 ลงไปกิน HP จริงของบอส (กฎเดียวกับ `AB_PREC_1/2`)
— `cdHitMonster()` อ่าน `abSealLeft()` เองแล้วจำกัดเพดานให้

### 20. เทสต์ที่แคชค่า `hunterAtk()` / `G.maxHp` ไว้ตั้งแต่ต้นไฟล์จะ fail แบบหลอก ๆ

ตอนเขียนชุดเทสต์ของ v4.7 รอบแรก จับ `const atk = await ev(() => hunterAtk())` ไว้ครั้งเดียว
ตอนต้นไฟล์แล้วเอาไปเทียบทุกเคส ผลคือเทสต์ช่วงท้าย fail หมดทั้งที่โค้ดถูก **เพราะเล่นไปเรื่อย ๆ
แล้วเลเวลขึ้น** ATK กับ HP สูงสุดเลยขยับตามระหว่างทาง

และการเซ็ต `G.maxHp = 300` เองในเทสต์ก็ไม่อยู่ทน — `renderStats()` เรียก `recalcStats()`
ซึ่งคำนวณ `maxHp` ใหม่จาก level + VIT ทุกครั้ง ค่าที่ยัดเข้าไปจะถูกทับในทิกถัดไป

กฎ: **อ่าน `hunterAtk()` / `G.maxHp` / `wrongDamage()` สด ๆ ทันทีก่อนยิงเทสต์แต่ละเคสเสมอ
และอย่ายัด `G.maxHp` เอง** ให้ใช้ `G.hp = G.maxHp` แทนถ้าต้องการหลอดเต็ม

ตั้งแต่ v4.8 `wrongDamage()` **ผูกกับ `G.floor` ด้วย** (และกับธง `HD_TIMEOUT` ระหว่างตัดสินข้อ)
ค่าที่จับไว้ตอนอยู่ชั้น 3 จึงใช้เทียบเคสของชั้น 18 ไม่ได้เลย — เป็นกับดักเดียวกันแต่คนละแกน


### 21. `*/` ที่โผล่กลางคอมเมนต์ปิดบล็อกคอมเมนต์ทันที

ร่างแรกของชั้น v4.8.1 เขียนคอมเมนต์ว่า *"ไม่ต้องไล่ห่อ open\*/close\* ทีละบาน"* —
ตัว `*/` ตรงกลางปิดคอมเมนต์ตั้งแต่ตรงนั้น โค้ดที่เหลือของบรรทัดจึงกลายเป็นสตริงเปล่า ๆ ลอยอยู่
แล้วทั้งไฟล์พังด้วย `missing ) after argument list` ซึ่ง**ไม่ได้ชี้ไปที่บรรทัดที่ผิดเลย**

ไฟล์นี้มีคอมเมนต์ภาษาไทยยาว ๆ เต็มไปหมด และหลายอันพูดถึงชื่อฟังก์ชันแบบมี wildcard
(`open*` `close*` `*Ensure()`) **ห้ามเขียน `*` ติดกับ `/` ในคอมเมนต์เด็ดขาด**
ให้เขียนเป็นคำแทน (`ฟังก์ชันเปิด/ปิด`) หรือคั่นด้วยช่องว่าง

ตัวจับที่ได้ผลคือ `new Function(<เนื้อ script>)` ตามขั้นตอนก่อนส่งงาน — รันทุกครั้ง
**และเทียบกับไฟล์เดิมด้วย** (`git show HEAD:<ไฟล์>`) จะได้รู้ทันทีว่าพังเพราะของใหม่หรือของเก่า

### 22. MutationObserver ที่ไม่กรอง จะถูกปลุกวินาทีละ 10 ครั้งจากหลอดเวลา

ชั้น v4.8.1 ใช้ MutationObserver ตัวเดียวเฝ้าหน้าต่างระบบทุกบาน แทนการไล่ห่อฟังก์ชันเปิด/ปิด
กว่า 20 ตัว (ซึ่งชั้นบน ๆ ยังสร้างเพิ่มตอนรันไทม์อีก) ร่างแรกไม่กรอง mutation เลย

`QUESTION_TICK` เขียนตัวเลขใหม่ลง `#gTimerText` และสลับคลาส `danger` บน `#gTimer` **ทุก 100ms**
วัดจริงแล้ว `acSync()` ถูกเรียก **53 ครั้งต่อ 5 วินาที** แล้วยิง `querySelector` ทั้งเอกสารทิ้งฟรีทุกครั้ง
ตลอดเกม — บนมือถือนักเรียนนี่คือค่าใช้จ่ายที่จ่ายไปโดยไม่ได้อะไรกลับมา

`acRelevant(muts)` กรองก่อนถึงจะ `requestAnimationFrame`: สนใจเฉพาะ
`childList` ที่เพิ่ม **Element** ซึ่งเป็น/มีหน้าต่างระบบอยู่ข้างใน กับ `attributes` บนโหนดที่เป็น
หน้าต่างระบบหรือ `#gameScreen` เท่านั้น หลังกรองแล้วเหลือ **0 ครั้งต่อ 5 วินาที**

**จุดที่พลาดง่ายคือ `textContent`** — การเขียนทับสร้าง text node ใหม่ = `childList` mutation
ถ้ากรองแค่ `m.type === 'childList' && m.addedNodes.length` จะยังโดนปลุกทุกทิกอยู่ดี
ต้องเช็ก `nodeType !== 1` ทิ้งก่อนเสมอ

### 23. จอง "แถบปลอดภัย" ที่ขอบบนด้วย padding หรือด้วย sticky offset — รั่วทั้งคู่

ชั้น v4.9 ต้องกันไม่ให้แบนเนอร์แจ้งเตือนทับโจทย์จีน · พินอิน · คำแปล · ตัวเลือก A-D
ลองมาสองทางก่อนจะเจอทางที่ถูก **อย่าย้อนกลับไปใช้สองทางแรก**

1. **`padding-top` ที่กล่องเลื่อน** — เนื้อหาไหลผ่านช่องนั้นให้เห็น ไม่ถูกตัดขอบ
   (v4.8.1 เจอมาแล้วตอนทำการ์ดโจทย์ติดหนึบ จึงต้องใช้ `.ac-space` ในสายเลื่อนแทน)
2. **จองที่ด้วย `.ac-space` ในสายเลื่อน + ดัน `top` ของการ์ดโจทย์ที่ติดหนึบลงมาเท่าความสูงแถบ** —
   กันได้เฉพาะตอนเลื่อนอยู่บนสุด พอเลื่อนลงไปเรื่อย ๆ ของที่อยู่ **ใต้** การ์ด (แถวตัวเลือก A-D)
   จะไต่ขึ้นมาโผล่ใน "ช่องว่างเหนือการ์ดที่ติดหนึบ" เพราะการ์ดกินพื้นที่แค่ `42px` ลงไป
   ไม่ได้เป็นฝาปิดให้ทั้งแถบ — วัดจริงแล้วรั่วที่ `scrollTop` 980 ขึ้นไปบนจอ 360×800 และ 390×844
   ส่วนจอเตี้ยกว่า 620px ที่ปิด sticky ไว้ยิ่งรั่วหนัก โจทย์จีนโผล่มาให้เห็นตั้งแต่ scrollTop 720

ทางที่ปิดสนิทคือ **ดันกล่องเลื่อนทั้งใบลงมาแล้วหักความสูงคืนเท่ากัน**
(`margin-top` + `height:calc(100% - ...)`) ของข้างในจึงถูกตัดขอบที่ขอบบนกล่อง

**และทางที่ 2 ยังลากปัญหาที่สองมาด้วย** — `acFocusQa()` ของ v4.8.1 คำนวณจุดที่จะเลื่อนไปจาก
"ขอบบนกล่องเลื่อน + ความสูงการ์ด" ซึ่งผูกกับ `sticky top: 0` พอดัน `top` ลงมา ระยะเลื่อนจะสั้นไป
เท่าความสูงแถบ แล้วแถวตัวเลือกจะไปมุดใต้การ์ดพอดี — **จอค้างในสภาพ "หยุดอยู่" ทันทีที่ขึ้นข้อใหม่
โดยไม่มี error ให้เห็นเลย** พอเปลี่ยนมาดันทั้งกล่อง `sticky top` กลับเป็น 0 ตามเดิม
จึงไม่ต้องแตะ `acFocusQa` อีก

**บทเรียนกว้างกว่านั้น: การกันพื้นที่บนจอต้องพิสูจน์ด้วยการกวาดทุกตำแหน่งเลื่อน ไม่ใช่จุดเดียว**
และต้องเทียบแบบ **คิดการซ้อนทับ** (`document.elementFromPoint` หลังซ่อนชั้น toast) ไม่ใช่เทียบ
กรอบสี่เหลี่ยมดิบ ๆ เพราะการ์ดโจทย์ที่ติดหนึบมีพื้นทึบ + `z-index:6` บังของที่มุดอยู่ข้างใต้ไว้แล้ว
เทียบกรอบเฉย ๆ จะได้ **ผลบวกลวง** ตรงนั้น และ **ผลลบลวง** ตรงที่รั่วจริง

### 24. กู้ข้อมูลจากคลาวด์ก่อนเรียก `exitGame` = ข้อมูลที่เพิ่งดึงมาถูกก้อนเก่าเขียนทับทันที

ชั้น v5.2 ต้องสลับ "ก้อนข้อมูลของบัญชีที่กำลังเล่นอยู่" ตอนพบว่าคลาวด์ใหม่กว่า
ร่างแรกเขียนว่า `csAdopt()` แล้วค่อย `exitGame()` + `startGame()` ซึ่งดูเป็นลำดับที่เป็นธรรมชาติ
ผลคือ **ก้อนที่ดึงมาหายเกลี้ยงทุกครั้ง** เพราะ `exitGame` ของ v4.0 เรียก `saveProgress()` ข้างใน
ซึ่งเอา `G` (ก้อนเก่าที่ยังค้างในหน่วยความจำ) เขียนทับ store ที่เพิ่งอัปเดตไป

ลำดับที่ถูกคือ **`exitGame()` → `csAdopt()` → `startGame()`** ให้ของเดิมเซฟก้อนเก่าจนจบก่อน
แล้วค่อยทับด้วยก้อนใหม่ ตอน `startGame` จึงอ่านของใหม่เข้ามาครบ

กฎทั่วไป: **ชั้นบนที่จะเอาข้อมูลจากที่อื่นมาทับบัญชีที่กำลังเล่นอยู่ ต้องพา `G` ออกจากเกมให้จบก่อนเสมอ**
ไม่ใช่แค่เขียน store แล้วหวังว่าจะมีใครอ่านใหม่ให้ (เป็นอาการตระกูลเดียวกับกับดักข้อ 16
แต่คนละทาง — ข้อ 16 คือ "ตอนเข้า" เขียนทับ "ตอนออก" ส่วนข้อนี้คือ "ตอนออก" เขียนทับ "ตอนกู้คืน")

และระหว่างสลับก้อนต้องมีสวิตช์ปิดการส่ง (`CS_HALT`) ไม่งั้น `saveStore` ที่เกิดระหว่างทาง
จะยิงคิวส่งก้อนเก่าขึ้นคลาวด์ซ้ำ แล้ววนกลับมาเจอว่าชนกันอีกรอบไม่รู้จบ

## วิธีทดสอบ

ไม่มี test runner — ใช้ Playwright + Chromium ที่ติดตั้งมาแล้วในเครื่อง

```js
chromium.launch({
  executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome',
  args: ['--no-sandbox', '--disable-dev-shm-usage']
});
```

ข้อควรระวังที่เสียเวลาไปเยอะแล้ว:

- **บล็อก Google Fonts ก่อนเสมอ** `page.route('**fonts.googleapis.com**', r => r.abort())`
  แล้วใช้ `goto(url, { waitUntil: 'domcontentloaded' })` ไม่งั้น `load` ค้างยาวเพราะเน็ตออกไม่ได้
- **ห้ามใช้ `page.click()`** — toast ลอยทับปุ่ม ทำให้ Playwright รอ actionability ค้างจนหมดเวลา
  ใช้ `page.evaluate(() => document.querySelector(sel).click())` แทน
- **เขียน log ลงไฟล์ด้วย `fs.appendFileSync`** ไม่ใช่ `console.log` เพราะ stdout ถูกบัฟเฟอร์
  จนกว่าโปรเซสจะจบ ถ้าค้างจะไม่เห็นว่าค้างตรงไหนเลย
- ลัดเข้าเกมได้ด้วย `enterGate()` → รอ 600ms → `switchTab('register')` → เซ็ต
  `#reg-id` `#reg-pw` `#reg-pw2` → `handleSubmit()`
- ตัวแปรสถานะ `G` และฟังก์ชันทั้งหมดอยู่ระดับ global เรียกจาก `page.evaluate` ได้ตรง ๆ
  แต่ `G` ประกาศด้วย `let` จึง **ไม่ได้อยู่บน `window`** — เช็ก `window.G` จะได้ `undefined`
  เสมอทั้งที่เกมรันอยู่ ให้เขียน `G` เปล่า ๆ ใน `page.evaluate` เท่านั้น
- `playwright` ติดตั้งไว้แบบ global รันเทสต์ต้องนำหน้าด้วย `NODE_PATH=/opt/node22/lib/node_modules`
- **เล่นจริงต้องรอ `G.locked` คลายเอง** v4.0 ไหลไปข้อถัดไปด้วย `scheduleTurn` ไม่มีปุ่ม "ถัดไป"
  ในด่านย่อย ให้ `waitForFunction(() => !G.locked && G.currentMonster)` แล้วค่อยกด และดักหน้าต่าง
  `#snGate` (เรียก `snGateConfirm()`) กับประตูวาป (`warpGo()`) ที่มาคั่นหลังบอสด้วย
- **ทดสอบคำสั่ง GM ที่มี modal ยืนยัน** `gmPrompt`/`gmConfirm` ตั้ง `onclick` ให้ `#gmModalOk` แบบซิงโครนัส
  จึงสั่งได้ตรง ๆ: เรียกฟังก์ชัน → เซ็ต `#gmModalInput.value` → `document.getElementById('gmModalOk').click()`
  (ทดสอบ "กดยกเลิก" ด้วย `gmModalClose()` แล้วยืนยันว่าไม่มีผลข้างเคียงหลงเหลือ)
- **ทดสอบ CODEX ไม่ต้องไต่หอคอยจริง** — เสก `G.wordStats[id] = { seen:1, wrong:0 }` ตรง ๆ แล้วเรียก
  `cxCount(G)` / `cxMpBonus(G)` / `maxMpOf(G)` / `goldMul()` วัดผลได้เลย ส่วนการแจ้งเตือนขั้นสะสม
  ให้เรียก `cxSyncMilestone(false)` แล้วดัก `#snGate` (ปิดด้วย `snGateConfirm()`)
- **ทดสอบเรดาร์หอคอยไม่ต้องไต่จริง** — เซ็ต `G.floor` / `G.maxFloor` / `G.floorProgress` แล้วเรียก
  `renderFloor()` ตรง ๆ กล้องอ่านได้จาก `trCam(f)` (ตรวจว่า `trY(f)*k + ty` อยู่ในช่วง 0–190 ทุกชั้น)
  · อนิเมชันทะลุชั้นดูจาก `#trFx .tr-burst` (ต้องเหลือ 1 ตอนยิง และ 0 หลัง 1.4 วิ)
  · แผงข้อมูลย่อยเรียก `trTipOpen(f)` / `trTipClose()` · มุมกล้องเต็มหอคอยเรียก `trToggleOpen()`
  **ก่อนวัดผลต้องล้าง `#snLayer` ก่อน** ไม่งั้น toast สีฟ้าจะลอยทับเรดาร์จนถ่ายภาพมาดูไม่ได้
- **ทดสอบชั้น ABYSS ไม่ต้องไต่จริง** — ประตูผนึกเซ็ต `G.floor = 5/10/15/20` แล้วเรียก `nextMonster()`
  ตรง ๆ · เกราะที่เหลืออ่านจาก `abSealLeft()` และตัวเกราะเต็มจาก `AB_SEAL.max`
  · ทลายด้วยไอเทมเรียก `useItem('mystery')` ซ้ำ ๆ · ทลายด้วยการตอบเรียก `answer(G.currentMonster.answer, null)`
  · สตรีคควิกเช็กด้วย `abQuakeOf(n)` แล้วดู `#abFx .ab-aur` (ต้องเหลือ 0 หลัง 1.5 วิ)
  · หมอกนับจาก `#abFog .ab-fog-b:not(.clear)` ต้องเท่ากับ `FLOOR_MAX - maxFloor` เป๊ะ
  · โหมดเหวลึกต้องเทียบ `questionMs()` **หลัง** `nextMonster()` เท่านั้น เพราะ `AB_LIVE`
  ถูกล็อกที่ `startQuestionTimer` (เรียกกลางข้อจะได้ค่าของข้อเดิม — ตั้งใจให้เป็นแบบนั้น)
  · แกนกลางระบบเสก `G.ab.shards` แล้วเรียก `abBuyCore('gold')` วัดผลที่ `goldMul()` / `questionMs()` / `G.mp`
- **ทดสอบว่าค่าถาวรไม่หายต้องผ่าน exitGame + login ใหม่เสมอ** (กับดักข้อ 16) เช็กแค่
  `saveProgress()` แล้ว `loadStore()` ไม่พอ เพราะตัวที่เขียนทับอยู่ในสาย `startGame` ตอนล็อกอินกลับเข้ามา
  ช่องกรอกล็อกอินคือ `#login-id` / `#login-pw` (ไม่ใช่ `#log-id`)
- **ทดสอบชั้น CARD DRAFTING ไม่ต้องไต่จริง** — เสกการ์ดใส่มือด้วย `CD_CARD = CD_BY_ID['edge']`
  · `CD_BAND = cdBandOf(G.floor)` · `CD_ST = { ward:0, noItem:0, noHeal:0, atk:0, perfect:true, hit:0, miss:0 }`
  แล้ว `cdPaintUi()` + `nextMonster()` · ตอบถูกด้วย `answer(G.currentMonster.answer, null)`
  ตอบผิดด้วย `answer(G.currentMonster.choices.filter(c => c !== G.currentMonster.answer)[0], null)`
  (เซ็ต `G.questionStart = Date.now()` ก่อนเสมอ ไม่งั้น 🗡️ ตัดสินเทพเจ้าจะไม่เข้าเงื่อนไข 3 วิ)
  · เวลาวัดที่ `questionMs()` · ทองวัดที่ `goldMul()` · รางวัลจบชั้นเรียก `clearFloor(false/true)` ตรง ๆ
  · ก่อนวัดดาเมจให้ปิดสุ่มด้วย `critChance = () => 0` และ `expDoubleChance = () => 0`
  · เส้นทางย้ายชั้น (⚖️ / 🎲) บังคับผลด้วย `Math.random = () => 0.1 / 0.5 / 0.9` แล้วเช็ก
  `G.floor` กับ `QUESTION_TIMER === null` (ต้องเป็น null = ล้างข้อเดิมแล้วจริง)
  · **ทดสอบว่าการ์ดรอดข้ามการล็อกอิน** ต้องผ่าน `exitGame()` แล้วล็อกอินใหม่จริง ๆ (กับดักข้อ 16)
  หลัง `exitGame()` ตัว `CURRENT_USER` ถูกล้างแล้ว จะอ่าน store ต้องใส่ชื่อบัญชีตรง ๆ
  (`loadStore()['ชื่อ'].cd.cur`) ไม่ใช่ `loadStore()[CURRENT_USER]` ซึ่งจะได้ `undefined`
  · **ทดสอบโควตารายชั้น** เสกการ์ดใส่มือแล้วเรียก `clearFloor(false)` ตรง ๆ
  แล้วอ่าน `CD_ST` ใหม่ (`ward` ต้องกลับมา · `perfect` ต้องเป็น true · `atk` ของ 🩸 ต้องไม่ถูกล้าง)
  · **ปลดล็อกแรงค์ก่อนเทสต์ไอเทมเสมอ** (`G.maxFloor = FLOOR_MAX; recalcStats()`) ไม่งั้น `useItem`
  จะถูก `FEATURE_RANK` ปัดตกตั้งแต่บรรทัดแรก แล้วเทสต์ "ถูกบล็อก" จะผ่านด้วยเหตุผลผิด
  · ความสูงหน้าต่างจั่ววัดจาก `inner.scrollHeight` เทียบ `inner.clientHeight` ด้วยชุดการ์ดที่ยาวที่สุด
  (`[CD_BY_ID.focus, CD_BY_ID.blood, CD_BY_ID.sand]`) — ดูหัวข้อชั้น v4.7 เรื่องจอ 320px
  · **รอยต่อช่วงชั้นเช็กได้โดยไม่ต้องเล่น** — ไล่ `G.floor = 1..20` แล้วอ่าน `cdNeedDraft()` /
  `cdActive()` ตรง ๆ (ต้องเป็น true เฉพาะตอนข้ามช่วง) · Zone Cleanup เรียก `clearFloor(true)`
  ที่ชั้น 5/10/15/20 แล้วเช็ก `CD_BAND === -1` กับคลาสของ `#gameScreen` ที่ต้องไม่เหลือ `cd-*`
  · เคลียร์ชั้นกลางช่วงเรียก `clearFloor(false)` แล้ว `cdActive()` ต้องยังไม่เป็น `null`
  · **ทดสอบภาพประกอบการ์ดต้องเทียบสองสภาพเสมอ** — ไฟล์ที่ยังไม่ฝังภาพ (`img` ว่างทั้ง 17 ใบ)
  ต้องได้อีโมจิครบ ไม่มีคลาส `has-art` และไม่มี `<img>` สักตัว · ไฟล์ที่ฝังแล้วต้องได้
  `.cd-art img` + `.cd-ico.art img` ครบทุกใบและ `naturalWidth > 0` (data URI เสียจะเงียบสนิท
  ไม่มี error ให้เห็น) · แผนที่ชื่อไฟล์เช็กด้วย `CD_CARDS.map(c => [c.id, c.image])` เทียบสารบัญตรง ๆ
  · ความสูงหน้าต่างจั่วต้องวัดใหม่ทั้งชุดหลังฝังภาพ ตามกฎเปิดหน้าเพจใหม่ต่อ 1 ความกว้าง
- **ทดสอบไอคอนไอเทมต้องเทียบสองสภาพเสมอ** — สภาพฝังแล้วต้องได้ `.g-slot-ico.has-art img` ·
  `.g-tip-ico.has-art img` · `.g-shop-icon.has-art img` ครบและ `naturalWidth > 0` ทุกตัว
  (data URI เสียจะเงียบสนิท ไม่มี error ให้เห็น) · สภาพไม่มีภาพเสก `ITEMS.forEach(i => i.img = '')`
  แล้วต้องได้อีโมจิครบ 7 ตัว ไม่มี `<img>` และไม่มีคลาส `has-art` เหลือในโซนไอเทม
  **ต้องกรอง `.has-art` เฉพาะใน `#gBagGrid,#gTipName,#gShopBody`** เพราะการ์ดของ v4.7
  ใช้ชื่อคลาสเดียวกัน (`.cd-card.has-art`) ถ้าเก็บทั้งหน้าจะเจอการ์ด 3 ใบปนมาด้วย
  · เสกของใส่กระเป๋าด้วย `G.items[key] = n` แล้ว `renderItems()` · ร้านค้าเรียก `openShop()`
  · แถบรายละเอียดเรียก `pickSlot(i)` · **ต้อง `waitForFunction` ให้ `img.complete &&
  naturalWidth > 0` ก่อนวัดเสมอ** ไม่งั้นจะอ่านได้ 0 เพราะ data URI ยัง decode ไม่เสร็จ
  · วัดขนาดกรอบต้องวัดที่ **`<span>` ไม่ใช่ `<img>`** (span เป็น border-box ภาพข้างในจึงเล็กกว่า 2px)
  · **ก่อนกด `useItem()` ต้องเลือกการ์ดของ v4.7 ให้จบก่อน** (`document.querySelector('#cdDraft .cd-card').click()`
  แล้ว `waitForFunction(() => !G.locked && G.currentMonster)`) ไม่งั้น `canAct()` เป็น false
  ตลอดแล้วเทสต์ "ใช้ไอเทมได้" จะตกด้วยเหตุผลผิด
- **ทดสอบของ Balance Patch v4.7.4** — ราคาอ่านจาก `ITEMS.map(i => [i.key, i.price, sellPrice(i)])`
  · **ปุ่มซื้อของที่ราคา ≥ `SN_PRICEY` (500 ตั้งแต่ v4.7.9.1) มีหน้าต่างยืนยันมาคั่น** ต้องเรียก `snGateConfirm()`
  ต่อท้ายเสมอ ไม่งั้นทองจะไม่ถูกหักและเทสต์จะตกด้วยเหตุผลผิด
  · ล็อกไอเทมช่วยตอบ: `useItem('scroll')` แล้ว `useItem('glass')` ในข้อเดียวกัน ตัวหลังต้องไม่หักของ
  · คืนสิทธิ์ทดสอบด้วย `startQuestionTimer()` ตรง ๆ (หรือเล่นจบข้อจริงแล้วรอ `!G.helperUsed`)
  · UI นับจาก `#gBagGrid .g-slot.spent` และปุ่ม `#gTipUse` (`disabled` + ข้อความ `⛔`)
  · 🔥 เผาผนึก: เซ็ต `G.floor = 5/10/15/20` · `CD_CARD = CD_BY_ID['inferno']` · `CD_BAND = cdBandOf(G.floor)`
  แล้ว `nextMonster()` — `abSealLeft()` ต้องเท่ากับ `AB_SEAL.max - Math.round(AB_SEAL.max * CD_INF_SEAL)`
  และ `AB_SEAL.infernoBurned` ต้องเป็น true (เรียก `cdInfernoSealStrike()` ซ้ำต้องไม่เผาเพิ่ม)
  · ตอบผิดขณะเกราะยังตั้งอยู่ เกราะต้อง **เพิ่มขึ้น** (ผนึกซ่อมตัวเองของ v4.6 ยังทำงาน)
  · 🚫 คริต: เทียบ `critChance()` ก่อน/หลังเสก `CD_CARD = CD_BY_ID['seal']` ต้องต่างกัน `CD_SEAL_CRIT`
  พอดี · ดันให้ `CD_BAND` ไม่ตรงช่วงชั้นแล้วต้องคืนค่าเดิม · แผงสถานะอ่านที่ `renderStatus()` →
  `#gStatusBody` (ไม่ใช่ `renderStats()` ซึ่งเป็นคนละแผง)
- **ทดสอบของ Ultra-Hardcore Economy v4.7.9.1** — ตารางราคาอ่านจาก
  `ITEMS.map(i => [i.key, i.price, sellPrice(i)])` ต้องได้ 150/112 · 160/120 · 200/150 · 300/225
  · 350/262 · 500/375 · 950/712 ครบทั้ง 7 ตัว
  · **ต้องเช็กข้อความบนไอเทมด้วย ไม่ใช่แค่ตัวเลขในโค้ด** (`i.effect + i.how`) เพราะจุดที่พังง่ายสุด
  คือข้อความค้างเลขเก่าไว้ขณะที่ค่าคงที่เปลี่ยนไปแล้ว — regex หา `/อีก 5 วินาที/` ·
  `/×2 ต่อเนื่อง 5 ข้อ/` · `/ทิ้ง 1 ข้อ เหลือให้เลือก 3/` · เรต `70%|22%|8%` ของกล่อง
  · 📜 ตัดตัวเลือก: `useItem('scroll')` แล้ว `G.currentMonster.choices.length` ต้องเป็น **3**
  (ไม่ใช่ 2) · เฉลยต้องยังอยู่ · ปุ่มบนจอต้องเหลือ 3 ใบ
  · ⏳ ต่อเวลา: จับ `QUESTION_ENDS` ก่อน/หลัง ต้องต่างกัน 5000 พอดี แล้ว `G.helperUsed`
  **ต้องยังเป็น true** (ต่อเวลาไม่คืนสิทธิ์ไอเทมช่วยตอบ)
  · **กล่องสุ่มบังคับผลด้วย `Math.random` ต้องยัดเป็น "ลำดับ" ไม่ใช่ค่าเดียว** เพราะ `rollMystery()`
  เรียก `Math.random()` สองครั้งต่อรอบ (ครั้งแรกเลือกระดับ ครั้งที่สองโยนหัวก้อยทอง/ของ)
  `[0.95, 0.20, …]` = แจ็คพอต-ทอง · `[0.95, 0.80, …]` = แจ็คพอต-ไอเทม
  · **และห้ามยัด `1.0`** เพราะ `Math.random()` จริงคืนช่วง `[0,1)` — `mbGold([350,500])` จะได้ 501
  แล้วเทสต์จะตกด้วยเหตุผลผิดทั้งที่โค้ดถูก (เจอมาแล้ว) ใช้ `0.99999` แทน
  · เรตจริงวัดด้วยการวน `rollMystery()` ~60,000 รอบ นับจากข้อความที่คืนมา (`/เกลือ/` ·
  `/คุ้มค่า/` · ที่เหลือ = แจ็คพอต) แล้วเทียบ 70/22/8 ที่ ±1.2% · EV รวมมูลค่าไอเทมด้วย
  `sellPrice()` ต้องได้ราว 108 (ล้าง `G.gold` / `G.items` ทุกรอบก่อนนับ)
  · `SN_PRICEY`: `ITEMS.filter(i => i.price >= SN_PRICEY)` ต้องเหลือแค่ `scroll` + `insurance`
  และ ⏳ ราคา 200 ต้องซื้อได้ **โดยไม่มี** gate มาคั่น (ซื้อแล้วทองหักทันที)
  · **หน้าต่างจั่วการ์ดของ v4.7 ค้างอยู่ใน DOM ตลอด** `document.querySelector('#cdDraft .cd-card')`
  จึงเจอการ์ดเสมอแม้ปิดไปแล้ว — ลูปที่รอ `!G.locked` ต้องเช็ก
  `#cdDraft.classList.contains('active')` ไม่ใช่แค่ว่ามีการ์ดอยู่ ไม่งั้นลูปจะไม่จบ (เจอมาแล้ว)
- **ทดสอบชั้น HARDCORE DAMAGE (v4.8) ไม่ต้องไต่จริง** — ตารางทั้ง 20 ชั้นอ่านจากสูตรตรง ๆ
  `hdDamage(f, f % 5 === 0, false)` / `hdDamage(f, f % 5 === 0, true)` ต้องได้
  20/40 · 50/100 · 35/70 · 87/175 · 45/90 · 112/225 · 55/110 · 137/275 ตามช่วงชั้น
  · `wrongDamage()` วัดโดยเซ็ต `G.floor` แล้ว `recalcStats()` — ค่าต้อง **ไม่ขยับตาม VIT**
  · **หักจริงต้องวัดจากส่วนต่าง `G.hp` ก่อน/หลัง `resolveAnswer` เท่านั้น** อย่าเชื่อ `wrongDamage()`
  เฉย ๆ เพราะการ์ดของ v4.7 คูณทับทีหลัง — เซ็ต `G.floor` · `G.floorProgress = 0` · `G.shield = 0`
  · `CD_CARD = null` แล้ว `nextMonster()` · `G.hp = G.maxHp` · `G.locked = false` ก่อนยิงทุกเคส
  · หมดเวลายิงด้วย `resolveAnswer(null, null, true)` · ตอบผิดยิงด้วย
  `resolveAnswer(m.choices.filter(c => c !== m.answer)[0], null, false)`
  · **เพดานต้องเทียบกับ `Math.min(ตารางดิบ, เพดาน)` เสมอ ไม่ใช่ตารางดิบเปล่า ๆ** ไม่งั้นเคสบอส
  ชั้น 10/15/20 หมดเวลาจะ fail แบบหลอก ๆ ทั้งที่โค้ดถูก (175/225/275 ถูกหนีบเหลือ 150 ตามกติกา)
  · ⚖️ เคสเพดานกันตาย: `CD_CARD = CD_BY_ID['scales']` · `G.floor = 20` · `G.hp = 200` แล้วหมดเวลา
  → `G.floor` ต้องกลายเป็น `zoneOf(20).from` (= 14) และ `G.hp > 0`
  · **ตายจริงต้องยังตายได้** — ตั้ง `G.hp` ต่ำกว่าเพดาน (เช่น 60) แล้วหมดเวลาบนชั้น 20
  ต้องได้ `G.hp <= 0` แล้ว **ล้าง `G.items` ก่อนเสมอ** ไม่งั้น 🛡️ ประกันภัยที่ดรอปมาระหว่างเทสต์
  จะชุบชีวิตให้แล้วเคสตกด้วยเหตุผลผิด
  · หลัง `onHunterDown` **จอค้างที่ประตูวาปโดยตั้งใจ** (`G.warpOpen === true` · `G.locked === true`)
  อย่าเช็ก `!G.locked` ตรง ๆ ต้องกด `warpGo()` ก่อน (เจอมาแล้ว เข้าใจผิดว่าเป็นบั๊ค)
  · ธงหมดเวลาไม่ค้าง: ทำให้ชั้นล่างโยน error กลางคัน (ยัด `renderStats` ที่ throw) แล้ว
  `HD_TIMEOUT` ต้องกลับเป็น `false`
- **ทดสอบชั้น ANTI-CHEAT PAUSE (v4.8.1) ไม่ต้องไต่จริง** — หยุดด้วย `acTogglePause()` ·
  เล่นต่อด้วย `acResumeClick()` · สลับแท็บจำลองด้วย
  `Object.defineProperty(document,'hidden',{configurable:true,get:()=>true})` แล้ว
  `document.dispatchEvent(new Event('visibilitychange'))`
  · เวลาที่เหลืออ่านจาก `AC_LEFT` เทียบ `QUESTION_ENDS - Date.now()` ก่อนหยุด (ต่างกันได้ไม่เกิน ~60ms)
  · หยุดจริงเช็กว่า **ทั้ง `QUESTION_TIMER` และ `QUESTION_TICK` เป็น `null`** ไม่ใช่ตัวใดตัวหนึ่ง
  · หน้ากากเช็กที่ `getComputedStyle(el).filter` ของ `#gWord` `#gPinyin` `#gQuestion` `#gChoices`
  ต้องมี `blur` ครบ และ `#gChoices` ต้องได้ `pointer-events:none`
  · **ม่าน `#acVeil` จะไม่โผล่ถ้ามีหน้าต่างระบบเปิดอยู่** — เคสเปิดร้านค้า/สถานะต้องเช็ก
  `ac-mask` แทน อย่าเช็ก `#acVeil.active` แล้วสรุปว่าพัง
  · เปิดหน้าต่างซ้อนหลายบาน ต้องปิดครบทุกบานถึงจะเดินต่อ (ปิดบานเดียวยังต้องหยุดอยู่)
  · **การเปิด/ปิดหน้าต่างถูกจับด้วย MutationObserver จึงมาถึงใน rAF ถัดไป** ต้องรอ
  `await new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r)))` ก่อนวัดเสมอ
  (เรียก `acTogglePause()` ตรง ๆ ไม่ต้องรอ เพราะมันเรียก `acSync()` แบบซิงโครนัส)
  · **ต้องยืนยันก่อนทุกเคสว่า `QUESTION_TIMER !== null` จริง** ไม่งั้นเคสจะผ่าน/ตกด้วยเหตุผลผิด —
  ถ้าการ์ดที่ถืออยู่คือ 🧘 สมาธิแน่วแน่ ทุกข้อจะเริ่มด้วยการแช่แข็ง `QUESTION_TIMER` เป็น `null`
  โดยชอบธรรม (เจอมาแล้ว เสียเวลาไล่หาอยู่นาน) ในเทสต์ให้สลับเป็นใบที่ไม่ยุ่งกับนาฬิกา:
  `CD_CARD = CD_BY_ID['mana']; CD_BAND = cdBandOf(G.floor); cdPaintUi();` แล้ว
  `clearQuestionTimer(); startQuestionTimer();` **ตั้งเป็น `null` ไม่ได้** เพราะ "มือเปล่าบนชั้นแรก
  ของช่วง" คือเงื่อนไขจั่วใหม่ของ v4.7 — หน้าต่างจั่วจะเด้งมาเองแล้วค้างเป็นหน้าต่างเปิดตลอดกาล
  · **หลังคลิกการ์ดต้องรอเกิน `CD_OUT_MS` (620ms)** ก่อนทำอย่างอื่น ไม่งั้น `setTimeout` ที่ถอด
  `.active` จะไปยิงทับหน้าต่างที่เทสต์เพิ่งเปิดขึ้นมาใหม่
  · ต้นทุน MutationObserver วัดด้วยการห่อ `acSync` แล้วนับจำนวนครั้งใน 5 วินาทีของการเล่นปกติ
  ต้องได้ **0** (ห่อ `acKick` แล้วนับไม่ได้ผล เพราะ `new MutationObserver(acKick)` จับ reference
  ตัวเดิมไว้ตั้งแต่ตอนติดตั้ง)
  · เลย์เอาต์: `document.body.scrollHeight <= window.innerHeight` (body ไม่เลื่อนแล้ว) และวัดว่า
  โจทย์ยังอยู่ในจอหลัง `gameScreen.scrollTop = gameScreen.scrollHeight` · จอสูง ≤620px
  ต้องได้ `position:relative` ไม่ใช่ `sticky`
  · **เลื่อนหนี** จำลองด้วย `gameScreen.scrollTop = gameScreen.scrollHeight` แล้วรอ 2 rAF
  (เหตุการณ์ scroll อยู่ที่ `#gameScreen` ไม่ใช่ `window` — ดักที่ `window` จะไม่ได้ยินอะไรเลย)
  · **`scrollTop = 0` ไม่ใช่ "กลับมาที่โจทย์"** การ์ดสถานะสูง 545px ดันตัวเลือกพ้นจอ
  ต้องยังหยุดอยู่ · จะกลับมาให้เดินต่อต้องเลื่อนถึงแถวตัวเลือกจริง ๆ
  (`scrollTop += gChoices.top − (การ์ดที่ติดหนึบ).bottom − 8`) เทสต์ที่เช็ก `scrollTop = 0`
  แล้วคาดว่าจะเดินต่อจะ fail แบบหลอก ๆ ทั้งที่โค้ดถูก
  · **ต้องพิสูจน์ว่าไม่มีอะไรบังปุ่ม** ด้วย `document.elementFromPoint()` ที่กลางปุ่ม
  แล้วเทียบว่าเป็นปุ่มนั้นจริง — เช็กแค่ `#acVeil.active === false` ไม่พอ
- **ทดสอบชั้น TRIPLE QUEST ENGINE (v4.9.3) ไม่ต้องไต่จริง** — ชุดเทสต์อยู่ที่ `test_triple_quest.js`
  · ตัวนับเสกตรง ๆ ด้วย `tqAdd('ans', n)` / `tqSet('acc', 1)` แล้วเรียก `tqAfter()` วัดผลที่ `G.tq`
  · **ต้องเรียก `tqSeedSeen()` ทุกครั้งหลังยัดค่า `G.tq` เอง** ไม่งั้นธง edge-trigger จะค้าง
  แล้วรางวัลไม่ถูกจ่าย เคสจะตกด้วยเหตุผลผิด
  · Daily Unlocking: `G.tq.unlocked = false` แล้ว `tqAdd('ans', 1)` ต้องไม่ขยับ · ปลดล็อกจริง
  ด้วย `clearFloor(false)` แล้วรอเกิน 1500 (ประตูก่อนบอส/คิวเทิร์นของ v4.0)
  · Anti-farm สกิล: `openShop()` ค้างไว้แล้ว `tqInBattle()` ต้องเป็น `false` · ปิดแล้ว
  `nextMonster()` ใหม่ต้องเป็น `true` — **ต้องยืนยันว่า `QUESTION_TIMER !== null` จริงก่อนวัด**
  (ถ้าถือ 🧘 สมาธิแน่วแน่อยู่ ทุกข้อเริ่มด้วยการแช่แข็ง ให้สลับเป็น `CD_BY_ID['mana']` ก่อน)
  · 📖 เซียน A1 ผูกกับ `onMonsterDefeated` **ตอบถูกเฉย ๆ ไม่พอ ต้องล้มมอนสเตอร์จริง**
  (เซ็ต `G.monsterHp = 1` ก่อนตอบ) เทสต์ที่ตอบถูก 2 ครั้งแล้วคาดว่าตัวนับขยับจะตกด้วยเหตุผลผิด
  · 👑 ล้มยักษ์: `G.floor = FLOOR_MAX` · `TQ_HIT_F = FLOOR_MAX` · `TQ_HITS = TQ_SBOSS_ROUND`
  แล้ว `clearFloor(true)` · การ์ดที่ได้ต้องอยู่ใน `TQ_RARE_POOL` และ `CD_BAND` ต้องถูกปั๊ม
  · 🛡️ สายไร้รอยขีดข่วน: วน `[5,10,15]` เซ็ต `G.floor` + `TQ_POT_F = 0` แล้ว `clearFloor(true)`
  · เคสไม่ได้เครดิตเซ็ต `TQ_POT_F` เป็นชั้นนั้น
  · Silence Mode: `tqNotify('DONE', …)` ขณะนาฬิกาเดิน → `#snLayer` ต้องว่างและ `TQ_HOLD.length`
  เพิ่ม · ตอบจบแล้ว `TQ_HOLD` ต้องว่างและเหลือการ์ด **1 ใบ** (`NC_MAX = 1` รวมเป็น `DONE_MANY`)
  · **ค่าคงที่ของชั้นนี้อยู่ในหน้าเพจ ไม่ใช่ใน Node** — `TQ_RW_SBOSS` / `TQ_RARE_POOL` ต้องคืนออกมา
  จาก `page.evaluate` ก่อนใช้เทียบ ไม่งั้นเทสต์จะพังด้วย `ReferenceError` (เจอมาแล้ว)
  · ข้อมูลเพี้ยน: `tqEnsure()` ที่ยัด `day` ไม่ตรงวันนี้จะ **รีเซ็ตรอบวันก่อน** ตัวกรอง id ซ้ำ
  จึงไม่ได้ทำงาน — เทสต์ตัวกรองต้องตั้ง `day: qDayKey()` ให้ตรงวันนี้เสมอ (เจอมาแล้ว)
  · ค่าถาวรไม่หายต้องผ่าน `exitGame()` + ล็อกอินใหม่จริง (กับดักข้อ 16)

- **ทดสอบชั้น DYNAMIC AUDIO ENGINE (v5.1) ไม่ต้องไต่จริง** — ชุดเทสต์อยู่ที่ `test_audio_engine.js`
  (80 เคส) รันด้วย `NODE_PATH=/opt/node22/lib/node_modules node test_audio_engine.js`
  · **ตัวดักเสียงห่อที่ `auPlay` ตัวเดียวพอ** เพราะทุก wrapper เรียกมันด้วยชื่อ การเขียนทับ
  binding ระดับ global จึงครอบทุกจุดยิง — `const _p = auPlay; auPlay = function(k){ LOG.push(k); return _p.apply(this, arguments); }`
  · เปิดเบราว์เซอร์ด้วย `--autoplay-policy=no-user-gesture-required` ไม่งั้น context ค้างที่
  `suspended` แล้วเคสจะตกด้วยเหตุผลผิด (ตัวโค้ดถูก — เบราว์เซอร์แค่ยังไม่ยอมให้เล่น)
  · **ล้าง throttle ก่อนวัดทุกเคส** (`AU_LAST = {}`) ไม่งั้นเสียงคีย์เดิมที่เพิ่งยิงไปในเคสก่อน
  จะถูกปัดตกแล้วเคสถัดไปจะตกทั้งที่โค้ดถูก
  · Audio Pooling วัดด้วยการวน `auPlay()` 300 รอบโดยปิด throttle แล้วนับ `AU_VOICES.filter(v => v.busy)`
  ต้องไม่เกิน `AU_POOL_MAX` และ `AU_VOICES.length` ต้องไม่งอก
  · เสียง "ตอนของถูกหักจริง" ต้องทดสอบเคสลบด้วยเสมอ — กดยาตอน HP เต็ม · ร่ายสกิลตอน MP ไม่พอ
  · ตอบผิดขณะมีเกราะ ทั้งสามเคสต้องเงียบสนิท
  · คริต: บังคับด้วย `critChance = () => 100` แล้วต้องได้ `SFX_HIT_CRIT` **และไม่มี** `SFX_HIT_CORRECT`
  · toast: สลับ `ncSetMode('off')` แล้วยิง `snShowToast()` ต้องไม่มีเสียง (v4.9.1 ปัดตกทั้งใบ)
  · **`#cdDraft` ที่ค้างเปิดอยู่ทำให้เกม "หยุดถาวร" แล้วเคสของบล็อกหยุดเวลาจะตกยกชุด** —
  ทุกเส้นทางที่ข้ามช่วงชั้น (ร่วงเพราะ HP หมด · `clearFloor(true)` ที่ชั้นบอส) จะเด้งหน้าต่างจั่ว
  ของ v4.7 มาคั่น ต้องเลือกการ์ดให้จบทุกครั้ง (`#cdDraft .cd-card` แล้วรอเกิน `CD_OUT_MS`)
  เจอมาแล้ว เสียเวลาไล่หาว่า "ทำไมนาฬิกาไม่เดิน" ทั้งที่เกมหยุดอยู่โดยชอบธรรม
  · เช่นเดียวกัน หลังเปิด/ปิดประตูวาปต้องพาจอกลับมาที่โจทย์ (`acFocusQa(); acSync(true);`)
  ก่อนวัด ไม่งั้น `acQaVisible()` เป็น false แล้วทุกเคสจะเห็นเกมในสถานะหยุด
  · Auto-Pause: `acTogglePause()` แล้ว `AU_HP_T` ต้องเป็น 0 · `AU_BGM_DUCKED` ต้องเป็น true ·
  และต้องพิสูจน์เพิ่มว่า **ปลุกลูปเองก็ยังไม่มีเสียง** (`auHpTick(); auTimeTick();` แล้ว log ต้องว่าง)
  เพราะสถานะหยุดเกิดได้โดยไม่ข้ามรอยต่อ ตัวเช็กในลูปคือด่านสุดท้าย
  · BGM: `auBgmWant()` เทียบตรง ๆ ได้เลย (ชั้นปกติ / ชั้นบอส / `G.warpOpen` + `DW_WON`)
  · ค่าสวิตช์เสียงต้องรอดข้ามการล็อกอินใหม่ (กับดักข้อ 16) — `exitGame()` พากลับ **หน้าแรก**
  ต้อง `enterGate()` + รอ 600ms + `switchTab('login')` ก่อนถึงจะกรอกช่องล็อกอินได้

- **ทดสอบชั้น CLOUD SYNC (v5.2) ไม่ต้องมีเซิร์ฟเวอร์จริง** — ชุดเทสต์อยู่ที่ `test_cloud_sync.js`
  (104 เคส) รันด้วย `NODE_PATH=/opt/node22/lib/node_modules node test_cloud_sync.js`
  · **ใช้คลาวด์ปลอมที่เขียนทับ `window.fetch` ผ่าน `page.addInitScript`** เลียนแบบชุดคำสั่งย่อย
  ของ PostgREST เท่าที่ชั้นนี้ใช้ (`GET ?usr=eq.` · `GET ?room=eq.` · `PATCH ?usr=eq.&ts=lt.` · `POST`)
  แถวทั้งหมดอยู่ที่ `window.__CS.rows` · ตัดเน็ตด้วย `window.__CS.fail = 1` · คำขอทุกครั้งจดไว้ที่ `window.__CS.log`
  · **ต้องติดตั้งก่อนหน้าโหลด** เพราะ `csInstall` ยิง `csFlush()` ที่ 300ms
  · เปิดคลาวด์ระหว่างเทสต์ด้วย `csCfgSave({provider:'supabase', url:'https://fake.test', key:'k', table:'hunters'})`
  — **คอนฟิกเริ่มต้นว่างเสมอ** เคสของบล็อกแรกจึงยืนยันได้ว่า `window.__CS.log.length === 0` จริง
  · **ส่งจริงต้องเรียก `csFlush()` เอง** อย่ารอ `CS_DEBOUNCE` 6 วินาที (เทสต์ที่รอจะช้าและวืดง่าย)
  · **`saveProgress()` เฉย ๆ ไม่ทำให้แถวขึ้นคลาวด์** เพราะเป็นทางหน่วง — เทสต์ที่ดึงข้อมูลทั้งห้อง
  แล้วคาดว่าจะเห็นตัวเองด้วยต้อง `csFlush()` ก่อน ไม่งั้นจะได้แต่แถวของเพื่อนที่เสกไว้ (เจอมาแล้ว)
  · ข้อมูลชนกันบังคับด้วยการยัด `window.__CS.rows[u].ts = Date.now() + 600000` แล้ว `csFlush()`
  → ต้องได้ `#snGate.active` · กด `snGateConfirm()` แล้วรอ ~900ms ค่อยวัด `G.gold`
  · ล็อกอินข้ามเครื่องจำลองด้วย `localStorage.setItem('yao_students','{}')` แล้วเสกแถวบนคลาวด์
  (สร้าง `doc` จาก `blankAccount()` แล้วตัด `pw` ออก · `pwh` ต้องมาจาก `csPwh(id, pw)` ของหน้าเพจ)
  · ล็อกอินเงียบต้องเซ็ต `CS_GATE = false` ก่อนเรียก `csSilent()` เสมอ เพราะ `enterGate()`
  ในขั้นตอนเข้าเกมปกติปั๊มธงนั้นเป็น true ไปแล้ว แล้ว `csSilent()` จะคืน false ทันทีอย่างถูกต้อง
  · จุดยิงอัตโนมัตินับจาก `window.__CS.log.filter(l => /PATCH|POST/.test(l)).length` ก่อน/หลัง

- ทดสอบตรรกะคำถามไม่ต้องเล่นจริงก็ได้ — เรียก `getMonster._build(VOCAB[i], 'A')` วนหลายพันรอบ
  แล้วตรวจผลรวมทีเดียว (ตัวเลือกครบ 4 · ไม่ซ้ำ · มีเฉลย · ไม่มีอักษรจีน · `choiceMeta` ครบ)
  เร็วกว่าและครอบคลุมกว่าการไล่กดปุ่มมาก

- **ทดสอบชั้น NOTIFY CONTROL (v4.9) ไม่ต้องไต่จริง** — สลับโหมดด้วย `ncSetMode('all'|'crit'|'off')`
  หรือ `ncCycleMode()` · โหมดที่จำไว้อ่านจาก `localStorage['yao_notify']`
  · **ตัวกรองวิกฤตต้องยืนยันก่อนทุกเคสว่า `QUESTION_TIMER !== null` จริง** ไม่งั้นเคสจะผ่าน/ตก
  ด้วยเหตุผลผิด (กติกาเดียวกับ v4.8.1) — ถ้าการ์ดที่ถืออยู่คือ 🧘 สมาธิแน่วแน่ ทุกข้อจะเริ่มด้วย
  การแช่แข็ง ให้สลับเป็น `CD_CARD = CD_BY_ID['mana']` ก่อน
  · **การ์ดเต็มใบโชว์รหัสอ้างอิงเต็ม** (`ANALYTICS 24/25` ไม่ใช่ `24/25`) เทสต์ที่เทียบเฉพาะ
  ตัวเลขจะตกด้วยเหตุผลผิด — ร่าง Mini-Banner ของ v4.9 เคยตัดเหลือเฉพาะเลข
  · **วัดความสูงการ์ดต้องยิงครบทุก def ของทุกชั้น** (51 ใบ) ด้วยข้อมูลยาว ๆ แล้วเทียบขอบล่าง
  กับขอบบนของ `#gWord` — ใบที่ยาวสุดคือ `CD_PICK_2` / `CD_PICK_3` / `CD_DROP` ไม่ใช่ของ `SYS_NOTIF`
  · **กวาดตำแหน่งเลื่อนต้องรอ 2 rAF ทุกก้าว** เพราะ `ncRefit()` หน่วงด้วย rAF และการ์ด
  หมดอายุระหว่างกวาดได้ ต้องยิงใบใหม่ทดแทนเมื่อหาย ไม่งั้นจะวัดกับจอเปล่า
  · Batching: เซ็ต `SN_MP_SEEN = {}` · `G.skillCd = {}` · `G.mp = maxMpOf(G)` แล้ว `snCheckMpReady()`
  ต้องได้ใบเดียวแท็บ `03/03` · ตั้ง `G.mp = 40` (พอสำหรับ eye เท่านั้น) ต้องได้แท็บ `20/25`
  · เตือนเวลาใกล้หมด: `startQuestionTimer()` แล้วยัด `QUESTION_ENDS = Date.now() + 3400`
  ตามด้วย `ncDisarmLow(); ncLowTick();` แล้วรอ ~900ms
  · 🛡️ ประกันภัย: `G.items = { insurance: 2 }` · `G.hp = 1` · `CD_ST.noHeal = 0` แล้ว `onHunterDown()`
  · จอเตี้ยกว่า 620px ยังเหลือการคร่อมได้ราว 5 ตำแหน่งจาก ~60 (ดูหัวข้อ Fit Guard)
  เทสต์จึงตั้งเพดานไว้ ไม่ได้บังคับ 0 เหมือนจอสูง
  · **แถบปลอดภัยต้องกวาดทุกตำแหน่งเลื่อน ไม่ใช่จุดเดียว** (กับดักข้อ 23) ไล่
  `gs.scrollTop = 0 .. gs.scrollHeight - gs.clientHeight` ทีละ 20px แล้วทุกจุดยิง
  `document.elementFromPoint()` ทั่วกรอบ toast **หลังซ่อนชั้น `#snLayer` ชั่วคราว**
  ไล่ขึ้น `parentElement` หาว่าโดน `gWord/gPinyin/gQuestion/gChoices` หรือไม่
  ต้องได้ 0 ทุกความกว้าง **และต้องมีจอเตี้ยกว่า 620px อยู่ในชุดด้วย** เพราะ v4.8.1 ปิด sticky ไว้
  ที่นั่น พฤติกรรมคนละแบบกับจอสูง
  · **`openShop()` บนบัญชีชั้น 1 เปิด `#gLock` ไม่ใช่ `#gShop`** (ร้านค้าล็อกที่ D-RANK) แล้ว
  `closeShop()` ปิดไม่ลง เกมค้างหยุดอยู่เพราะยังมีหน้าต่างเปิดค้าง — ต้อง
  `G.maxFloor = FLOOR_MAX; recalcStats();` ก่อน ไม่งั้นเคสตกด้วยเหตุผลผิด (เจอมาแล้ว)
  · นับต้นทุน `acSync` ต้อง **ตั้งนาฬิกาให้เสร็จก่อนค่อยเริ่มนับ** เพราะ `startQuestionTimer`
  ของ v4.8.1 เรียก `acSync(true)` เองหนึ่งครั้งโดยชอบธรรม ไม่ใช่การถูกปลุกฟรี

- **ทดสอบชั้น DUAL WARP GATE (v4.9.2) ไม่ต้องไต่จริง** — ชุดเทสต์อยู่ที่ `test_dual_warp_gate.js`
  · พาไปยืนชั้นที่ต้องการด้วย `G.floor = f` · `G.floorProgress = MONSTERS_PER_FLOOR - 1`
  แล้ว `nextMonster()` · ประตูก่อนบอสยิงด้วย `clearFloor(false)` แล้ว **รอเกิน `DW_WAIT` 1500**
  · ประตูหลังบอสยิงด้วย `clearFloor(true)`
  · ชั้นที่ต้องเปิดประตูอ่านจาก `dwIsPreBossFloor(f)` ตรง ๆ ไล่ทั้ง 20 ชั้นได้ในทีเดียว
  · ข้อความปุ่มเทียบเต็มสตริง (`'⚔️ เข้าสู่ห้องบอส ชั้น 5 ▶'`) เพราะเลขชั้นในปุ่มคือจุดที่พังง่ายสุด
  · ยอดรางวัลเทียบกับ **ส่วนต่าง `G.gold` จริงก่อน/หลัง** ไม่ใช่เลข 500 ที่จำมา (การ์ด ⛓️ 💀 คูณทับได้)
  · **กดปุ่มเข้าห้องบอสแล้วต้องเรียก `snGateConfirm()` ก่อนวัดนาฬิกาเสมอ** — v4.1/v4.6 เด้ง
  หน้าต่างบังคับกดยืนยันตอนก้าวเข้าห้องบอส แล้ว v4.8.1 หยุดนาฬิกาไว้ให้อย่างถูกต้อง
  เทสต์ที่วัดทันทีจะได้ `QUESTION_TIMER === null` แล้วตกด้วยเหตุผลผิด (เจอมาแล้ว)
  · **หน้าต่างนั้นมาช้ากว่าที่คิด** ต้องวนเช็กแบบ poll (ทุก ~180ms จนเงียบติดกันหลายรอบ)
  ไม่ใช่เช็กครั้งเดียวหลัง `waitForTimeout` คงที่
  · กันแทรกซ้ำ: เรียก `renderWarp('preboss'/'floor'/'down')` วนหลายสิบรอบ แล้ว
  `#gWarp .dw-reward` · `.dw-shop` · `.wp-art` ต้องเหลืออย่างละ **1** เท่านั้น
  · **ปุ่มอยู่ในจอหรือไม่ ต้องวัดด้วยกรอบเทียบ `window.innerHeight`** อย่าใช้
  `document.elementFromPoint()` อย่างเดียว เพราะมันคืน `null` ทั้งตอน "มีอะไรมาบัง" และตอน
  "อยู่นอกจอ" ซึ่งเป็นคนละอาการกันคนละทางแก้ (เจอมาแล้ว เสียเวลาไล่หาว่าอะไรมาบังทั้งที่ไม่มีอะไรบัง)
  · โหมดฝึกจุดอ่อนต้องไม่มีประตู: `G.practiceMode = true` แล้ว `clearFloor(false)` ที่ชั้น 4

ก่อนส่งงานทุกครั้ง: เช็ก syntax ด้วย `new Function(<เนื้อ script>)` แล้วรันชุดเทสต์
ยืนยันว่า **ไม่มี pageerror** และเลย์เอาต์ไม่ล้นแนวนอน (`document.body.scrollWidth <= window.innerWidth`)

ข้อความไทยยาว ๆ บนจอ 390px ตัดคำกลางคันได้ง่าย (เจอมาแล้วกับบรรทัดรางวัลเควส "⭐ 400" ขึ้นบรรทัดใหม่
ทิ้ง "EXP" ไว้ข้างล่าง) — ห่อรางวัลทีละก้อนด้วย `white-space:nowrap` แทนการปล่อยให้ไหลทั้งบรรทัด

## ข้อมูลผู้เล่น

เก็บใน `localStorage` ล้วน ไม่มีเซิร์ฟเวอร์

- `SKEY = 'yao_students'` — บัญชีนักเรียนทั้งหมด
- `BKEY = 'yao_broadcast'` — ข้อความประกาศจาก GM
- `GC_EKEY = 'yao_errlog'` — Error Log ของเครื่อง (ไม่ผูกบัญชี · เพดาน 60 รายการ · ข้อความซ้ำถูกยุบ
  เป็นรายการเดียวพร้อมตัวนับ `n` กัน log ล้นจาก error ที่เกิดในลูป render)
- `NC_KEY = 'yao_notify'` — โหมดแจ้งเตือนของชั้น v4.9.1 (`all` / `crit` / `off`)
  ระดับเครื่อง ไม่ผูกบัญชี เพราะเป็นค่าการแสดงผล ไม่ใช่ความคืบหน้าของผู้เล่น
- `CS_K_CFG = 'yao_cloud'` — คอนฟิกคลาวด์ของชั้น v5.2 (provider · url · key · table · auto · strict)
  ระดับเครื่อง ไม่ผูกบัญชี **และไม่ได้ฝังลงไฟล์เกมโดยเจตนา** เพราะ repo เป็น public
  · `yao_cloud_out` กล่องส่งออกตอนเน็ตหลุด (รายการเดียวต่อคน) · `yao_cloud_ses` เซสชันจำอัตโนมัติ
  (อายุ 14 วัน) · `yao_cloud_bak` ก้อนสำรองอันสุดท้ายก่อนถูกคลาวด์ทับ · `yao_cloud_dev` รหัสเครื่อง
- `AU_KEY = 'yao_audio'` — สวิตช์เสียงของชั้น v5.1 (`on` / `off`) ระดับเครื่อง ไม่ผูกบัญชี
  เพราะลำโพงเป็นของเครื่องไม่ใช่ของคน · **เป็นตัวตัดสินเหนือ `a.muted` ของบัญชี** ตอนล็อกอิน
  (`a.muted` ยังถูกเขียนอยู่ตามเดิม แต่ถูกเขียนทับด้วยค่าในเครื่องทุกครั้งที่ `startGame`)
- `TEACHER_PIN` — รหัสเข้าแผง GM (อยู่ในซอร์สแบบ plaintext และ repo เป็น public
  ถ้าจะทำให้ปลอดภัยจริงต้องมีเซิร์ฟเวอร์ ตอนนี้ถือว่ากันเด็กกดเล่นเฉย ๆ)

`migrateAccount()` ทำหน้าที่อัปเกรดบัญชีเก่าให้มีฟิลด์ใหม่ **เพิ่มฟิลด์ใน `blankAccount()`
เมื่อไหร่ ต้องเติม default ใน `migrateAccount()` ด้วยเสมอ** ไม่งั้นนักเรียนที่เล่นมาก่อนจะพัง

ฟิลด์ที่ชั้นบน ๆ เติมเข้ามาเอง (ผ่าน wrapper ของ `migrateAccount` ไม่ได้แก้ตัว v4.0):
`quest` (v4.2) · `gcv` `gcLog` `sessions` `loginCount` `lastLogin` `playMs` (v4.3) ·
`codex` `cxv` (v4.4) · `trFloors` `trv` (v4.5) · `ab` `abv` (v4.6) · `cd` `cdv` (v4.7) · `tq` (v4.9.3)
· `cs` (v5.2 — `{rev, ts, dev}` ไม่มีตรารุ่นเพราะไม่มีอะไรให้ backfill)
แต่ละตัวมีฟังก์ชัน `*Ensure()` ของตัวเองและต้อง idempotent ทั้งหมด
· **v4.8 · v4.8.1 · v4.9 · v4.9.2 ไม่เพิ่มฟิลด์ในบัญชีเลย** (v4.8 คำนวณสดจาก `G.floor` · v4.8.1 เก็บแต่สถานะ
ของข้อที่กำลังตอบ · v4.9 เก็บโหมดไว้ที่ `localStorage` ระดับเครื่อง) จึงไม่มี `hdEnsure()` /
`acEnsure()` / `ncEnsure()` / `dwEnsure()` และไม่มีตรารุ่น `hdv` / `acv` / `ncv` / `dwv`
(v4.9.2 เก็บแค่ยอดรางวัลของประตูที่กำลังเปิดอยู่ที่ `DW_WON`)
· **v4.9.3 เพิ่มฟิลด์ `tq` แต่ไม่มีตรารุ่น `tqv`** เพราะไม่มีอะไรให้ backfill — `tqEnsure()`
เติม default ให้บัญชีเก่าตอน migrate แล้วเริ่มนับจากศูนย์
· **v5.1 ไม่เพิ่มฟิลด์ในบัญชีเลย** (ค่าเสียงอยู่ที่ `localStorage['yao_audio']` ระดับเครื่อง)
จึงไม่มี `auEnsure()` ไม่มีตรารุ่น `auv` และ **ไม่ได้ห่อ `migrateAccount` ด้วยซ้ำ**
· **v5.2 เพิ่มฟิลด์ `cs` ฟิลด์เดียว** (`{rev, ts, dev}`) เติมให้ใน wrapper ของ `migrateAccount`
โดยตรงเพราะสั้นมาก ไม่ต้องมี `csEnsure()` แยก — แต่ยังต้อง idempotent เหมือนกันทุกประการ

## การส่งงานขึ้น repo

`git push` จาก session ของ Claude **ถูก GitHub ปฏิเสธ 403** (GitHub App ติดตั้งด้วยสิทธิ์
อ่านอย่างเดียว — ทั้ง git push และ REST API `contents` เด้งเหมือนกัน) การอ่าน/clone/fetch ปกติ

วิธีที่ใช้อยู่: Claude ส่งไฟล์ให้ → เจ้าของ repo อัปโหลดผ่านเว็บ GitHub
(**Add file → Upload files** ลากไฟล์ชื่อเดิมทับได้เลย ไม่ต้องลบไฟล์เก่าก่อน)

ถ้าจะให้ push ได้เอง ต้องเปิดสิทธิ์ write ให้ GitHub App ที่
https://claude.ai/admin-settings/claude-in-slack

ยืนยันอีกครั้ง (ส.ค. 2026): `git push` ลองซ้ำ 4 ครั้งก็ยัง 403 ทุกครั้ง ไม่ใช่ปัญหาเน็ตชั่วคราว
และ MCP `push_files` ก็ใช้แทนไม่ได้ เพราะต้องแนบเนื้อไฟล์ทั้งก้อน (~585KB) ไปกับคำสั่ง

**อย่าเสียเวลาไล่เช็กซ้ำ** — ตรวจครบทุกทางแล้ว ทุกทางตันที่สิทธิ์เดียวกัน (Contents: write):

- `git push` → `403`
- MCP `create_branch` → `403 Resource not accessible by integration`
- MCP `create_or_update_file` / `push_files` → ทางเดียวกัน ปิดเหมือนกัน

และ **`git branch -a` หลอกตา** — มันโชว์ `remotes/origin/claude/...` เหมือนแบรนช์มีอยู่จริงบน GitHub
แต่ `git fetch origin <branch>` ตอบ `couldn't find remote ref` และ `list_branches` เห็นแค่ `main`
อันนั้นเป็น ref ค้างที่ตัว session สร้างไว้ตอน clone ไม่ใช่แบรนช์จริง `git push` จึงต้อง *สร้าง* แบรนช์ใหม่
ซึ่งยิ่งต้องใช้สิทธิ์เขียน

**ผลที่ตามมา:** commit ที่ Claude ทำไว้บนแบรนช์ `claude/...` อยู่แค่ในเครื่อง session และจะหายไป
พร้อม container ดังนั้นทุกครั้งที่ทำงานเสร็จให้ **ส่งไฟล์ `hanzi_hunter_tower_v3_1_intro.html`
(และ `CLAUDE.md` ถ้าแก้) ให้เจ้าของ repo ทันที** อย่ารอให้ push สำเร็จก่อน
