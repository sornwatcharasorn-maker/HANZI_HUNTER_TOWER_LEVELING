# CODEX_MASTERY_MATH.md — Patch v9.6 · SUBCONSCIOUS PURE GOLD ENDGAME

เอกสารนี้เป็นส่วนหนึ่งของ deliverable ตามสเปกงาน "OVERWRITE CODEX SEAL WITH
SUBCONSCIOUS PURE GOLD ENDGAME (PATCH v9.5)" — เขียนทับความหมายของผนึก
"📖 คลังอักขระ (xx / 329)" ในหน้าต่างระบบความสำเร็จ 6 ผนึก (Monarch Ascension
ของ v8.0) ให้นับเฉพาะคำที่ถึง **Tier 4 · ศิลาทองคำบริสุทธิ์ 👑** เท่านั้น

**เลขรุ่นจริงคือ v9.6 ไม่ใช่ v9.5** — v9.5 ถูกใช้ไปแล้วโดย
"DYNAMIC CLASS SKILL MODAL SYNC" เลขชั้นเดินตามลำดับการห่อจริงเสมอ ไม่ใช่ตาม
เลขในคำสั่ง (precedent เดียวกับ v4.9.2 ที่คำสั่งเรียกว่า "v4.8.2" · v5.2 ที่
คำสั่งเรียกว่า "v5.0")

ชั้นนี้อยู่ในเนมสเปซ `ba` ของ v6.0 เดิม (`BA_SM_*` / `baSm*`) วางท้ายสุดของไฟล์
ต่อจาก `baInstallV95()` และก่อน `baInstall()` — **ไม่แก้โค้ดของ v4.0-v9.5 เลย
แม้แต่บรรทัดเดียว** ทุกจุดเป็น wrapper (`const _old = fn; fn = function(){...}`)

## 1) สูตร netScore — แก้จากที่สเปกเขียนไว้ตรง ๆ

สเปกให้สูตรมาว่า `netScore = seen - wrong` **แต่ยังเขียนกำกับไว้ในวรรคเดียวกันว่า**
"หากตอบผิดให้หักแต้มทันที (netScore-1)" — สองข้อนี้ขัดกันเองทางคณิตศาสตร์:

```
ตอบผิดหนึ่งครั้ง (ทุกครั้งที่ตอบ ไม่ว่าถูกหรือผิด ต้องนับเข้า seen ด้วยเสมอ):
  seen += 1, wrong += 1
  netScore เดิม = seen_old - wrong_old
  netScore ใหม่ (สูตร seen-wrong) = (seen_old+1) - (wrong_old+1) = seen_old - wrong_old  ← ไม่ขยับเลย (Δ=0)
```

สูตร `seen - wrong` ตรง ๆ ให้ **Δ = 0** ตอนตอบผิด ไม่ใช่ Δ = -1 ตามที่สเปกสั่งไว้ตรง ๆ
ในประโยคเดียวกัน — จึงต้องเลือกสูตรที่ทำให้ "หักแต้มทันที 1 หน่วยต่อการตอบผิด" เป็นจริง

**สูตรที่ใช้จริง:**

```
netScore = seen - 2 × wrong
```

พิสูจน์ว่าให้ Δ = -1 พอดีตามที่สเปกสั่ง:

```
ตอบผิดหนึ่งครั้ง: seen += 1, wrong += 1
  netScore ใหม่ = (seen_old+1) - 2×(wrong_old+1) = seen_old - 2×wrong_old - 1 = netScore เดิม - 1   ✓
ตอบถูกหนึ่งครั้ง: seen += 1, wrong ไม่ขยับ
  netScore ใหม่ = (seen_old+1) - 2×wrong_old = netScore เดิม + 1   ✓ (ตรงกับที่ไม่มีข้อขัดแย้งอยู่แล้ว)
```

สูตรนี้จึงเป็นสูตรเดียวที่ทำให้ทั้ง "ตอบถูก +1" และ "ตอบผิด -1 ทันที" เป็นจริงพร้อมกัน
โดยไม่ขัดกันเอง — เขียนไว้ที่ `baSmNet(w)`:

```js
function baSmNet(w) { return (w && (((w.seen || 0)) - 2 * (w.wrong || 0))) || 0; }
```

**Speed Streak รีเซ็ตเป็น 0 ทันทีที่ตอบผิด** ตามที่สเปกสั่งตรง ๆ — ทำที่ wrapper ของ
`recordWord(id, wasCorrect, ms)`:

```js
w.fastStreak = (wasCorrect && ms <= BA_SM_SPEED) ? ((w.fastStreak || 0) + 1) : 0;
```

## 2) ตาราง 4 ขั้น — ค่าคงที่ที่เคาะไว้

| ขั้น | ชื่อ | เกณฑ์ | ผลในคลัง |
|---|---|---|---|
| Tier 1 | คุ้นเคย | `netScore >= 4` | ปลดล็อกพินอิน (ซ่อนคำแปล) |
| Tier 2 | เขียวมรกต | `netScore >= 8` | ปลดล็อกคำแปลไทย |
| Tier 3 | ประกายทองคำ | `netScore >= 18` **และ** Gatekeeper (ชั้น 12+ หรือเหวลึก) | กรอบการ์ดแสงทอง Shimmer |
| Tier 4 | ศิลาทองคำบริสุทธิ์ 👑 | `netScore >= 30` **และ** Gatekeeper **และ** Speed Streak: ตอบถูกภายใน ≤2.5 วิ ติดกันล่าสุด ≥3 ครั้ง | ตัวอักษรทอง+มงกุฎ · **นับเข้าหลอดผนึก** |

ค่าคงที่ในซอร์ส (`BA_SM_N1`..`BA_SM_N4` · `BA_SM_GATE_FL` · `BA_SM_SPEED` · `BA_SM_K`):

```
N1 = 4      N2 = 8      N3 = 18     N4 = 30
GATE_FLOOR = 12 (ชั้นสูงสุด)   หรือ  s.ab.abyss === true (โหมดเหวลึกเปิดอยู่)
SPEED_MS = 2500   K = 3
```

**ทำไม N1-N4 เว้นระยะไม่เท่ากัน (4→8→18→30 ไม่ใช่ 4→8→12→16)** — แต่ละขั้นถัดไป
ต้องใช้ "ความพยายามสุทธิเพิ่ม" มากกว่าขั้นก่อนเสมอ เพราะการไต่ netScore ยากขึ้นเรื่อย ๆ
โดยธรรมชาติอยู่แล้ว (ตอบผิดหนึ่งครั้งเสีย 2 หน่วยของสิ่งที่สะสมมา ยิ่งอยู่สูงยิ่งเจ็บมาก
เมื่อเทียบเป็นสัดส่วน) ระยะห่างที่ถ่างออกจึงจำลองความรู้สึก "ยากขึ้นแบบทวีคูณ" ของเกม
RPG ทั่วไป โดยไม่ต้องเปลี่ยนตัวสูตรหลัก

**Gatekeeper (ชั้น 12+ หรือเหวลึก)** — กันไม่ให้ผู้เล่นปั๊ม netScore สูง ๆ จากคำง่าย ๆ
ในชั้นต้น ๆ (ซึ่งตอบซ้ำได้เร็วเพราะโจทย์คุ้นแล้ว) แล้วได้ Tier 3/4 ก่อนที่จะพิสูจน์ตัวเอง
ในหอคอยขั้นสูง — ผูกกับ "ความคืบหน้าสูงสุดที่เคยไปถึง" (`s.maxFloor`) ไม่ใช่ "ชั้นที่ยืนอยู่
ตอนนี้" (`s.floor`) เพราะ Codex เป็นบันทึกสะสม ไม่ใช่สถานะชั่วคราว ผู้เล่นที่เคยไปถึงชั้น 12
แล้วร่วงกลับมาก็ยังปลดล็อก Tier 3/4 ได้ต่อ — และเปิดทางสำรองผ่านโหมดเหวลึกด้วย เพราะ
เหวลึกเป็นโหมดที่ยากกว่าหอคอยปกติอยู่แล้วโดยไม่ต้องอิงเลขชั้น

**Speed Streak (Tier 4 เท่านั้น)** — พิสูจน์ว่า "จำได้จริงจนตอบตอบสนองอัตโนมัติ"
(Subconscious Mastery ตามชื่อแพตช์) ไม่ใช่แค่ไล่ netScore ให้สูงพอแบบตอบช้า ๆ คิดนาน
ก็ได้ ตัวเลข ≤2.5 วิ ติดกัน 3 ครั้งล่าสุดถูกเลือกให้อยู่กึ่งกลางของช่วง 2.0-2.5 วิ ที่สเปก
ระบุไว้ (เข้มกว่า 3.5 วิ ของ "ตอบไว = คริตแน่นอน" ในระบบต่อสู้ของ v6.3 ซึ่งเป็นมาตรฐาน
"ไว" ของทั้งเกมอยู่แล้ว — Codex Mastery เข้มกว่านั้นอีกขั้นเพราะเป็นเกณฑ์ปลายทาง)

## 3) โบนัสผู้พิชิต 100% (Grand Mastery)

ฉายา **👑 ผู้หยั่งรู้รากอักขระ** (`allknowing`) ปลดล็อกเมื่อ **ทุกคำใน VOCAB (329 คำ)**
ถึง Tier 4 พร้อมกัน — `TITLES.push()` เข้าไปตามรูปแบบเดิมของทุกชั้น (v4.2/v4.6/v4.7/…)

| โบนัส | ทำที่ไหน | ทำไม |
|---|---|---|
| คูลดาวน์สกิลทุกช่อง -1 เทิร์น | wrapper ของ `baV91Activate(i,g,r)` ของ v9.1 | จุดเดียวที่ตั้ง `BA_V91_CD[i]` จริงหลังใช้สกิลสำเร็จ — ห่อ "หลัง" เรียกของเดิม แล้วลด CD ที่เพิ่งตั้งลงอีก 1 |
| ดาเมจทุกโหมด +15% | wrapper ของ `hunterAtk()` | ทางผ่านเดียวของดาเมจฮีโร่ทั้งเกม (v4.0) — **ต้องกดด้วย `BA_LIVE`** (ธงที่เป็นจริงเฉพาะระหว่างตัดสินข้อของ `resolveAnswer`) ไม่งั้น HP มอนสเตอร์ (ซึ่งคำนวณจาก `hunterAtk()` เหมือนกัน) จะพองตามไปด้วย แล้วผู้เล่นจ่ายมาแต่ไม่ได้อะไรเลย — บทเรียนเดียวกับ "แกนพิฆาต" ของ v7.9 |
| ประกายทองคำรอบตัวในสนามรบ | `renderStats()` wrap → toggle class `.ba-sm-grand` บน `#baArena` | CSS `filter:drop-shadow(...)` ล้วนบน `#baHero` ตามแบบ static-glow ของ `.ba-elite` (v6.4) — ไม่มี DOM ใหม่ ไม่มี `animation:infinite` |

**สกิลไม่มีคูลดาวน์สายไม้ตาย (`r.cd < 0`)** ยังไม่ถูกแตะ เพราะระบบเกจ 8 พิพของ v8.5/v9.0
ไม่มีแนวคิดเทิร์นคูลดาวน์ตั้งแต่ต้น การไปลด "เกจ" จะเป็นกลไกคนละแบบที่สเปกไม่ได้ขอ

**บัฟถาวรทำงานทันทีที่ `s.titles` มีคีย์ `allknowing` — ไม่ผูกกับการ "สวมฉายา"**
ต่างจากฉายาทั่วไปที่ผูกผ่านฟิลด์ `bonus` (ซึ่งมีผลเฉพาะตอนสวม) เพราะโบนัสนี้เป็นรางวัล
ถาวรของการพิชิต ไม่ใช่ของประดับที่เลือกใส่ได้ — `TITLES.push()` ของรายการนี้จึงตั้งใจ
**ไม่มีฟิลด์ `bonus`** เลย ป้องกันไม่ให้ระบบสวมฉายาเดิมของ v4.0 (ซึ่งอ่าน `bonus` แล้วบวก
ให้เฉพาะตอนสวม) ไปกินความหมายผิด

## 4) หลอดผนึก "📖 คลังอักขระ" — ตัวนับแบบใหม่

Monarch Ascension ของ v8.0 มีเสาหลัก 6 ต้น ต้นแรกอ่านจาก `BA_MN_PILL[0].get(g)`
เดิมนับ `cxCount(g)` (คำที่ปลดล็อกแบบสะสมของ v4.4) แพตช์นี้เขียนทับ `.get` ของช่องนั้น
ให้คืน `[baSmTier4Count(g), CX_TOTAL]` แทน — **ไม่แก้โครงสร้างของ `BA_MN_PILL` เลย**
เพราะเป้าหมาย/หน่วย (329) เหมือนเดิมทุกประการ เปลี่ยนแค่ "อะไรถูกนับ"

### บทเรียนสถาปัตยกรรม: ทำไมต้องแยกเป็นสองฟังก์ชัน ไม่ใช่ฟังก์ชันเดียว

**ตัวอ่านด่วน (cheap precheck) ของฉายาแบบ `archivist` ใช้ผิดที่ไม่ได้** — ฉายาหลายใบ
ในเกมนี้เช็กว่า "ครบเงื่อนไขหรือยัง" ด้วยการหนีบก่อนวนลูป 329 คำ เช่น
`if (s.correct < ค่าต่ำสุดที่เป็นไปได้) return false;` ก่อนจะวนตรวจทีละคำจริง ๆ — ท่านี้
**ถูกต้องเฉพาะกับคำถามที่เป็น boolean "ครบหรือยัง"** เพราะสามารถพิสูจน์ได้ทางพีชคณิตว่า:

```
sum(correctCount_i) เหนือทุกคำ i ทั้ง 329 คำ  ≡  s.correct  (เป๊ะเสมอ ไม่ว่ากรณีใด)
โดย correctCount_i = seen_i - wrong_i (นับความถูกสุทธิของคำนั้น)

เพราะ netScore_i = seen_i - 2×wrong_i  <=  correctCount_i  เสมอ (wrong_i >= 0)
ถ้าทุกคำต้องมี netScore_i >= 30 พร้อมกัน (ครบ Tier 4 ทั้ง 329 คำ)
=> correctCount_i >= 30 ทุกคำ
=> s.correct = sum(correctCount_i) >= 329 × 30 = 9,870
```

เส้นนี้เป็นเงื่อนไข**จำเป็น**สำหรับ "ครบทั้ง 329 คำ" — ถ้า `s.correct < 9,870` การันตีได้เลย
ว่ายังไม่ครบ ไม่ต้องวนตรวจสักคำ ปลอดภัยเพราะเป็นเงื่อนไข **จำเป็น** ไม่ใช่ **เพียงพอ**
(การครบ 9,870 ไม่ได้แปลว่าครบทั้ง 329 คำเสมอไป ต้องวนตรวจจริงต่อ แต่ไม่ครบ 9,870
รับประกันได้ว่ายังไม่ครบแน่นอน)

**แต่ด่านเดียวกันนี้ใช้กับตัวนับหลอดผนึกที่ต้องโชว์ตัวเลขระหว่างทาง (150/329) ไม่ได้เลย**
ผู้เล่นที่ทำได้ 200 จาก 329 คำจริง จะมี `s.correct` ต่ำกว่า 9,870 แน่นอน (เพราะยังไม่ครบ)
— ถ้าใช้ด่านเดียวกันหนีบไว้ก่อนวนนับ ฟังก์ชันจะคืน 0 ทันที **ทั้งที่ทำได้จริง 200 คำแล้ว**
ผู้เล่นจะเห็นหลอดค้างที่ 0/329 ตลอดจนกว่าจะครบทุกคำพร้อมกัน ซึ่งทำลายจุดประสงค์ของการ
โชว์ความคืบหน้าทั้งหมด — บั๊คนี้ถูกจับได้ระหว่างทดสอบด้วยเคส "1 คำผ่าน Tier 4 คำเดียว
แล้ววัด `t4count`" ซึ่งคืน 0 แทนที่จะเป็น 1

**ทางแก้ — แยกเป็นสองฟังก์ชันตามหน้าที่:**

```js
/* ตัวนับจริง — ใช้กับหลอดผนึก + audit · ไม่มีด่านถูกแบบเหมา (แค่กันกรณีว่างเปล่า) */
function baSmTier4Count(s) {
  if (!s || !s.wordStats || !(s.correct || 0)) return 0;
  let n = 0;
  for (let i = 0; i < VOCAB.length; i++) { if (baSmTier(s, VOCAB[i][0]) === 4) n++; }
  return n;
}

/* boolean ล้วน — ใช้กับ checkTitles() ซึ่งถูกเรียกทุกครั้งที่อ่าน store (ทุกบัญชี)
   จึงต้องมีด่านถูกแบบเหมาก่อนวนเสมอ เพื่อประสิทธิภาพ */
function baSmAllGold(s) {
  if (!s || (s.correct || 0) < VOCAB.length * BA_SM_N4) return false;
  for (let i = 0; i < VOCAB.length; i++) { if (baSmTier(s, VOCAB[i][0]) !== 4) return false; }
  return true;
}
```

`baSmTier4Count` ไม่มีต้นทุนประสิทธิภาพเพิ่มในทางปฏิบัติ เพราะถูกเรียกเฉพาะตอนเปิดแผง
Monarch Ascension หรือ audit เท่านั้น (ไม่ใช่ทุกครั้งที่อ่าน store แบบ `checkTitles`)
· `baSmAllGold` ยังใช้ด่านถูกแบบเหมาได้เต็มที่เพราะมันตอบแค่ boolean "ครบหรือยัง"
ซึ่งเป็นคำถามเดียวที่พีชคณิตข้างบนพิสูจน์ไว้ให้

## 5) เลขบัญชี (grind math) ภายใต้การเล่นไร้ที่ติ

ถ้าไม่พลาดสักข้อเลย (`wrong = 0` ตลอด) netScore ของแต่ละคำ = `seen` ตรง ๆ
ต้องตอบถูกคำนั้นซ้ำอย่างน้อย **30 ครั้ง** ถึงจะแตะ Tier 4 (ผ่าน gatekeeper + speed
streak แยกต่างหาก) ดังนั้นขั้นต่ำภายใต้การเล่นไร้ที่ติแบบเพอร์เฟกต์:

```
329 คำ × 30 ครั้ง/คำ = 9,870 ครั้งที่ตอบถูกขั้นต่ำ (theoretical floor)
```

ในทางปฏิบัติสูงกว่านี้มาก เพราะ:
- Speed Streak (≥3 ครั้งติดกันภายใน 2.5 วิ) ต้องคง "ความไว" ไว้ในช่วงท้าย ซึ่งไม่ได้
  การันตีจากแค่ตอบถูกอย่างเดียว — ผู้เล่นบางคนตอบถูกแต่ช้า ต้องฝึกซ้ำเพิ่มถึงจะไวพอ
- คำที่ตอบผิดแม้แต่ครั้งเดียวเสีย 2 หน่วยทันที (`netScore - 2`) ต้องตอบถูกชดเชย 2 ครั้ง
  ถึงจะกลับมาที่จุดเดิม — ยิ่งพลาดบ่อยยิ่งต้องตอบมากกว่าฐาน 9,870 มาก
- Gatekeeper บังคับให้ต้องไต่ถึงชั้น 12 ก่อน (หรือเข้าเหวลึก) คำที่ทำ Tier 3/4 ได้จริง
  จึงกระจุกอยู่ในช่วงท้ายเกม ไม่สามารถปั๊มจากชั้นต้น ๆ ได้เลย

ตัวเลข 9,870 จึงเป็น **พื้นทางทฤษฎีที่ไปไม่ถึงในทางปฏิบัติ** (theoretical lower bound
ไม่ใช่ตัวเลขที่นักเรียนคนไหนจะทำได้จริงเป๊ะ) — สอดคล้องกับปรัชญาการออกแบบเลเวลของ v7.6
ที่เขียนไว้ว่า "Lv99 เป็นเป้าหมายเชิงสัญลักษณ์ ไม่ใช่เป้าที่นักเรียนคนไหนจะไปถึง"
👑 ผู้หยั่งรู้รากอักขระ จึงเป็นฉายาระดับตำนานของทั้งเกมโดยธรรมชาติของตัวเลขเอง
โดยไม่ต้องตั้งเพดานพิเศษเพิ่ม

## 6) สิ่งที่แพตช์นี้ไม่ได้ทำ

**ไม่เพิ่มฟิลด์ในบัญชีเลย** — `fastStreak` piggyback อยู่ใน `wordStats[id]` ก้อนเดิม
ของ v4.4 ซึ่งถูกเซฟผ่าน `saveProgress()`'s `a.wordStats = G.wordStats;` (reference
assignment) อยู่แล้ว ไม่ต้องเขียนโค้ดเซฟ/โหลดเพิ่มสักบรรทัด · ไม่มี `baSmEnsure()`
ไม่มีตรารุ่นระดับบัญชี และไม่ได้ห่อ `migrateAccount` — บัญชีเก่าที่ไม่มี `fastStreak`
เลยจะได้ `0` โดยอัตโนมัติผ่าน `(w.fastStreak || 0)` ทุกจุดที่อ่าน
· **ไม่เพิ่ม toast · ไม่เพิ่ม gate · ไม่เพิ่มปุ่มสักใบ · ไม่แทรกคอลัมน์ในตารางไหน
· ไม่เพิ่มภาพหรือเสียงสักไบต์** (ยืม `BA_SH_ROAR` ของ v6.6 มาใช้ตอนประกาศฉายา)
· **ไม่ได้ตั้งชื่อคลาส CSS ใหม่ที่ชนกับของเดิม** (`grep -c 'ba-sm-grand\|BA_SM_\|baSm'`
ได้ 0 ก่อนตั้งชื่อ)

## 7) งบไบต์ (Byte Budget)

| | ก่อนแพตช์ v9.6 | หลังแพตช์ v9.6 |
|---|---|---|
| ต้นฉบับ | 3,752,046 ไบต์ | 3,769,306 ไบต์ |
| ไฟล์แจก (distributed) | 1,992,930 ไบต์ | 1,996,815 ไบต์ |
| เหลือถึงเพดาน 2,000,000 | 7,070 ไบต์ | **3,185 ไบต์** |

**ไม่มีภาพหรือเสียงเพิ่มสักไบต์** — ทั้งแพตช์เป็นตรรกะ + CSS ล้วน (สอง `filter:drop-shadow`
บนคลาสเดียว) แพตช์นี้ใช้ที่ว่างไปทั้งหมด ~3,885 ไบต์ · **ที่ว่างเหลือน้อยมาก
แพตช์ถัดไปแทบแน่นอนว่าต้องคืนที่ก่อน และคืนที่ "ที่ภาพ" เท่านั้น** ตามกติกาเดิมของ repo
(ดูตารางคืนที่ในหัวข้อ "ที่มาของที่ว่าง" ของ `CLAUDE.md` — ตัวเลือกที่เหลืออยู่คือย่อภาพ
การ์ด 8 ใบจาก 224px ลงไปอีก หรือย่อสไปรต์อสูรลงจาก 144×95)

## 8) ทดสอบ

`test_monarch.js` (85 เคส · 8 บล็อก) เป็นเจ้าของพื้นที่ทดสอบ — แก้ `fill()` helper
2 จุดให้ตรงกับความหมายใหม่ของหลอดผนึกดวงที่ 1:

1. `w.codex` ตั้งทุกคำใน `wordStats` เป็น `{seen:30, wrong:0, fastStreak:3}` (ผ่าน Tier 4
   ทุกคำ) แล้วดัน `G.correct` ให้ผ่านด่านถูกของ `baSmAllGold` (`>= 329×30`)
2. **บั๊คที่พบระหว่างแก้**: `w.acc` (branch เดิม `G.correct=200; G.wrong=0;`) รันทีหลัง
   `w.codex` ในฟังก์ชันเดียวกัน ทำให้ค่า 9,870 ที่ `w.codex` เพิ่งตั้งถูกเขียนทับกลับเป็น
   200 เมื่อทั้งสองแฟล็กถูกขอพร้อมกัน (`fill(page,{codex:true,...,acc:true,...})` — ใช้ใน
   บล็อก 4/5/6/8) — แก้ด้วย `Math.max(G.correct||0, 200)` แทนการเขียนทับตรง ๆ
3. **บั๊คที่สอง**: `w.codex` เพียงอย่างเดียว (ไม่มี `w.acc`) ทำให้ `G.correct=9,870`
   ขณะที่ `G.wrong` ยังเป็น 0 (ค่าเริ่มต้นของบัญชีใหม่) → ความแม่นยำกลายเป็น 100%
   โดยไม่ตั้งใจ → ผนึก "acc" (เป้าหมาย 95%) ติดไปด้วยทั้งที่ไม่ได้ขอ ทำลาย isolation
   ของเทสต์ที่ต้องการเห็นผนึกดวงเดียว — แก้โดยดัน `G.wrong = 1000` ในกรณีที่ `w.acc`
   ไม่ได้ถูกขอมาด้วย (เมื่อขอมาด้วย branch ของ `w.acc` ที่รันทีหลังจะรีเซ็ตกลับเป็น 0
   ทับอยู่ดี ให้ผลตามที่ตั้งใจ)

ผลหลังแก้: **85 ผ่าน 0 ตก** (จากเดิม 60 ผ่าน 25 ตก ก่อนอัปเดต fixture)

รันควบกับชุดที่แตะจุดห่อเดียวกัน (`hunterAtk` · `renderStats` · `recordWord` ·
`announceTitles` · `baV91Activate`) — ทุกชุดผ่านครบไม่มี regression:
`test_codex_cores` 93/0 · `verify_arena` 144/0 · `verify_monsters` 80/0 ·
`test_polarized_class` 140/0 · `test_skill_dispatch` 94/0 · `test_skill_anim` 173/0 ·
`test_boss_resilience` 76/0 · `test_lv_scaling` 81/0 · `test_gm_admin` 142/0 ·
`test_wave_loop` 147/0 · `test_ultimate_abyss` 61/0 · `test_dedicated_abyss` 66/0 ·
`test_high_tier_boss` 85/0 · `test_telegraph_grace` 75/0 · `test_combat_v9` 76/0 ·
`test_contact_dash` 29/0 · `test_sprite_fit` 48/0
