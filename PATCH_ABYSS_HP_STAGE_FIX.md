# Patch · ABYSS HP & STAGE DESYNC FIX

อยู่ในเนมสเปซ `ba` ของชั้น v6.0 เดิม **ไม่ได้ตั้งพรีฟิกซ์ใหม่** (กติกาเดียวกับ v6.1–v8.3)
ก้อนเริ่มที่คอมเมนต์ `Patch · ABYSS HP & STAGE DESYNC FIX` ซึ่งวางไว้ **ท้ายสุดของไฟล์
ต่อจากชั้น v8.3 (WEAKNESS PURIFY) และก่อน `baInstall()`** จึงห่อทับทุกชั้นได้
· **ไม่แก้โค้ดของ v4.0-v8.3 เลยแม้แต่บรรทัดเดียว** ที่เพิ่มคือ wrapper **3 ตัว**
(`monsterHpFor` · `renderMonster` · `baBattleAudit`)

## อาการที่แพตช์นี้แก้

ชั้น v8.3 · DECOUPLED DEDICATED ABYSS ENGINE แยก **"ทัพเงาตัวไหนโผล่"** ออกจากชั้นหอคอย
เรียบร้อยแล้ว (`baOdIdx()` ไล่ 1→25 อิสระผ่าน `account.ax.idx` ไม่ผูกกับ `G.floor`)
แต่ลืมแยกอีกสองเรื่องที่ยังผูกกับ `G.floor` ค้างอยู่ — **HP ของทัพเงาตัวนั้น** และ
**ป้ายชื่อที่ค้างอยู่บนจอ** (`.g-monster-tag` ในสนามรบ ซึ่งต่างจากแบนเนอร์ประกาศชั่วคราว
`baOdAnnounce()` ที่ v8.3 sync ให้ถูกต้องอยู่แล้ว)

ผลคือ: กดเข้าโหมดเหวลึกเฉพาะกิจตอนยืนอยู่ชั้น 3 ของหอคอย แล้วไล่ทัพเงาไปจนถึงตัวที่ 20
(ซึ่งควรจะแรงมากตามลำดับ) — HP ที่คำนวณได้ยังอิงสูตรของ **ชั้น 3** อยู่ ทั้งที่ดัชนี
ในตัวคือ 20 · และป้ายชื่อค้างจอยังบอก "ชั้น 3" ทั้งที่กำลังสู้กับทัพเงาลำดับ 20 อยู่จริง

## จุดที่แก้ — สองฟังก์ชัน

### 1) `monsterHpFor(floor, boss)` — HP Decoupling

```js
if (typeof monsterHpFor === 'function') {
  const _ahsHpFor = monsterHpFor;
  monsterHpFor = function (floor, boss) {
    try {
      if (baOdMode()) {
        const idx = baOdIdx();
        const foe = baOdFoeAt(idx);
        const synthFloor = Math.max(1, Math.min(FLOOR_MAX, idx));
        const synthBoss = !!(foe && foe.tier === 'mythic');
        return _ahsHpFor.call(this, synthFloor, synthBoss);
      }
    } catch (e) { baAhsErr(e, 'hp'); }
    return _ahsHpFor.apply(this, arguments);
  };
}
```

**เฉพาะตอน `baOdMode()` เป็นจริง** (กำลังอยู่ในรัน "🕳️ ABYSS" เฉพาะกิจของ v8.3) เท่านั้น —
ทางเดินปกติของหอคอยและการบุกรุก 15% ของ v6.6 ไม่ถูกแตะเลย (`_ahsHpFor.apply(this,
arguments)` ส่งอาร์กิวเมนต์เดิมทะลุไปตรง ๆ)

- **แปลงดัชนี 1-25 เป็น "ชั้นเทียม" ที่หนีบอยู่ในช่วง `[1, FLOOR_MAX]`** เพื่อป้อนให้สูตร
  `hitsForFloor`/`monsterHpFor` ของ v4.0 ที่คาดหวังอาร์กิวเมนต์ในช่วงนั้นอยู่แล้ว — ดัชนี
  ยิ่งสูง (ทัพเงายิ่งลึก) ยิ่งได้ชั้นเทียมสูง = HP สเกลขึ้นตามลำดับความลึกจริง ไม่ใช่ตามชั้น
  หอคอยที่ผู้เล่นบังเอิญยืนอยู่ตอนกดเข้าโหมด
- **`synthBoss` มาจาก `foe.tier === 'mythic'`** ไม่ใช่จากชั้นเทียม — ทัพเงาลำดับ 21-25
  (มังกร Kamish ตาม `BA_FOES_ABYSS`) ถูกจัดเป็น boss-tier เสมอไม่ว่าชั้นเทียมที่คำนวณได้
  จะตรงกับ `isBossFloor()` ของหอคอยจริงหรือไม่ — สอดคล้องกับที่ v6.6/v8.3 ให้ tier 21-25
  เป็น "mythic" ไว้แต่ต้น
- **`try/catch` ครอบเฉพาะโค้ดใหม่** ถ้า `baOdMode`/`baOdIdx`/`baOdFoeAt` โยน error กลางทาง
  จะตกกลับไปเรียกของเดิมด้วยอาร์กิวเมนต์ดิบทันที (`baAhsErr` เขียนลง Error Log ของ v4.3)
  — เกมไม่มีวันค้างเพราะแพตช์นี้พัง

### 2) `renderMonster()` — Stage Display Sync

```js
if (typeof renderMonster === 'function') {
  const _ahsRenderMon = renderMonster;
  renderMonster = function () {
    const r = _ahsRenderMon.apply(this, arguments);
    try {
      if (baOdMode()) {
        const idx = baOdIdx();
        const foe = baOdFoeAt(idx);
        const tag = document.querySelector('.g-monster-tag');
        if (tag) {
          tag.textContent = (foe && foe.tier === 'mythic')
            ? '[ระบบ] 👑 จอมมารเหวลึกตัวที่ ' + idx + ' / ' + BA_OD_MAX + ' ปรากฏ!'
            : '[ระบบ] 🌌 ทัพเงาเหวลึก ตัวที่ ' + idx + ' / ' + BA_OD_MAX + ' ปรากฏ!';
        }
      }
    } catch (e) { baAhsErr(e, 'tag'); }
    return r;
  };
}
```

**ทำงาน "หลัง" ให้ของเดิมวาดจบเสมอ** แล้วเขียนทับข้อความบน `.g-monster-tag` เฉพาะตอน
`baOdMode()` — โหนดนี้ถูก v6.0 · 2D BATTLE ARENA ย้ายเข้าไปอยู่ในสนามรบแล้ว (เดิมเป็นของ
v4.0) การอ่าน `document.querySelector` สด ๆ ทุกครั้งจึงตามโหนดไปได้เองไม่ว่าจะอยู่ที่ไหน
ในโครง DOM ปัจจุบัน — **ไม่ผูกกับ id ที่อาจเปลี่ยนตำแหน่งในอนาคต**

รูปแบบข้อความ (`'[ระบบ] 🌌 ทัพเงาเหวลึก ตัวที่ N / 25 ปรากฏ!'`) ล้อกับที่ `baOdAnnounce()`
เดิมของ v8.3 ใช้กับแบนเนอร์ชั่วคราวอยู่แล้ว — เพิ่มแยกกรณี "จอมมารเหวลึก 👑" สำหรับทัพเงา
ระดับ mythic (Kamish ลำดับ 21-25) เพื่อให้ผู้เล่นแยกออกว่ากำลังเจอมังกรระดับสูงสุดอยู่

## Save State — ไม่ต้องเขียนโค้ดเพิ่มเลยสักบรรทัด

สเปกข้อ 3 ("Rely strictly on `account.ax.idx`... Preserve normal `G.floor` on exit")
**ได้รับการรับประกันไว้ครบแล้วโดยชั้น v8.3 เอง** — `baOdSave()` กับ wrapper บน
`saveStore`/`startGame`/`exitGame`/`migrateAccount` ที่มีอยู่ก่อนแพตช์นี้:

- `account.ax.floor` ถูกตรึงไว้ที่ `0` เสมอ (v8.3 ตัดการผูกกับชั้นหอคอยไปแล้วทั้งหมด)
- `G.floor` ไม่เคยถูกแตะจากทางฝั่งเหวลึกเฉพาะกิจเลยสักจุด
- ดัชนี 1-25 อ่าน/เขียนผ่าน `baOdIdx()` ที่ผูกกับ `account.ax.idx` ตัวเดียว

แพตช์นี้จึง **ไม่เพิ่มฟิลด์ในบัญชีเลย ไม่มี `baAhsEnsure()` ไม่มีตรารุ่นระดับบัญชี และไม่ได้
ห่อ `migrateAccount`** — `BA_AHS_N` เป็นตัวนับของรอบการเล่นล้วน (ไม่ต้องเซฟ)

## ทำไมไม่ต้องแก้ `hitsForFloor` แยก

`monsterHpFor` ของ v4.0 เป็น **ทางผ่านเดียว** ที่ทั้งเกมใช้คำนวณ HP มอนสเตอร์ (v4.6 · v6.4
· v6.7 ก็ห่อชั้นนี้เป็นจุดเดียวกันมาตลอด) และมันเรียก `hitsForFloor` เข้าไปข้างในตัวเองอยู่แล้ว
การห่อ `monsterHpFor` แล้วป้อน `synthFloor` เข้าไปแทนที่ `floor` จริง จึงครอบ `hitsForFloor`
ไปในตัวโดยไม่ต้องแตะมันแยกต่างหาก — สอดคล้องกับกฎ "ห่อสูตร ไม่ไล่แก้ตามจุดเรียก" ของ
v4.4/v4.6/v6.3/v7.6/v7.9 ที่ทั้ง repo ใช้มาตลอด

## ⚠️ ทำไมชั้นนี้ไม่ต้องแตะระบบบุกรุก 15% ของ v6.6 (`BA_INC_F`)

`baOdMode()` อ่าน `abyssOn(g)` ซึ่งคือ `g.ab.abyss` — สวิตช์ "โหมดเหวลึกเฉพาะกิจ" ของ v4.6/v8.3
**คนละระบบกับ** `BA_INC_F`/`BA_INC_AT` ที่เป็นลูกเต๋าบุกรุก 15% ต่อชั้นในหอคอยปกติ (v6.6)
ทั้งสองระบบแยกกันโดยสมบูรณ์อยู่แล้ว — แพตช์นี้เกาะที่ `baOdMode()` เท่านั้น จึงไม่มีทาง
ไปรบกวนกลไกบุกรุกของ v6.6 เลยไม่ว่าทางใด

## สิ่งที่แพตช์นี้ไม่ได้ทำ

**ไม่เพิ่มฟิลด์ในบัญชี · ไม่เพิ่ม toast · ไม่เพิ่ม gate · ไม่เพิ่ม/ลดปุ่มสักใบ · ไม่แทรก
คอลัมน์ในตารางไหน · ไม่เพิ่มภาพหรือเสียงสักไบต์ · ไม่แตะ `Math.random` สักครั้ง**
(กับดักข้อ 32) · ไม่มี `baAhsEnsure()` ไม่มีตรารุ่นระดับบัญชี และไม่ได้ห่อ `migrateAccount`
· `BA_AHS_N` เป็นสถานะของรอบการเล่นทั้งหมด · **ชื่อทั้งชุดยังไม่มีใครใช้ก่อนแพตช์นี้**
(กับดักข้อ 36 — `grep -c 'BA_AHS_\|baAhs'` ได้ 0 ก่อนตั้งชื่อ)

**สวิตช์ `localStorage['yao_arena'] = 'off'` ไม่ได้ปิดแพตช์นี้** เพราะไม่มี wrapper ตัวไหน
ผูกกับกรอบสนามรบเลย — จงใจ เพราะ HP กับป้ายชื่อของเหวลึกเป็นกติกาของเกม ไม่ใช่ของประดับ
(ท่าเดียวกับ "ห้ามเฉลย" ของ v6.3 · ราคา SCAN ของ v6.5)

## ทางเข้าสาธารณะสำหรับชุดเทสต์

`baBattleAudit().abyssHpStageFix` — `ver` (`'1.0'`) · `on` (`baOdMode()` ตอนนี้) · `idx`
(`baOdIdx()`) · `max` (`BA_OD_MAX`) · `n` (**สำเนา** ของตัวนับ `hp`/`tag` — คืน reference
ไม่ได้ เพราะชุดเทสต์จับค่าก่อน/หลังไว้เทียบกัน แล้วจะได้ส่วนต่าง 0 ทุกครั้ง — บทเรียนของ v8.3)

## ผลการทดสอบ

**ขนาดไฟล์** — ต้นฉบับ 3,925,373 ไบต์ → ไฟล์แจก **1,982,050 ไบต์** (−49.5%)
เหลือถึงเพดาน 2,000,000 อีก **17,950 ไบต์** — ผ่านเกณฑ์ `< 2MB` ตามสเปก

**สคำสั่งตรวจตามสเปกเป๊ะ**

```
node build_minify.js --check && node test_cloud_sync.js && node test_monarch.js && node test_skill_dispatch.js
```

| ชุด | ผล | หมายเหตุ |
|---|---|---|
| `build_minify.js --check` | ผ่าน — 1,982,050 / 2,000,000 ไบต์ | HARD mode (auto-fallback ปกติของ repo) |
| `test_cloud_sync.js` | **104/0** | ไม่แตะเลย |
| `test_monarch.js` | **85/0** | ไม่แตะเลย |
| `test_skill_dispatch.js` | **54 ผ่าน / 40 ตก** | **ของเดิมอยู่แล้ว ไม่ใช่ของแพตช์นี้** — ยืนยันด้วย `git stash` แล้ว build+รันชุดเดิมกับซอร์สที่ยังไม่แก้ ได้ผลเท่ากันเป๊ะ (54/40) ก่อนแตะอะไร |

**ชุดเสริมที่รันเพิ่มเพื่อความมั่นใจ** (ไม่ได้บังคับในสเปก แต่เกี่ยวข้องโดยตรงกับพื้นที่ที่แก้)

| ชุด | ผล |
|---|---|
| `test_dedicated_abyss.js` | **66/0** — ชุดของชั้น v8.3 เอง ที่ไฟล์นี้ต่อยอด |
| `verify_arena.js` | **144/0** — CLS = 0 ยืนยันแล้ว ไม่มีอะไรขยับในเลย์เอาต์สนามรบ |
| `verify_monsters.js` | **80/0** × 6 รอบติดต่อกัน (ดูหัวข้อถัดไป) |

### บันทึกกับดักที่เจอระหว่างทาง — `verify_monsters.js` เคย "ตก" หนึ่งครั้งระหว่างพัฒนา

ระหว่างตรวจงาน รันชุดนี้ครั้งหนึ่งแล้วเจอ crash ที่
`BA_MON[BA_FOE_ID].map(x => x.k)` เป็น `undefined` ที่บล็อกทดสอบท่าฟาดของชั้น 20
ตอนแรกสงสัยว่าเป็น regression จากแพตช์นี้ — ไล่ตรวจตามลำดับ:

1. **`git stash` แล้ว build baseline (ไม่มีแพตช์นี้เลย) มารันชุดเดิมซ้ำ** → ได้ 80/0 สะอาด
   รวมถึงบล็อกเดียวกับที่ crash ด้วยแพตช์ติดตั้งอยู่ — ชี้ว่าน่าจะเป็น regression จริง
2. **แต่รันซ้ำไฟล์ที่มีแพตช์ติดตั้งอยู่ (ไม่แก้อะไรเพิ่ม) อีก 3 ครั้งติด** → ได้ 80/0 สะอาดทุกรอบ
   — ถ้าเป็น regression จริง ต้องพังซ้ำทุกครั้งด้วยโค้ดเดียวกัน แต่กลับไม่พัง
3. **ไล่ดูต้นเหตุที่ `baIncSync()`** (บรรทัด 24081 ของต้นฉบับ) พบว่า:
   `if (f !== BA_INC_F) baIncRoll(f);` — ทอยลูกเต๋าบุกรุก **ทุกครั้งที่ `G.floor` ต่างจาก
   `BA_INC_F`** โดยไม่เกี่ยวกับ `abyssOn()`/`baOdMode()` เลย — คนละระบบกับที่แพตช์นี้แตะ
4. **`verify_monsters.js` เองมีบล็อกทดสอบท้าย ๆ (stable-id check · Math.random-ban check ·
   ท่าฟาด) ที่เปลี่ยน `G.floor` (ไป 3 แล้วไป 20) โดยไม่ปั๊ม `BA_INC_F`/`BA_INC_AT`/`BA_INC_M`
   ก่อนเหมือนที่บล็อกหลักของมันเองทำ** (ซึ่งเป็นกติกาที่ CLAUDE.md ของ repo เขียนไว้ชัดเจนอยู่แล้ว
   ว่า "ชุดที่พาไปยืนชั้นใดชั้นหนึ่งต้องปิดระบบบุกรุกของ v6.6 ก่อนวัดเสมอ") — ถ้าลูกเต๋าที่ทอยตรง
   จุดนั้นสุ่มได้ทัพเงาบุกรุก (`BA_FOE_ID` กลายเป็นไอดีจาก `BA_FOES_ABYSS` เช่น `'s12'`)
   `BA_MON` (ทะเบียนอสูรประจำหอคอยเท่านั้น) จะไม่มีคีย์นั้น แล้ว `.map()` จะพังทันที
5. **`baRand()` ของ v6.1 เป็นตัวสุ่มที่ seed จาก `Date.now()` ครั้งเดียวตอนโหลดหน้า** (ไม่ใช่
   `Math.random` — จงใจตามกับดักข้อ 32 เพื่อไม่ไปกินคิวของชุดเทสต์อื่น) ไม่ได้ reseed ต่อการ
   เรียกแต่ละครั้ง ผลลัพธ์ ณ จุดเรียกหนึ่ง ๆ จึงขึ้นกับเวลาที่กระบวนการ Node/Chromium เริ่มทำงาน
   ในรอบนั้น — **ต่างกันได้ทุกครั้งที่รันใหม่ ไม่ว่าจะมีแพตช์นี้อยู่หรือไม่**

**สรุป: นี่คือความไม่เที่ยงตรง (flakiness) ที่มีอยู่ก่อนแล้วในตัวชุดเทสต์เอง** (บล็อกทดสอบ
ท่าฟาดของ `verify_monsters.js` ไม่ปั๊ม `BA_INC_F` ก่อนกระโดดชั้นเหมือนบล็อกหลักของมันทำ)
ไม่ใช่ข้อบกพร่องของแพตช์นี้ — แพตช์นี้ไม่เคยแตะ `BA_INC_F`/`baIncSync`/`baIncRoll`/
`BA_FOES_ABYSS` เลยสักจุด และ `baOdMode()` (เงื่อนไขเดียวที่ทำให้ wrapper ของแพตช์นี้ทำงาน)
ไม่เคยเป็นจริงในสถานการณ์ที่ `verify_monsters.js` ทดสอบเลย (ไม่มีจุดไหนในชุดนั้นเปิด
`g.ab.abyss = true`) ยืนยันด้วยการรันซ้ำ 6 ครั้งติดต่อกันบนไฟล์ที่มีแพตช์อยู่ ได้ 80/0
สะอาดทุกครั้ง — **ไม่ได้แก้ไขไฟล์เทสต์ในแพตช์นี้** เพราะอยู่นอกขอบเขตของงานที่สั่งมา
(สเปกระบุไฟล์เป้าหมายเดียวคือ `.src.html` เท่านั้น) แต่บันทึกไว้ตรงนี้ตามธรรมเนียมของ repo
ที่เก็บกับดักทุกอันไว้เป็นบทเรียน — ถ้าจะแก้ถาวรให้เติม
`BA_INC_F = <ชั้นนั้น>; BA_INC_AT = -1; BA_INC_M = null;` ก่อนทุกจุดที่ `verify_monsters.js`
เปลี่ยน `G.floor` แล้วเรียก `nextMonster()` ในบล็อกที่ไม่ใช่ลูปหลัก
