# Patch v9.14 · ABYSS TOGGLE RESYNC & DEDICATED-ABYSS CLOUD MERGE

รายงานการแก้ตามคำสั่ง "FIX ABYSS PROGRESSION & FLOOR DECOUPLING (ONE-SHOT)"

## สรุปสั้น ๆ

คำสั่งเดิมอ้างชื่อ `G.abyssFloor` / `winFloor` / `wbPush`/`wbRestore` (ในฐานะระบบชั้นเหวลึก)
ซึ่ง **ไม่ตรงกับโค้ดจริงในไฟล์** — ระบบ "แยกชั้นเหวลึกออกจากชั้นหอคอย" มีอยู่แล้วสมบูรณ์ตั้งแต่
**Patch v8.3 · DECOUPLED DEDICATED ABYSS ENGINE** (`account.ax.idx` / `baOdIdx()` / `baOdMode()`)
และผ่านชุดเทสต์ของตัวเองครบอยู่แล้ว การสร้าง `G.abyssFloor` ขึ้นมาใหม่จะเป็นการสร้าง
**สารบัญข้อมูลคู่ขนานที่ต้องซิงก์กันเอง** ซึ่งเป็นกับดักที่ CLAUDE.md ของ repo นี้เตือนไว้ตรง ๆ
(กับดักข้อ 35 — "ห้ามสร้างสารบัญข้อมูลชุดที่สอง")

สิ่งที่ทำจริงคือ **ตรวจสอบ → ยืนยันว่าของเดิมถูกต้องแล้ว → แก้เฉพาะจุดที่พังจริง → ต่อยอด
ระบบซิงก์ข้ามเครื่องที่มีอยู่แล้ว** ตามกฎเหล็กของ repo (ห่อฟังก์ชันเดิมเสมอ ห้ามแก้โค้ดชั้นก่อนหน้า)

## 1) สิ่งที่ตรวจแล้วว่า "ถูกต้องอยู่แล้ว ไม่ต้องแก้"

**Decouple Floors** — `account.ax.idx` (1-25) แยกขาดจาก `G.floor` (1-20) มาตั้งแต่ v8.3
`baOdIdx()`/`baOdFoeAt()`/`baOdMode()` ทำหน้าที่นี้ครบ และ **`account.ax.floor` ถูก
v8.3 ตรึงไว้ที่ 0 เสมอโดยตั้งใจ** (ดูหัวข้อ Patch v8.3 ใน CLAUDE.md — "ความก้าวหน้าย้ายไปอยู่ที่
ลำดับทัพเงา แทน 'ชั้นที่ตกรอบ' ตามสเปก") — ไม่มีอะไรให้แก้ตรงนี้

**Victory Loop** — `baOdWin()` ของ v8.3 เดินดัชนี `min(25, idx+1)` แล้วยังอยู่ในเหวลึกต่อ
ไม่เด้งกลับหอคอยปกติจนกว่าจะปิดสวิตช์หรือกุญแจหมด — ตรงตามที่คำสั่งขอ ("stay in Abyss mode,
spawn next boss immediately")

## 2) บั๊คจริงที่พบและแก้ — Toggle Handler

**อาการ:** กดสวิตช์เข้า/ออกโหมดเหวลึกแล้วป้ายประกาศ (banner) ไม่ตรงกับตัวที่ยืนอยู่จริง
บางครั้งค้างเป็นตัวเก่า

**ต้นเหตุ:** `baAxRunStart()`/`baAxRunEnd()` (v8.1) เรียก `nextMonster()` **แบบมีเงื่อนไข**
เท่านั้น (เฉพาะตอน `jump && ...` ตอนเปิดสวิตช์) และ **ไม่เรียกเลย** ตอนปิดสวิตช์ ในขณะที่
ป้ายประกาศของทั้ง v8.1 และ v8.3 ยิงแบบไม่มีเงื่อนไข — ผลคือป้ายกับตัวละครที่ยืนอยู่จริงไม่ตรงกัน

**การแก้ (wrap ชั้นใหม่ ไม่แก้โค้ดเดิม):** ห่อ `abToggleAbyss` เป็นชั้นนอกสุด (ชั้นที่ 3
ต่อจาก v8.1 กับ v8.3) ตรวจว่าสถานะเปลี่ยนจริงหรือไม่ (`baOdMode()` ก่อน/หลัง) แล้วบังคับ
`nextMonster()` ทันทีเมื่อปลอดภัย (`!g.locked && !g.warpOpen`) — กลไกที่มีอยู่แล้ว
(`renderMonster()` → `baIncSync()` → `baShAnnounce()`) จะซิงก์ป้าย/ภาพ/ค่าพลังให้เองครบ
ทั้งสองทิศทางโดยไม่ต้องแก้ downstream logic เลยสักบรรทัด

```js
if (typeof abToggleAbyss === 'function') {
  const _v914Toggle = abToggleAbyss;
  abToggleAbyss = function () {
    const was = baOdMode();
    const r = _v914Toggle.apply(this, arguments);
    const now = baOdMode();
    if (was !== now && !g.locked && !g.warpOpen) nextMonster();
    return r;
  };
}
```

## 3) ช่องว่างจริงที่พบและปิด — Storage/Sync

**อาการ:** `account.ax` (idx/soul/runs/clears/best) ไม่ได้เดินทางไปกับระบบซิงก์ข้ามเครื่อง
ของ v9.13 (`wbPayload`/`wbRestore`) — เล่นเครื่องหนึ่งแล้วสลับไปอีกเครื่องจะไม่เห็นความคืบหน้า
เหวลึกของตัวเอง

**การแก้:** ห่อ `wbPayload` ให้แนบ `p.ax` เข้าไปในก้อนที่ส่งขึ้นคลาวด์ และห่อ `wbRestore`
ให้ดึงเอกสารเดียวกัน (ผ่าน `fbFetch` ท่าเดียวกับที่ v9.13 ใช้เอง) แล้ว merge เข้า `baG().ax`
ด้วยกติกาที่เหมาะกับแต่ละฟิลด์:

| ฟิลด์ | กติกา merge | เหตุผล |
|---|---|---|
| `idx` | `Math.max` | ตัวนับที่มีแต่ขึ้น ไม่มีทางถอยหลัง |
| `soul` | bitwise OR | บิตแมสก์การ์ดวิญญาณ — สะสมได้จากทุกเครื่อง |
| `runs` / `clears` | `Math.max` | สถิติสะสม |
| `best` | `Math.min` (ไม่นับ 0) | สถิติ speedrun — ยิ่งน้อยยิ่งดี |
| `keys` / `day` | **ไม่ merge** | โควตารายวันผูกกับเครื่อง/วันที่ในเครื่องนั้น |
| `floor` | **ไม่ merge** | v8.3 บังคับให้เป็น 0 เสมอตามสถาปัตยกรรมเดิม — merge ค่าอื่นเข้ามาจะขัดกับ invariant นั้น |

Merge สำเร็จแล้วเรียก `baAxSave()` (v8.1) ซึ่งภายในจะให้ v8.3's `saveStore` wrap
ประทับ `idx` กลับให้ครบอัตโนมัติ แล้ว `wbPush(user)` ส่งกลับขึ้นคลาวด์เป็น pushback

## 4) การตรวจสอบ — ทำครบตามที่สั่งและมากกว่า

**ตามคำสั่ง (ทั้งสามคำสั่ง ผ่านครบ):**

```
node build_minify.js --check   → ผ่าน
node test_cloud_sync.js        → PASS 104 · FAIL 0
node test_monarch.js           → ผ่าน 85  ตก 0
```

**เพิ่มเติมเพื่อความมั่นใจ** (ชุดที่แตะฟังก์ชันเดียวกันหรือใกล้เคียง):

| ชุดเทสต์ | ผล |
|---|---|
| `test_dedicated_abyss.js` | 66/0 |
| `test_ultimate_abyss.js` | 61/0 |
| `test_abyss_army.js` | 67/0 |
| `test_wave_loop.js` | 147/0 |
| `verify_arena.js` (CLS=0) | 144/0 |
| `verify_monsters.js` | 80/0 |

**รวมทั้งหมด 8 ชุด · 654 เคส · ตก 0 เคส — ไม่มี regression**

**ขนาดไฟล์แจก:** 1,980,339 ไบต์ (เพดาน 2,000,000) — เหลือที่ว่างอีก **19,661 ไบต์**

## 5) สิ่งที่ตั้งใจไม่ทำ

- **ไม่สร้าง `G.abyssFloor`** — จะเป็นสารบัญคู่ขนานกับ `account.ax.idx` ที่มีอยู่แล้ว (กับดักข้อ 35)
- **ไม่แก้ `winFloor`** — ไม่มีฟังก์ชันชื่อนี้ในไฟล์ ระบบ progression loop ที่ใกล้เคียงที่สุด
  (`baOdWin`) ทำงานถูกต้องอยู่แล้วตามที่ตรวจในข้อ 1
- **ไม่แตะ Free Class Switch/Reset** — ไม่ได้ถูกแตะเลยแม้แต่บรรทัดเดียวตามที่คำสั่งกำชับ
- **ไม่แก้โค้ดของ v8.1/v8.3/v9.13** — ทุกอย่างทำผ่านการห่อ (wrap) ชั้นใหม่ (v9.14) เท่านั้น
  ตามกฎเหล็กของ repo

## ไฟล์ที่แก้

- `src...` *(ไม่มีโฟลเดอร์ src/ จริงใน repo — ต้นฉบับอยู่ที่รากrepo)*
  `/hanzi_hunter_tower_v3_1_intro.src.html` — เพิ่มโค้ดที่บรรทัด ~43,723 (ก่อน `baInstall()` สุดท้าย)
- `/hanzi_hunter_tower_v3_1_intro.html` — regenerate ผ่าน `node build_minify.js` เท่านั้น
  (ไม่ได้แก้มือ)
