# Patch · ALL 7 SYSTEM CORES REBALANCE (Peak Impact Pass)

อยู่ในเนมสเปซ `ba`/`ab` ของชั้น v4.6 (แกนกลางระบบ 3 เส้นเดิม) กับชั้น v7.9
(แกนกลางระบบเพิ่ม 4 เส้น — HP/ATK/SHLD/CRIT) — **ไม่ได้ตั้งพรีฟิกซ์ใหม่ ไม่ได้เพิ่มชั้นใหม่**
แพตช์นี้คือการแก้ตัวเลข "ต่อขั้น" ของค่าคงที่ที่มีอยู่แล้ว 7 ก้อน โดยไม่แตะโครงสร้าง สูตร
หรือจุดห่อฟังก์ชันสักบรรทัด — ตรงตามกติกาเดิมของทั้งสองชั้นที่เขียนไว้ว่า "แก้ตัวเลขที่ค่าคงที่
ก้อนเดียว"

**ไม่มีการเพิ่มชั้นใหม่ ไม่มี wrapper ใหม่ ไม่มี DOM ใหม่ ไม่มีฟิลด์บัญชีใหม่**
ราคาซื้อขั้น (`AB_CORE_COST = [8,14,22,32,46,64]`) และเพดานขั้นสูงสุด
(`AB_CORE_MAX = 6`) **ไม่ถูกแตะเลยตามคำสั่ง** — แพตช์นี้เปลี่ยนแค่ "ผลที่ได้ต่อขั้น"
ไม่ได้เปลี่ยน "ราคาที่ต้องจ่าย"

## ตารางเปรียบเทียบ — เดิม → ใหม่

| # | แกน | ค่าคงที่ | ต่อขั้น (เดิม → ใหม่) | เพดานเต็ม 6 ขั้น (เดิม → ใหม่) |
|---|---|---|---|---|
| 1 | 🔮 แกนมานา | `AB_CORE_MP_PCT` | 5 → **12.5** | 30% → **75%** |
| 2 | ⏱️ แกนเวลา | `AB_CORE_TIME_MS` | 400 → **600** | +2.4 วิ → **+3.6 วิ** |
| 3 | 🪙 แกนโชคลาภ | `AB_CORE_GOLD` | 1 → **25** | +6% → **+150%** |
| 4 | ❤️ แกนชีวิต | `BA_C7_HP[]` | ขั้นละ ~67 (สะสม) → **200/ขั้น** | +400 → **+1200** |
| 5 | 🗡️ แกนพิฆาต | `BA_C7_ATK[]` | ขั้นละ ~6.67 (สะสม) → **16.66/ขั้น** | +40% → **+100%** |
| 6 | 🛡️ แกนเกราะ | `BA_C7_SHLD[]` | ขั้นละ ~4 (สะสม) → **6.66/ขั้น** | −24% → **−40%** |
| 7 | ⚡ แกนสังหาร | `BA_C7_CRIT[]` | ขั้นละ ~3.33 (สะสม) → **10/ขั้น** | +20% → **+60%** |

**สามเส้นแรก (มานา/เวลา/โชคลาภ) เป็นค่าคงที่เดี่ยวคูณด้วย `lv`** ตามสูตรของ v4.6
(`eff: function (lv) { return lv * AB_CORE_MP_PCT; }` เป็นต้น) แก้ตัวคูณตัวเดียวจึงขยับ
ทุกขั้นตามสัดส่วนเดิม — ไม่ต้องแตะสูตร `eff()` เลยสักตัว

**สี่เส้นหลัง (HP/ATK/SHLD/CRIT) เป็นตารางค่า `[ขั้น0..ขั้น6]`** ของ v7.9
(`BA_C7_HP` / `BA_C7_ATK` / `BA_C7_SHLD` / `BA_C7_CRIT`) — แก้ค่าทั้งตารางให้ไต่แบบเชิงเส้น
เท่ากันทุกขั้นและจบที่เพดานใหม่พอดี (`200*6=1200` · `16.66*6≈100` · `6.66*6≈40` · `10*6=60`)
**ยังใช้ท่าเดียวกับเดิม** (`baC7At(TBL, lv)` = `TBL[hnieb(lv, 0, AB_CORE_MAX)]`) ไม่ได้เปลี่ยน
ตัวอ่านตารางเลยสักบรรทัด

## ที่มาของแต่ละแกน — ยังห่อจุดเดิมทุกจุด ไม่ได้แก้สูตร

| แกน | ห่อที่ไหน (ของเดิม ไม่แตะ) | หมายเหตุ |
|---|---|---|
| 🔮 มานา | wrapper ของ `useSkill` (v4.6) | วัดจาก "MP ที่ถูกหักจริง" ก่อน/หลัง แล้วคืนเป็นเปอร์เซ็นต์ — เคารพเพดาน MP สูงสุดเสมอ (`Math.min(headroom, refund)`) |
| ⏱️ เวลา | wrapper ของ `questionMs` (v4.6) | บวกก่อนตัวคูณโหมดเหวลึก (`AB_ABYSS_MUL`) เพื่อให้เหวลึกยังยากตามสัดส่วนเดิม |
| 🪙 โชคลาภ | wrapper ของ `goldMul` (v4.6) | **บวกเข้าตัวคูณตรง ๆ ไม่คูณทับ** — กันไม่ให้ไปขยายโบนัสทองก้อนอื่น (LUK · คลังอักขระ · แกนกลางระบบ) ทับซ้อนกัน |
| ❤️ ชีวิต | wrapper ของ `maxHpFor` (v7.9) | บวกค่าคงที่ตรง ๆ (ไม่ใช่ตัวคูณ) |
| 🗡️ พิฆาต | wrapper ของ `hunterAtk` (v7.9) — **กดด้วย `BA_LIVE`** | HP มอนสเตอร์คิดจาก `hunterAtk()` ตัวเดียวกัน (`monsterHpFor`) จึงต้องกดด้วย `BA_LIVE` (จริงเฉพาะระหว่างตัดสินข้อ) กันไม่ให้เลือดมอนสเตอร์พองตามสัดส่วนเดียวกัน — ยืนยันแล้วว่า `monsterHpFor(5,1)` เท่ากันทั้งก่อน/หลังแพตช์เมื่อวัดนอก `BA_LIVE` |
| 🛡️ เกราะ | wrapper ของ `wrongDamage` + `baHurtHero` (v7.9) | คูณดาเมจที่ได้รับด้วย `(1 - pct/100)` |
| ⚡ สังหาร | wrapper ของ `baStrike` (v6.0) → `baC7Bonus(ex)` (v7.9) | เฉพาะตอนคริต/ท่าไม้ตาย บวกดาเมจส่วนเกิน **หลัง** เพดานดาเมจต่อข้อของ v6.7 ทำงานแล้ว และหนีบไม่ให้ฆ่ามอนสเตอร์จากโบนัสนี้ (เหลืออย่างน้อย 1 HP เสมอ ตามกับดักข้อ 19 — ห้ามฆ่าโดยไม่มีใครสั่งคิวเทิร์นถัดไป) |

**ไม่มีการแตะ `useSkill` / `questionMs` / `goldMul` / `maxHpFor` / `hunterAtk` /
`wrongDamage` / `baHurtHero` / `baStrike` / `baC7Bonus` ของชั้นล่างเลยสักบรรทัด**
— ทั้งเจ็ดจุดยังเป็นของเดิมที่ห่อไว้ตั้งแต่ v4.6/v6.0/v6.7/v7.9 ครบทุกประการ แพตช์นี้แก้แค่
ค่าคงที่ที่ wrapper เหล่านั้นอ่านไปคำนวณ

## UI — ข้อความเพดานประกอบจากค่าคงที่เอง ไม่ต้องแก้ข้อความ

`abRenderCore()` ของ v4.6/v7.9 เขียนสตริง `cap` เป็นตัวอักษรตรง ๆ ในแต่ละ entry ของ
`AB_CORES`/`baC7AddCores()` (ไม่ได้ประกอบจากค่าคงที่แบบไดนามิก) — แพตช์นี้จึงแก้ข้อความ
`cap` ทั้ง 7 บรรทัดให้ตรงกับเพดานใหม่ไปพร้อมกับตัวเลขคำนวณ (`'เพดาน 75%'` · `'เพดาน +3.6 วิ'`
· `'เพดาน +150%'` · `'เพดาน +1200'` · `'เพดาน +100%'` · `'เพดาน −40%'` · `'เพดาน +60%'`)
**เพื่อไม่ให้เลขบนจอกับเลขที่ทำงานจริงเพี้ยนกัน** (กติกาเดิมของ repo — ราคาร้านค้าของ v4.7.9.1)

## ขนาดไฟล์ — ยังต่ำกว่าเพดาน 2,000,000 ไบต์ (ในสถานะที่ตึงที่สุดของ repo ตอนนี้)

ต้นฉบับ 3,914,304 ไบต์ → ไฟล์แจก **1,999,393 ไบต์** (เหลือถึงเพดาน **607 ไบต์**)
**แพตช์นี้เป็นการแทนที่ค่าคงที่ในที่ทั้งหมด — ไม่ได้เพิ่มบรรทัดโค้ด ไม่ได้เพิ่มข้อความ UI
ยาวเกินของเดิม** (ป้ายเพดานยาวเท่าของเดิมทุกป้าย เพียงแค่ตัวเลขเปลี่ยน) ขนาดไฟล์จึงไม่ขยับ
จากก่อนแพตช์เลยแม้แต่ไบต์เดียว — สอดคล้องกับ "Critical Size Guardrail" ของงานที่สั่งมา
ซึ่งเตือนว่าที่ว่างเหลือน้อยมาก **ที่ว่างยังเหลือน้อยเท่าเดิม (607 ไบต์) แพตช์ถัดไปแทบแน่นอนว่า
ต้องคืนที่ก่อน และคืนที่ "ที่ภาพ" เท่านั้น** ตามกติกาเดิมของ repo

## สิ่งที่แพตช์นี้ไม่ได้ทำ

**ไม่เพิ่มฟิลด์ในบัญชี · ไม่เพิ่ม toast · ไม่เพิ่ม gate · ไม่เพิ่ม/ลดปุ่มสักใบ · ไม่แทรก
คอลัมน์ในตารางไหน · ไม่เพิ่มภาพหรือเสียงสักไบต์ · ไม่เพิ่ม DOM สักโหนด · ไม่เพิ่มชั้นใหม่
· ไม่เพิ่ม wrapper ใหม่สักตัว · ไม่แตะ `AB_CORE_COST`/`AB_CORE_MAX` ตามคำสั่ง**
ไม่มีตรารุ่นใหม่ และไม่ได้ห่อ `migrateAccount` เพิ่ม เพราะ `account.ab.core` เป็นฟิลด์เดิม
ของ v4.6 ที่ `abEnsure()`/`abSave()` จัดการอยู่แล้วครบถ้วน — บัญชีเก่าที่เคยซื้อขั้นไว้จะได้
ผลลัพธ์ใหม่ทันทีที่เปิดเกม (สูตร `eff(lv)`/`baC7At(TBL,lv)` อ่านค่าคงที่สดทุกครั้งที่วาด
ไม่มีอะไรให้ backfill)

## ทดสอบ

`test_system_cores.js` (51 เคส · 11 บล็อก · **ไฟล์ใหม่**) รันด้วย
`NODE_PATH=/opt/node22/lib/node_modules node test_system_cores.js`

- **ต้อง stub `fetch` + `EventSource` ก่อนโหลดหน้าเสมอ** (เหตุผลเดียวกับชุดของ v5.5/v5.6)
- **เข้าเกมด้วยเส้นทางจริงเสมอ** — ป๊อปอัปกติกาของ v5.6 → เกท → สมัครบัญชีทดสอบแบบสุ่ม
  → ปิดหน้าต่างจั่วการ์ดของ v4.7 ให้จบ
- **ปลดล็อกแรงค์ก่อนเทสต์แกนกลางระบบเสมอ** (`G.maxFloor = FLOOR_MAX; recalcStats();
  G.ab.shards = 99999;`) ไม่งั้น `abOpenCore()` โดน `showLock()` ปัดตกตั้งแต่บรรทัดแรก
- **❄️ frost ('นาฬิกาเยือกแข็ง') เช็ก `QUESTION_TIMER`/`FREEZE_TIMER` ก่อนร่ายเสมอ**
  (`useSkill('frost')` มีบรรทัด `if (!QUESTION_TIMER && !FREEZE_TIMER) { skillTip(...);
  return; }`) — ต่างจาก `eye`/`blade`/`ward` ที่ไม่มีเงื่อนไขนี้ ทดสอบแกนมานาด้วย `frost`
  ต้องเรียก `nextMonster()` ตั้งมอนสเตอร์ให้มีคำถามค้างอยู่ก่อนเสมอ (ซึ่ง `renderMonster()`
  เรียก `startQuestionTimer()` ปิดท้ายให้เองอยู่แล้ว) ไม่งั้น MP จะไม่ขยับเลยสักหน่วย
  แล้วเคสจะตกด้วยเหตุผลผิด (เจอมาแล้วตอนเขียนชุดนี้ — บันทึกไว้กันเดินซ้ำ)
- **วัดแกนพิฆาตต้องกด `BA_LIVE` เท่านั้นตอนวัดผลที่มีตัวคูณ** และต้องวัด `monsterHpFor()`
  แบบนอก `BA_LIVE` เพื่อพิสูจน์ว่าเลือดมอนสเตอร์ไม่พองตามสัดส่วนเดียวกัน (บทเรียนข้อ 2.1
  ของ v7.9 — HP อสูรกับดาเมจฮีโร่ใช้ `hunterAtk()` ตัวเดียวกัน)
- **วัดแกนสังหารต้องผ่าน `baStrike(dmg,crit,sup)` ไม่ใช่ `baC7Bonus(ex)` ตรง ๆ**
  (`baC7Bonus` รับพารามิเตอร์เดียวคือดาเมจส่วนเกินที่คำนวณไว้แล้ว ไม่ใช่ `(dmg,crit,sup)`)
  ต้องตั้งมอนสเตอร์ HP สูงมาก ๆ ก่อนวัด กันไม่ให้ตายกลางทางแล้วเด้งหน้าต่างอื่นมาคั่น
- **ต้องรันควบกับ `test_codex_cores.js` เสมอ** เพราะทั้งสองชุดแตะ `AB_CORES` เดียวกัน
  (ชุดนั้นแยก System Core ออกจาก Codex Mastery tables อย่างชัดเจนอยู่แล้ว — `BA_C7_CX_HP`/
  `BA_C7_CX_CD` ผูกกับคลังคำศัพท์ 329 คำ **ไม่ถูกแตะในแพตช์นี้เลย**)

วัดจริงหลังแพตช์ (รันทั้งสี่ชุดตามที่งานสั่งมา):
`test_system_cores.js` **51/0** · `test_cloud_sync.js` **104/0** ·
`test_monarch.js` **85/0** · `test_codex_cores.js` **93/0**
— ไม่มี regression สักเคส ไม่มี pageerror ตลอดการทดสอบ
