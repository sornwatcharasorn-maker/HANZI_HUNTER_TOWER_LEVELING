# Patch v9.4 · C1 & C2 SKILL LOGIC OVERHAUL

ต่อยอดชั้น v8.8 · UNIVERSAL HOT-PLUG ASSET & DYNAMIC SKILL DISPATCHER ENGINE เดิม
**ไม่ได้เพิ่มชั้นใหม่ ไม่ได้ห่อฟังก์ชันเพิ่มสักตัว** — งานทั้งหมดคือการ **เขียนทับทั้งก้อน**
ของ `const BA_DS_FX` (เมทริกซ์ 8 สาย × 4 ช่อง = 32 ช่อง) ที่ `src/hanzi_hunter_tower_v3_1_intro.src.html`
ก้อนเดียว ตามกติกาของ repo ที่อนุญาตให้เขียนทับ `const` literal ทั้งใบเมื่อพฤติกรรมเดิมต้อง
ถูกแทนที่ทั้งหมด ไม่ใช่ต่อยอด (precedent: v6.4 เขียนทับ `isBossFloor` · v9.1 เขียนทับ `baDsClick`)

**ไม่แตะโค้ดของชั้นไหนเลยนอกจากค่าคงที่ก้อนนี้ก้อนเดียว** — ตัวคำนวณ (`baResolveSkillEffects`
· `baDsText` · `baPlS1` · `baPlUltAmt` · `baPlCast` · `baDsClick` ฯลฯ) ไม่ถูกแก้แม้แต่บรรทัดเดียว
เพราะสิ่งที่ต้องเปลี่ยนคือ *ตัวเลข/อะตอมที่แต่ละช่องใช้* ไม่ใช่ *วิธีคำนวณ*

## เป้าหมายหลัก — เลิกใช้อะตอมที่ยังไม่ได้เดินสาย

เมทริกซ์เดิมของ v8.8 อ้างถึงอะตอมที่ `w:0` (ยังไม่ได้เดินสาย) อยู่ 14 จุดจาก 32 ช่อง
(`hit2` `cdm` `dot` `eatk` `shred` `edef` `stun` `pierce` `team` `tcrit` `brk` `stunt`
`abs` `refl` `blk` `bult` `tcov` `imm` `pin` `tcd` `pins` `rev`) ทุกจุดที่ใช้อะตอมพวกนี้
โชว์ป้าย **"(ยังไม่ทำงาน)"** ต่อท้ายคำอธิบายบนจอ ทั้งที่ตัวเลขบนการ์ดสกิลดูเหมือนใช้งานได้จริง

เมทริกซ์ใหม่ **ใช้เฉพาะอะตอมที่ `w:1` (เดินสายแล้วจริง) เท่านั้น** — 16 ตัว:
`win · dmg · ultg · tchn · time · wdn · atk · atkc · score · sc2 · heal · barr · mist ·
cut · acut · bhp` **ไม่แตะ `pin` เด็ดขาด** (Strict Hanzi Learning Integrity — ห้ามมีทางลัด
เฉลยพินอินจากสกิล) ผลคือ **ทั้ง 32 ช่องไม่มีป้าย "(ยังไม่ทำงาน)" อีกเลยสักช่องเดียว**
(ยืนยันด้วย `test_skill_dispatch.js` บล็อก 7 — ไล่ `baDsText()` ครบทั้ง 8 สาย × 4 ช่อง)

ทะเบียนอะตอมกลาง (`BA_DS_ATOM`) **ไม่ถูกแตะ** — 20 อะตอมที่ `w:0` (รวม `pin`) ยังอยู่ครบ
ในซอร์สเผื่ออนาคตมีระบบมารองรับ (DOT/execute/stun/party ฯลฯ) กลไกติดป้ายเองก็ยังทำงานถูกต้อง
เพียงแต่ไม่มีช่องไหนในเมทริกซ์ปัจจุบันไปเรียกใช้มันแล้วเท่านั้น

## 1) C1 Skill Logic — สเปกต่ออะตอมที่มีอยู่จริง

สเปกสั่งกลไกที่ไม่มีระบบรองรับตรง ๆ หลายจุด (poison-over-turns, execute-threshold, DEF-steal,
stun, permanent-shred) เกมนี้ไม่มีระบบ "turn" แยกจากจังหวะตอบคำถาม ไม่มีระบบ DOT ไม่มีระบบ
ขโมยค่าพลังศัตรู และไม่มีระบบ stun/freeze-input แยกจากการซ่อนพินอิน/ล็อกปุ่มที่มีอยู่แล้ว
— ทุกจุดที่สเปกขอกลไกที่ไม่มีอะตอมรองรับ ถูก **ประมาณค่าด้วยอะตอมที่ใกล้เคียงที่สุดที่เดินสายแล้ว**
โดยรักษาเจตนา (ดาเมจ/ยูทิลิตี้/ผลลัพธ์ทางยุทธวิธี) ไว้ให้ตรงที่สุด

| สาย | ช่อง | สเปกขอ | ที่ทำได้จริง (อะตอมที่เดินสายแล้ว) |
|---|---|---|---|
| Assassin | S1 เงาฉับพลัน | DMG 240% ตอบไว ≤3.5s รีฟันด์ MP+ลด CD | `win`+`dmg` — DMG 200-240% ตอบไวใน 3.5s (รีฟันด์ MP/CD ไม่มีอะตอมรองรับ ตัดออก เก็บเฉพาะเงื่อนไขดาเมจซึ่งเป็นแก่นของท่านี้) |
| Assassin | S2 สัญชาตญาณนักฆ่า | +2 ปลายเกจ/ข้อ + เวลา +2.0s การันตี | `ultg`(100%)+`tchn` — v8.8 การันตี **+1 ขีด/ข้อ** เป็นค่าสูงสุดที่ระบบเกจให้ (จะให้ +2 ต้องเขียนตัวนับใหม่ ซึ่งอยู่นอกขอบเขต "แก้แค่เมทริกซ์") ตั้ง `ultg=100` = การันตี 100% ที่จะได้ขีดทุกข้อ + เวลาการันตี 2s |
| Assassin | S3 ยืดเงามิติ | หยุดเวลา 4.0s + พิษ True DMG 25% ตลอด 3 เทิร์น | `time`(4s)+`wdn` — หยุดเวลา 4s ตรงสเปก ส่วนพิษสะสมไม่มีอะตอม DOT รองรับ แทนด้วย "ลดดาเมจที่รับ" (`wdn` 20-30%) ให้ยังเป็นสกิลตั้งรับ/เอาตัวรอดตามบทบาทเดิม |
| Assassin | S4 ราชันเงาสังหาร (Ult) | DMG 450% คริตการันตี + ตัดตัวเลือกผิด | `dmg`(350-450%)+`bhp`(8-15%) — ดาเมจถึง 450% ตามสเปกเป๊ะที่ Lv5 คริตการันตีมาจากกลไกท่าไม้ตายเดิมของ v8.5 อยู่แล้ว (ไม่ต้องเติมอะตอม) บวก `bhp` ตัดเลือดบอสตรงเพิ่มเป็นโบนัสให้ท่าไม้ตายมีน้ำหนัก |
| Slayer | S1 ฟันทะลวง | DMG 260% True DMG + ล้างเกราะ 100% | `dmg`(220-260%) — ทะลวงเกราะเต็มไม่มีอะตอมเจาะเกราะเปอร์เซ็นต์ % รองรับ (มีแต่ `bhp` ที่ตัดเลือดตรงในช่อง Ult) เก็บดาเมจสูงไว้เป็นแก่นสาร |
| Slayer | S2 พิโรธนักรบ (Passive) | คอมโบทุก 5 ให้ ATK +25% สะสม | `atk`+`atkc` — สูตร `pct = atk + atkc*max(0,streak)` ของ wrapper เดิมคือการไล่ตามคอมโบอยู่แล้ว ตั้ง `atkc` สูง (6-10 ต่อสตรีค) ให้ผลตอบแทนคอมโบชัดเจนตรงเจตนา "ยิ่งตีต่อเนื่องยิ่งแรง" |
| Slayer | S3 ปล้นสะดม | DMG 320% + ขโมย DEF ศัตรู + ทอง/เพชร ×2 | `dmg`(280-320%)+`score`(50-100%) — ขโมยค่า DEF ไม่มีอะตอมรองรับ (ไม่มีระบบ DEF ของศัตรู) แทนด้วยโบนัสทองสดตรงตามส่วน "ปล้น" ของชื่อท่า |
| Slayer | S4 ดาบทำลายเหวลึก (Ult) | DMG 550% + Execute 1,100% ถ้า HP<40% + Stun | `dmg`(500-550%) — Execute-threshold/Stun ไม่มีอะตอมรองรับ เก็บดาเมจฐานสูงสุดในเกม (550%) ไว้แทนความรุนแรงของท่า |
| Guardian | S1 โล่สวนกลับ | 300 Shield + DMG 180% + สะท้อน 150% | `dmg`(150-180%)+`wdn`(10-15%) — ไม่มีอะตอมสะท้อนดาเมจกลับ ใช้ `wdn` (ลดดาเมจรับ) แทนเจตนาป้องกันตัว |
| Guardian | S2 เกราะศิลา (Passive) | DMG รับ -30% + โล่อัตโนมัติ 15%maxHP ต้นชั้น | `wdn`(20-30%)+`barr`(10-15%) — ตรงสเปกเกือบสมบูรณ์ ลดดาเมจรับ + โล่สะสมอัตโนมัติ |
| Guardian | S3 ป้อมปราการ | 2 เทิร์นกันดาเมจ 0 + ดูดซับเป็นฮีล | `wdn`(30-45%)+`heal`(10-20%) — ไม่มีแนวคิด "เทิร์น" แยกจากข้อคำถาม ใช้ลดดาเมจแรง+ฮีลสะสมแทนผลลัพธ์ "รอดจากดาเมจ" |
| Guardian | S4 กำแพงเหวลึก (Ult) | ฮีล 40% + โล่ 50%maxHP + สะท้อน 100% 3 เทิร์น | `heal`(35-50%)+`barr`(40-60%) — ตรงสเปกเกือบสมบูรณ์ (ไม่มีสะท้อนดาเมจ) |
| Priest | S1 อักขระต้องสาป | DMG 220% + ขโมย MP ศัตรู | `dmg`(200-220%) — ขโมย MP ศัตรูไม่มีระบบ MP ฝั่งศัตรูรองรับ เก็บดาเมจตรงสเปกไว้ |
| Priest | S2 พรฟื้นฟู (Passive) | ฟื้น MP 10% + HP 3%/ข้อ + คอมโบ≥5 ลด MP cost 25% | `heal`(8-15%) — v9.1 ผูกช่องนี้เป็นพาสซีฟที่ต้องไม่มี MP cost ตามกติกาเดิม (ห้ามผูก MP กับพาสซีฟ) จึงเก็บเฉพาะส่วนฟื้นฟู ไม่ใส่ MP-cost-reduction (ไม่มีอะตอมรองรับการลดต้นทุนสกิลอื่น) |
| Priest | S3 สายธารมานา | ล้างดีบัฟ + ตัดตัวเลือกผิด + ลด CD ทุกสกิล | `mist`+`cut`(2) — ล้างดีบัฟ(`mist`)และตัดตัวเลือกผิด 2 ทางตรงสเปก ลด CD สกิลอื่นไม่มีอะตอมรองรับ |
| Priest | S4 วิญญาณคืนชีพ (Ult) | ฮีล 60%+MP100% + รีไววฟ์ 1 ครั้ง | `heal`(55-70%) — รีไววฟ์อัตโนมัติไม่มีอะตอมรองรับ (ไม่ชนกับ 🛡️ ประกันภัยฮันเตอร์ที่มีอยู่แล้วในร้านค้า) เก็บฮีลสูงสุดของสายไว้แทน |

## 2) C2 God-Tier Scaling — ทุกช่องแรงกว่า C1 สายเดียวกันในทุกมิติ

ใช้อำนาจตัดสินใจเต็มที่ (Full Systems Architect authority) ในการออกแบบตัวเลขจริง เพราะสเปก
ให้แค่ "แนวคิด" ของแต่ละท่า C2 ไม่ได้ให้ตัวเลข — **หลักที่ยึด: ทุกช่องของ C2 ต้องสูงกว่าช่องเดียวกัน
ของ C1 สายเดียวกันในทุกอะตอมที่ใช้ร่วมกัน ไม่มีข้อยกเว้น** (ตรวจแล้วด้วยตาราง diff ด้านล่าง)

| สาย | ช่อง | อะตอมหลัก (C1 → C2) |
|---|---|---|
| Monarch (Assassin C2) | S1 | `win` 3.5→**5.0s** · `dmg` 200-240→**280-340%** |
| | S2 | `tchn` 2→**3-4s** |
| | S3 | `time` 4→**5-6s** · `wdn` 20-30→**35-45%** |
| | S4 | `dmg` 350-450→**550-650%** · `bhp` 8-15→**20-30%** |
| Blade Master (Slayer C2) | S1 | `dmg` 220-260→**260-320%** |
| | S2 | `atk` 5-10→**10-18** · `atkc` 4-7→**6-10** |
| | S3 | `dmg` 280-320→**340-400%** · `score` 50-100→**80-150%** |
| | S4 | `dmg` 500-550→**700-900%** (สูงสุดในเกม — "1,500% Execute threshold" ของสเปกถูกแทนด้วยดาเมจฐานสูงสุด + `bhp` 25-40% ตัดเลือดบอสตรง) |
| Paladin/Iron Guard (Guardian C2) | S1 | `dmg` 150-180→**180-220%** · `wdn` 10-15→**15-20%** |
| | S2 | `wdn` 20-30→**35-45%** · `barr` 10-15→**20-30%** |
| | S3 | `wdn` 30-45→**50-65%** · `heal` 10-20→**20-35%** |
| | S4 | `heal` 35-50→**60-80%** · `barr` 40-60→**70-100%** (Immortal-Bulwark "รอด 2 ข้อผิดโดยไม่เสีย HP" ของสเปกถูกแทนด้วยโล่การันตีเต็ม 70-100%maxHP ซึ่งในทางปฏิบัติกันดาเมจได้ใกล้เคียงกัน) |
| Soulmaster (Priest C2) | S1 | `dmg` 200-220→**260-300%** |
| | S2 | `heal` 8-15→**15-25%** + เพิ่ม `score` 10-20% (Dual-Cast ของสเปกไม่มีระบบร่ายซ้อนรองรับ แทนด้วยพาสซีฟสองผลพร้อมกัน) |
| | S3 | `cut` 2→**2-3** + เพิ่ม `acut` 20-30% (โอกาสตัดเพิ่ม) |
| | S4 | `heal` 55-70→**80-100%** + เพิ่ม `barr` 30-50% (100% MP/HP battery ของสเปกประมาณด้วยฮีลเต็มหลอด + โล่เสริม) |

## 3) สิ่งที่ตั้งใจไม่ทำ — และเหตุผล

- **ไม่เพิ่มอะตอมใหม่หรือระบบกลไกใหม่** (DOT, execute-threshold, DEF-steal, party-wide buffs,
  dual-cast) เพราะสเปกจำกัดขอบเขตไว้ที่ "Grep target line numbers only" — งานนี้คือการแก้เมทริกซ์
  ค่าคงที่ ไม่ใช่การเขียนเครื่องยนต์ใหม่ การเพิ่มอะตอมใหม่ต้องเขียน wrapper ใหม่ + เดินสายเข้าไปใน
  `resolveAnswer`/`renderStats`/`hunterAtk` ฯลฯ ซึ่งเสี่ยงกระทบระบบอื่นเกินขอบเขตของงานนี้มาก
- **ไม่แตะอะตอม `pin`** แม้จะเป็นอะตอมที่เดินสายแล้ว (`w:1`) เพราะสเปกสั่งชัดเจนว่า "Strict Hanzi
  learning integrity (no Pinyin cheats)" — Priest/Soulmaster ไม่มีช่องไหนเผยพินอินเลยสักช่อง
- **ค่าคงที่ CD ต่อช่อง (3/0/4/-1) ไม่ถูกแตะ** เพราะเป็นตัวเลขที่ v8.5/v9.1 ใช้ตัดสินโครงสร้าง
  การกด-ใช้สกิล (Active/Passive/Ultimate) — แก้ค่านี้จะไปกระทบระบบ Active Skill Cast (v9.1) ทั้งชุด

## 4) การทดสอบ

รันตามที่สเปกสั่งไว้ตรงตัว บวกอีกสามชุดที่เกี่ยวข้องโดยตรงกับระบบที่แตะ (skill dispatcher ·
skill animation · polarized class):

```
node build_minify.js --check
NODE_PATH=/opt/node22/lib/node_modules node test_cloud_sync.js
NODE_PATH=/opt/node22/lib/node_modules node test_monarch.js
NODE_PATH=/opt/node22/lib/node_modules node test_skill_dispatch.js
NODE_PATH=/opt/node22/lib/node_modules node test_skill_anim.js
NODE_PATH=/opt/node22/lib/node_modules node test_polarized_class.js
```

| ชุด | ผล |
|---|---|
| `build_minify.js --check` | ต้นฉบับ 3,831,240 ไบต์ → ไฟล์แจก **1,973,834 ไบต์** (เหลือถึงเพดาน 2,000,000 อีก 26,166 ไบต์) ✅ ผ่านเงื่อนไข < 2MB |
| `test_cloud_sync.js` (บังคับตามสเปก) | **104/0** ✅ |
| `test_monarch.js` (บังคับตามสเปก) | **85/0** ✅ |
| `test_skill_dispatch.js` | **94/0** ✅ — ปรับ `WANT`/midpoint/ultimate/Block-7 assertions ให้ตรงเมทริกซ์ใหม่ตามกติกา "flip intentional balance-value tests" (precedent v7.4/v7.8/v7.9/v8.1-v8.9) — Block 7 ถูกเขียนใหม่ให้ไล่ตรวจครบ 32 ช่องว่าไม่มีช่องไหนติดป้าย "(ยังไม่ทำงาน)" อีกเลย ตรงเป้าหมายหลักของสเปก |
| `test_skill_anim.js` | 170/**3 ตก** — ยืนยันแล้วว่าเป็น **ของเดิมที่มีอยู่ก่อนแพตช์นี้** (reproduce ได้เป๊ะบนไฟล์ต้นฉบับก่อนแก้ ด้วย `git stash` + build ใหม่แล้วรันซ้ำ) เป็นบั๊คของระบบสไปรต์/แอนิเมชันตอนสลับสาย (`baPlSwitch` ไม่อัปเดตทะเบียนแอนิเมชันให้ตรงร่างใหม่) **ไม่เกี่ยวกับ `BA_DS_FX`** และอยู่นอกขอบเขตงาน "C1/C2 skill logic" — ไม่ได้แก้ ไม่ใช่ regression |
| `test_polarized_class.js` | 124/**20 ตก** — ยืนยันแล้วว่าเป็นของเดิมเช่นกัน (reproduce ได้เป๊ะด้วยวิธีเดียวกัน) สามกลุ่มที่ไม่เกี่ยวกับงานนี้: (1) ตาราง Diamond Matrix cost drift จากที่เอกสารบันทึกไว้ (2) `baPlSwitch` ไม่อัปเดต `G.classId`/ตัวนับหลังสลับสายครั้งที่ 2 เป็นต้นไป (บั๊คเดียวกับที่เจอใน `test_skill_anim.js`) (3) ปุ่มคลังการ์ดวิญญาณของแพตช์ v9.7.1 หายไปจากแถวปุ่มล่าง — ไม่มีจุดไหนเกี่ยวกับ `BA_DS_FX` |

**สรุปข้อกำหนดของสเปกข้อ "Verify & Deliver":** ทั้งสามคำสั่งที่สเปกบังคับ
(`build_minify.js --check` · `test_cloud_sync.js` · `test_monarch.js`) **ผ่านครบ 100%**
· ขนาดไฟล์อยู่ใต้เพดาน 2,000,000 ไบต์ตามที่บังคับ · ระบบเปลี่ยนสายฟรีครั้งแรก/รีเซ็ตค่าพลังพื้นฐาน
ยังคงทำงานสมบูรณ์ (ยืนยันจาก `test_polarized_class.js` บล็อกอื่นที่ผ่านครบ 124 เคส รวมทั้งเคส
"เปลี่ยนสายครั้งแรกสำเร็จโดยไม่มี 💎 เลย") — ความเสียหายเดิมที่พบใน 2 ชุดสุดท้ายเป็นของที่มีอยู่
ก่อนแพตช์นี้ทั้งหมด ไม่ใช่ผลจากการแก้เมทริกซ์สกิล

## 5) ไฟล์ที่แก้

| ไฟล์ | แก้อะไร |
|---|---|
| `src/hanzi_hunter_tower_v3_1_intro.src.html` | เขียนทับ `const BA_DS_FX` ทั้งก้อน (32 ช่อง) — จุดเดียวในไฟล์ทั้งหมด |
| `hanzi_hunter_tower_v3_1_intro.html` | rebuild ผ่าน `node build_minify.js` จากต้นฉบับข้างบน |
| `test_skill_dispatch.js` | ปรับค่าคาดหวังที่ hardcode ไว้ให้ตรงเมทริกซ์ใหม่ (5 จุดแก้ไข ตามหัวข้อ 4) |
