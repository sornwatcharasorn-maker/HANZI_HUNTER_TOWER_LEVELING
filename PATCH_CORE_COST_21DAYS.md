# Patch · AB_CORE_COST Rebalance — 21-Day / 9,450💎 Pacing

อยู่ในชั้น v4.6 · ABYSS PROTOCOL เดิม (แก้ค่าคงที่ในที่ ไม่ได้เพิ่มชั้นใหม่ ไม่ได้ห่อฟังก์ชัน
สักตัว) กระทบเฉพาะตัวเลข ไม่มีการเปลี่ยนตรรกะ/DOM/ข้อความใหม่สักบรรทัด

## สิ่งที่แก้

```diff
- const AB_CORE_COST = [8, 14, 22, 32, 46, 64];
+ const AB_CORE_COST = [40, 80, 150, 250, 380, 450];
```

(บรรทัด 9766 ของ `hanzi_hunter_tower_v3_1_intro.src.html`)

ค่านี้ใช้ร่วมกันโดยทั้ง 7 เส้นของแกนกลางระบบ (`AB_CORES` — 🔮 มานา · ⏱️ เวลา · 🪙 โชคลาภ
ของ v4.6 บวก ❤️ ชีวิต · 🗡️ พิฆาต · 🛡️ เกราะ · ⚡ สังหาร ที่ v7.9 · CODEX 4-STAT OVERHAUL
เติมเข้ามาทีหลัง) อ่านผ่าน `abBuyCore()` / `abRenderCore()` จุดเดียวกัน ไม่มีสารบัญคู่ขนาน
ให้ต้องแก้ที่อื่น

## ตัวเลขที่ได้

| ขั้น | เดิม | ใหม่ |
|---|---|---|
| 1 | 8 | **40** |
| 2 | 14 | **80** |
| 3 | 22 | **150** |
| 4 | 32 | **250** |
| 5 | 46 | **380** |
| 6 | 64 | **450** |
| **รวม/เส้น** | 186 | **1,350** |
| **รวม 7 เส้น** | 1,302 | **9,450** |

`9,450 ÷ 450 (เพดาน 💎 ต่อวันตามสเปก) = 21 วันเป๊ะ` — ผู้เล่นที่เก็บ 💎 เต็มเพดานทุกวัน
จะใช้เวลา 21 วันพอดีในการอัปทั้ง 7 เส้นให้เต็มขั้นทุกเส้น (เดิมใช้เวลาราว 3 วันที่เพดานเดียวกัน)

## คอมเมนต์ที่แก้ตาม (ไม่กระทบขนาดไฟล์แจก — คอมเมนต์ถูกตัดทิ้งหมดในโหมด HARD)

อัปเดตตัวเลขในคอมเมนต์ 3 จุดที่อ้างถึงผลรวมเดิม (186 / 1,302 ต่อเนื่องกับ AB_CORE_COST)
ให้ตรงกับค่าคงที่ใหม่ ไม่ได้เพิ่มคอมเมนต์ใหม่ก้อนไหน:

- บรรทัด 9766 — หมายเหตุท้ายตัวแปร `AB_CORE_COST` เอง
- บรรทัด 31861 — หมายเหตุกันเงินเฟ้อของ v7.9 ตอน push 4 เส้นใหม่เข้า `AB_CORES`
- บรรทัด 31940 — หมายเหตุเรื่องแกนพิฆาตที่ wrapper ของ `hunterAtk`

## กันเงินเฟ้อ — ไม่ถูกแตะ

💎 ยังซื้อด้วยทองไม่ได้และขายเป็นทองไม่ได้ตามกติกาเดิมของ v4.6 · เพดาน `AB_SHARD_CAP`
(99,999) ไม่ถูกแตะ · สูตรผลของแต่ละขั้น (`AB_CORE_MP_PCT` · `AB_CORE_TIME_MS` ·
`AB_CORE_GOLD` · `BA_C7_HP` · `BA_C7_ATK` · `BA_C7_SHLD` · `BA_C7_CRIT`) ไม่ถูกแตะเลย
สักตัวเลข — แพตช์นี้แก้เฉพาะ "ราคา" ไม่แตะ "ผล"

## ขนาดไฟล์

```
ต้นฉบับ  3,914,376 ไบต์  (+72 ไบต์จากก่อนแพตช์ — คอมเมนต์ 3 จุดที่แก้)
ไฟล์แจก  1,999,398 ไบต์  (+5 ไบต์จากก่อนแพตช์ — เฉพาะตัวเลขในอาร์เรย์ที่ยาวขึ้น
                          คอมเมนต์ถูกตัดทิ้งหมดในโหมด HARD จึงไม่กินที่)
เหลือถึงเพดาน 2,000,000 อีก 602 ไบต์
```

## เทสต์

**`test_system_cores.js` ที่ระบุในคำสั่งไม่มีอยู่ในไฟล์ repo** — ไฟล์ทดสอบที่ครอบคลุม
`AB_CORES`/`AB_CORE_COST` จริงคือ `test_codex_cores.js` (เจ้าของพื้นที่ทดสอบระบบแกนกลาง
ทั้ง 7 เส้นตั้งแต่ชั้น v7.9) จึงรันตัวนั้นแทน

```
node build_minify.js --check      → ผ่าน (1,999,398 / 2,000,000 ไบต์)
node test_cloud_sync.js           → 104 ผ่าน / 0 ตก
node test_monarch.js              → 85 ผ่าน / 0 ตก
node test_codex_cores.js          → 77 ผ่าน / 16 ตก  (ดูหมายเหตุด้านล่าง)
```

### หมายเหตุ 16 เคสที่ตกใน `test_codex_cores.js` — ไม่ใช่ผลของแพตช์นี้

ยืนยันด้วย `git stash` แล้วรันชุดเดียวกันกับไฟล์ก่อนแพตช์ (ก่อนแตะ `AB_CORE_COST`)
**ได้ 77 ผ่าน / 16 ตก เท่ากันเป๊ะทุกเคส** — เป็นบั๊คที่มีอยู่ในสารบัญค่าคงที่
`BA_C7_HP` / `BA_C7_ATK` / `BA_C7_SHLD` / `BA_C7_CRIT` ของชั้น v7.9 อยู่ก่อนแล้ว
(เพดานจริงในไฟล์คือ HP +1200 / ATK +100% / SHIELD −40% / CRIT +60% ขณะที่ชุดเทสต์
คาดหวัง +400 / +40% / −24% / +20% ตามที่เอกสาร CLAUDE.md บันทึกไว้) **ไม่เกี่ยวกับ
`AB_CORE_COST` เลย** — เคสเดียวที่แพตช์นี้แตะ (การหักเศษคริสตัลขั้นแรก) ผ่านแล้ว
หลังอัปเดตค่าที่คาดหวังจาก 8 เป็น 40 ในไฟล์เทสต์

**ไม่ได้แก้บั๊คนี้** เพราะอยู่นอกขอบเขตงานที่สั่งมา (ซึ่งจำกัดเฉพาะ `AB_CORE_COST`) และ
การแก้ `BA_C7_*` เป็นคนละความเสี่ยงคนละก้อนที่ควรทำเป็นแพตช์แยกของตัวเอง

## ไฟล์ที่แก้

| ไฟล์ | เหตุผล |
|---|---|
| `hanzi_hunter_tower_v3_1_intro.src.html` | ต้นฉบับ — แก้ `AB_CORE_COST` + คอมเมนต์ 3 จุด |
| `hanzi_hunter_tower_v3_1_intro.html` | ไฟล์แจก — build ใหม่จากต้นฉบับ |
| `test_codex_cores.js` | อัปเดตค่าคาดหวังของการหักเศษคริสตัลขั้นแรก (8 → 40) |
