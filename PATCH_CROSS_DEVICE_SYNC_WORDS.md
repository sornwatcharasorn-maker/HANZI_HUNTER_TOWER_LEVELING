# Patch v9.13 · CROSS-DEVICE FULL-PROFILE SYNC

**ไฟล์ที่แก้:** `hanzi_hunter_tower_v3_1_intro.src.html` (ต้นฉบับ) → build เป็น
`hanzi_hunter_tower_v3_1_intro.html` (ไฟล์แจก) ด้วย `node build_minify.js`

**ตำแหน่งในไฟล์:** อยู่ในเนมสเปซ `wb` ของชั้น v9.9 · DEDICATED WORDBANK SYNC เดิม
วางท้ายสุดของไฟล์ ต่อจาก `Patch v9.12 · SKILL AWAKENING & BALANCED LADDER` และ**ก่อน**
`(function baInstall() { ... })();` — ห่อทับ `wbPayload` / `wbRestore` เป็นชั้นที่สาม
ในสายการห่อ (v9.9 นิยาม → v9.12 ห่อเพิ่ม `account.skills` → **v9.13 ห่อเพิ่มอีกชั้นนี้**)

**ไม่แก้โค้ดของ v4.0-v9.12 เลยแม้แต่บรรทัดเดียว** — ทุกจุดเป็น wrapper ล้วน ตามกฎเหล็ก
ของ repo (const _old = fn; fn = function(){...})

---

## อาการที่แพตช์นี้แก้

ระบบซิงก์ข้ามเครื่องของเกมมีอยู่แล้วสามชั้นที่ทำงานคนละหน้าที่ และ **ไม่มีชั้นไหนซิงก์
โปรไฟล์ผู้เล่นครบทุกฟิลด์**:

| ชั้น | ทำอะไร | ช่องว่างที่เหลือ |
|---|---|---|
| v5.2 · CLOUD SYNC (`CS_*`) | ซิงก์บัญชีทั้งก้อนแบบ full save/restore | **ปิดอยู่โดยค่าเริ่มต้น** ต้องให้ครูตั้งคอนฟิกเองทีละเครื่อง — ในทางปฏิบัติแทบไม่มีใครเปิด |
| v5.3/v5.4 · FIREBASE GM REALTIME / AUTO-SYNC | ส่งสรุปข้อมูลแบบเรียลไทม์ให้ครูดู | เป็น **display-only** ไม่มีรหัสผ่าน ไม่ได้ตั้งใจให้ใช้กู้คืนโปรไฟล์ |
| v9.9-v9.12 · DEDICATED WORDBANK SYNC (`wb*`) | ซิงก์คลังคำศัพท์ (`wordStats`) + สายอาชีพ (`account.skills`) ข้ามเครื่อง | **เพชร · ทอง · กระเป๋า · ชั้นหอคอย · สายอาชีพที่เลือกอยู่ ยังไม่ถูกซิงก์** |

ผลคือผู้เล่นที่สลับมือถือ/คอมยังเจอปัญหาความคืบหน้าไม่ตรงกัน (ตอบคำศัพท์ถูกจากเครื่องหนึ่ง
แล้วเปิดอีกเครื่องไม่เห็นเพชร/ทอง/ชั้นหอคอยที่ควรจะได้) แม้ระบบซิงก์คำศัพท์จะทำงานถูกต้องแล้ว

**คลังอักขระ (Codex) ไม่ต้องแก้อะไรเลย** — `account.codex` ({ms, first}) คำนวณจาก
`wordStats` อยู่แล้ว (`cxReached(g)`) และ `wbRestore()` เดิมของ v9.9 เรียก
`cxSyncMilestone(true)` ต่อท้ายทุกครั้งที่ผสาน `wordStats` สำเร็จ ซึ่งคำนวณ `g.codex.ms`
ใหม่ให้ทันที การเพิ่มโค้ดซิงก์ Codex แยกต่างหากจะเป็นการสร้างสารบัญคู่ขนานที่ต้องคอยซิงก์
เอง (กับดักข้อ 35 ของ CLAUDE.md) จึงไม่ได้ทำเพิ่ม

---

## สิ่งที่แพตช์นี้เพิ่ม — ต่อยอดทางเดินเดิมของ v9.9/v9.12 อีกชั้น

### 1) `wbPayload(g)` — แนบฟิลด์เพิ่มเข้า payload ที่ส่งขึ้นคลาวด์

```js
p.classId = g.classId || '';
p.gold = g.gold || 0;
p.floor = g.floor || 1;
p.floorProgress = g.floorProgress || 0;
p.level = g.level || 1;
p.exp = g.exp || 0;
p.maxExp = g.maxExp || 0;
p.items = Object.assign({}, g.items || {});
p.ab = { shards, core: {...}, seals: {...}, abyss };   /* คัดลอกลึก กันแก้ทับของจริง */
```

### 2) `wbRestore(user)` — ดึงเอกสารเดิมของ v9.9 ซ้ำ (คนละคำขอ) แล้วผสานฟิลด์ที่เพิ่มมา

ทำตามแบบแผนเดิมของ v9.9/v9.12 เป๊ะ (จุดดักเดียวกัน · จังหวะเดียวกัน · ท่าเดียวกัน):
1. บันทึกว่า `G` "สะอาด" หรือไม่ (`baV913Clean`) **ก่อน**เรียกของเดิม
2. `await fbFetch(wbUrl('/'+user))` ดึงเอกสารของตัวเอง (คนละคำขอกับที่ `wbRestore` เดิมทำ)
3. `await` ให้สายของเดิมทั้งหมด (v9.9 → v9.12) ทำงานจบก่อน
4. ตรวจ `CURRENT_USER === user` อีกครั้ง (กันแข่งกับการล็อกอินซ้อน)
5. ผสานสองแบบตามชนิดฟิลด์ (ดูหัวข้อถัดไป)
6. ถ้ามีอะไรเปลี่ยน → `recalcStats()` → `saveProgress()`/`abSave()`/`baPlSave()`
   → `renderStats()`/`renderItems()`/`renderFloor()`/`renderSkills()`
   → `baPlRender()`/`cxRender()` ถ้าแผงนั้นเปิดอยู่ → `wbPush(user)` ส่งกลับขึ้นคลาวด์

### 3) สองกลยุทธ์ผสาน — เลือกตามว่าฟิลด์นั้น "สะสมตลอดชีพ" หรือ "สถานะปัจจุบัน"

**(A) Always-monotonic merge (`baV913MergeAbMono`)** — ใช้ `Math.max(local, remote)`
ต่อรายการ ปลอดภัยเสมอเพราะพิสูจน์ได้ว่ามีแต่ขึ้น ไม่มีทางลง:

- `ab.core[key]` — ขั้นแกนกลางระบบที่ซื้อถาวรด้วยเพชร (v4.6) เพิ่มเท่านั้น ไม่มีทางลด
- `ab.seals[floor].broken` / `.flawless` — ธงความสำเร็จถาวร (ทลายผนึก/ทลายแบบไร้ที่ติ)
  ติดแล้วติดตลอด ไม่มีทางถูกถอด

**(B) Clean-device-only overwrite (`baV913AdoptClean`)** — ทับเฉพาะตอนเครื่องนี้เป็น
"เครื่องใหม่/ว่างเปล่า" (`baV913Clean`: level=1, exp=0, gold=0, floor=1, maxFloor=1,
correct=0, wrong=0, ไม่มีไอเทมในกระเป๋า) ใช้กับฟิลด์ที่**ไม่ใช่**ตัวสะสมตลอดชีพ:

- `classId`, `gold`, `floor`, `floorProgress`, `level`, `exp`, `maxExp`, `items`,
  `ab.shards` (เพชร — สกุลเงินที่ใช้จ่ายได้ ต่างจาก `ab.core` ที่เป็นของที่ซื้อไปแล้ว),
  `ab.abyss`

**ทำไมแยกสองกลุ่ม** — `level`/`exp` **ไม่ใช่ตัวสะสมตลอดชีพ** มีเครื่องมือ GM ที่รีเซ็ต
`level = 1, exp = 0` ได้จริง (ยืนยันจากซอร์ส) การ max-merge ฟิลด์นี้จะทำให้ GM รีเซ็ตแล้ว
เครื่องอื่นดันค่าเก่ากลับมาทันที ส่วน `gold`/`items` ก็ไม่ใช่ตัวสะสม (ใช้จ่ายได้ ลดได้)
การ max-merge จะเปิดช่องปั๊มของ (เล่นสองเครื่องพร้อมกัน เลือกก้อนที่มีทองเยอะกว่าเสมอ)
— ทางที่ปลอดภัยคือ "ทับเฉพาะตอนเครื่องนี้ยังไม่เคยมีความคืบหน้าอะไรเลย" ซึ่งตรงกับกรณี
ใช้งานจริงของสเปก (มือถือเครื่องใหม่/ล้างเครื่อง แล้วอยากได้ของเดิมกลับมาครบ)

### 4) `baBattleAudit().crossDeviceSync` — ทางเข้าสาธารณะสำหรับชุดเทสต์

```js
{ ver: 'v9.13', clean: boolean|null,
  cleanFields: ['classId','gold','floor','floorProgress','level','exp','maxExp','items','ab.shards','ab.abyss'],
  monoFields: ['ab.core.*','ab.seals.*.broken','ab.seals.*.flawless'] }
```

---

## สิ่งที่แพตช์นี้ไม่ได้ทำ — ตั้งใจเว้นไว้

- **ไม่แตะ `CS_CFG`/`CS_*` ของ v5.2** — ชั้นนั้นเป็นระบบ full-profile sync ที่มีอยู่แล้ว
  แยกก้อนกันโดยสิ้นเชิง และปิดอยู่โดยค่าเริ่มต้น ไม่ใช่หน้าที่ของแพตช์นี้จะไปเปิด/แก้
- **ไม่แตะ `hp`/`mp`/`shield`** — เป็นสถานะของการต่อสู้ที่กำลังดำเนินอยู่ ซิงก์ข้ามเครื่อง
  ไม่มีความหมาย (นักเรียนไม่ได้เล่นสองเครื่องพร้อมกันกลางไฟต์เดียวกัน)
- **ไม่แตะ `streak`/`recentTimes`** — สถิติของรอบการเล่นปัจจุบัน ไม่ใช่ของสะสมถาวร
- **ไม่แตะ `wordStats`/`account.skills`** — v9.9/v9.12 ทำไว้ครบแล้ว ไม่ต้องซ้ำ
- **ไม่แตะ `account.codex`** — คำนวณจาก `wordStats` อัตโนมัติอยู่แล้ว (ดูหัวข้ออาการ)
- **ไม่บล็อก `saveStore`/local save ระหว่างรอคลาวด์** ตามที่สเปกขอตรง ๆ — เหตุผล:
  1. JS เป็น single-threaded ช่วงเวลาแข่งจริงมีแค่ตรง `await` ซึ่งแบบแผนเดิมของ v9.9/v9.12
     (snapshot ก่อน → await → ตรวจ `CURRENT_USER` ซ้ำหลัง await → ค่อยเขียน) ปิดช่องนี้แล้ว
  2. ระบบ Conflict Detection ของ v5.2 (ทดสอบผ่านใน `test_cloud_sync.js` บล็อก 8) พิสูจน์แล้ว
     ว่ารูปแบบ "ซิงก์แล้วดันข้อมูลกลับหลังพบว่าคลาวด์เก่ากว่า" ปลอดภัยกว่าการล็อกทั้งระบบ
  3. บล็อก `saveStore` ทั้งระบบเสี่ยงทิ้งของที่ผู้เล่นทำระหว่างนั้นจริง (ตอบคำศัพท์ถูก ซื้อของ)
     ซึ่งขัดกับข้อกำหนด "Zero data loss" ของสเปกเองตรง ๆ

---

## ผลการตรวจสอบ

```
node build_minify.js --check
  ต้นฉบับ  3,853,536 ไบต์
  ไฟล์แจก  1,977,849 ไบต์ (−48.7% · เหลือถึงเพดาน 2,000,000 อีก 22,151 ไบต์)

node build_minify.js          → เขียนไฟล์แจกสำเร็จ

NODE_PATH=/opt/node22/lib/node_modules node test_cloud_sync.js
  ═══ PASS 104 · FAIL 0 ═══

NODE_PATH=/opt/node22/lib/node_modules node test_monarch.js
  ═══ ผ่าน 85  ตก 0 ═══
```

โครงสร้างการห่อฟังก์ชันตรวจแล้วว่าเรียงถูกลำดับ (v9.9 นิยาม → v9.12 ห่อ →
**v9.13 ห่อเป็นชั้นนอกสุด**) ไม่มีการห่อซ้อนตัวเองและไม่มีการแก้โค้ดของชั้นก่อนหน้า
ยืนยัน syntax ผ่าน `new Function()` ก่อน build ทุกครั้งตามกติกาเดิมของ repo

**เลขรุ่น v9.13 ยึดตามลำดับการห่อจริงในไฟล์ ไม่ใช่ตามที่สเปกเรียกตัวเอง** (สเปกไม่ได้
ระบุเลขรุ่นมาตรง ๆ — ตั้งตามกติกาเดิมของ repo ที่ให้เลขชั้นเดินตามลำดับการห่อ)
