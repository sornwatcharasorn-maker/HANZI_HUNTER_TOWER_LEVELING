# PATCH · RECLAIM HEADROOM AUDIT (One-Shot)

**สถานการณ์ก่อนแพตช์:** ไฟล์แจก `hanzi_hunter_tower_v3_1_intro.html` อยู่ที่
**1,999,868 ไบต์ — เหลือถึงเพดาน 2,000,000 อีกแค่ 132 ไบต์** เดินหน้าแตะไฟล์
สักตัวอักษรเดียวก็ทะลุเพดานได้ทันที

## Step 1 — ผลตรวจสอบ (อ่านอย่างเดียว)

| ก้อน | ขนาดปัจจุบัน (base64) | วิธีคืนที่ | ความเสี่ยง | ประมาณที่คืนได้ |
|---|---|---|---|---|
| `BA_ANM_DEF` + `BA_DS_ART` (สไปรต์ต่อสู้ Assassin/Blade/Slayer/Guardian/Guard/Priest/Soulmaster · 39 เฟรม) | ~232.3 KB — ฝังซ้ำที่ q52 (คุณภาพปัจจุบัน) จากศูนย์วันนี้ **ไม่พอที่แล้ว** | `embed_skill_art.py --q 40` (ขั้นบันได `[52,46,40,34]` — ต้นฉบับยังอยู่ครบ บีบรุ่นเดียว ไม่ใช่บีบทับของที่บีบมาแล้ว) | **ต่ำ-กลาง** | **~15.1 KB** ✅ ใช้จริง |
| `BA_DS_ICO` (ไอคอนสกิลบนแถบ 32 ช่อง 40×40) | ~29.2 KB (ประมาณ q42 อยู่แล้ว) | ลดเป็น q36 | ต่ำ | ~1.0–1.3 KB — เล็กเกินไปจนไม่คุ้มเปิดจุดแก้ที่สอง |
| `BA_MON` (อสูร 50 เฟรม) | รีดถึงเพดานคุณภาพที่มองเห็นได้แล้วตามที่เอกสารเดิมเตือนไว้ | ลดจานสีต่อ | **สูง** | ข้าม — จะเห็นสีแบนทันที |
| `BA_SH_ART` (ทัพเงา 50 เฟรม) | เหมือนข้างต้น | เหมือนข้างต้น | **สูง** | ข้าม |
| `CHAR_IMG_SRC` (มาสคอต) | ไม่มีไฟล์ต้นฉบับสะอาดใน repo — ฝังซ้ำ = บีบทับของที่บีบมาแล้ว | ลดคุณภาพ | **กลาง-สูง** | ได้น้อย + อัตราส่วนกับความกว้างจริงยังตึงอยู่ (~1.17 เท่า) |
| `TR_ART` (หอคอยในเรดาร์) | 416×516 ต่ำกว่าความกว้างสูงสุดที่ต้องรองรับ (448px) แล้ว 1 เท่า | ลดกว้าง/คุณภาพ | **สูง** | ข้าม — ใกล้พื้นความปลอดภัยแล้ว |
| `CD_CARDS` (การ์ด 8 ใบ) | อยู่ที่ 208px ซึ่งเป็นขั้นสุดท้ายที่เอกสารเดิมบันทึกไว้ว่าปลอดภัย | ลดกว้างต่อ | **สูง** | ข้าม |
| `MI_ART` / `ITEMS` / `SKILLS` (ไอคอนเมนู/ไอเทม/สกิลกลาง) | เล็กอยู่แล้ว (8–12 KB) และอยู่ที่คุณภาพซึ่งจับคู่กับ DPR จอจริงแล้วตามประวัติ repo | — | กลาง | ข้าม — ได้น้อยมากเทียบความเสี่ยง |
| ฟังก์ชันที่ตายแล้ว (`rollMystery()`, `MB_*` ฯลฯ) | เล็กน้อย | ลบทิ้ง | — | **ไม่ทำตามกฎเหล็กของ repo** — ห้ามลบโค้ดของชั้นล่างแม้ไม่มีใครเรียกแล้ว (สถาปัตยกรรมห่อชั้นแบบตั้งใจ) — บันทึกไว้เป็นข้อมูล ไม่ดำเนินการ |
| base64 ซ้ำกัน | — | ยุบซ้ำ | — | **ไม่มีเลย** — ไล่เทียบทั้ง 244 ก้อนแล้วไม่ซ้ำสักคู่ |

**เลือกทำ:** `embed_skill_art.py --q 40` เพียงจุดเดียว — ต้นฉบับ `.png`/`.jpg` ทุกไฟล์ยังอยู่ครบใน repo
(บีบรุ่นเดียว ไม่มีความเสี่ยงบีบทับของที่บีบมาแล้วซ้ำ) คืนที่ได้มากกว่าเป้าขั้นต่ำ >8KB ของ repo เกือบเท่าตัว

## Step 2 — สิ่งที่ทำจริง

```
python3 embed_skill_art.py --q 40
```

ย่อสไปรต์ต่อสู้ทั้ง 39 เฟรม (8 สาย/ร่าง) จาก q52 ลงเป็น **q40** ตามขั้นบันไดที่สคริปต์กำหนดไว้เอง
เขียนกลับลงต้นฉบับแล้วสั่ง build ทับไฟล์แจกให้เองอัตโนมัติ (ตามพฤติกรรมเดิมของสคริปต์)

**ไม่ได้แตะ:** โค้ดตรรกะ, CSS, HTML, ฟังก์ชันใด ๆ, ภาพก้อนอื่นทั้งหมด, และไม่ได้ลบโค้ดที่ตายแล้วสักบรรทัด

### ผลลัพธ์

| | ก่อน | หลัง |
|---|---|---|
| ต้นฉบับ | 3,925,636 ไบต์ | 3,913,520 ไบต์ |
| ไฟล์แจก | 1,999,868 ไบต์ | **1,987,752 ไบต์** |
| เหลือถึงเพดาน 2,000,000 | **132 ไบต์** | **12,248 ไบต์** |

**คืนที่ได้สุทธิ 12,116 ไบต์** (มากกว่าประมาณการ ~15.1KB ที่ base64 อย่างเดียว เพราะ terser/clean-css
ยังบีบ whitespace ที่เหลือได้อีกเล็กน้อยจากไฟล์ต้นฉบับที่เล็กลง)

## การยืนยัน

```
node build_minify.js --check
```
```
ไฟล์แจก  1,987,752 ไบต์ (−49.2% · เหลือถึงเพดาน 2,000,000 อีก 12,248 ไบต์)
```

**ชุดเทสต์ 3 ชุดตามที่สั่ง:**

| ชุด | ผลบนไฟล์แจกใหม่ | เทียบไฟล์แจกเดิม (ก่อนแก้) |
|---|---|---|
| `test_skill_dispatch.js` | ✅ 54 ผ่าน / ❌ 40 ตก | **เท่ากันเป๊ะ** — ยืนยันแล้วว่าตกอยู่แล้วก่อนแตะไฟล์ (ไม่ใช่ regression ของแพตช์นี้ — ไม่เกี่ยวกับภาพเลย เพราะแก้แค่ base64) |
| `test_monarch.js` | ✅ 85 / ❌ 0 | ตรงตามฐานเดิมที่เอกสาร repo บันทึกไว้ |
| `test_cloud_sync.js` | ✅ 104 / ❌ 0 | ตรงตามฐานเดิมที่เอกสาร repo บันทึกไว้ |

**ไม่มี pageerror สักครั้งในทุกชุด · ไม่มีจอไหนล้นแนวนอน · ความสูงการ์ดโจทย์ยัง 340.8px
ทุกจอเท่าเดิมเป๊ะ (CLS = 0) ตามที่ยืนยันในทุกชุดเทสต์ที่รัน**

## Step 3 — ดันต่ออีกรอบตามคำขอ "ปลอดภัยมากกว่านี้ มากที่สุดเท่าที่จะทำได้"

หลัง Step 2 เหลือที่ว่าง 12,248 ไบต์แล้ว ไล่ตรวจซ้ำทุกก้อนใน Step 1 อีกครั้งว่ายังมีขั้นบันได
ที่ปลอดภัย (ตามที่สคริปต์แต่ละตัวกำหนดไว้เอง) เหลืออยู่หรือไม่ — พบสองก้อนที่ยังไม่ถึงขั้นสุดท้าย
ของบันไดตัวเอง ส่วนก้อนที่เหลือทั้งหมดยืนยันแล้วว่าอยู่ที่พื้นความปลอดภัยตามเอกสาร repo

| ก้อน | ขั้นบันไดที่กำหนดไว้ | ก่อน Step 3 | ทำอะไร | หลัง Step 3 |
|---|---|---|---|---|
| `BA_ANM_DEF` + `BA_DS_ART` | `[52,46,40,34]` | q40 (จาก Step 2) | `embed_skill_art.py --q 34` — **ขั้นล่างสุดของบันได** | q34 ✅ ถึงพื้นแล้ว |
| `BA_DS_ICO` (ไอคอนสกิล 32 ช่อง) | `[55,48,42,36]` | ~q42 (ค่าเดิม) | `embed_skill_icons.py --q 36` — **ขั้นล่างสุดของบันได** | q36 ✅ ถึงพื้นแล้ว |

**ก้อนที่ตรวจแล้วและ "ไม่แตะ" เพราะเกินขั้นบันไดที่เอกสาร repo ยืนยันไว้ว่าปลอดภัย:**

| ก้อน | เหตุผลที่หยุด |
|---|---|
| `CD_CARDS` (การ์ด 8 ใบ) | ถอด PIL ตรวจจริงได้ 208×277-279px — คือขั้นล่างสุดที่ตารางประวัติ patch ของ `CLAUDE.md` บันทึกไว้ว่าปลอดภัย (260→240→224→**208px** · margin เหลือ 1.47×) ต่ำกว่านี้ไม่มีขั้นที่เอกสารรับรองแล้ว |
| `MI_ART` / `ITEMS` / ไอคอนสกิลของ `embed_icon_art.py` | เอกสาร repo ยืนยันตรง ๆ ว่า "อยู่ที่มาตรฐาน DPR 2.9 ของ v7.8 พอดี" และ "นี่คือก้อนสุดท้ายที่เอกสารรุ่นก่อนชี้ไว้ว่ายังพอรีดได้ — ใช้ไปแล้ว" ลดต่อจะต่ำกว่าความละเอียดจอจริง เห็นเบลอทันที |
| `BA_MON` / `BA_SH_ART` / `TR_ART` / `CHAR_IMG_SRC` | ยืนยันซ้ำแล้วว่ายังเป็นสถานะเดิมจาก Step 1 (รีดถึงพื้นแล้ว / ไม่มีต้นฉบับสะอาด / อัตราส่วนตึงอยู่แล้ว) |

**คืนที่ได้เพิ่มสุทธิรอบนี้ 9,432 ไบต์** (12,248 → 21,680) — รวมสองรอบคืนที่ได้ **21,548 ไบต์**
จากจุดเริ่มต้น 132 ไบต์

### ผลลัพธ์สะสม

| | เริ่มต้น | Step 2 | Step 3 (ปัจจุบัน) |
|---|---|---|---|
| ต้นฉบับ | 3,925,636 | 3,913,520 | **3,904,088 ไบต์** |
| ไฟล์แจก | 1,999,868 | 1,987,752 | **1,978,320 ไบต์** |
| เหลือถึงเพดาน | 132 | 12,248 | **21,680 ไบต์** |

### การยืนยันซ้ำหลัง Step 3

```
node build_minify.js --check
```
```
ต้นฉบับ  3,904,088 ไบต์
ไฟล์แจก  1,978,320 ไบต์  (−49.3% · เหลือถึงเพดาน 2,000,000 อีก 21,680 ไบต์)
```

**ชุดเทสต์ 3 ชุดตามที่สั่ง (รันซ้ำหลัง Step 3):**

| ชุด | ผล | เทียบฐานเดิม |
|---|---|---|
| `test_skill_dispatch.js` | ✅ 54 / ❌ 40 | เท่ากันเป๊ะกับก่อน Step 2 และก่อน Step 3 — ตกอยู่แล้วก่อนแตะไฟล์ ไม่เกี่ยวกับภาพ |
| `test_monarch.js` | ✅ 85 / ❌ 0 | ไม่เปลี่ยน |
| `test_cloud_sync.js` | ✅ 104 / ❌ 0 | ไม่เปลี่ยน |

**ชุดเทสต์เพิ่มเติมที่รันเองเพื่อความปลอดภัย** (นอกเหนือจาก 3 ชุดที่สั่งไว้ — เพราะ Step 3
แตะสไปรต์ต่อสู้โดยตรงมากกว่ารอบก่อน จึงตรวจฝั่งสนามรบ/แอนิเมชันเพิ่มเพื่อความมั่นใจ):

| ชุด | ผล | หมายเหตุ |
|---|---|---|
| `verify_arena.js` | ✅ 144 / ❌ 0 | เลย์เอาต์สนามรบ 6 ฉาก × 5 ความกว้าง ไม่มีจอไหนล้น/ขยับ |
| `test_sprite_fit.js` | ✅ 48 / ❌ 0 | สัดส่วนเวทีสไปรต์ยังตรงกับสัดส่วนภาพ 100% (ไม่ถูกครอป) ทั้ง 42 ช่อง |
| `verify_monsters.js` | ✅ 80 / ❌ 0 | อสูรประจำชั้นทั้ง 18 ตัวยังไม่บังโจทย์ ไม่ล้นแนวนอน |
| `test_skill_anim.js` | ✅ 170 / ❌ 3 | **ยืนยันแล้วว่าเป็นของเดิม** — รันชุดเดียวกันกับไฟล์ต้นฉบับก่อนแตะอะไรเลย (`git show d17af88`) ได้ 170/3 เท่ากันเป๊ะ ไม่ใช่ regression |

**ไม่มี pageerror สักครั้งในทุกชุด (รวม 6 ชุด) · ไม่มีจอไหนล้นแนวนอน · CLS = 0 ยืนยันซ้ำ**

## สรุป

- เพดานไฟล์: **ปลอดภัยแล้ว** เหลือ **21,680 ไบต์** (จากจุดเริ่มต้น 132 ไบต์ — คืนที่เพิ่มสุทธิ 21,548 ไบต์)
- ทั้งสองก้อนที่แตะ (สไปรต์ต่อสู้ + ไอคอนสกิล) อยู่ที่ **ขั้นล่างสุดของบันไดคุณภาพที่สคริปต์กำหนดไว้เอง**
  แล้ว — ไม่มีขั้นถัดไปให้ลดต่อโดยไม่ออกนอกกรอบที่ตัวสคริปต์เองรับรองว่าปลอดภัย
- ก้อนอื่นทั้งหมดถูกตรวจซ้ำและยืนยันว่าอยู่ที่พื้นความปลอดภัยตามเอกสาร repo (DPR floor / ไม่มี
  ต้นฉบับสะอาด / ประวัติ patch บันทึกไว้แล้วว่าเป็นขั้นสุดท้าย) — **จงใจไม่แตะ** เพื่อรักษาคำว่า "ปลอดภัย"
  ตามที่ผู้ใช้ระบุไว้ชัดเจน
- CLS: **0** (พิสูจน์โดย `verify_arena.js` · `test_sprite_fit.js` · `verify_monsters.js` เพิ่มเติมจากสามชุดเดิม)
- สถาปัตยกรรมไฟล์เดียว: **ไม่ถูกแตะ** ไม่มีการแยกไฟล์ ไม่มีการอ้าง path ภายนอกใหม่
- กฎห้ามลบโค้ดชั้นล่าง: **ยึดตามเดิม** ไม่มีการลบฟังก์ชันที่ตายแล้วสักบรรทัด
- 40 เคสที่ตกใน `test_skill_dispatch.js` และ 3 เคสที่ตกใน `test_skill_anim.js` เป็นของเดิมที่มีอยู่
  ก่อนแพตช์นี้ทั้งคู่ ไม่เกี่ยวกับการเปลี่ยนคุณภาพภาพ (ยืนยันด้วยการรันชุดเดียวกันกับไฟล์แจกใบเก่า
  ก่อนแตะอะไรเลย แล้วได้ตัวเลขเท่ากันเป๊ะทั้งสองชุด) — อยู่นอกขอบเขตงานนี้
