### Patch v9.19 · HIGH-IMPACT COMBO RESONANCE & SKILL CUE

อยู่ในเนมสเปซ `ba` ของชั้น v6.0 เดิม **ไม่ได้ตั้งพรีฟิกซ์ใหม่** (กติกาเดียวกับ v6.1–v9.18)
ก้อนเริ่มที่คอมเมนต์ `Patch v9.19 · HIGH-IMPACT COMBO RESONANCE & SKILL CUE` ซึ่งวางไว้
**ท้ายสุดของไฟล์ ต่อจาก `baAhsInstall()` ของแพตช์ก่อนหน้า และก่อน bootstrap block สุดท้าย**
(`try { baBuild(); } catch (e) { baErr(e, 'install'); }`) จึงห่อทับทุกชั้นได้
· **ไม่แก้โค้ดของ v4.0-v9.18 เลยแม้แต่บรรทัดเดียว** ที่เพิ่มคือ wrapper **3 ตัว**
(`resolveAnswer` · `exitGame` · `startGame`) บวก `baBattleAudit` ต่อท้ายอีกหนึ่งจุด
· เติมกฎ CSS ใหม่หนึ่งก้อน (`#baResStyle`) พร้อมคีย์เฟรม `@keyframes baResGlow`
กินที่ในไฟล์แจกราว **2,309 ไบต์** ที่ตรรกะกับ CSS ล้วน ไม่มีภาพไม่มีเสียงเพิ่มสักไบต์
· ไฟล์แจก 1,982,050 → **1,984,359 ไบต์** เหลือถึงเพดาน 2,000,000 อีก **15,641 ไบต์**

**ความสูงการ์ดโจทย์ไม่ขยับเลยแม้แต่พิกเซลเดียว** — วัดจริงยังได้ **354.8px ที่จอ 320
· 340.8px ที่จอ 360/390/430** (`test_monarch.js` 85/0 ยืนยันค่าเดิมเป๊ะทั้งสี่ความกว้าง)
เพราะทุกเอฟเฟกต์ใหม่แตะแค่ `box-shadow`/`opacity`/`filter` — **ไม่มีกฎไหนแตะ
`padding`/`margin`/`width`/`height`/`display` เลยสักบรรทัด** (CLS = 0 จริง)

##### สองคำสั่งของสเปกที่ต้องแปลก่อนลงมือ — `#baSkillDeck` และ `baPlS4` ไม่มีอยู่จริง

สเปกอ้างถึง `#baSkillDeck` และ `baPlS4` เป็นจุดเกาะ — ทั้งสองชื่อ **ไม่มีอยู่ในไฟล์เลย**
(`grep -c` ยืนยันแล้วทั้งคู่ได้ 0) ของจริงคือ:

| สเปกเรียก | ของจริงในไฟล์ |
|---|---|
| `#baSkillDeck` | `#gSkills` (`<div class="g-skills" id="gSkills"></div>`) — คอนเทนเนอร์แถบสกิลจริงที่อยู่ใน HTML แบบสแตติกตั้งแต่แรก |
| `baPlS4` | **ไม่มีฟังก์ชันชื่อนี้** — ช่องท่าไม้ตาย (slot 4) ของสายอาชีพเดินผ่าน `baPlCast()` ของ v8.5 ซึ่งมีอยู่แล้ว |
| combo counter | `G.streak` — ตัวนับคอมโบตัวจริงของ v4.0 ที่ทุกชั้นตั้งแต่ v6.9 ใช้ร่วมกัน |

แพตช์นี้จึงเกาะกับของจริงทั้งสามตัว (`#gSkills` · `baPlCast` · `G.streak`)
ไม่ได้สร้างโหนด/ฟังก์ชันปลอมขึ้นมาให้ตรงชื่อสเปก ซึ่งจะเป็นการสร้างสารบัญคู่ขนาน
ที่ไม่มีใครอ่านจริง (กับดักข้อ 35 — ห้ามสร้างของซ้ำที่ต้องซิงก์กันเอง)

##### ทำไมไม่แก้ `baDsBarHtml()` — บั๊ค `BA_DS_SIG` ที่ทำให้แก้ไม่ได้

"เส้นขอบเหลืองจาง ๆ ที่มีอยู่แล้วตอนคอมโบ ≥ 5" ที่สเปกอ้างถึงคือของ **Patch v9.18**
(`baInstallV918()`) ซึ่งห่อ `baDsBarHtml` แล้ว string-replace ใส่
`style="filter:drop-shadow(0 0 6px gold)"` ลงบน `data-ds="0">`/`data-ds="2">` ตอน
`g.streak >= 5` — วิธีนี้ใช้ไม่ได้กับสิ่งที่สเปกขอ ("breathing golden-cyan aura" ที่ต้อง
เปิด/ปิดทันทีตามคอมโบที่ขยับทุกข้อ) เพราะ **`baSyncCurrentClassSkills(force)` แคชผลไว้ที่
`BA_DS_SIG = role + '|' + slotLevels.join(',') + '|' + pips`** แล้วข้ามการวาดซ้ำ
(`innerHTML`) ถ้าลายเซ็นไม่เปลี่ยน — **ลายเซ็นนี้ไม่รวม `g.streak`/คอมโบเลย** ตอบถูกแล้ว
คอมโบขยับจาก 4 เป็น 5 จึงไม่การันตีว่าแถบสกิลจะถูกวาดใหม่ในทิกนั้น ถ้าไปเพิ่ม logic ใน
`baDsBarHtml` เอง เอฟเฟกต์จะ "มาช้า" หรือ "ค้าง" ตามจังหวะที่แคชบังเอิญ invalidate

**ทางแก้ที่ใช้จริง: ปล่อยให้ `baDsBarHtml`/`baSyncCurrentClassSkills` ทำงานตามเดิม
ทุกประการ แล้วเกาะ CSS attribute-selector บนแอตทริบิวต์ `data-ds` ที่ยืนอยู่ถาวรแทน**
— `data-ds="0"`/`data-ds="2"` ถูกเขียนลง DOM ทุกครั้งที่วาดแถบอยู่แล้ว (ยืนยันจาก
template จริงของ `baDsBarHtml`: `'<button class="g-skill ' + cls + '" data-ds="' + i + '">'`)
ชั้นนี้จึงแค่สลับ **คลาสบนตัวคอนเทนเนอร์แม่** (`#gSkills`) ด้วย
`classList.toggle('ba-res-on', on)` ทุก ~150ms ผ่านนาฬิกาของตัวเอง แล้วให้ CSS ทำงานผ่าน
`#gSkills.ba-res-on [data-ds="0"], #gSkills.ba-res-on [data-ds="2"] { animation: baResGlow ... }`
— **ไม่ต้องรอ `baDsBarHtml` วาดใหม่เลย** ลายเซ็นแคชของ v9.18 จึงไม่เกี่ยวข้องอีกต่อไป
และเอฟเฟกต์เปิด/ปิดได้ทันทีทุกครั้งที่คอมโบข้ามเส้น 5 ไม่ว่าแถบสกิลจะถูกวาดซ้ำหรือไม่

##### ทำไมใช้ `box-shadow` ล้วน ไม่แตะ `filter`

Guardrail ของสเปกอนุญาตทั้ง `box-shadow` และ `filter: drop-shadow` แต่แพตช์นี้เลือกใช้
**`box-shadow` เพียงอย่างเดียว** เพื่อไม่ต้องยุ่งกับการซ้อนทับ CSS cascade กับ inline
`style="filter:drop-shadow(0 0 6px gold)"` ที่ v9.18 เขียนสดลงปุ่มโดยตรง — CSS animation
ที่มาจากคลาสจะ "ชนะ" ค่า author declaration ปกติตาม cascade spec อยู่แล้วโดยไม่ต้องมี
`!important`, แต่การให้ทั้ง `filter` (inline, จาก v9.18) และ `box-shadow` (คลาส, จากแพตช์นี้)
ทำงานพร้อมกันคนละคุณสมบัติ CSS ทำให้ผลลัพธ์แน่นอนกว่า และ "ยกระดับ" เส้นขอบเดิมของ v9.18
ให้เป็นออร่าที่เข้มขึ้นได้จริงตามที่สเปกขอ ("upgrade the existing... border... into a vivid...
aura") โดยไม่ต้องแตะ/เขียนทับ logic ของ v9.18 เลยสักบรรทัด

##### 1) Golden-Cyan Breathing Aura — คีย์เฟรม `baResGlow`

```css
@keyframes baResGlow{
  0%,100%{box-shadow:0 0 6px 1px rgba(255,215,0,.55),0 0 2px 1px rgba(34,211,238,.35)}
  50%{box-shadow:0 0 16px 4px rgba(255,215,0,.9),0 0 8px 3px rgba(34,211,238,.7)}
}
#gSkills.ba-res-on [data-ds="0"],
#gSkills.ba-res-on [data-ds="2"]{
  animation:baResGlow 1.1s ease-in-out infinite;
  border-color:rgba(255,215,0,.85)!important
}
```

- ไล่ระดับสองสี (ทอง `rgba(255,215,0,*)` + ฟ้า cyan `rgba(34,211,238,*)`) ซ้อนกันเป็น
  double `box-shadow` เพื่อให้ได้เอฟเฟกต์ "aura" สองชั้นตามที่สเปกขอ ("golden-cyan aura")
- `border-color` ใช้ `!important` **เฉพาะจุดนี้จุดเดียว** เพื่อชนะ `border-color` ที่ v9.18
  เขียนไว้ผ่าน inline style (`style="filter:...` ไม่ได้ตั้ง border-color แต่ CSS พื้นฐาน
  ของ `.g-skill.ready{border-color:rgba(255,200,69,.6)}` มีความจำเพาะสูสีกับคลาสนี้
  — ใส่ `!important` ให้ชนะแน่นอนแทนที่จะพึ่งลำดับการประกาศ CSS ซึ่งเปราะบางกว่า)
- **ยิงเข้า `[data-ds="0"]`/`[data-ds="2"]` ตรง ๆ ไม่ใช่ `.g-skill` ทั้งหมด** เพื่อคงพฤติกรรม
  เดิมของ v9.18 ที่เลือกเฉพาะสองช่องนั้น (ช่องอื่นไม่ได้ aura)

##### 2) In-Arena Resonance Banner — ใช้ `baBannerText` ตัวเดิม ไม่สร้างกลไกใหม่

`baBannerText('⚡ RESONANCE ACTIVE', 'BONUS GOLD & EXP!')` ยิงเข้า `#baSkill` ผ่าน
กลไกเดิมของ v6.3 ทุกประการ (absolute, `opacity:0` โดย default, toggled ด้วย `.on` ผ่าน
`@keyframes baSkl`, ไม่กินพื้นที่เลย์เอาต์เลย) — **ไม่ได้เขียนแบนเนอร์/DOM ใหม่ตามที่สเปก
เสนอ `#baSkillDeck`** เพราะ `baBannerText` ครอบข้อกำหนด "eye-catching dynamic cue...
without shifting layout" ไว้ครบอยู่แล้ว (มันคือของประเภทเดียวกับที่สเปกขอเป๊ะ) การสร้าง
กลไกคู่ขนานจะเสี่ยงชนกับแบนเนอร์อื่นที่ใช้ `#baSkill` เดียวกันโดยไม่จำเป็น

- ยิงเพียง **ครั้งเดียว** ตอนคอมโบข้ามเส้น 5 จากด้านล่างขึ้นบน (edge-trigger บน
  `BA_RES_ON` state) ไม่ใช่ทุกข้อที่คอมโบยังคง ≥ 5 — กันไม่ให้แบนเนอร์เด้งรัวจนบัง
  แบนเนอร์อื่นที่สำคัญกว่า (Single Active Replace ของ v4.9.1 มีเพดาน 1 ใบพร้อมกันอยู่แล้ว)
- อายุแบนเนอร์เป็นของเดิมของ `baBannerText` (`BA_BANNER_MS + 40` = 1640ms) **ไม่ได้แก้
  ค่าคงที่นั้น**

##### 3) Instant Reset — สามทางที่ต้องดับพร้อมกันทันที

| เหตุการณ์ | ห่อที่ไหน | ทำอะไร |
|---|---|---|
| คอมโบตกต่ำกว่า 5 (ตอบผิด) | `resolveAnswer` (`try/finally`) | `baResSync()` อ่าน `G.streak` สดหลังของเดิมทำงานจบ แล้วถอด `ba-res-on` ทันที |
| หมดเวลา (turn timeout) | `resolveAnswer(null, null, true)` เส้นทางเดียวกัน | ไหลผ่าน `resolveAnswer` ปกติ จึงถูกครอบไปด้วยจุดเดียวกัน ไม่ต้องดักซ้ำ |
| error กลางทาง | `try { ... } finally { baResSync(); }` | `finally` การันตีว่า `baResSync()` ถูกเรียกแม้ `_old.apply(...)` throw — **เป็นข้อยกเว้นจากธรรมเนียมเดิมของไฟล์นี้ที่ปกติเรียก `_old.apply(...)` เปล่า ๆ ไม่มี try/catch ล้อม** ใส่ไว้เฉพาะจุดนี้เพราะสเปกสั่งชัดว่า "extinguish immediately... on turn timeout/error" ต้องการันตีแม้ error |
| ออกจากเกม / เข้าเกมใหม่ | `exitGame` · `startGame` | เรียก `baResSync()` ทันทีเพื่อล้าง class ค้าง (`ba-res-on`) ไม่ให้ตกค้างข้ามรอบเล่น |

`baResSync()` เป็นตัวประสานจุดเดียว (single source of truth) — อ่าน `G.streak` สดทุกครั้ง
ที่ถูกเรียก แล้วเทียบกับ `BA_CB_W1` (ค่าคงที่ของ v6.9 "Combo Milestone Passives" ซึ่งเป็น
5 พอดี — **ใช้ค่าคงที่นี้ตรง ๆ แทนที่จะฮาร์ดโค้ด `5` ซ้ำ** ผูกกับบาลานซ์ของ v6.9 ทำให้ถ้า
วันหนึ่งขั้น Tier-I ของ v6.9 ขยับ เกณฑ์ของแพตช์นี้ขยับตามโดยอัตโนมัติ) แล้ว
`classList.toggle('ba-res-on', want)` — toggle เป็น idempotent อยู่แล้ว เรียกซ้ำกี่ครั้ง
ก็ไม่มีผลข้างเคียง จึงเรียกได้อย่างปลอดภัยจากทุกจุดข้างต้นโดยไม่ต้องมี debounce/guard เพิ่ม

**โหมดฝึกจุดอ่อนไม่มีเอฟเฟกต์นี้** — `baResSync()` เช็ก `g.practiceMode` ก่อนเสมอ (กติกา
เดียวกับที่ v6.9 เขียนไว้ว่า "โหมดฝึกจุดอ่อนไม่มีบัฟสักขั้น") ถ้าอยู่ในโหมดฝึก จะบังคับ
`ba-res-on` ออกทันทีไม่ว่าคอมโบจะเท่าไร

##### 4) นาฬิกาของตัวเอง — ไม่เกาะ `baTick5`/`baIdleLife` เพื่อกันผลข้างเคียง

ใช้ `setInterval(baResSync, 150)` เดินอิสระของตัวเอง **ไม่ได้เกาะนาฬิกากลางที่มีอยู่แล้ว**
(`baTick5` ของ v6.5) เพราะ `baResSync()` เป็นการอ่าน+toggle-class ล้วน ไม่มีผลข้างเคียงต่อ
แอนิเมชันสไปรต์/เกจต่อสู้ใด ๆ การแยกนาฬิกาของตัวเองทำให้โค้ดอ่านง่ายและตรวจสอบได้อิสระ
โดยไม่เพิ่มความเสี่ยงชนกับตรรกะที่ `baTick5` ควบคุมอยู่แล้ว (กติกาเดียวกับกับดักข้อ 31 —
คาบ 150ms คงที่ ไม่ผูกกับ requestAnimationFrame จึงไม่ทำให้ชุดเทสต์ที่ไวต่อจังหวะเวลา
เช่น `test_dual_warp_gate.js` แกว่งเพิ่มขึ้น) `setInterval` ID ถูกเก็บไว้ที่ `BA_RES_TIMER`
เพื่อให้ตรวจสอบได้ผ่าน `baBattleAudit()` แต่ **ไม่มีการ clear/reset ระหว่างเล่น** เพราะ
เป็นนาฬิการะดับ page-lifetime เดียวกับกลไกอื่น ๆ ที่ตั้งครั้งเดียวตอนติดตั้งแพตช์

##### สิ่งที่แพตช์นี้ไม่ได้ทำ

**ไม่เพิ่มฟิลด์ในบัญชีเลย** — สถานะทั้งหมด (`BA_RES_ON`, ไทม์เมอร์) เป็นสถานะรอบการเล่น
ล้วน ไม่ต้องเซฟ ไม่มี `baResEnsure()` ไม่มีตรารุ่นระดับบัญชี และไม่ได้ห่อ `migrateAccount`
· **ไม่เพิ่ม toast · ไม่เพิ่ม gate** (ใช้แบนเนอร์ `baBannerText` ที่มีอยู่แล้วเท่านั้น
กับดักข้อ 18 — ไม่ยิง gate ระหว่างนาฬิกาต่อข้อเดินอยู่) **· ไม่เพิ่ม/ลดปุ่มสักใบ
· ไม่แทรกคอลัมน์ในตารางไหน · ไม่เพิ่มภาพหรือเสียงสักไบต์ · ไม่เพิ่ม DOM โหนดใหม่สักโหนด**
(เกาะกับ `#gSkills`/`#baSkill` ที่มีอยู่แล้วทั้งคู่) **· ไม่แตะ `Math.random` สักครั้ง**
(ไม่ใช้ RNG เลย — เกณฑ์เป็น deterministic threshold ล้วน จึงไม่เข้าข่ายกับดักข้อ 32)
· **ไม่แก้/เขียนทับ `baDsBarHtml`, `baSyncCurrentClassSkills`, หรือโค้ดของ v9.18 เลยสักบรรทัด**
· **ชื่อทั้งชุดยังไม่มีใครใช้ก่อนแพตช์นี้** (กับดักข้อ 36 — ตรวจ `grep -c` ทุกตัวก่อนตั้งชื่อ
ได้ 0 ทั้งหมด: `ba-resonance` · `BA_RES_VER` · `BA_RES_N` · `baResStyle` · `baResSync` ·
`baResErr` · `baInstallV919` · `baResGlow` · `BA_RES_ON` · `BA_RES_TIMER`)

**สวิตช์ `localStorage['yao_arena'] = 'off'` ไม่ได้ปิดแพตช์นี้** เพราะไม่มี wrapper ตัวไหน
ผูกกับกรอบสนามรบ (`#baArena`) เลย — จงใจ เพราะแถบสกิล `#gSkills` เป็น UI ระดับเกม ไม่ใช่
ของประดับในสนามรบ (คล้ายกับที่ "ห้ามเฉลย" ของ v6.3 ไม่ผูกกับสวิตช์สนามรบเช่นกัน)

##### ทางเข้าสาธารณะสำหรับชุดเทสต์

`baBattleAudit().resonance` — `ver` (`'v9.19'`) · `on` (`BA_RES_ON` ปัจจุบัน) ·
`streak` (`G.streak` สด) · `threshold` (`BA_CB_W1`) · `styled` (`#baResStyle` ถูกฉีดแล้ว
หรือยัง) · `n` (**สำเนา** ของตัวนับ `sync`/`bannerFired` — คืน reference ไม่ได้ เพราะ
ชุดเทสต์จับค่าก่อน/หลังไว้เทียบกัน แล้วจะได้ส่วนต่าง 0 ทุกครั้ง — บทเรียนของ v8.3)

##### ทดสอบ

**ไม่มีชุดเทสต์ Playwright เต็มรูปแบบแยกไฟล์ใหม่** — งานสั่งระบุชุดทดสอบที่ต้องรันไว้ตรง ๆ
สี่คำสั่ง ซึ่งครอบทั้ง build-size compliance และ regression ของสองชั้นที่เกี่ยวข้องมากที่สุด
(cloud sync ไม่ถูกแตะเลย, monarch ยืนยัน CLS=0 โดยตรง, skill dispatch ยืนยันว่าแถบสกิล
ยังทำงานถูกต้องหลังแพตช์)

```
node build_minify.js --check
node test_cloud_sync.js
node test_monarch.js
node test_skill_dispatch.js
```

| คำสั่ง | ผล |
|---|---|
| `build_minify.js --check` | ผ่าน — ไฟล์แจก 1,984,359 ไบต์ (เพิ่มขึ้น 2,309 ไบต์จากเดิม 1,982,050) เหลือถึงเพดาน 2,000,000 อีก **15,641 ไบต์** |
| `test_cloud_sync.js` | **104/0 PASS** — ไม่มี regression เลย (แพตช์นี้ไม่แตะ cloud sync แต่รันตามที่งานสั่ง) |
| `test_monarch.js` | **85/0 PASS** — รวมยืนยัน CLS=0 ตรง ๆ: ความสูงการ์ดโจทย์เท่าเดิมทุกความกว้าง (354.8px @ 320px · 340.8px @ 360/390/430px) |
| `test_skill_dispatch.js` | 54 PASS / 40 FAIL |

**⚠️ 40 เคสที่ตกของ `test_skill_dispatch.js` เป็นของเดิมทั้งหมด ไม่เกี่ยวกับแพตช์นี้** —
พิสูจน์ด้วยการเทียบ baseline อย่างเข้มงวด: ดึงไฟล์แจกใบก่อนแพตช์นี้ (`git show HEAD:
hanzi_hunter_tower_v3_1_intro.html`) มาไว้ที่ `/tmp/baseline_check/` แล้วรัน
`test_skill_dispatch.js` ชุดเดียวกันกับไฟล์นั้น (สคริปต์ resolve พาธด้วย
`path.resolve(__dirname, ...)` จึงชี้ไปยังไฟล์ baseline โดยอัตโนมัติเมื่อรันจากโฟลเดอร์
นั้น) ได้ **54/40 เท่ากันเป๊ะ** แล้วสกัดบรรทัด `❌` ของทั้งสองฝั่งมาเรียงและ `diff` กัน
— **ผลลัพธ์เหมือนกันทุกตัวอักษร ไม่มีผลต่างเลยสักบรรทัด** ยืนยันว่าทั้ง 40 ความล้มเหลว
มีอยู่ก่อนแพตช์นี้แล้วทั้งหมด (เป็นความไม่ตรงกันของค่าดาเมจ/ชื่อในเมทริกซ์ `BA_DS_FX`
จากยุค v9.17/v9.18 ซึ่ง `PATCH_SKILL_4PILLARS_INCENTIVE.md` เองก็บันทึกไว้แล้วว่าเคย
ยืนยันย้อนไปถึง commit `ba7609d` ว่าเป็นบั๊คที่มีมาก่อน v9.18 — เป็นความรู้ที่บันทึกซ้ำ
ในหลายแพตช์ ไม่ใช่สิ่งที่แพตช์นี้ต้องแก้) **แพตช์นี้เพิ่ม wrapper ให้ `resolveAnswer` ·
`exitGame` · `startGame` · `baBattleAudit` เท่านั้น ไม่ได้แตะ `BA_DS_FX`/skill-matrix
เลยสักตัวเลข จึงไม่มีทางเป็นสาเหตุของ 40 เคสนี้**
