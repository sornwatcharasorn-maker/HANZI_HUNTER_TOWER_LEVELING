# Patch v9.16.1 · CHROMAKEY ANGEL SPRITE (TRUE ALPHA) & 10S LIFECYCLE

แก้ในที่ของ Patch v9.16 (ป้ายเลเวลอัปทูตสวรรค์) โดยตรง — ไม่ได้ตั้งชั้นใหม่ เพราะ v9.16
เป็นแพตช์เฉพาะกิจของ session นี้เองที่ยังไม่ถูก merge เข้าไฟล์หลัก ไม่ใช่ส่วนหนึ่งของสาย
ประวัติศาสตร์ v4.0-v9.15 ที่ผูกกับชุดเทสต์เดิม การแก้ในที่จึงปลอดภัยกว่าการซ้อนชั้นทับ

## ปัญหาที่แก้

ภาพ `level up effec JPG.jpg` ที่ฝังไว้ใน v9.16 มีกล่องดำเห็นได้ชัดรอบตัวทูตสวรรค์
ทั้งที่ใช้ `mix-blend-mode:screen` ไปแล้ว เพราะ:

1. `mix-blend-mode:screen` ต้องพึ่งสีของพื้นหลังใต้มันถึงจะล้างสีดำออกได้สมบูรณ์ —
   พื้นสนามรบไม่ได้ดำสนิทเสมอไป กล่องดำจึงโผล่เป็นเงาจาง ๆ
2. การบีบอัด WebP แบบ lossy ทิ้งค่าพิกเซลตกค้างที่ไม่ใช่ศูนย์ไว้ตามขอบภาพ ยิ่งทำให้
   ขอบดำเห็นชัดขึ้น

## ทางแก้ — True Alpha Chroma-Key

### 1) Soft chroma-key ด้วย alpha ramp เชิงเส้น

```
HARD_CUT = 18.0   (max(r,g,b) < 18 → alpha 0 ทันที)
SOFT_TOP = 46.0   (ไล่ระดับความโปร่งใสจนถึงจุดนี้)
alpha = clamp((max(r,g,b) - HARD_CUT) / (SOFT_TOP - HARD_CUT), 0, 1)
```

ให้ขอบภาพที่เคยเป็นเงาดำแข็ง ๆ กลายเป็นการไล่โทนโปร่งใสที่นุ่มนวล แทนที่จะตัดขาดที่
ค่าเดียว ซึ่งจะทิ้งรอยหยักคมไว้ตามขอบเส้นผมและปีก

### 2) Premultiplied-alpha resize — ตัวแก้ขอบดำจริง ๆ

การย่อภาพ RGBA ทั่วไป (LANCZOS ตรง ๆ) จะเบลอค่าสี RGB ของพิกเซลโปร่งใส (ที่ยังมีค่า
สีดำเดิมติดอยู่) เข้าไปปนกับพิกเซลทึบข้างเคียง เกิดเป็นรอยพรมดำจาง ๆ รอบขอบ (dark
fringing) แม้ค่า alpha channel เองจะถูกต้องแล้ว

ทางแก้คือ **premultiply สี RGB ด้วย alpha ก่อนย่อ** (RGB ×= alpha) แล้วค่อย
**un-premultiply กลับ** หลังย่อเสร็จ (RGB /= alpha โดยหนีบ alpha ไม่ให้ต่ำกว่า
epsilon `1e-4` กันหารด้วยศูนย์) — พิสูจน์แล้วด้วยภาพจริงว่าไม่มีรอยดำเหลือทั้งบนพื้น
น้ำเงินเข้มและพื้นแดงสด

### 3) ภาพใหม่ — 128×128 RGBA WebP q70

วัดขนาดจริงที่ 128×128: q90=16,188 · q85=14,728 · q80=13,712 · q75=12,864 ·
**q70=12,544 ไบต์** (base64) — เลือก q70 เพราะแยกด้วยตาไม่ออกจาก q90 ที่ขนาดแสดงผล
จริงบนจอ แต่ประหยัดที่ในไฟล์แจกได้มากกว่า

`BA_V916_ANGEL_SRC` ถูกแทนที่ทั้งก้อนด้วยภาพใหม่นี้ · `mix-blend-mode:screen` และ
`isolation:isolate` ถูกถอดออกทั้งหมด (พึ่ง native alpha channel ล้วน ไม่ต้องพึ่ง
blend mode อีกต่อไป)

## Lifecycle ใหม่ — 5.0s → 10.0s

| ช่วง | สัดส่วนเวลา (คิดจาก keyframe %) | ทำอะไร |
|---|---|---|
| 0.0-0.5s | 0% → 5% | ลอยขึ้น + จางเข้า (`opacity 0→1` · `translate3d(0,26px,0) scale(.72)` → `translate3d(0,0,0) scale(1)`) |
| 0.5-8.5s | 5% → 85% | ลอยนิ่งเหนือหัวฮีโร่ (มองเห็นชัดนาน 8 วินาที) |
| 8.5-10.0s | 85% → 100% | ลอยสูงขึ้นอีกนิด + จางออก แล้วลบตัวเองออกจาก DOM |

Keyframe percentages คำนวณจากสัดส่วนเวลาจริง (0.0/0.5/8.5/10.0 ÷ 10s) ไม่ใช่การเอา
เปอร์เซ็นต์เดิมของเวอร์ชัน 5 วินาที (0%/10%/80%/100%) มาคูณสอง

- `.ba-angel-fx` animation: `5000ms` → **`10000ms`**
- `#baAngelFlash.on` animation: `5000ms` → **`10000ms`**
- `#baAngelFlash.on img` animation: `5000ms` → **`10000ms`**
- `baSpawn(html, life)` ใน `baV916ArenaFx` และ `setTimeout` ใน `baV916FallbackFx`:
  `5000` → **`10000`**

ทุกอนิเมชันยังเป็น `transform`/`opacity` ล้วน (`will-change:transform,opacity`)
· `pointer-events:none` ทั้งชุด · ไม่มี DOM node ใหม่ที่ไม่ล้างตัวเอง (`baSpawn` ลบให้
อัตโนมัติ) · **CLS = 0** — ไม่มีกฎไหนแตะขนาด/ตำแหน่งของสิ่งอื่นในหน้าเล่นเลย

## ที่มา/ที่ไปของขนาดไฟล์

ที่ว่างก่อนแก้ (v9.16): 7,370 ไบต์ · ภาพเดิมกินไป 5,836 ไบต์ (ถูกแทนที่) → งบใหม่
ที่ใช้ได้ = 7,370 + 5,836 = 13,206 ไบต์ · ภาพใหม่ (q70) กิน 12,544 ไบต์ เหลือ margin
~662 ไบต์สำหรับส่วนต่างของโค้ด (CSS/คอมเมนต์ที่เปลี่ยน)

**ผลจริงหลัง build:** ไฟล์แจก **1,999,380 ไบต์** เหลือถึงเพดาน 2,000,000 อีก
**620 ไบต์**

## ผลทดสอบ

### คำสั่งตรวจที่กำหนดไว้ (ทั้งสามต้องผ่าน)

```
node build_minify.js --check   →  1,999,380 ไบต์ (< 2,000,000 ✓)
node test_cloud_sync.js        →  PASS 104 / FAIL 0 ✓
node test_monarch.js           →  ผ่าน 85 / ตก 0 ✓
```

### เทสต์เสริมที่รันเพิ่มเพื่อยืนยันไม่มี regression

```
node verify_arena.js       →  ผ่าน 144 / ตก 0 (CLS=0 ยืนยันแล้ว)
node test_audio_engine.js  →  PASS 79 / FAIL 1
```

เคสที่ตกใน `test_audio_engine.js` คือ `ไอเทมอื่น → SFX_ITEM_USE` ซึ่งเป็นเคสตกที่มีอยู่
แล้วก่อนแพตช์นี้ (บันทึกไว้ใน `CLAUDE.md` ว่าตัวนับของสคริปต์นี้เองมี `❌` อยู่ในบรรทัด
สรุปด้วย ทำให้นับซ้ำ) ไม่ใช่ regression จากแพตช์นี้

### ยืนยันภาพจริงด้วย Playwright (สคริปต์เฉพาะกิจ)

- **t=0.2s** — สไปรต์ทูตสวรรค์ขึ้นจอชัดเจน ปีก/รัศมีเป็นเส้นสะอาด **ไม่มีกล่องดำ**
  กลืนเข้ากับพื้นหลังสนามรบที่มืดได้สนิท
- **t=2.0s** — ช่วงลอยนิ่งเต็มความทึบ ยังไม่มีกล่องดำหรือรอยพรมดำตามขอบ
- **t=5.0s** — ยังคงอยู่บนจอ (จุดที่เวอร์ชัน 5 วินาทีเดิมจะจางหายไปแล้วโดยสมบูรณ์)
- **t=9.0s** — ตรวจสอบ DOM/computed style โดยตรง: element ยังอยู่จริง (`inDom:true`)
  วางเลย์เอาต์อยู่ (`offsetParentNull:false`) แต่ opacity ต่ำมาก (~0.056) ตามเส้นโค้ง
  `cubic-bezier(.22,.68,.32,1)` ที่เร่งความคืบหน้าให้เร็วในช่วงต้นของแต่ละ segment —
  ไม่ใช่บั๊ค เป็นพฤติกรรมที่ถูกต้องของเส้นโค้งจางหายที่ออกแบบไว้
- **t=10.5s** — DOM node ถูกลบออกเองครบถ้วน (`arenaImgGone:true`)
- **ไม่มี horizontal overflow** (`document.body.scrollWidth <= window.innerWidth`)
- เส้นทางที่ทำงานจริงคือ arena rendering path (ไม่ใช่ fallback/legacy) ยืนยันผ่าน
  `baBattleAudit().angelFx` (`ver:'v9.16.1'`)

## ไฟล์ที่แก้

- `src/hanzi_hunter_tower_v3_1_intro.src.html` — ต้นฉบับ แก้บล็อก `baLvUpFx` ในที่
- `hanzi_hunter_tower_v3_1_intro.html` — ไฟล์แจก build ใหม่จากต้นฉบับข้างบน
