# Patch v9.16 · RO ANGEL SPRITE — 5-SECOND LEVEL-UP OVERLAY

อยู่ในเนมสเปซ `ba` ของชั้น v6.0 เดิม **ไม่ได้ตั้งพรีฟิกซ์ใหม่** (กติกาเดียวกับ v6.1–v9.15)
วางท้ายสุดของไฟล์ ต่อจาก `baInstallV915()` (Patch v9.15 · RO-STYLE LEVEL-UP ASCENSION FX)
และก่อน `baInstall()` จึงห่อทับทุกชั้นได้
· **ไม่แก้โค้ดของ v4.0-v9.15 เลยแม้แต่บรรทัดเดียว** ที่เพิ่มคือการ **เขียนทับ binding**
ของ `baLvUpFx` ตัวเดียว (เก็บของเดิมไว้เป็นทางสำรองชั้นสุดท้าย) กับ wrapper อีก 1 ตัว
(`baBattleAudit`) บวกฟังก์ชันใหม่ล้วน 3 ตัว (`baV916Style` · `baV916ArenaFx` ·
`baV916FallbackFx`)

**ขนาดหลังแพตช์** — ต้นฉบับ 3,905,921 ไบต์ → ไฟล์แจก **1,992,630 ไบต์**
เหลือถึงเพดาน 2,000,000 อีก **7,370 ไบต์**

## สิ่งที่ผู้ใช้สั่งมา — และสิ่งที่แพตช์นี้ทำจริง

| ข้อสั่งงาน | ทำจริงยังไง |
|---|---|
| 1. ฝังสไปรต์ `level up effec JPG.jpg` เป็น base64 | ย่อเหลือ 128×128 → เข้ารหัส WebP lossy q85 → 5,836 ไบต์ base64 (ดูเหตุผลด้านล่าง) |
| 2. เอฟเฟกต์เลเวลอัป 5.0 วินาที ผูกกับ `levelUpCheck` เหนือ `baSpot('baHero')` | แทนที่ระบบภาพของ v9.15 (ปีก CSS ล้วน 1.2 วิ) ด้วยภาพสไปรต์จริง 5.0 วิ ผ่านการเขียนทับ `baLvUpFx` — **ไม่แตะ `levelUpCheck` เลย** (ดูเหตุผลด้านล่าง) |
| 3. Verify & Deliver | `node build_minify.js --check` < 2MB ✓ · `node test_cloud_sync.js` 104/0 ✓ · `node test_monarch.js` 85/0 ✓ · ส่งไฟล์ครบ 3 ใบ |

## ทำไมห่อ `baLvUpFx` แทนที่จะห่อ `levelUpCheck` อีกชั้น — จุดที่พลาดง่ายที่สุดของแพตช์นี้

`levelUpCheck()` ของ v4.0 ถูกเรียกทุกครั้งที่ได้ EXP (แทบทุกข้อที่ตอบถูก) ไม่ใช่แค่ตอน
เลื่อนระดับจริง — v9.15 แก้ปัญหานี้ไปแล้วด้วยการเทียบ `g.level` ก่อน/หลังเรียกของเดิม
แล้วเรียก `baLvUpFx()` **เฉพาะตอนขยับจริง** (กับดักข้อ 44 ในเอกสารประกอบ repo)

**ถ้าแพตช์นี้เพิ่ม wrapper ของ `levelUpCheck` อีกชั้น จะได้เอฟเฟกต์ยิงซ้อนสองครั้งต่อการ
อัปเลเวลหนึ่งครั้ง** — เดิมของ v9.15 ยิงหนึ่งครั้ง (ปีก CSS) แล้วชั้นนี้ยิงอีกหนึ่งครั้ง (ภาพ
สไปรต์) พร้อมกัน ซึ่งเป็นบั๊คตระกูลเดียวกับ MP หักซ้ำสองเด้งของ v9.1 (กับดักข้อ 43)

**อ่านซอร์สของ v9.15 แล้วพบว่า `baLvUpFx()` ถูกเรียกผ่านชื่อระดับบนสุดล้วน**
(bare top-level identifier ไม่ใช่ closure ที่จับ reference ไว้ตั้งแต่ตอนติดตั้ง) จึงเขียนทับ
`baLvUpFx` ตัวเดียวได้อย่างปลอดภัย — wrapper ของ `levelUpCheck` ที่ v9.15 เขียนไว้ยังเรียก
`baLvUpFx()` ชื่อเดิม ซึ่งตอนนี้ชี้ไปที่ฟังก์ชันใหม่ของแพตช์นี้แล้วโดยอัตโนมัติ **ไม่ต้องแก้
โค้ดของ v9.15 สักบรรทัด**

```js
if (typeof baLvUpFx === 'function') {
  const _v916LvUpFx = baLvUpFx;      /* ของเดิมของ v9.15 เก็บไว้เป็นทางสำรองชั้นสุดท้าย */
  baLvUpFx = function () {
    try { baV916Style(); } catch (e) { baV916Err(e, 'style'); }
    let done = false;
    try {
      if (baCanPlay()) {
        const spot = baSpot('baHero');
        if (spot) done = !!baV916ArenaFx(spot);
      }
    } catch (e) { baV916Err(e, 'arena'); }
    if (done) { BA_V916_N.arena++; return; }
    try { baV916FallbackFx(); BA_V916_N.fallback++; return; } catch (e) { baV916Err(e, 'fallback'); }
    try { _v916LvUpFx(); BA_V916_N.legacy++; } catch (e) { baV916Err(e, 'legacy'); }
  };
}
```

## การเข้ารหัสภาพ — ทำไมเลือก Lossy WebP แทนที่จะเป็น Lossless แบบที่ repo เคยแนะนำ

เอกสารประกอบ repo (CLAUDE.md) เขียนไว้ว่า pixel art พื้นเรียบ/ขอบคมมักย่อด้วย
**lossless WebP + ลดจำนวนสี** ได้เล็กกว่า lossy (เช่น `embed_monster_art.py`
· `embed_hero_art.py`) — **แต่ภาพนี้เป็นข้อยกเว้นที่วัดแล้วตรงกันข้าม**

ต้นฉบับ `level up effec JPG.jpg` (1264×1264 · JPEG) เป็นพื้นดำสนิทเกือบทั้งภาพ
มีแค่ตัวอสูรทองคำ/ขาวลอยตรงกลางที่เป็นแสงไล่เฉด (soft glow) ไม่ใช่ขอบคม
**พื้นดำล้วนบีบด้วย lossy WebP ได้เกือบเป็นศูนย์บิต** (block-based codec ยุบบล็อกสีเดียว
ทั้งบล็อกทันที) ส่วนแสงไล่เฉดก็เป็นสิ่งที่ lossy ทำได้ดีอยู่แล้ว (ต่างจาก pixel art ที่มีขอบคม
ซึ่ง lossy จะสร้าง ringing รอบขอบ)

วัดจริงด้วยสคริปต์ Pillow ไล่ความละเอียด/คุณภาพหลายชุด — 128×128 คุณภาพ 85
ให้ผลดีที่สุดในกรอบงบที่มี:

| ขนาด | คุณภาพ | ไบต์ดิบ | ไบต์ base64 |
|---|---|---|---|
| **128×128** | **q85** | **4,376** | **5,836** |
| 128×128 | q70 | — | 4,040 |
| 160×160 | q85 | — | สูงกว่างบ |

**เลือก q85 แทน q70 ที่เล็กกว่า** เพราะที่ว่างในไฟล์แจกก่อนแพตช์นี้เหลือ 15,731 ไบต์
5,836 ไบต์กินไปแค่ 37% ของงบ เหลือพื้นที่มากพอสำหรับโค้ด JS/CSS ที่เหลือ โดยไม่ต้อง
บีบภาพจนคุณภาพต่ำเกินจำเป็น (ยืนยันด้วยภาพตัวอย่างที่ถอดออกมาดู — ปีก/รัศมี/ขนนก/
ประกาย/ลำแสง ยังชัดเจนครบทุกองค์ประกอบหลังย่อ)

`mix-blend-mode: screen` ใช้แทนช่องอัลฟา — พื้นดำผ่านสูตร `screen(black, X) = X`
ทางคณิตศาสตร์ จึงโปร่งใสไปเองโดยไม่ต้องเก็บ alpha channel เพิ่ม ลดไบต์ได้อีกชั้นหนึ่ง
โดยไม่กระทบภาพที่เห็นเลย

## `isolation: isolate` — ทำไมต้องมีในเส้นทางสำรอง แต่ไม่ต้องมีในสนามรบ

| บริบท | มี `isolation:isolate` ไหม | ทำไม |
|---|---|---|
| ในสนามรบ (`#baFx`) | ไม่ต้อง | ไม่มีพื้นทึบของตัวเองมาแย่ง blend — ภาพผสานกับฉากสนามที่มีอยู่แล้วโดยอัตโนมัติ |
| แฟลชเต็มจอ (`#baAngelFlash`) | **ต้องมี** | ต้องมีพื้นควบคุมได้ของตัวเอง (`rgba(4,4,10,.6)`) ไม่งั้นภาพจะ screen-blend กับพื้นหลังจริงของหน้าเว็บ (อาจเป็นสีขาว/ไม่แน่นอน) เสี่ยงภาพจางหายไปเลย |

## Timeline 5.0 วินาที — คีย์เฟรมชุดเดียวใช้ร่วมกันสองเส้นทาง

| ช่วงเวลา | สัดส่วนคีย์เฟรม | ทำอะไร |
|---|---|---|
| 0–0.5 วิ | 0% → 10% | ลอยขึ้นจากใต้ + จางเข้า (`translate3d(0,26px,0) scale(.72)` → `translate3d(0,0,0) scale(1)`) |
| 0.5–4.0 วิ | 10% → 80% | ลอยนิ่งเหนือหัว (ขยับเล็กน้อย `scale(1.02)` ให้รู้สึกมีชีวิต) |
| 4.0–5.0 วิ | 80% → 100% | ลอยขึ้นต่อ + จางออก (`translate3d(0,-58px,0) scale(1.08)`) แล้วลบตัวเองออกจาก DOM |

**คีย์เฟรม `@keyframes baAngelRise` ตัวเดียวถูกใช้ทั้งสองเส้นทาง** เพราะแตะแค่
`transform`/`opacity` ซึ่งประกอบ (compose) ได้อย่างอิสระไม่ว่าแต่ละบริบทจะตั้งตำแหน่งฐาน
(`left`/`top` แบบ inline สำหรับสนามรบ · `margin` แบบ static สำหรับแฟลชเต็มจอ) ต่างกันแค่ไหน

## GPU-only + CLS=0

ทั้งสองเส้นทางเป็น `position:absolute`/`fixed` + `pointer-events:none` แตะแค่
`opacity`/`transform` (ผ่าน `will-change:transform,opacity`) — ไม่มีกฎไหนแตะ
`width`/`height`/`margin`/`padding` ของอะไรที่มีอยู่แล้วเลยสักบรรทัด

**ไม่มี `animation:infinite` สักตัว** (กับดักข้อ 31) — คีย์เฟรมเป็น `forwards` จบตัวเองที่
5.0 วินาทีคงที่ทั้งสองเส้นทาง ไม่มีงานที่วิ่งค้างจนไปแย่ง CPU กับชุดเทสต์ที่จับเวลา
(`test_dual_warp_gate.js` ยังนิ่งที่ค่าปกติเดิม)

`baSpot('baHero')` ของ v6.0 วัดตำแหน่งจริงทุกครั้ง ไม่ฮาร์ดโค้ดพิกัด (กรอบสนามกว้างไม่เท่ากัน
ทุกจอ) แล้วหนีบขอบซ้าย-ขวาด้วยสูตรเดียวกับที่ `baFloat()` ใช้กันข้อความลอยหลุดขอบจอแคบ:
`Math.max(4, Math.min(spot.w - w - 4, spot.x - w / 2))` — ใช้ `baSpawn()`/`baLater()` ของ
v6.0 สร้าง+ล้างอัตโนมัติ ผูกกับ `baClearFx()` ให้เก็บกวาดครบเสมอ ไม่ต้องเขียนตัวล้างเอง

วัดจริงแล้วการ์ดโจทย์ยังคง 340.8px/354.8px เท่าเดิมทุกจอ ไม่ล้นแนวนอน ยืนยันด้วย
`verify_arena.js` 144/0 และ `test_monarch.js` บล็อก 8 (CLS=0 ครบ 4 ความกว้าง)

## ไม่เพิ่มเสียงใหม่ — `SFX_LEVEL_UP` ยังเป็นของ v5.1 เหมือนเดิม

`AU_SFX_DEF.SFX_LEVEL_UP` มีอยู่แล้วตั้งแต่ v5.1 · DYNAMIC AUDIO ENGINE และยิงถูกจังหวะ
อยู่แล้วจาก wrapper ของ `levelUpCheck` ที่นั่น — แพตช์นี้ไม่แตะเสียงเลยสักบรรทัด
ยืนยันด้วย `test_audio_engine.js` ที่เคส `เลเวลอัป → SFX_LEVEL_UP` ยังผ่านปกติ
(ยิงครั้งเดียวเหมือนเดิม ไม่ซ้ำสอง)

## สิ่งที่แพตช์นี้ไม่ได้ทำ

**ไม่เพิ่มฟิลด์ในบัญชีเลย · ไม่เพิ่ม toast · ไม่เพิ่ม gate · ไม่เพิ่มปุ่มสักใบ · ไม่แทรกคอลัมน์
ในตารางไหน · ไม่เพิ่มเสียงสักไบต์ (SFX_LEVEL_UP ของ v5.1 ยังทำหน้าที่เดิม ไม่ซ้ำสอง) ·
ไม่เพิ่ม DOM สักโหนดที่ไม่ล้างตัวเอง · ไม่แตะ `Math.random` สักครั้ง** (กับดักข้อ 32)
ไม่มี `baV916Ensure()` ไม่มีตรารุ่นระดับบัญชี และไม่ได้ห่อ `migrateAccount`
· `BA_V916_N` เป็นสถานะของรอบการเล่นทั้งหมด (ตัวนับล้วน ไม่ต้องเซฟ)
· **ไม่ได้แก้โค้ดของ v9.15 (หรือชั้นใดก่อนหน้า) เลยสักบรรทัด** — เขียนทับแค่ binding
ของ `baLvUpFx` ซึ่งเป็นชื่อระดับบนสุดที่ v9.15 เรียกอยู่แล้ว
· **ชื่อทั้งชุดยังไม่มีใครใช้ก่อนแพตช์นี้** (กับดักข้อ 36 —
`grep -c 'BA_V916\|baV916\|ba-v916\|ba-angel\|baAngelFx\|BA_ANGEL\|baAngelFlash'` ได้ 0
ก่อนตั้งชื่อ)

**สวิตช์ `localStorage['yao_arena'] = 'off'` ไม่ได้ปิดเสียงหรือการเลื่อนระดับ** — ปิดแค่
เส้นทางแสดงผลในสนามรบ แล้วตกไปใช้ fallback เต็มจอโดยอัตโนมัติ (จงใจ เพราะ `baCanPlay()`
คืนเท็จเมื่อ `BA_READY`/`baArena()` ไม่พร้อมอยู่แล้ว — พฤติกรรมเดียวกับ v9.15)

## ทางเข้าสาธารณะสำหรับชุดเทสต์

`baBattleAudit().angelFx` — `ver` (`'v9.16'`) · `arena`/`fallback`/`legacy` (**สำเนา** ของ
ตัวนับ — คืน reference ไม่ได้ เพราะชุดเทสต์จับค่าก่อน/หลังไว้เทียบกัน แล้วจะได้ส่วนต่าง 0
ทุกครั้ง — บทเรียนของ v8.3) · `styled` (`#baV916Style` ถูกฉีดแล้วหรือยัง)

## ผลการทดสอบ

| คำสั่ง | ผล |
|---|---|
| `node build_minify.js --check` | ไฟล์แจก 1,992,630 ไบต์ (< 2,000,000, เหลือ 7,370 ไบต์) — ผ่าน |
| `node test_cloud_sync.js` | 104 ผ่าน · 0 ตก |
| `node test_monarch.js` | 85 ผ่าน · 0 ตก (รวมบล็อก 8 CLS=0 ครบ 4 ความกว้าง) |
| `node verify_arena.js` | 144 ผ่าน · 0 ตก (CLS=0 ครบ 6 ความกว้าง) |
| `node test_audio_engine.js` | 79 ผ่าน · 1 ตก — **ตกเดิมอยู่แล้วก่อนแพตช์นี้** (`❌ ไอเทมอื่น → SFX_ITEM_USE`) เคส `เลเวลอัป → SFX_LEVEL_UP` ยังผ่านปกติ ไม่ใช่ regression |
| `new Function()` บนไฟล์แจก | parse ผ่าน ไม่มี syntax error (สคริปต์ยาว 1,769,826 อักขระ) |
| ตรวจภาพจริงด้วย Playwright | ยิง `baLvUpFx()` โดยตรง → สไปรต์ทองคำลอยขึ้นเหนือหัวฮีโร่ในสนามรบจริง ปีก/รัศมีชัดเจน ไม่ล้นแนวนอน ลบตัวเองครบหลัง 5.2 วิ |

ไม่มี pageerror ในทุกชุดเทสต์ที่รัน ไม่มีการล้นแนวนอนในทุกความกว้างที่วัด
