# Patch v9.15 · RO-STYLE LEVEL-UP ASCENSION FX

อยู่ในเนมสเปซ `ba` ของชั้น v6.0 เดิม **ไม่ได้ตั้งพรีฟิกซ์ใหม่** (กติกาเดียวกับ v6.1–v9.14)
วางท้ายสุดของไฟล์ ต่อจาก `baInstallV914()` (Patch v9.14 · ABYSS TOGGLE RESYNC & DEDICATED-ABYSS
CLOUD MERGE) และก่อน `baInstall()` จึงห่อทับทุกชั้นได้
· **ไม่แก้โค้ดของ v4.0-v9.14 เลยแม้แต่บรรทัดเดียว** ที่เพิ่มคือ wrapper **2 ตัว**
(`levelUpCheck` · `baBattleAudit`) กับฟังก์ชันใหม่ล้วน 4 ตัว (`baLvUpStyle` · `baLvUpArenaFx`
· `baLvUpFallbackFx` · `baLvUpFx`)

**ขนาดหลังแพตช์** — ต้นฉบับ 3,879,714 ไบต์ → ไฟล์แจก **1,984,269 ไบต์**
เหลือถึงเพดาน 2,000,000 อีก **15,731 ไบต์**

## สิ่งที่ผู้ใช้สั่งมา 3 ข้อ — และสิ่งที่แพตช์นี้ทำจริง

| ข้อสั่งงาน | ทำจริงยังไง |
|---|---|
| 1. Level-Up Visuals (RO Angel Rising) | ทำใหม่ทั้งหมด — เสาแสงทองคำ + ปีกกางออกลอยขึ้น (`translate3d(0,-40px,0)`) จางหายใน ~1.2 วิ |
| 2. Level-Up Audio (RO Chime Synthesis) | **ไม่เพิ่มโค้ดใหม่ — มีอยู่แล้วตั้งแต่ชั้น v5.1** (ดูหัวข้อถัดไป) |
| 3. Verify & Deliver | build < 2MB ✓ · `test_cloud_sync.js` 104/0 ✓ · `test_monarch.js` 85/0 ✓ · ส่งไฟล์ครบ 3 ใบ |

## ทำไมไม่เพิ่มเสียงใหม่ — เสียงอยู่แล้วตั้งแต่ v5.1

ไล่โค้ดทั้งไฟล์ก่อนเขียนแพตช์นี้ พบว่า **`AU_SFX_DEF.SFX_LEVEL_UP` มีอยู่แล้ว** และถูกยิง
ถูกจังหวะอยู่แล้วจาก wrapper ของ `levelUpCheck` ในชั้น v5.1 · DYNAMIC AUDIO ENGINE —
เทียบ `G.level` ก่อน/หลังเรียกของเดิม แล้วยิงเฉพาะตอนขยับจริง ผ่าน `auPlay()` ซึ่งเช็ก
`AU_MUTE` และเขียนค่าลง `localStorage`/บัญชีให้เองอยู่แล้ว

**การเพิ่มโค้ดสังเคราะห์เสียงใหม่ในแพตช์นี้จะทำให้เสียงดังซ้อนสองครั้งต่อการอัปเลเวลหนึ่งครั้ง**
ซึ่งเป็นบั๊คตระกูลเดียวกับ MP หักซ้ำสองเด้งของ v9.1 (กับดักข้อ 43 ใน CLAUDE.md) และ toast
ซ้อนของ v4.9.1 — ทั้งสองกรณีเกิดจาก wrapper ใหม่ที่ยิงผลข้างเคียงซ้ำกับ wrapper เดิมที่ทำ
หน้าที่นั้นอยู่แล้ว แพตช์นี้จึงมีแค่ภาพ ไม่แตะเสียงเลยสักบรรทัด — ยืนยันด้วย
`test_audio_engine.js` ที่เคส `เลเวลอัป → SFX_LEVEL_UP` ยังผ่านปกติ (ยิงครั้งเดียวเหมือนเดิม)

## จุดดักมีจุดเดียว — เทียบเลเวลก่อน/หลังเหมือน v5.1 เป๊ะ

`levelUpCheck()` ของ v4.0 ถูกเรียกทุกครั้งที่ได้ EXP (แทบทุกข้อที่ตอบถูก) ไม่ใช่แค่ตอน
เลื่อนระดับจริง **ต้องเทียบ `g.level` ก่อน/หลังเรียกของเดิมเสมอ** ไม่ใช่ถือว่า "ถูกเรียก =
อัปเลเวลแล้ว" (กับดักข้อ 44 ใน CLAUDE.md — บั๊คเดียวกับที่ v9.4 ต้องแก้ใน
`baDsHook('level')` ของ v8.8) · wrapper ของแพตช์นี้เป็นชั้นนอกสุดเพราะถูกห่อทีหลังสุด —
พอเรียก `_v915Lv.apply(...)` จบ `g.level` จึงเป็นค่าสุดท้ายหลังผ่านเพดาน 99 ของ v8.6
และเอฟเฟกต์เสียง/คลาวด์ซิงก์ของชั้นก่อนหน้าทำงานไปหมดแล้ว

```js
if (typeof levelUpCheck === 'function') {
  const _v915Lv = levelUpCheck;
  levelUpCheck = function () {
    const g = baG();
    const before = g ? g.level : 0;
    const r = _v915Lv.apply(this, arguments);
    try { if (g && g.level > before) baLvUpFx(); } catch (e) { baV915Err(e, 'levelUpCheck'); }
    return r;
  };
}
```

## สองเส้นทางแสดงผล — ในสนามรบ กับ fallback เต็มจอ

`levelUpCheck()` ถูกเรียกได้จากหลายบริบท (ต่อสู้ · รางวัลเคลียร์ชั้น · รับเควส · เลือกการ์ด)
ซึ่งบางบริบทไม่มีสนามรบให้แขวนภาพ (`yao_arena='off'` หรือ `BA_READY` ยังไม่พร้อม) —
`baLvUpFx()` จึงเช็ก `baCanPlay()` ก่อนเสมอ

| เงื่อนไข | ทำอะไร |
|---|---|
| `baCanPlay()` เป็นจริง + `baSpot('baHero')` หาตำแหน่งได้ | วาดเสาแสง+ปีกลอยขึ้นเหนือหัวฮีโร่ ผ่าน `baSpawn()` ของ v6.0 ลง `#baFx` |
| อย่างอื่นทั้งหมด | แฟลชทองคำเต็มจอ (`#baLvUpFlash`) — ท่าเดียวกับ `baMnFlash()` ของ v8.0 |

**เส้นทางในสนามใช้ `baSpot('baHero')` ของ v6.0 วัดตำแหน่งจริงทุกครั้ง** ไม่ฮาร์ดโค้ดพิกัด
(กรอบสนามกว้างไม่เท่ากันทุกจอ) · `baSpawn(html, 1300)` ของ v6.0 สร้าง+ล้างอัตโนมัติให้เอง
ผูกกับ `baLater()` แล้ว `baClearFx()` เก็บกวาดให้ครบเสมอ ไม่ต้องเขียนตัวล้างเอง

**ทั้งสองเส้นทางเป็น `position:absolute`/`fixed` + `pointer-events:none` แตะแค่
`opacity`/`transform` (GPU ล้วน) ไม่มีกฎไหนแตะ `width`/`height`/`margin`/`padding` ของ
อะไรที่มีอยู่แล้วเลยสักบรรทัด — CLS = 0** (วัดจริงแล้วการ์ดโจทย์ยังคง 340.8px/354.8px
เท่าเดิมทุกจอ — ยืนยันด้วย `verify_arena.js` 144/0 และ `test_monarch.js` บล็อก 8)

## ปีกซ้าย-ขวาต้องมีคีย์เฟรมแยกกันคนละชุด

`transform` เป็น property เดียวที่รับค่าได้ครั้งละหนึ่งชุด — จะเอา `scaleX(-1)` เป็นค่าตั้งต้น
คงที่ (static base transform) แล้วให้ keyframe ของอนิเมชันมาอนิเมท `transform` ทับอีกทีไม่ได้
เพราะ keyframe จะเขียนทับค่าตั้งต้นทั้งก้อน ปีกขวาจึงมีคีย์เฟรมของตัวเอง (`baLvWR`) ที่ผสาน
`scaleX(-1)` ไว้ในทุกสเตจของมันเอง แทนที่จะยืมคีย์เฟรมของปีกซ้าย (`baLvWL`)

## ไม่มี `animation:infinite` สักตัว (กับดักข้อ 31)

ทุกคีย์เฟรมเป็น `forwards` จบตัวเองที่ระยะเวลาคงที่ (1.2 วิ) ทั้งสองเส้นทาง — ไม่มีงานที่
วิ่งค้างจนไปแย่ง CPU กับชุดเทสต์ที่จับเวลาแบบ `test_dual_warp_gate.js`

## สิ่งที่แพตช์นี้ไม่ได้ทำ

**ไม่เพิ่มฟิลด์ในบัญชีเลย · ไม่เพิ่ม toast · ไม่เพิ่ม gate · ไม่เพิ่มปุ่มสักใบ · ไม่แทรกคอลัมน์
ในตารางไหน · ไม่เพิ่มภาพหรือเสียงสักไบต์ · ไม่เพิ่ม DOM สักโหนดที่ไม่ล้างตัวเอง · ไม่แตะ
`Math.random` สักครั้ง** (กับดักข้อ 32) ไม่มี `baLvUpEnsure()` ไม่มีตรารุ่นระดับบัญชี และไม่ได้
ห่อ `migrateAccount` · `BA_V915_N` เป็นสถานะของรอบการเล่นทั้งหมด (ตัวนับล้วน ไม่ต้องเซฟ)
· **ชื่อทั้งชุดยังไม่มีใครใช้ก่อนแพตช์นี้** (กับดักข้อ 36 —
`grep -c 'BA_V915\|baV915\|baLvUp\|ba-lvup'` ได้ 0 ก่อนตั้งชื่อ)

**สวิตช์ `localStorage['yao_arena'] = 'off'` ไม่ได้ปิดเสียงหรือการเลื่อนระดับ** — ปิดแค่
เส้นทางแสดงผลในสนามรบ แล้วตกไปใช้ fallback เต็มจอโดยอัตโนมัติ (จงใจ เพราะ `baCanPlay()`
คืนเท็จเมื่อ `BA_READY`/`baArena()` ไม่พร้อมอยู่แล้ว)

## ทางเข้าสาธารณะสำหรับชุดเทสต์

`baBattleAudit().levelUpFx` — `ver` (`'v9.15'`) · `arena`/`fallback` (**สำเนา** ของตัวนับ —
คืน reference ไม่ได้ เพราะชุดเทสต์จับค่าก่อน/หลังไว้เทียบกัน แล้วจะได้ส่วนต่าง 0 ทุกครั้ง —
บทเรียนของ v8.3) · `styled` (`#baV915Style` ถูกฉีดแล้วหรือยัง)

## ผลการทดสอบ

| คำสั่ง | ผล |
|---|---|
| `node build_minify.js --check` | ไฟล์แจก 1,984,269 ไบต์ (< 2,000,000) — ผ่าน |
| `node test_cloud_sync.js` | 104 ผ่าน · 0 ตก |
| `node test_monarch.js` | 85 ผ่าน · 0 ตก (รวมบล็อก 8 CLS=0 ครบ 4 ความกว้าง) |
| `node verify_arena.js` | 144 ผ่าน · 0 ตก (CLS=0 ครบ 6 ความกว้าง) |
| `node test_lv_scaling.js` | 81 ผ่าน · 0 ตก |
| `node test_progression.js` | 92 ผ่าน · 0 ตก |
| `node test_audio_engine.js` | 79 ผ่าน · 1 ตก — **ตกเดิมอยู่แล้วก่อนแพตช์นี้** (`❌ ไอเทมอื่น → SFX_ITEM_USE`) เคส `เลเวลอัป → SFX_LEVEL_UP` ยังผ่านปกติ ไม่ใช่ regression |
| `new Function()` บนไฟล์แจก | parse ผ่าน ไม่มี syntax error |

ไม่มี pageerror ในทุกชุดเทสต์ที่รัน ไม่มีการล้นแนวนอนในทุกความกว้างที่วัด
