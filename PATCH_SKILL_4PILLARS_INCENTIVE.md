### Patch v9.18 · 4-PILLAR SKILL INCENTIVE ENGINE

อยู่ในเนมสเปซ `ba` ของชั้น v6.0 เดิม **ไม่ได้ตั้งพรีฟิกซ์ใหม่** (กติกาเดียวกับ v6.1–v9.17)
ก้อนเริ่มที่คอมเมนต์ `Patch v9.18 · 4-PILLAR SKILL INCENTIVE ENGINE` ซึ่งวางไว้ **ท้ายสุดของไฟล์
ต่อจาก `baInstallV917()` และก่อน bootstrap `try { baBuild(); } catch (e) { baErr(e, 'install'); }`**
จึงห่อทับทุกชั้นก่อนหน้าได้ครบ · **ไม่แก้โค้ดของ v4.0-v9.17 เลยแม้แต่บรรทัดเดียว**
ที่เพิ่มคือ wrapper **7 ตัว** (`baDsBarHtml` · `hunterAtk` · `cdHitMonster` · `cdExecute` ·
`baPlCast` · `resolveAnswer` · `onMonsterDefeated`) กินที่ในไฟล์แจกรวม **1,999,868 ไบต์**
เหลือถึงเพดาน 2,000,000 อีก **132 ไบต์** — เหลือน้อยที่สุดเท่าที่เคยบันทึกไว้ในเอกสารนี้
**แพตช์ถัดไปแทบแน่นอนว่าต้องคืนที่ก่อน และคืนที่ "ที่ภาพ" เท่านั้น** ตามกติกาเดิม

**ความสูงการ์ดโจทย์ไม่ขยับเลยแม้แต่พิกเซลเดียว** — ไม่มี DOM ใหม่ ไม่มี CSS class ใหม่สักคลาส
ทั้งแพตช์ ทุกเอฟเฟกต์ยืมของเดิมทั้งหมด (`baFloat` · `baBannerText` · `baSpot` · `baLater`)

#### ⚠️ ข้อจำกัดไบต์บีบให้ต้องรีดโค้ดสองรอบ — บันทึกไว้กันเดินซ้ำ

ร่างแรกของแพตช์นี้ (มีฟังก์ชัน `baV918Err()` แยกต่างหากสำหรับ log error ทุกจุด · เรียก
`baFlash()` พร้อม CSS gradient ยาวตอนฟันจบด้วยสกิล · ข้อความไทยยาวกว่านี้) ทำให้ไฟล์แจก
ทะลุเพดานไป **649 ไบต์** (`2,000,649` ไบต์) แม้เปิด HARD minify แล้ว

**สิ่งที่ตัดออก — ทั้งหมดเป็นการรีดโค้ด ไม่ใช่การรีดภาพ (กติกาเดิมของ repo: เพดานที่ตัน
ต้องคืนที่ "ที่ภาพ" เท่านั้น แต่แพตช์นี้เป็นตรรกะล้วนไม่มีภาพให้แตะ จึงรีดที่โค้ดของตัวเองแทน)**

- ตัด `baV918Err(e, where)` ทิ้ง แล้วเปลี่ยนทุกจุด `catch` เป็น `catch (e) {}` เปล่า ๆ
  (กติกาเดียวกับหลายชั้นก่อนหน้าที่ไม่ผูก Error Log ทุกจุดเมื่อพื้นที่ตึง)
- ตัดเอฟเฟกต์หน้าจอวาบ (`baFlash`) ของแบนเนอร์ฟันจบด้วยสกิลออก — สเปกสั่งแค่ "center banner"
  ไม่ได้สั่งแฟลชหน้าจอ จึงตัดได้โดยไม่ผิดสเปก
- รวมโค้ด "ลอย 🛡️ BLOCK ดีเลย์" ที่เคยเขียนซ้ำสามที่ (ใน `hunterAtk`/`cdHitMonster`/`cdExecute`)
  เป็นฟังก์ชันร่วม `baV918Blk(delay)` ตัวเดียว
- รวมตัวเช็ก "เกราะยังตั้งอยู่ไหม" ที่เคยแยกสองฟังก์ชันเป็น `baV918Gate(g)` ตัวเดียว
  ให้ทั้งสามจุด (`hunterAtk`/`cdHitMonster`/`cdExecute`) เรียกร่วมกัน
- ย่อชื่อตัวแปรท้องถิ่นและข้อความสั้นลง — **ปลอดภัยเพราะ `mangle.toplevel:false` ป้องกันแค่
  ชื่อระดับบนสุด** ตัวแปร/ฟังก์ชันที่ประกาศ**ข้างใน** IIFE ของแพตช์นี้ (ไม่ได้ผูกกับชื่อ
  ระดับบนสุด) ยังถูก terser ย่อทับเป็นชื่อสั้น ๆ ตามปกติอยู่ดี — **ตัวที่กินที่จริงคือปริมาณ
  ตรรกะ/ข้อความ ไม่ใช่ความยาวชื่อตัวแปรที่อ่านง่าย**

หลังรีดแล้วไฟล์แจก **1,999,868 ไบต์** ผ่านเพดานพอดี — วัดจริงด้วย `node build_minify.js --check`
ก่อนเขียนทับจริงเสมอ (กติกาเดิมของ repo)

#### สี่เสาหลัก — ทุกเสาห่อทับฟังก์ชันของชั้นก่อนหน้า ไม่แก้ในที่สักบรรทัด

##### เสา 1 · Combo Resonance (คอมโบ ≥ 5)

**Strict Streak Lock — ทำโดยไม่ต้องเขียนโค้ดเลย** สเปกสั่งว่าการร่ายสกิล S1/S3 **ห้าม**
รีเซ็ต/กินคอมโบไม่ว่ากรณีใด — แพตช์นี้ **ไม่มีโค้ดจุดไหนเขียนลง `g.streak`/`G.streak`
เลยสักบรรทัด** พฤติกรรมเดิมของ v4.0 (คอมโบบวกทุกครั้งที่ตอบถูก ไม่ว่าจะใช้สกิลหรือไม่)
จึงไม่ถูกแตะและทำงานถูกต้องโดยอัตโนมัติ

| ผล | ทำที่ไหน |
|---|---|
| ปุ่มช่อง 1/3 เรืองทองเมื่อคอมโบ ≥ 5 | wrapper ของ `baDsBarHtml` — regex string-replace แทรก `style="filter:drop-shadow(...)"` เข้า tag ปุ่มที่มี `data-ds="0">`/`data-ds="2">` โดยตรง (**ไม่มีคลาส CSS ใหม่**) |
| ดาเมจสกิล S1/S3 ×1.3 | wrapper ของ `hunterAtk` — เฉพาะตอน `armed` (ช่อง 0/2 ติดอาวุธจริง) และ `g.streak >= 5` |
| คืน MP 50% ของราคาที่จ่ายไป | wrapper ของ `resolveAnswer` — หลังตอบถูกด้วยสกิลที่ armed อยู่ |

**การเรืองแสงผูกกับปุ่มด้วย string-replace บนแอตทริบิวต์ `data-ds="N">` ที่มีอยู่แล้ว**
(ยืนยันจาก `baDsBarHtml()` ของ v8.8 — เทมเพลตปุ่มคือ `'<button class="g-skill ' + cls +
'" data-ds="' + i + '">'`) ไม่ต้องสร้างคลาส `.ba-v918-glow` ขึ้นมาใหม่เลย

##### เสา 2 · Proportional Shield Break

**กติกา:** ดาเมจปกติ (ตอบถูกธรรมดา) และการ์ดเวทมนตร์ (v4.7) ทะลุเกราะ/บาเรียไม่ได้เลย
(`🛡️ BLOCKED`) — **มีแต่สกิลติดอาวุธ (S1/S3-atk/S4) เท่านั้นที่กัดเกราะได้ตามสัดส่วนดาเมจจริง**

`baV918Gate(g)` เป็นตัวเช็กร่วมจุดเดียว: `BA_BAR` มีอยู่ · ยังไม่แตก (`!BA_BAR.broken`) ·
เป็นก้อนของมอนสเตอร์ตัวที่กำลังสู้อยู่จริง (`BA_BAR.m === g.currentMonster`) · และยัง
มีเกราะเหลือ (`baBarLeft() > 0`) — สามจุดต่อไปนี้เรียกมันร่วมกัน:

| จุดบล็อก | ทำอะไร |
|---|---|
| `hunterAtk` | ถ้าเกราะยังตั้งอยู่และ **ไม่ armed** สกิล S1/S3 → คืนดาเมจ 0 ทันที + ลอย `🛡️ BLOCK` |
| `cdHitMonster` (ดาเมจจากการ์ดเวทมนตร์) | ถ้าเกราะยังตั้งอยู่และ**ไม่ได้กำลังร่าย S4** → คืน 0 ก่อนเรียกของเดิมเลย ไม่ปล่อยให้การ์ดกัดเกราะ |
| `cdExecute` (สังหารทันทีจากการ์ด) | เงื่อนไขเดียวกัน — บล็อกการสังหารทันทีไม่ให้ทะลุเกราะ |

**"SHIELD CRUSHED" ไม่ต้องเขียนโค้ดใหม่เลยสักบรรทัด** — เกราะของ v6.4/v6.7 (`BA_BAR`) มี
`baBarSync()` เฝ้าอยู่แล้วทุกครั้งที่ `renderMonsterHp()` ทำงาน พอ HP มอนสเตอร์ทะลุ `BA_BAR.base`
(จากดาเมจสกิลที่ปล่อยผ่านมา) มันจะเรียก `baBreakFx()` ของตัวเองอัตโนมัติ ซึ่งมีทั้งแบนเนอร์
`💥 ARMOR BREAK — เกราะแตกสลาย` · เอฟเฟกต์ประกาย · จอสั่น · แฟลชครบอยู่แล้วตั้งแต่ v6.4/v6.7
แพตช์นี้แค่ **เปิดทาง** ให้ดาเมจสกิลไหลเข้าไปถึงจุดนั้นได้เท่านั้น

**ธง `BA_SK4_CASTING`** — ตั้งเป็น `true` ตลอดสาย `baPlCast()` (ท่าไม้ตายช่อง 4) เพื่อให้
`cdHitMonster`/`cdExecute` ที่สกิล S4 บางแบบอาจเรียกผ่าน ทะลุเกราะได้เหมือน S1/S3
คืนค่าเดิมใน `finally` เสมอ (กันซ้อนเรียก)

##### เสา 3 · Skill Bounty

ตอบถูกด้วยสกิลติดอาวุธ (S1/S3) สำเร็จ → เกจท่าไม้ตาย (`baPlPips()`) **+2** ทันที
(หนีบเพดานเองผ่าน `baPlPipSet(g, n)` ซึ่งรับทั้ง `g` และ `n` — ไม่ต้องหนีบเอง)
บวก **20%** โอกาส (`baRand(100) < 20` — ใช้ `baRand` ของ v6.1 **ไม่ใช่** `Math.random`
กันชนกับชุดเทสต์ที่ยึด `Math.random` ไว้เป็นลำดับค่าคงที่) ลอยข้อความ `💎+1` เหนือฮีโร่
แล้วให้ 1 เศษคริสตัลผ่าน `baQdGem(1)` (ตัวจ่ายกลางของ Micro-Patch เควสประจำวัน — เคารพ
`AB_SHARD_CAP` เอง) มีทางสำรองยัด `g.ab.shards` ตรง ๆ ถ้า `baQdGem` ไม่พร้อม

**ทั้งหมดทำใน wrapper ของ `resolveAnswer` จุดเดียว** — จับ `slot` (ช่อง 0 หรือ 2 ที่ armed
อยู่ตอนก่อนเรียกของเดิม เพราะ v9.1's wrapper จะเคลียร์ธง ARM **หลัง** จบ resolveAnswer
ทั้งสาย) แล้วเช็ก `g.correct` ขยับขึ้นจริงหลังเรียกของเดิม (กันนับซ้ำตอนตอบผิด)

##### เสา 4 · Skill Finisher

ฟันจบมอนสเตอร์ด้วยสกิลใด ๆ (S1/S3/S4) → **+25%** โบนัสทอง/EXP ของรางวัลที่เพิ่งจ่ายจริง
พร้อมแบนเนอร์กลางจอ `💥 SKILL FINISHER! 💥`

**จุดสังเกตคือ `BA_SK4_PENDING`** — ปั๊มเป็น `true` ที่สามจุด (หลังปล่อยผ่าน `cdHitMonster`/
`cdExecute` ตอน `BA_SK4_CASTING`, หรือหลัง `resolveAnswer` ตอนตอบถูกด้วย S1/S3 แล้ว
`monsterHp` ตกจาก >0 ลง ≤0) แล้ว `onMonsterDefeated` wrapper อ่านธงนี้ก่อนรีเซ็ตมันทิ้ง

**ทำไมต้องวัดคร่อมทั้งสาย `onMonsterDefeated` ไม่ใช่ `resolveAnswer`/`baPlCast` ตรง ๆ** —
รางวัลทอง/EXP ของการเคลียร์ชั้น/บอส (150/60 ปกติ · 500/250 บอส) จ่ายแบบซิงโครนัส
**ข้างใน** `clearFloor()` ซึ่งถูกเรียกจาก `onMonsterDefeated()` เท่านั้น (ตัว
`onMonsterDefeated` เองถูกหน่วง 1100ms ผ่าน `scheduleTurn` จากภายใน `cdHitMonster`/
`cdExecute` — แต่เมื่อมันถูกเรียกจริง มันเรียก `clearFloor` แบบซิงโครนัสในทิกเดียวกัน)
วัดคร่อมทั้งสายจึงจับส่วนต่างทองได้ตรง ไม่พลาด

**ยืมตัวช่วยของ v4.9.2 คำนวณส่วนต่าง EXP** (`dwExpGained(g, lv0, e0, m0)`) เพราะ
`levelUpCheck()` หัก `maxExp` ทุกครั้งที่ขึ้นเลเวล ทำให้ส่วนต่าง `g.exp` ดิบ ๆ ติดลบได้
ถ้าข้ามเลเวลระหว่างจ่ายรางวัล — ตัวช่วยนี้ไล่บวกเพดานของทุกขั้นที่ข้ามคืนให้ถูกต้อง
· จ่าย EXP ผ่าน `baXpPay(eb)` (ทางเดียวที่เคารพ Flag ห้ามฟื้นของ 💀 พันธสัญญาโลหิตทมิฬ)
· ทองบวกเข้า `g.gold` และ `g.totalGoldEarned` ตรง ๆ (มาจากการไต่หอคอยจริง)

**ฟันจบด้วยการ์ด/ดาเมจปกติ (ไม่ใช่สกิล) จะไม่ตั้ง `BA_SK4_PENDING` เลย** — โบนัสนี้จึง
เป็น 0 อย่างเงียบ ๆ (ไม่มีแบนเนอร์) ตามที่สเปกกำหนดว่าต้องเป็น "ฟันจบด้วยสกิล" เท่านั้น

#### สิ่งที่แพตช์นี้ไม่ได้ทำ

**ไม่เพิ่มฟิลด์ในบัญชีเลย · ไม่เพิ่ม toast · ไม่เพิ่ม gate · ไม่เพิ่มปุ่มสักใบ · ไม่แทรก
คอลัมน์ในตารางไหน · ไม่เพิ่มภาพหรือเสียงสักไบต์ · ไม่เพิ่ม DOM สักโหนด · ไม่เพิ่มคลาส CSS
ใหม่สักชื่อ** ไม่มี `baV918Ensure()` ไม่มีตรารุ่นระดับบัญชี และไม่ได้ห่อ `migrateAccount`
· `BA_SK4_CASTING` / `BA_SK4_PENDING` เป็นสถานะของรอบการเล่นทั้งหมด (ตัวแปรล้วน ไม่ต้องเซฟ)

**สวิตช์ `localStorage['yao_arena'] = 'off'` ไม่ได้ปิดแพตช์นี้ทั้งหมด** — เสา 2/3/4 ผูกกับ
`resolveAnswer`/`cdHitMonster`/`cdExecute`/`onMonsterDefeated` ซึ่งไม่ผูกกับกรอบสนามรบ
(จงใจ เพราะเป็นกติกาความเสี่ยง/รางวัลของเกม ไม่ใช่ของประดับ — ท่าเดียวกับ "ห้ามเฉลย"
ของ v6.3) มีแต่เสา 1a (การเรืองแสงบนปุ่ม) ที่ไม่กระทบอะไรถ้าปิดสนามรบเพราะแถบสกิล
`#gSkills` เป็นคนละส่วนกับกรอบสนาม

#### ⚠️ ผลที่ตามมาที่ต้องรู้ — ดาเมจสกิลกลายเป็นทางเดียวที่กัดเกราะได้

ก่อนแพตช์นี้ ดาเมจปกติกัดเกราะได้ตามปกติ (เกราะเป็นแค่ HP ชั้นนอกที่บวกทับ) — **หลังแพตช์นี้
ดาเมจปกติ (ตอบถูกธรรมดา) และการ์ดเวทมนตร์ถูกบล็อกไม่ให้กัดเกราะเลย 100%** ผู้เล่นที่ไม่มี
สกิลติดอาวุธพร้อมใช้ (คูลดาวน์/MP ไม่พอ) จะตีเกราะไม่ลงเลยจนกว่าจะใช้สกิลได้อีกครั้ง
**นี่คือกติกาที่สเปกสั่งไว้ตรงตัว** ("Normal answers ... deal 0 damage to monster
barrier/shields") ไม่ใช่ผลข้างเคียงที่ไม่ได้ตั้งใจ — แต่เป็นการเปลี่ยนบาลานซ์ที่แรงมาก
เทียบกับกติกาเกราะเดิมของ v6.4/v6.7 **ถ้าจะผ่อนให้ดาเมจปกติกัดเกราะได้บางส่วน ต้องแก้ที่
`hunterAtk` wrapper จุดเดียว (เงื่อนไข `baV918Gate(g) && !armed`)**

#### ทดสอบ

**ไม่มีชุดเทสต์ใหม่แยกไฟล์** — งานที่มอบหมายระบุให้รันสามชุดที่มีอยู่แล้วเป็นตัวตรวจสอบ
การรวมเข้าระบบ (regression) แทน:

```
node build_minify.js --check   → ผ่าน (1,999,868 ไบต์ · เหลือ 132 ไบต์ถึงเพดาน)
node test_cloud_sync.js        → ผ่าน 104 / ตก 0
node test_monarch.js           → ผ่าน 85 / ตก 0
node test_skill_dispatch.js    → ผ่าน 54 / ตก 40
```

**⚠️ 40 เคสที่ตกใน `test_skill_dispatch.js` เป็นของเดิมที่ตกอยู่แล้วก่อนแพตช์นี้ — ไม่ใช่
regression ของ Patch v9.18** ยืนยันด้วยการ `git stash` เอาไฟล์กลับไปที่ commit ล่าสุด
(`ba7609d` · Patch v9.17) แล้วรันชุดเดียวกัน ได้ผลเป๊ะ **54/40 เท่ากันทุกเคส** — ทั้ง 40
เคสที่ตกเป็นเรื่องค่าตัวเลขในเมทริกซ์ `BA_DS_FX` (ของ v8.8) กับสูตร `baPlS1`/`baPlUltAmt`/
`baPlSlotText` (ของ v8.5) ซึ่งเป็นคนละระบบกับที่ Patch v9.18 แตะ (`hunterAtk` · `cdHitMonster`
· `cdExecute` · `baPlCast` · `resolveAnswer` · `onMonsterDefeated` · `baDsBarHtml`)
เมทริกซ์ที่ทดสอบไว้ในไฟล์เทสต์ (ค่า `lo`/`hi` ต่อช่อง) กับค่าที่เกมใช้จริงตอนนี้ไม่ตรงกัน
มาตั้งแต่ก่อนแพตช์นี้แล้ว — **บั๊คนี้เป็นของ patch ก่อนหน้า (น่าจะ v9.17 ที่แก้ตัวเลขในเมทริกซ์
ไปโดยไม่ได้อัปเดตชุดเทสต์คู่กัน) ไม่ใช่งานของแพตช์นี้ที่จะไปแก้** — บล็อกที่เกี่ยวกับสี่เสาหลัก
ของ v9.18 เอง (บล็อก 8 "ห้ามแตะ Math.random" และบล็อก 9 "CLS=0") **ผ่านครบ 100%** ทั้งก่อน
และหลังแพตช์นี้เหมือนกัน ยืนยันว่าโค้ดใหม่ไม่ได้ทำให้อะไรเสียเพิ่ม
