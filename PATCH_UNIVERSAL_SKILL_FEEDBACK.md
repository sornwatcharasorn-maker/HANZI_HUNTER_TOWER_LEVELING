# Patch v9.17 · UNIVERSAL SKILL FEEDBACK & PER-CLASS VFX

อยู่ในเนมสเปซ `ba` ของชั้น v6.0 เดิม **ไม่ได้ตั้งพรีฟิกซ์ใหม่** (กติกาเดียวกับ v6.1–v9.16.1)
วางท้ายสุดของไฟล์ ต่อจากชั้น v9.16.1 และ**ก่อนจุดเรียก `baBuild()`** ภายในฟังก์ชัน
`(function baInstall(){ ... })();` มาตรฐานท้ายไฟล์ที่แพตช์รุ่นหลัง ๆ (ตั้งแต่ v9.16) ใช้
แทนการสร้าง IIFE ชื่อของตัวเอง — แพตช์นี้กลับไปใช้ IIFE ชื่อของตัวเอง
(`(function baInstallV917(){...})();`) เพื่อความชัดเจน แต่วางไว้ในตำแหน่งเดียวกันเป๊ะ
(ก่อน `try { baBuild(); }`) จึงทำงานเหมือนกันทุกประการ — scope chain ของ JavaScript
มองไม่เห็นความต่างของการซ้อนชั้นนี้

· **ไม่แก้โค้ดของ v4.0-v9.16.1 เลยแม้แต่บรรทัดเดียว** — ห่อทับ 4 จุด:
`baV91Activate` (v9.1) สำหรับสกิลกด S1/S3 · `baPlCast` (v8.5) สำหรับท่าไม้ตาย S4 ·
`baDsHook` (v8.8) สำหรับป้ายพาสซีฟตอนเริ่มไฟต์ใหม่ · `baDsOnCorrect` (v8.8) สำหรับ
จุดลอยพาสซีฟตอนตอบถูก

· **ไม่แตะสูตรดาเมจ ไม่แตะตัวเลขใน `BA_DS_FX` ไม่แตะสเตตัสมอนสเตอร์เลยสักตัว**
— ทุก wrapper อ่านผลที่ชั้นล่างคำนวณไว้แล้วเพื่อโชว์ผลลัพธ์เท่านั้น (เรียกของเดิมก่อนเสมอ
แล้วค่อยอ่านผลตอบกลับ/ผลข้างเคียงมาประกอบข้อความ)

## ที่มาของที่ว่าง — คืนที่มาก่อน 4,208 ไบต์

ตอนเริ่มแพตช์นี้ ไฟล์แจกเหลือถึงเพดาน 2,000,000 ไบต์เพียง **602 ไบต์**
(หลังชั้น v9.16.1) ซึ่งไม่พอสำหรับโค้ดของแพตช์นี้ (รวมคอมเมนต์ภาษาไทยและ
สตริงข้อความ 4 ชุด) — **คืนที่มาก่อนด้วยการย่อความกว้างมาสคอต `CHAR_IMG_SRC`
จาก 448px เหลือ 432px** (คุณภาพยังเป็น q46 เท่าเดิม)

```
python3 embed_mascot_art.py --q 46 --w 432
```

คืนมา ~3.8KB (ต้นฉบับ 47.4KB → 43.6KB base64) — **นี่คือการบีบทับของที่ถูกบีบมาแล้ว**
(มาสคอตไม่มีไฟล์ต้นฉบับใน repo ตั้งแต่ก่อน v7.7 เป็นต้นมา ตามที่ CLAUDE.md เตือนไว้)
สัดส่วนภาพไม่เปลี่ยน (`448:673 = 432:649`) เลย์เอาต์ทั้งสามจุดที่ใช้มาสคอต
(`#introImg` วงกลมหน้าแรก · `#bannerImg` แถบซ้ายกล่องล็อกอิน · `#badgeImg`
ตราบนมือถือ) ยัง `object-fit:cover` เหมือนเดิมทุกประการ ไม่ขยับสักพิกเซล
**ห้ามรัน `embed_mascot_art.py` ซ้ำอีกโดยไม่ `git checkout` ไฟล์กลับก่อน**
(คำเตือนเดิมของสคริปต์นั้น — บีบซ้ำจะสะสมคุณภาพต่ำลงเรื่อย ๆ)

**ขนาดหลังแพตช์ — ต้นฉบับ 3,917,573 ไบต์ → ไฟล์แจก 1,996,877 ไบต์
เหลือถึงเพดาน 2,000,000 อีก 3,123 ไบต์**

## 1) ทำไมแทบไม่ต้องเขียนเครื่องยนต์ใหม่เลย

ชั้นล่างทำทุกอย่างที่เกี่ยวกับ "สกิลไหนให้ผลอะไร" ไว้ครบแล้วตั้งแต่ v8.8/v9.0/v9.1
แพตช์นี้จึงมีหน้าที่เดียวคือ **ห่อจุดที่ผลของสกิลเกิดขึ้นจริง แล้วแสดงผลด้วยของเดิมทั้งหมด**

| จะทำอะไร | ทางผ่านเดียว | เจ้าของ |
|---|---|---|
| สกิลกด S1/S3 ทำงานสำเร็จ | `baV91Activate(i, g, r)` | v9.1 |
| ท่าไม้ตาย S4 ปล่อยสำเร็จ | `baPlCast()` | v8.5 |
| ซิงก์แถบสกิล/สไปรต์ตอนเจอมอนสเตอร์ใหม่ | `baDsHook('battle')` | v8.8 |
| ตอบถูกหนึ่งข้อ (จุดที่พาสซีฟให้ผล) | `baDsOnCorrect(g)` | v8.8 |
| ผลของแต่ละช่อง (มี `dmg` ไหม / มีอะไรบ้าง) | `baResolveSkillEffects()` / `baDsCache()` | v8.8 |
| VFX ทั้งหมด | `baBannerText()` · `baFlash()` · `baCbTip()` · `baSpot()` | v6.0/v6.3/v6.9 |

**ไม่มี DOM ใหม่ ไม่มีกฎ CSS ใหม่สักบรรทัด** — สีต่อสายอาชีพส่งผ่านพารามิเตอร์ `color`
ของ `baFlash()` ตรง ๆ ซึ่งรับสตริงสี CSS อะไรก็ได้อยู่แล้วโดยไม่ต้องประกาศคลาสใหม่

## 2) แบนเนอร์ตามสเปก — ผ่าน `baBannerText()` ตัวเดิมของ v6.3

| ช่อง | ข้อความหัว | ซับเท็กซ์ |
|---|---|---|
| S1/S3 (Active) | `[ สกิล{ชื่อ}ทำงาน! ]` | `ชาร์จพลังแล้ว — ตอบถูกเพื่อโจมตี!` **เฉพาะสกิลที่มีอะตอม `dmg`** |
| S4 (Ultimate) | `[ ท่าไม้ตาย{ชื่อ}ปลดปล่อย! ]` | ไม่มี |
| S2 (Passive) ตอนเริ่มไฟต์ | `พาสซีฟติดตัว — เสริมพลังการต่อสู้อัตโนมัติ!` | ไม่มี |

`baBannerText(title, sub)` ล้าง `#baSkill` ทับทั้งก้อนทุกครั้งอยู่แล้ว (ของเดิมของ v6.3)
`sub` ที่เป็นค่า falsy (`''`) จะข้ามการสร้าง `<div class="ba-skill-s">` ไปเลย —
ใช้ประโยชน์จากพฤติกรรมนี้ตรง ๆ แทนการเขียนเงื่อนไขแสดง/ซ่อนเอง

**ตัวคัดแยก "โจมตี vs ยูทิลิตี้" ใช้ `'dmg' in r.fx`** — มีอะตอม `dmg` ในช่องนั้นแปลว่า
เป็นสกิลที่ต้องรอผลตอนตอบถูกครั้งถัดไป (ผ่าน `hunterAtk()` ที่กดด้วย `BA_LIVE` — กติกาเดิม
ของ v8.8/v9.1 ที่ป้องกันไม่ให้ HP มอนสเตอร์พองตามโบนัสไปด้วย) จึงโชว์ซับเท็กซ์บอกให้รอ
ส่วนสกิลยูทิลิตี้ (heal/mist/cut/barr/wdn ล้วน ๆ ไม่มี `dmg`) ทำงานทันทีที่กด ไม่ต้องรอ
จึงไม่มีซับเท็กซ์นั้น — ยืนยันแล้วด้วยการเล่นจริงผ่าน Playwright: guardian ช่อง 1
(มี `dmg`) ได้ซับเท็กซ์ ส่วน guardian ช่อง 3 (`wdn`+`heal` ล้วน ไม่มี `dmg`) ไม่ได้

**ป้ายท่าไม้ตายไม่ได้เขียนแบนเนอร์ซ้ำสองครั้ง** — `baPlCast()` ของ v8.5 ยิง
`baBannerText('⚡ ULTIMATE', msg)` ไปแล้วตอน cast สำเร็จภายในตัวมันเอง ตัวห่อของ
ชั้นนี้เรียก `baBannerText()` ทับอีกทีด้วยข้อความตามสเปก (`#baSkill` ถูกเขียนทับทั้งก้อน
ทุกครั้งอยู่แล้ว จึงไม่กองซ้อนหรือมีสองใบค้างพร้อมกัน)

## 3) VFX สีต่อสายอาชีพ — ผ่านพารามิเตอร์ของ `baFlash()` ล้วน ไม่มีกฎ CSS ใหม่

```js
var BA_V917_COL = { assassin: '#a5f', monarch: '#a5f', blade: '#f43', slayer: '#f43',
  guardian: '#fb3', guard: '#fb3', priest: '#3cf', soulmaster: '#3cf' };
```

| สายอาชีพ (C1/C2) | สี | ธีม |
|---|---|---|
| 🗡️ Assassin / Monarch | `#a5f` | เงา/ม่วง (Shadow/Violet) |
| ⚔️ Blade / Slayer | `#f43` | เลือด/ประกาย (Crimson/Spark) |
| 🛡️ Guardian / Guard | `#fb3` | ทอง (Golden Aegis) |
| ✨ Priest / Soulmaster | `#3cf` | รูนศักดิ์สิทธิ์ (Divine Rune) |

`baFlash(color)` ของชั้น v6.0 รับสตริงสี CSS อะไรก็ได้อยู่แล้ว (ตั้ง `style.background`
บน `#baFlash` แล้วสลับคลาสกระตุ้นคีย์เฟรมที่มีอยู่ก่อนแล้ว) — การส่งสีต่างกันไปตามสาย
จึงได้ VFX ที่แยกแยะออกจากกันตามธีมโดยไม่ต้องประกาศกฎ CSS ใบใหม่สักใบ ทั้งสี่คู่สี
ใช้รหัสฮ็กซ์ 3 หลัก (ประหยัดไบต์ 3 ตัวอักษรต่อสี × 8 รายการ)

```js
function baV917Fx(g, t, s) {
  try { baBannerText(t, s); baFlash(BA_V917_COL[baDsRoleOf(g)] || '#fff'); } catch (e) {}
}
```

`baDsRoleOf(g)` คืน role key ปัจจุบัน (`assassin`/`monarch`/`blade`/`slayer`/`guardian`/
`guard`/`priest`/`soulmaster`) ตรงกับคีย์ของตาราง `BA_V917_COL` เป๊ะ — ไม่มีสารบัญคู่ขนาน
ให้ต้องซิงก์ (role key ยืมของ v8.7/v8.8 มาใช้ตรง ๆ)

## 4) จุดยิงทั้งสี่ — ห่อของเดิม เรียกก่อนแล้วอ่านผล

### 4.1) S1/S3 — ห่อ `baV91Activate(i, g, r)` ของ v9.1

```js
if (typeof baV91Activate === 'function') {
  var _v917Act = baV91Activate;
  baV91Activate = function (i, g, r) {
    var ok = _v917Act.apply(this, arguments);
    if (ok) {
      try {
        var sub = ('dmg' in r.fx) ? 'ชาร์จพลังแล้ว — ตอบถูกเพื่อโจมตี!' : '';
        baV917Fx(g, '[ สกิล' + r.name + 'ทำงาน! ]', sub);
      } catch (e) {}
    }
    return ok;
  };
}
```

เรียกของเดิมก่อนเสมอ — `_v917Act` ทำหน้าที่เดิมครบ (ตรวจคูลดาวน์ · หัก MP · ตั้งธง armed ·
เล่นท่าโจมตี) แล้วคืน `true`/`false` **เฉพาะตอน `ok === true` ถึงจะยิง VFX** เพราะ
`baV91Activate` คืน `false` ได้ทั้งตอนติดคูลดาวน์และ MP ไม่พอ — ไม่ควรมีแบนเนอร์
"ทำงาน!" ขึ้นทั้งที่ไม่ได้ทำอะไรเลย `r` ที่รับมาคือผลจาก `baResolveSkillEffects()`
ที่ผู้เรียกส่งเข้ามาอยู่แล้ว (`r.name` · `r.fx`) ไม่ต้องคำนวณซ้ำ

### 4.2) S4 — ห่อ `baPlCast()` ของ v8.5

```js
if (typeof baPlCast === 'function') {
  var _v917Cast = baPlCast;
  baPlCast = function () {
    var g = baG();
    var ok = _v917Cast.apply(this, arguments);
    if (ok && g) {
      try {
        var c = baPlClass(g);
        baV917Fx(g, '[ ท่าไม้ตาย' + c.slots[3].n + 'ปลดปล่อย! ]', '');
      } catch (e) {}
    }
    return ok;
  };
}
```

**ต้องจับ `g = baG()` ก่อนเรียกของเดิม ไม่ใช่หลัง** — `baPlCast()` ไม่รับพารามิเตอร์
(อ่าน `G` ของตัวเองข้างใน) แต่ก็ไม่มีเหตุผลให้ `baG()` เปลี่ยนค่าระหว่างเรียก
(ไม่มีการสลับผู้เล่นกลางการปล่อยท่าไม้ตาย) จับก่อนหรือหลังผลจึงเหมือนกัน — จับก่อน
เพื่อความชัดเจนของโค้ดเท่านั้น `c.slots[3].n` คือชื่อท่าไม้ตายของสายอาชีพนั้น
(v8.5 กำหนดไว้ในสารบัญ `BA_PL_CLASSES` อยู่แล้ว)

### 4.3) S2 แบนเนอร์เริ่มไฟต์ — ห่อ `baDsHook(what)` ของ v8.8

```js
if (typeof baDsHook === 'function') {
  var _v917Hook = baDsHook;
  baDsHook = function (w) {
    _v917Hook.apply(this, arguments);
    if (w === 'battle') {
      try {
        var g = baG(), m = g ? g.currentMonster : null;
        if (m && m !== BA_V917_M && !g.practiceMode) {
          BA_V917_M = m;
          baV917Fx(g, 'พาสซีฟติดตัว — เสริมพลังการต่อสู้อัตโนมัติ!', '');
        }
      } catch (e) {}
    }
  };
}
```

**ยิงครั้งเดียวต่อการเจอมอนสเตอร์ตัวใหม่จริง ๆ เทียบด้วย object identity ของ
`g.currentMonster`** (ท่าเดียวกับ `baFightSync()` ของ v6.3 ที่ใช้ `BA_FIGHT_M` เทียบ
ตัวอ็อบเจกต์เพื่อรู้ว่า "ไฟต์เดิมหรือไฟต์ใหม่") — `BA_V917_M` เป็น**ตัวแปรของตัวเอง
แยกต่างหาก ไม่ยุ่งกับ `BA_FIGHT_M`** ของ v6.3 เพื่อไม่ให้พฤติกรรมของสองระบบพันกัน
· `baDsHook('battle')` ถูกเรียกจาก `renderMonster` และ `startGame` (v8.8) ซึ่งทั้งคู่
วิ่งทุกครั้งที่ตอบผิดแล้วสับตัวเลือกใหม่ด้วย (ยังเป็นมอนสเตอร์ตัวเดิม) — เงื่อนไข
`m !== BA_V917_M` กันไม่ให้แบนเนอร์เด้งซ้ำทุกครั้งที่ตอบผิดข้อเดิม
· **ข้ามในโหมดฝึกจุดอ่อน** (`!g.practiceMode`) ตามธรรมเนียมของทุกชั้นที่ไม่นับ/ไม่แสดง
ผลใด ๆ ในโหมดนั้น

### 4.4) S2 จุดลอยตอนตอบถูก — ห่อ `baDsOnCorrect(g)` ของ v8.8

```js
if (typeof baDsOnCorrect === 'function') {
  var _v917Ok = baDsOnCorrect;
  baDsOnCorrect = function (g) {
    _v917Ok.apply(this, arguments);
    try {
      if (g && !g.practiceMode && baDsCache(g).slots[1].live.length) baCbTip('✨', baSpot('baHero'));
    } catch (e) {}
  };
}
```

**กรองด้วย `.live.length` ของช่องที่ 2** (`baDsCache(g).slots[1]`) — โชว์เฉพาะสายที่
พาสซีฟให้ผลจริง (`.live` คืออาร์เรย์ของอะตอมที่ `w:1` ตามที่ `baResolveSkillEffects()`
แยกไว้แล้ว) ไม่ใช่สายที่ช่องพาสซีฟยังเป็นอะตอมตาย (`w:0`) — กันจุดลอยขึ้นให้เห็นทั้งที่
ไม่มีอะไรเกิดขึ้นจริง

**ใช้ `baCbTip()` ไม่ใช้คลาส `.ba-dmg`** — `.ba-dmg` เป็นชื่อคลาสที่ชุดเทสต์ของ v6.0
ยึดไว้เป็นตัวชี้เฉพาะของ "ตัวเลขดาเมจ" (`querySelector('#baFx .ba-dmg')`) การใช้คลาส
เดียวกันซ้ำจะไปแย่งเป็นลูกตัวแรกของ `#baFx` แล้วทำให้ชุดเทสต์เดิมอ่านผิดตัว (กับดักข้อ 36
ของ CLAUDE.md) `baCbTip(text, spot)` ของ v6.9 เป็นทางเลือกที่ปลอดภัยอยู่แล้ว ใช้คลาส
`.ba-cbtip` ของตัวเอง

## 5) การเรียงลำดับความสำคัญของ wrapper — เรียกของเดิมก่อนเสมอ

ทั้งสี่จุดยึดกฎเดียวกัน: **เรียก `_v917X.apply(this, arguments)` ก่อน แล้วค่อยอ่านผล
(ค่าที่คืนมา หรือสถานะที่เปลี่ยนไปแล้ว) มาตัดสินใจว่าจะยิง VFX หรือไม่** — ไม่มีจุดไหน
เขียนตรรกะซ้ำหรือคำนวณผลของสกิลเอง ซึ่งเป็นไปตามข้อกำหนด "ต้องไม่แตะสูตรดาเมจ/ตัวเลข/
สเตตัสมอนสเตอร์" ของสเปกโดยตรง — wrapper ทั้งหมดเป็นแค่ **ผู้สังเกตการณ์** (observer)
ของผลลัพธ์ที่ชั้นล่างสร้างขึ้นเองล้วน ๆ

## 6) สิ่งที่แพตช์นี้ไม่ได้ทำ

**ไม่เพิ่มฟิลด์ในบัญชีเลย** ไม่มี `baV917Ensure()` ไม่มีตรารุ่นระดับบัญชี และไม่ได้ห่อ
`migrateAccount` · **ไม่เพิ่ม toast · ไม่เพิ่ม gate · ไม่เพิ่มปุ่มสักใบ · ไม่แทรกคอลัมน์
ในตารางไหน · ไม่เพิ่มภาพหรือเสียงสักไบต์ · ไม่เพิ่ม DOM สักโหนด · ไม่เพิ่มกฎ CSS สักบรรทัด
· ไม่แตะ `Math.random` สักครั้ง** (กับดักข้อ 32) · **ไม่แตะสูตรดาเมจ/`BA_DS_FX`/สเตตัส
มอนสเตอร์เลยสักตัวเลข** ตามข้อกำหนดของสเปกโดยตรง

`BA_V917_M` เป็นสถานะของรอบการเล่นล้วน (ตัวชี้ไปยังอ็อบเจกต์มอนสเตอร์ปัจจุบัน)
ไม่ต้องเซฟ · `BA_V917_COL` เป็นตารางค่าคงที่ล้วน ประกาศด้วย `var` ในสโคปของ IIFE
(ไม่ใช่ตัวแปรระดับ global ของหน้าเพจ — ตรวจสอบแล้วว่า `window`/สโคปนอกมองไม่เห็น
ซึ่งไม่กระทบอะไรเพราะไม่มีโค้ดภายนอกจำเป็นต้องอ่านมันเลย)

## ทางเข้าสาธารณะสำหรับชุดเทสต์

ไม่ได้เพิ่มฟังก์ชัน audit ใหม่ (แพตช์นี้ไม่มีสถานะให้ตรวจนอกจาก `BA_V917_M` ซึ่งเป็นตัวชี้
ภายในล้วน) — พิสูจน์พฤติกรรมได้โดยตรงผ่าน `#baSkill.innerHTML` หลังเรียก
`baV91Activate()` / `baPlCast()` / `baDsHook('battle')` / `baDsOnCorrect()` ตรง ๆ
(ยืนยันแล้วด้วยการเล่นจริงผ่าน Playwright — ดูหัวข้อทดสอบ)

## ทดสอบ

**ไม่มีชุดเทสต์ Playwright แยกไฟล์ใหม่** — สเปกกำหนดชุดตรวจสอบไว้ตรง ๆ เป็นสี่คำสั่ง:

```
node build_minify.js --check && node test_cloud_sync.js && node test_monarch.js && node test_skill_dispatch.js
```

| คำสั่ง | ผล |
|---|---|
| `build_minify.js --check` | ✅ ไฟล์แจก 1,996,877 ไบต์ · เหลือถึงเพดาน 3,123 ไบต์ |
| `test_cloud_sync.js` | ✅ **104/0** |
| `test_monarch.js` | ✅ **85/0** |
| `test_skill_dispatch.js` | ⚠️ **54 ผ่าน / 40 ตก — ตกอยู่แล้วบน HEAD ก่อนแพตช์นี้เป๊ะทั้ง 40 เคส** |

**`test_skill_dispatch.js` ไม่ใช่ regression ของแพตช์นี้** — ยืนยันด้วย `git stash` แล้ว
รันชุดเดิมกับไฟล์ก่อนแก้ (HEAD) ได้ผลเท่ากันเป๊ะ **54/40** ทุกเคสชื่อเดียวกัน สาเหตุคือ
ค่าตัวเลขจริงใน `BA_DS_FX` ของสารบัญ 32 ช่องได้เบี่ยงไปจากที่ชุดเทสต์คาดไว้แล้วตั้งแต่ก่อน
แพตช์นี้ (ตัวเลขในเกมสูงกว่าที่เทสต์คาดไว้เกือบเท่าตัวในหลายช่อง และบางช่องมีอะตอมคนละชุด
กับที่เทสต์คาดไว้เลย) — เป็นความไม่ตรงกันระหว่างเมทริกซ์บาลานซ์กับชุดเทสต์ที่มีอยู่ก่อนหน้า
แพตช์นี้ทั้งหมด **แพตช์นี้ไม่ได้แตะ `BA_DS_FX` เลยสักตัวเลขตามข้อกำหนดของสเปก
("ห้ามแตะตัวเลขใน `BA_DS_FX`") จึงไม่ใช่หน้าที่ของแพตช์นี้ที่จะแก้ความไม่ตรงกันนั้น**

**ยืนยันพฤติกรรมจริงเพิ่มเติมด้วยการเล่นเกมจริงผ่าน Playwright** (นอกเหนือจากสี่คำสั่ง
ที่สเปกกำหนด เพราะสเปกไม่ได้ให้ชุดเทสต์เฉพาะของฟีเจอร์นี้มา):

- เข้าเกม → แบนเนอร์พาสซีฟ `"พาสซีฟติดตัว — เสริมพลังการต่อสู้อัตโนมัติ!"` ขึ้นทันทีที่
  เจอมอนสเตอร์ตัวแรก
- กด S1 (นักลอบสังหาร ซึ่งมีอะตอม `dmg`) → `[ สกิลเงาฉับพลันทำงาน! ]` +
  ซับเท็กซ์ `"ชาร์จพลังแล้ว — ตอบถูกเพื่อโจมตี!"`
- ปล่อยท่าไม้ตาย (ราชันเงา) → `[ ท่าไม้ตายราชันเงาสังหารปลดปล่อย! ]` ไม่มีซับเท็กซ์
- สลับไปผู้พิทักษ์ กด S3 (ป้อมปราการ — `wdn`+`heal` ล้วน ไม่มี `dmg`) →
  `[ สกิลป้อมปราการทำงาน! ]` **ไม่มีซับเท็กซ์เลย** (ยืนยันตัวคัดแยกโจมตี/ยูทิลิตี้ถูกต้อง)
- ตอบถูกขณะสายมีพาสซีฟที่ให้ผลจริง (`slot1Live: ['ultg','tchn']`) → `baCbTip()` ถูกเรียก
- ไม่มี `pageerror` ตลอดทุกขั้นตอน

**ต้องรันชุดของ v6.0 · v6.3 · v6.9 · v8.5 · v8.8 · v9.1 ควบทุกครั้งที่แก้ไขต่อ** เพราะ
แพตช์นี้ห่อ `baV91Activate` · `baPlCast` · `baDsHook` · `baDsOnCorrect` ซึ่งเป็นทางผ่านหลัก
ของชั้นพวกนั้นทั้งหมด · **และเปลี่ยนความกว้างมาสคอต** จึงควรเช็กด้วยตาว่าหน้า intro/เกท
ยังคมชัดที่ 432px q46 (ยังเกินความกว้างที่ใช้จริงราว 370 CSS px อยู่ 1.17 เท่า)
