### Patch · WEAKNESS PURIFY & ANTI-FARM

อยู่ในเนมสเปซ `ba` ของชั้น v6.0 เดิม **ไม่ได้ตั้งพรีฟิกซ์ใหม่** (กติกาเดียวกับ v6.1–v9.18)
วางท้ายสุดของไฟล์ ต่อจาก `baInstallV918()` และก่อน `baBuild()` ปิดท้ายไฟล์
· **ไม่แก้โค้ดของ v4.0-v9.18 เลยแม้แต่บรรทัดเดียว** ที่เพิ่มคือ wrapper **6 ตัว**
(`resolveAnswer` · `migrateAccount` · `startGame` · `exitGame` · `saveProgress`
· `baBattleAudit`) กินที่ในไฟล์แจกราว **2.6KB** ที่ตรรกะล้วน — **ไม่มีภาพ ไม่มีเสียง
ไม่มี CSS สักกฎ ไม่มี DOM สักโหนด ไม่เพิ่ม/ลดปุ่มสักใบ**

ต้นฉบับ 3,913,669 → 3,916,253 ไบต์ · ไฟล์แจก 1,978,320 → **1,980,904 ไบต์**
(สุทธิ +2,584 ไบต์) เหลือถึงเพดาน 2,000,000 อีก **19,096 ไบต์**

**ความสูงการ์ดโจทย์ไม่ขยับเลยแม้แต่พิกเซลเดียว** — แพตช์นี้ไม่แตะ DOM/CSS เลยสักบรรทัด
จึงไม่ต้องวัดเลย์เอาต์ใหม่ ยืนยันทางอ้อมผ่าน `test_monarch.js` (85/0 — ชุดนั้นเช็ก CLS=0
ที่ 4 ความกว้างจอ 320/360/390/430 ทุกครั้งที่รัน)

##### ทำไมแทบไม่ต้องเขียนกลไกใหม่เลย

ทุกอย่างที่งานนี้ขอมี **"ทางผ่านเดียว" ของชั้นก่อนหน้าอยู่แล้วทั้งหมด** แพตช์นี้จึงห่อของเดิมล้วน

| จะทำอะไร | ทางผ่านเดียว | เจ้าของ |
|---|---|---|
| สุ่มคำถามในโหมดฝึกจากคำอ่อนสุด 10 คำ | `getMonster()` → `weakWordsOf(G, 10)` | v4.0 (Hunter Analytics) |
| ตัดสินว่าคำไหนติดลิสต์คำอ่อน | `weakWordsOf()` กรอง `!w.wrong` ทิ้ง | v4.0 |
| แจกเศษคริสตัล | `baQdGem(n)` (เพดาน + เซฟ + เลขลอยให้เองอัตโนมัติ) | Micro-Patch เควสประจำวัน |
| แสดงคำเตือน/คำอวยพรลอยกลางสนาม | `baCbTip(text, spot)` (คลาส `.ba-cbtip` เดิม) | v6.9 |
| หาพิกัดของฮีโร่ในสนาม | `baSpot('baHero')` | v6.0 |

##### 1) โหมดฝึกจุดอ่อนสุ่มจากคำอ่อนสุด 10 คำอยู่แล้ว — ไม่ต้องกรองซ้ำ

`getMonster()` ของ v4.0 เมื่ออยู่ใน `G.practiceMode` ดึงคำถามจาก `weakWordsOf(G, 10)`
โดยตรง (ตกกลับไปใช้ pool ปกติเฉพาะตอนลิสต์นั้นว่างเปล่า) **ทุกข้อที่ถูกถามระหว่างโหมดนี้
จึงเป็นหนึ่งใน 10 คำอ่อนสุดของผู้เล่นอยู่แล้วโดยไม่ต้องเขียนตัวกรองซ้ำสักบรรทัด**

##### 2) ตอบถูกติดกัน 2 ครั้ง = ชำระล้าง — นับที่ `resolveAnswer` จุดเดียว

`BA_WP_HITS` เป็นตัวนับชั่วคราวระดับรอบการเล่น (ไม่ต้องเซฟ — เหมือน `AB_LIVE` ของ v4.6)
คีย์ด้วย `wordId` เก็บว่าตอบถูกติดกันกี่ครั้งแล้ว **เฉพาะระหว่างอยู่ในโหมดฝึก**

```
ตอบถูก (โหมดฝึก) → hits[id]++  → ครบ 2 ครั้ง → baWpPurify(id) แล้วล้างตัวนับทิ้ง
ตอบผิด/หมดเวลา (โหมดฝึก) → ลบตัวนับของคำนั้นทิ้งทันที (ต้องติดกันจริง ไม่ใช่สะสมข้ามรอบพลาด)
```

**wrapper จับสถานะ "ก่อน" เรียกของเดิมเสมอ** (`m0 = g.currentMonster` ก่อน `_baWpResolve.apply(...)`)
เพราะ `resolveAnswer` ของ v4.0 เปลี่ยน `G.currentMonster` เป็นข้อถัดไปกลางฟังก์ชัน
(ผ่าน `nextMonster`/`scheduleTurn`) — อ่านทีหลังจะได้คำของข้อใหม่แทนคำที่เพิ่งตอบ
(กับดักข้อ 16/19 ของ CLAUDE.md — ทางแก้คือ snapshot ก่อนเรียกของเดิมเสมอ)

##### 3) ชำระล้าง = ล้าง `wordStats[id].wrong` เป็น 0 — คำหลุดจากลิสต์ทันที

`baWpPurify(g, wordId)` ล้าง `ws.wrong = 0` ตรง ๆ — `weakWordsOf()` ของ v4.0 กรองด้วย
เงื่อนไข `!w.wrong` (falsy) ทิ้งจากลิสต์คำอ่อนอยู่แล้ว **ไม่ต้องเขียนตัวกรอง/ตัวคัดออกใหม่
สักบรรทัด** เพียงล้างค่าที่เงื่อนไขเดิมของ v4.0 อ่านอยู่แล้วก็พอ

ยิงคำเตือนด้วย `baCbTip('✨ ชำระล้างสำเร็จ!', baSpot('baHero'))` — ทั้งสองฟังก์ชันเป็น
ของเดิมของ v6.0/v6.9 ทุกประการ (`baCbTip` วาดด้วยคลาส `.ba-cbtip` ที่มีอยู่แล้ว
`baSpot` คืนพิกัด `{x,y,w}` หรือ `null` อย่างปลอดภัยถ้าไม่มีสนามรบให้อ้างอิง —
`baCbTip` เองก็มีการ์ด `if (!spot) return;` กันไว้อีกชั้น) **ไม่มีคลาส CSS หรือ DOM
ใหม่สักตัวถูกสร้างขึ้น**

##### 4) Anti-Farm — เพดานเศษคริสตัล 5 ครั้ง/วัน ผ่าน `account.wp`

```
account.wp = { day: 'YYYY-MM-DD', n: 0-5 }
```

**เพดานกันเฉพาะ "รางวัล" ไม่ได้กันการชำระล้าง** — เกินโควตาแล้วคำยังหลุดจากลิสต์
คำอ่อนตามปกติทุกครั้ง (`ws.wrong = 0` ยังทำงานเสมอ) แค่ไม่ได้ 💎 เพิ่มเท่านั้น
(`BA_WP_N.cap` นับจำนวนครั้งที่ถูกบล็อกไว้ให้ audit อ่าน)

จ่ายผ่าน **`baQdGem(1)` ของ Micro-Patch เควสประจำวัน** ซึ่งเป็นช่องทางเดียวของทั้งเกม
ที่ให้เศษคริสตัล — ใช้ก้อนเดียวกับ `account.ab.shards` ของ v4.6 **ไม่มีสกุลเงินใหม่**
และไม่ต้องเขียนโค้ดแสดงตัวเลข `+1💎` ลอยเอง เพราะ `baQdGem()` ทำให้อัตโนมัติอยู่แล้ว
(เรียก `renderStats()` + โชว์เลขลอยผ่าน `baSpot('baHero')` ในตัว)

`baWpEnsure(a)` เป็นตัวรีเซ็ตรอบวันในตัว (`d.day !== qDayKey() → รีเซ็ต n = 0`)
เหมือนกติกาเดียวกับ `account.dq` ของ Micro-Patch เควสประจำวันทุกประการ (ใช้ `qDayKey()`
ตัวเดียวกันเป็นตัวตัดสินรอบวัน ไม่สร้างนาฬิกาชุดที่สอง) · `baWpEnsure` ต้อง idempotent
เพราะถูกเรียกทุกครั้งที่อ่าน store ผ่าน `migrateAccount` และ **ซ่อมข้อมูลเพี้ยนไปในตัว**
(หนีบ `n` ไว้ที่ 0-5 เสมอ)

##### 5) รอดข้ามการล็อกอิน — ตามกฎกับดักข้อ 16 ของ CLAUDE.md เป๊ะ

`saveProgress()` ของ v4.0 เป็นวอตช์ลิสต์ตายตัว **ไม่รู้จักฟิลด์ `wp`** จึงต้องมี
`baWpSave()` ของตัวเองที่เขียนลง `loadStore()`/`saveStore()` ตรง ๆ · มีสวิตช์
`BA_WP_READY` (ปิดตอนเข้า `startGame` ก่อนเรียกของเดิม เปิดหลังดึงค่าจาก store
กลับมาใส่ `G` ครบแล้ว) กันเขียนทับข้อมูลเก่าด้วยก้อนว่างระหว่างช่วง `startGame`
กำลังทำงานอยู่ (กฎเดียวกับ `AB_READY`/`CD_READY`/`BA_QD_READY`)

**`startGame` ของ v4.0 ปั้น `G` ขึ้นใหม่จากฟิลด์ที่มันรู้จักเท่านั้น** wrapper ของ
แพตช์นี้จึงต้องดึง `wp` จาก `loadStore()[username || CURRENT_USER]` มาใส่ `G` เอง
ก่อนเรียก `baWpEnsure(g)` — ไม่งั้นความคืบหน้ารายวันจะหายทุกครั้งที่ล็อกอิน
(ท่าเดียวกับ `baQdStart` ของ Micro-Patch เควสประจำวัน)

**ลำดับใน `exitGame` สำคัญ** — เซฟก่อน (`baWpSave()`) แล้วค่อยปิด `BA_WP_READY`
ก่อนเรียกของเดิม เพราะ `exitGame` ของ v4.0 เรียก `saveProgress()` ต่อข้างใน ถ้าปิด
สวิตช์ก่อนเซฟ รอบนั้นจะไม่มีอะไรถูกเขียนเลย (กับดักข้อ 16 ในอีกทางหนึ่ง)

##### สิ่งที่แพตช์นี้ไม่ได้ทำ

**ไม่เพิ่มฟิลด์ในบัญชีนอกจาก `account.wp` ก้อนเดียว** — ไม่มีตรารุ่น `wpv` เพราะไม่มี
อะไรให้ backfill (บัญชีเก่าเริ่มที่ `{day:'', n:0}` ซึ่งถูกต้อง — `baWpEnsure` เห็นวันไม่ตรง
แล้วรีเซ็ตให้เองในการอ่าน store ครั้งแรก) · **ไม่เพิ่ม toast · ไม่เพิ่ม gate · ไม่เพิ่ม/ลด
ปุ่มสักใบ · ไม่แทรกคอลัมน์ในตารางไหน · ไม่เพิ่มภาพหรือเสียงสักไบต์ · ไม่เพิ่ม DOM สักโหนด
· ไม่เพิ่ม CSS สักกฎ · ไม่แตะ `Math.random` สักครั้ง**

**ไม่แตะ `G.streak` / ทอง / EXP / HP / ความคืบหน้าหอคอยเลยสักบรรทัด** — โหมดฝึกยังไม่มี
ความเสี่ยง/ไม่มีรางวัลปกติเหมือนเดิมทุกประการ เพราะ `resolveAnswer` ของ v4.0 เช็ก
`G.practiceMode` ปิดกั้นทองกับ EXP ของตัวมันเองอยู่แล้วตั้งแต่ก่อนแพตช์นี้ — ระบบนี้แค่
"อ่าน" ผลของการตอบถูก/ผิดที่เกิดขึ้นแล้ว ไม่ได้ไปเปลี่ยนผลของมันแม้แต่นิดเดียว

**สวิตช์ `localStorage['yao_arena'] = 'off'` ไม่มีผลกับแพตช์นี้เลย** เพราะไม่มี wrapper
ตัวไหนผูกกับกรอบสนามรบ — ระบบนี้ทำงานอยู่ที่เมนู/สถิติล้วน (มีแต่คำเตือน `baCbTip` ที่
ต้องมีสนามรบให้แขวนถึงจะโผล่ แต่ `baSpot`/`baCbTip` เองมีการ์ดกันไว้แล้วให้เงียบอย่าง
ปลอดภัยถ้าไม่มีสนาม — ไม่ทำให้ระบบล้มเหลว มีแค่คำเตือนที่ไม่โผล่)

##### ⚠️ ผลที่ตามมาที่ต้องรู้ — คลังอักขระ Subconscious Mastery ขยับตาม

`baSmNet(w) = (w.seen||0) - 2*(w.wrong||0)` ของ v9.6 · Subconscious Mastery (คลังอักขระ
4 ขั้น "ศิลาทองคำบริสุทธิ์") อ่าน `w.wrong` ตัวเดียวกันนี้ — การล้าง `wrong = 0` จึงดัน
`netScore` ของคำนั้นขึ้น และอาจทำให้คำนั้นเลื่อนขั้น Codex สูงขึ้นทันที

**นี่ไม่ใช่บั๊ค เป็นผลตามมาที่ยอมรับไว้โดยตั้งใจ** — คำที่ถูกชำระล้างคือคำที่ผู้เล่น
เพิ่งพิสูจน์ตัวเองด้วยการตอบถูกสองครั้งติดในโหมดฝึก ซึ่งสมเหตุสมผลที่คะแนนความเชี่ยวชาญ
ของคำนั้นจะดีขึ้น และ Subconscious Mastery เป็น **ระบบความสำเร็จข้างเคียง**
(achievement/ผนึกหกดวงของ Monarch Ascension) ไม่ใช่ "tower streak หรือ main
progression" ตามขอบเขตที่งานนี้กันไว้ — จึงไม่ต้องมีตัวกันหรือชดเชยอะไรเพิ่ม

##### ทางเข้าสาธารณะสำหรับชุดเทสต์

`baBattleAudit().weaknessPurify` — `ver` (`'1.0'`) · `cap` (`BA_WP_CAP` = 5) · `day`/`n`
(สถานะโควตาวันนี้) · `hits` (**สำเนา** ของตัวนับติดกันปัจจุบัน) · `purify`/`gem`/
`blockedByCap` (**สำเนา** ของตัวนับสะสม — คืน reference ไม่ได้ เพราะชุดเทสต์จับค่า
ก่อน/หลังไว้เทียบกัน แล้วจะได้ส่วนต่าง 0 ทุกครั้ง — บทเรียนของ v8.3)

##### ทดสอบ

```
node build_minify.js --check
  → 1,980,904 ไบต์ (เหลือถึงเพดาน 19,096 ไบต์)

node test_cloud_sync.js     → 104 PASS / 0 FAIL
node test_monarch.js        →  85 PASS / 0 FAIL   (รวม CLS=0 ที่ 320/360/390/430)
node test_skill_dispatch.js →  54 PASS / 40 FAIL
```

**⚠️ 40 เคสที่ตกใน `test_skill_dispatch.js` เป็นของเดิมอยู่ก่อนแล้ว ไม่ใช่ผลจากแพตช์นี้**
ยืนยันด้วยการดึงไฟล์แจกใบก่อนแก้ออกมาด้วย `git show HEAD:hanzi_hunter_tower_v3_1_intro.html`
(1,978,320 ไบต์ — ตรงกับฐานก่อนแก้เป๊ะ) วางไว้ในสคแรทช์แพดแยกพร้อมสำเนา
`test_skill_dispatch.js` แล้วรันชุดเดียวกันกับไฟล์เดิมที่ยังไม่ได้แก้อะไรเลย
ได้ผลตรงกันเป๊ะ **54 PASS / 40 FAIL** เท่ากันทุกเคส — พิสูจน์ว่าแพตช์นี้ **ไม่มี
regression สักเคส** ในชุดนั้น ทั้ง 40 เคสที่ตกเป็นเรื่องสูตรเมทริกซ์สกิลของสาย/ร่าง
(`hunterAtk` · `baPlS1` · `baPlUltAmt` · `critChance` ของชั้น v8.5/v8.8) ซึ่งเป็นคนละ
ระบบกับที่แพตช์นี้แตะ (แพตช์นี้ห่อแค่ `resolveAnswer` · `migrateAccount` · `startGame`
· `exitGame` · `saveProgress` · `baBattleAudit` เท่านั้น ไม่ได้แตะสูตรค่าพลัง/สกิลเลย
สักบรรทัด)

**ต้องรันชุดของ v4.0 (Hunter Analytics) ควบเสมอถ้าวันหนึ่งมีใครแก้ `weakWordsOf()`
หรือ `getMonster()`** เพราะแพตช์นี้พึ่งพฤติกรรมของทั้งสองฟังก์ชันนั้นเต็มที่ (สุ่มจาก
top-10 คำอ่อน + กรองด้วย `!w.wrong`) โดยไม่มีตัวสำรองของตัวเอง — ถ้าพฤติกรรมนั้น
เปลี่ยน ระบบชำระล้างนี้จะพังเงียบ ๆ โดยไม่มี error ให้เห็นเลย
