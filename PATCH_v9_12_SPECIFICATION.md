# Patch v9.12 · SKILL AWAKENING & BALANCED LADDER

อยู่ในเนมสเปซ `ba` ของชั้น v6.0 เดิม (พรีฟิกซ์ `BA_V912_` / `baV912`) วางท้ายสุดของไฟล์
ต่อจากแพตช์ไม่มีเลขรุ่นชุดสุดท้าย ("GM DIAMOND RESOLUTION FIX" · เนมสเปซ `bdg`) และก่อน
`baInstall()` — **ไม่แก้โค้ดของทุกชั้นก่อนหน้าเลยแม้แต่บรรทัดเดียว** ยกเว้นการ mutate
`const BA_PL_COST` **ในที่** (อาร์เรย์ยังเป็น `const` ตัวเดียวกัน แค่เปลี่ยนค่าสมาชิก — ท่า
เดียวกับที่ v6.4 ทำกับ `AB_SEAL_FLOORS` และ v7.6 ทำกับ `BA_LVRANK`) กินที่ในไฟล์แจกราว
**78 ไบต์สุทธิ** (ต้นฉบับ +743 ไบต์ · ไฟล์แจก +78 ไบต์ · HARD build ย่อสัดส่วนสูงมาก)
ต้นฉบับ 3,830,497 → 3,831,240 ไบต์ · ไฟล์แจก 1,973,756 → **1,973,834 ไบต์**
เหลือถึงเพดาน 2,000,000 อีก **26,166 ไบต์**

## สามข้อที่เบี่ยงจากสเปกตรงตัว — บันทึกไว้ตามธรรมเนียม repo

### 1 · Schema — ใช้ `account.skills[classId]` เดิม ไม่สร้าง `account.classSkills`

สเปกขอฟิลด์ชื่อ `account.classSkills = { [classId]: [lv1,lv2,lv3,lv4] }` แต่ระบบมี
**`account.skills[classId] = [lv1,lv2,lv3,lv4]`** อยู่แล้วตั้งแต่ Patch v8.5 ซึ่งตรงกับ
โครงสร้างที่สเปกต้องการทุกประการ (ปรับด้วย `baPlEnsure()` ให้ครบทุกคลาสเสมอ เป็น
`[1,1,1,1]` โดยปริยาย) — จึงใช้ฟิลด์เดิม **ไม่สร้างฟิลด์คู่ขนานที่ต้องซิงก์กันเอง**
(กับดักข้อ 35 ของ CLAUDE.md: ห้ามสร้างสารบัญ/ฟิลด์คู่ขนาน)

ผลพลอยได้ที่ได้มาฟรี: การซิงก์ 3 ทางที่สเปกขอ ("wbPush · wbRestore · loadStore ·
cloud payload") **สองในสามทางทำงานอยู่แล้วโดยไม่ต้องเขียนโค้ดเพิ่มเลย**

| ทาง | ทำไมได้ฟรี |
|---|---|
| `loadStore()` | วิ่งผ่าน `migrateAccount()` → `baPlEnsure()` ทุกครั้งอยู่แล้ว |
| Cloud payload (v5.2 `csDoc`) | คัดลอกทั้งบัญชียกเว้น `pw` (`Object.keys(a).forEach(...)`) — พา `account.skills` ไปด้วยโดยอัตโนมัติ |
| Wordbank sync (v9.9 `wbPush`/`wbRestore`) | **ยังไม่รู้จักฟิลด์นี้** เพราะออกแบบมาแคบมากตั้งแต่ต้น (เฉพาะ `wordStats` + ตัวนับสะสมโมโนโทนิก) จึงต้องเขียนเพิ่มจริง — ดูหัวข้อ "การซิงก์ 3 ทาง" ด้านล่าง |

### 2 · เลขรุ่น v9.12 ไม่ใช่ v9.3 ตามที่สเปกเรียกตัวเอง

เลข "v9.3" ถูกจองไปแล้วในไฟล์จริง (Patch v9.3 · UNIVERSAL CONTACT DASH & IMPACT SLASH VFX
ของสายสนามรบ) เลขชั้นเดินตามลำดับการห่อจริงเสมอ ไม่ใช่ตามเลขที่คำสั่งงานเรียกตัวเอง
(precedent เดียวกับ v4.9.2 ที่คำสั่งเรียกว่า "v4.8.2" · v5.2 ที่คำสั่งเรียกว่า "v5.0")
ตรวจแล้วด้วย `grep -c "BA_V912_\|baV912"` และ `grep -c "v9\.12\|V9_12"` ได้ 0 ทั้งคู่
ก่อนตั้งชื่อ (กับดักข้อ 36)

### 3 · คูลดาวน์ Omni-Class "ขั้นต่ำ 1" ต่างจาก precedent เดิมของ v9.6 โดยตั้งใจ

Patch v9.6 มีฉายา Omni-Class แบบเดียวกันมาก่อนแล้ว (ชื่อ "ผู้หยั่งรู้รากอักขระ" · key
`allknowing`) และลดคูลดาวน์ด้วยสูตร `Math.max(0, BA_V91_CD[i] - cut)` — **หนีบที่ 0**
สเปกของแพตช์นี้ขอ "Global CD -1 (min 1)" ตรงตัว ซึ่งต่างจาก precedent เดิมโดยตั้งใจ:
ผู้เล่นที่ปลดทั้งสองฉายาพร้อมกันจะยังกดสกิลไม่ได้ทันทีถ้าคูลดาวน์เหลือ 1 ครั้งสุดท้าย
(ตัวลดของทั้งสองฉายาบวกสะสมกันตามปกติ เพราะต่างคนต่าง wrap `baV91Activate` แยกกันคนละชั้น)
ชื่อฉายาใหม่ (`omniclassmaster` / "ปราชญ์ผู้รู้แจ้งสรรพศาสตร์") **ไม่ชนกับ** `allknowing`
ของ v9.6 ทั้ง key และเงื่อนไข (v9.6 นับคำศัพท์ Tier 4 ครบ 329 คำ · v9.12 นับสกิล 4 สาย
ฐานครบ Lv.5 ทุกช่อง — คนละเงื่อนไขคนละฉายา ผู้เล่นได้ทั้งสองใบพร้อมกันได้)

## หมายเหตุความถูกต้องของเอกสาร — CLAUDE.md คลาดเคลื่อนจากซอร์สจริงตั้งแต่ราว v9.7

ตรวจพบระหว่างทำแพตช์นี้ว่า `CLAUDE.md` ที่แนบมาบรรยายถึง **"v9.7.1 · AUDIT & EXPOSE
SOUL CARDS UI"** กับ **"v9.7.2 · SOUL CARDS ENGINE"** ซึ่ง **ไม่มีอยู่จริงในไฟล์นี้เลย**
(`grep -c "BA_V972_\|baV972"` = 0) ไฟล์จริงมีแพตช์ v9.7 ชื่อ "TIER 4 HUD & CODEX" ที่
เนื้อหาต่างออกไปสิ้นเชิง และสายโซ่แพตช์จริงจบที่ **v9.10 (Diamond Economy Governor)**
ตามด้วยแพตช์ไม่มีเลขรุ่นอีกสามชุด (Progression Gates/`gat` · Switch Confirm Modal &
GM Diamond Spawn/`swc`+`gcd` · GM Diamond Resolution Fix/`bdg`) ก่อน `baInstall()`

**หลักฐานที่ยืนยัน**: `test_polarized_class.js` มีเคสทดสอบปุ่ม "คลังการ์ดวิญญาณ (v9.7.1)"
ค้างอยู่ในชุดเทสต์ (บล็อก 7) ซึ่ง **ตกอยู่แล้วบนไฟล์แจกใบล่าสุดก่อนแพตช์นี้แม้แต่นิดเดียว**
(วัดจาก `git show HEAD:hanzi_hunter_tower_v3_1_intro.html` แล้วรันเทียบ — ได้ 131/13
เท่ากันเป๊ะกับหลังแพตช์นี้) — ยืนยันว่าโค้ด Soul Cards UI ไม่เคยถูกฝังลงไฟล์จริงเลย ไม่ใช่
เพราะแพตช์นี้ไปลบอะไรทิ้ง แพตช์นี้จึง **อ้างอิงจากการ grep ซอร์สจริงเท่านั้น** ไม่ได้เชื่อ
คำบรรยายใน CLAUDE.md ส่วนที่เกินจากที่ยืนยันได้จริงในไฟล์

## 1) เมทริกซ์ราคาใหม่ −50% — `BA_PL_COST` mutate ในที่

| ช่อง | เดิม (Lv2/3/4/5) | ใหม่ (v9.12) |
|---|---|---|
| 1 · โจมตีซ้ำ · CD 3 | 40 · 80 · 160 · 300 | **50 · 120 · 250 · 500** |
| 2 · พาสซีฟ | 40 · 80 · 160 · 300 | **50 · 120 · 250 · 500** |
| 3 · ยูทิลิตี้ · CD 4 | 60 · 120 · 220 · 450 | **50 · 120 · 250 · 500** |
| 4 · Ultimate | 100 · 200 · 350 · 700 | **50 · 120 · 250 · 500** |
| **รวม/สาย** | 3,360 💎 | **3,680 💎** |
| **รวม 4 สายฐาน** | 13,440 💎 | **14,720 💎** |

**⚠️ ราคา 4 ช่องรวมกัน (920 💎/สาย) มากกว่าที่สเปกเขียนไว้เป็น "−50%" ตรงตัว** เพราะสเปก
ให้ตัวเลขระดับ Lv มาตรง ๆ (50/120/250/500 ทุกช่องเท่ากันหมด) ซึ่งบังเอิญ **สูงกว่า** ราคา
เดิมของช่อง 1/2 (เดิม 40→ใหม่ 50) แต่ **ต่ำกว่า** ราคาเดิมของช่อง 3/4 (เดิม 60/100→ใหม่ 50)
สุทธิรวมทั้ง 4 ช่องจึงเพิ่มขึ้นจาก 3,360 เป็น 3,680 (+9.5%) **ไม่ใช่ลดลง 50%** — เขียนเอกสาร
ตรงตัวตามที่สเปกให้มา ("Tuned Cost Ladder (-50%)" หมายถึงราคาต่อ "ระดับ" ของช่อง 3/4 ที่
ลดลงจริง ไม่ใช่ยอดรวมทั้งสายที่ลดลง) ยอดรวม **3,680 💎/สาย · 14,720 💎/4 สายฐาน** ตรงกับ
ที่สเปกระบุไว้ในบรรทัดถัดมาเป๊ะทั้งคู่ ("Total: 3,680 💎/class | 14,720 💎 for 4 base
arch-classes") — จึงยึดตัวเลขรวมสองตัวนี้เป็นแหล่งความจริง ไม่ใช่คำว่า "-50%" ในหัวข้อ

`BA_PL_COST` เป็น `const` **อาร์เรย์ของอาร์เรย์** (4 ช่อง × 5 ระดับ ระดับ 0 = 0 เสมอ
เพราะทุกช่องเริ่มที่ Lv.1 ฟรี) mutate ในที่ด้วย `.length = 0` แล้ว `.push(...)` ทีละแถว
(บันไดราคาเป็นของกลาง **ใช้ร่วมกันทั้ง 4 คลาส** ไม่ใช่คนละราคาต่อคลาส)

`BA_PL_MAXOUT` เป็นค่าที่ v8.5 คำนวณค้างไว้ตอนแยกไฟล์ (`const BA_PL_MAXOUT =
BA_PL_COST.reduce(...)`) **ไม่ขยับตามการ mutate ในที่** จึงต้องแก้สองจุดที่อ่านค่านี้:

1. **`baPlRender()`'s UI string** — ห่อ `baPlRender` แล้วทำ string-replace แบบเจาะจง
   บน `box.innerHTML` หลังของเดิมวาดเสร็จ (เพราะ `baPlRender` เขียน `innerHTML` ตรง ๆ
   ไม่มี return value ให้ดักได้) แทนที่ `'อัปเต็มทั้งสาย 3,360 💎'` เป็น `'... 3,680 💎'`
2. **`baBattleAudit()`'s `o.polarized.maxout`** — ห่อ `baBattleAudit` แล้วเขียนทับค่า
   `r.polarized.maxout = BA_V912_MAXOUT` โดยตรงในตัว wrapper (ท่าเดียวกับที่ v8.4 ทับ
   `superBoss.cut`/`apex.doomMax` ของ v8.2) **ไม่ปล่อยให้ audit field ค้างค่าเก่า** —
   นี่คือจุดที่ชุดเทสต์ `test_polarized_class.js` จับได้จริงระหว่างพัฒนา (ดูหัวข้อทดสอบ)

## 2) โบนัสเฉพาะคลาส 4 สาย — ปลดล็อกเมื่อสกิลฐานครบ Lv.5 ทุกช่อง

`baV912ClassMaxed(src, classId)` เป็นตัวอ่านกลางจุดเดียว (สกิลทั้ง 4 ช่องของสายนั้น
ต้องเป็น `BA_PL_LV_MAX` (5) ครบทุกช่อง) ทุกโบนัสด้านล่างเช็กผ่านตัวนี้ตัวเดียว

| สาย | โบนัส | ห่อที่ไหน | ท่าที่ใช้ |
|---|---|---|---|
| ⚔️ Blade (`slayer`) | ATK +5% | `hunterAtk()` | คูณกันเป็นทอด **กดด้วย `BA_LIVE` เสมอ** |
| ⚔️ Blade (`slayer`) | Skill DMG +10% | `baPlUltAmt(g)` | คูณตัวคูณ/เปอร์เซ็นต์ท่าไม้ตายเฉพาะ `classId==='slayer'` |
| 🛡️ Guardian | Max HP +300 | `maxHpFor()` | บวกกันเป็นก้อนตรง ๆ |
| 🛡️ Guardian | Shield (ลดดาเมจ) +15% | `baPlGuardCut(base)` | ลดผลลัพธ์เดิมอีกชั้น (ครอบทั้ง `wrongDamage`/`baHurtHero` โดยอัตโนมัติเพราะทั้งคู่เรียกชื่อนี้แบบ live binding) |
| 🗡️ Assassin | Crit +5 จุด% | `critChance()` | บวกกันเป็นก้อน **เฉพาะตอน `out > 0`** (เคารพศูนย์แข็งของสถานะที่ห้ามคริต) |
| 🗡️ Assassin | เวลาตอบ +0.5 วิ | `questionMs()` | บวกกันเป็นก้อนตรง ๆ |
| ✨ Priest | Max MP +50 | `maxMpOf(src)` | บวกกันเป็นก้อน — **อ่าน `src` ก่อนเสมอ** (สัญญาเดิมของ v8.9 รับพารามิเตอร์ optional) แล้วค่อยตกไปที่ `baG()` |
| ✨ Priest | MP Regen +10% | `resolveAnswer()` | snapshot MP ก่อน/หลัง (กับดักข้อ 19) แล้วต่อยอด 10% ของส่วนที่ฟื้นได้จริง |

### จุดที่ต้องระวังเป็นพิเศษ (ยืนยันด้วยการอ่านซอร์สจริงก่อนเขียน ไม่ได้เดา)

- **`hunterAtk()` คำนวณทั้งดาเมจที่ตีออกและ HP มอนสเตอร์** (`monsterHpFor` อิงจาก
  ค่านี้) โบนัส ATK ของ Blade จึงต้องกดด้วย `if (typeof BA_LIVE !== 'undefined' &&
  BA_LIVE)` เสมอ ไม่งั้นเลือดมอนสเตอร์จะพองตามสัดส่วนเดียวกัน กลายเป็นโบนัสที่ไม่มีผล
  สุทธิ (บทเรียนเดียวกับ "แกนพิฆาต" ของ v7.9 และ Blade ATK ของแพตช์ v8.8/v9.1)
- **`baPlGuardCut(base)` ไม่ใช่ `hdBaseDamage`** — ฟังก์ชันนี้เป็น `function`
  declaration ธรรมดา (ไม่ใช่ `const`) ที่ถูกเรียกจาก wrap ของทั้ง `wrongDamage` และ
  `baHurtHero` ด้วยชื่อตรง ๆ (live top-level binding resolution) การ reassign
  `baPlGuardCut = function(){...}` ที่ระดับบนสุดจึงมีผลกับทั้งสองเส้นทางดาเมจพร้อมกัน
  โดยไม่ต้องแตะ `wrongDamage`/`baHurtHero` เลยแม้แต่บรรทัดเดียว
- **`critChance()` ต้องเคารพ "ศูนย์แข็ง"** — สถานะบางอย่างในเกม (เช่น debuff ที่ห้าม
  คริตทุกทาง) กด `critChance()` ให้คืน 0 ตรง ๆ โบนัส Assassin ต้องไม่บวกทับกรณีนั้น
  (`if (out > 0) {...}`) มิฉะนั้นจะเป็นช่องโหว่ที่ปลดสถานะนั้นทิ้งโดยไม่ตั้งใจ
- **`maxMpOf(src)` รับพารามิเตอร์ optional** — ยืนยันสัญญาจริงของ v8.9 ก่อนเขียน:
  `const g = src || cxSrc();` ภายใน ผู้เรียกบางจุด (เช่นเครื่องมือ GM ที่ตรวจ MP ของ
  บัญชีอื่น) ส่ง `src` ตรง ๆ — wrapper ใหม่จึงต้องอ่าน `src` ก่อนเสมอ ไม่ใช่เรียก
  `baG()` (ผู้เล่นปัจจุบัน) ทันที ไม่งั้นจะได้โบนัสของคนละบัญชี
- **MP Regen +10% ต้อง snapshot ก่อน/หลัง ไม่คำนวณสูตรซ้ำ** (กับดักข้อ 19 ของ
  CLAUDE.md) — `resolveAnswer()` ของ v4.0 ฟื้น MP ผ่านหลายเส้นทาง (ตอบถูก ·
  โบนัสสตรีค · ฯลฯ) การจดค่า `g.mp` ก่อนเรียก แล้ววัดส่วนต่างจริงหลังเรียกจบ คือทาง
  เดียวที่ถูกต้อง — สอดคล้องกับ precedent ที่มีอยู่แล้วในไฟล์ 3 จุด (แถบสกิล ·
  ตัวนับ ⚡ ของเควส · wrapper `resolveAnswer` ชั้นนอกสุดของ Progression Gates)
- **`baPlUltAmt(g)` มีสองความหมายตามคลาส** — สายโจมตี (assassin/slayer) ใช้เป็น
  ตัวคูณดาเมจตรง (`cdHitMonster(Math.round(hunterAtk() * amt), ...)`) สายฮีล
  (guardian/priest) ใช้เป็นเปอร์เซ็นต์ของ Max HP (`Math.round(g.maxHp * amt / 100)`)
  โบนัส "Skill DMG +10%" ของ Blade จึงต้องเช็ก `classId === 'slayer'` ก่อนคูณ
  ไม่งั้นจะไปเพิ่มยอดฮีลของ Guardian/Priest โดยไม่ได้ตั้งใจ

## 3) Omni-Class Master — ฉายา + คูลดาวน์ทุกช่อง −1 (ขั้นต่ำ 1)

`baV912AllMaxed(src)` เช็กว่าทั้ง 4 สายฐาน (จาก `BA_PL_CLASSES`) ครบ `baV912ClassMaxed`
พร้อมกันหมด — ปลดฉายา `omniclassmaster` / "👑 ปราชญ์ผู้รู้แจ้งสรรพศาสตร์" ผ่าน
`TITLES.push()` แบบกันซ้ำด้วย `!TITLES.some(t => t.key === KEY)` (precedent เดียวกับ
v9.6's `allknowing` title — คนละ key คนละเงื่อนไข ไม่ชนกัน)

ห่อ `baV91Activate(i, g, r)` เพิ่มอีกชั้น (ซ้อนทับ wrap ของ v9.6 ที่มีอยู่แล้ว — ทั้งคู่
ทำงานอิสระต่อกัน บวกสะสมได้) เฉพาะตอนที่การเรียกของเดิม**สำเร็จจริง** (`out === true`)
และมีฉายานี้อยู่ใน `g.titles` แล้ว: `BA_V91_CD[i] = Math.max(1, BA_V91_CD[i] - 1)`
พร้อมเช็ก `(BA_V91_CD[i]||0) > 1` ก่อนเสมอ (กันลดตอนเหลือ ≤1 อยู่แล้ว)

## 4) การซิงก์ 3 ทาง — `account.skills` ผ่าน Wordbank Sync (v9.9)

สองในสามทางได้ฟรีจากการใช้ฟิลด์เดิม (ดูหัวข้อ "1 · Schema" ด้านบน) เหลือทางเดียวที่
ต้องเขียนเพิ่ม: **v9.9's `wbPush`/`wbRestore`** จงใจแคบแค่ `wordStats` + ตัวนับสะสม
โมโนโทนิกคงที่ (`WB_MONO_KEYS`) — ไม่รู้จักโครงสร้างซ้อน (nested per-class array) เลย

- ห่อ `wbPayload(g)` เติม `p.skills = g.skills` เข้าไปในก้อนที่ส่งขึ้นคลาวด์
- เขียนฟังก์ชันใหม่ `baV912MergeSkills(dstG, srcSkills)` — **one-way max-merge**
  ต่อสกิลต่อช่อง (`if (sv > dv) { dstG.skills[cid][i] = sv; n++; }`) ท่าเดียวกับหลัก
  ความปลอดภัยของ `wbMergeMono` เดิม (ไม่มีทางที่ merge จะทำให้ระดับสกิลถอยหลัง)
  แต่เขียนขึ้นใหม่ทั้งฟังก์ชันเพราะ `wbMergeMono` เดิมรองรับแค่ field ตัวเลขแบนราบ
  ไม่รองรับ nested object-of-arrays
- ห่อ `wbRestore(user)` ดึง doc จากคลาวด์แยกต่างหาก (เฉพาะตอน `fbOn()`) แล้วเรียก
  `baV912MergeSkills` **หลัง** ของเดิมทำงานจบ พร้อมเช็ก `CURRENT_USER === user`
  ก่อนเขียนทับ (กันข้อมูลข้ามบัญชีตอนแผง GM ดึงข้อมูลนักเรียนคนอื่น)

## 5) UI กะทัดรัด "⚡ ปลุกพลังสกิล" ใน `#baPlBoard`

ห่อ `baPlRender()` แทรก `#baV912Box` เข้าไปท้าย `#baPlBody` ทุกครั้งที่วาดแผงโปรไฟล์
— **กันแทรกซ้ำด้วยการลบใบเก่าก่อนเสมอ** (`const old = document.getElementById
('baV912Box'); if (old) old.remove();` — กับดักข้อ 2 ของ CLAUDE.md) แสดง 4 แถวสถานะ
(✓ ปลุกพลังแล้ว / ยังไม่ครบ) ต่อสาย บวกบรรทัดสรุปฉายา Omni-Class

CSS ใหม่ (`#baV912Style` กันแทรกซ้ำด้วยตัวเดียวกัน) ใช้ `transform:translateZ(0)`
ตามที่สเปกขอ (บังคับ layer ของตัวเอง กันการรีเพนต์ลามไปกระทบเลย์เอาต์รอบข้าง)
— **CLS = 0 ยืนยันแล้วจาก `test_polarized_class.js` บล็อก 10** (ความสูงการ์ดโจทย์
เท่าเดิมทุกความกว้างจอ ไม่ล้นแนวนอน)

## ผลการทดสอบ

**สามชุดที่สเปกสั่งไว้ตรง ๆ**

| ชุด | ผล |
|---|---|
| `node build_minify.js --check` | **ผ่าน** — ไฟล์แจก 1,973,834 ไบต์ (เหลือถึงเพดาน 26,166 ไบต์) |
| `test_cloud_sync.js` | **104/0** |
| `test_monarch.js` | **85/0** |
| `test_polarized_class.js` | **131 ผ่าน / 13 ตก** |

### เกี่ยวกับ 13 เคสที่ยังตกใน `test_polarized_class.js` — ไม่ใช่ regression ของแพตช์นี้

ยืนยันด้วยการรันชุดเทสต์เดียวกันกับไฟล์แจก **ก่อน** แพตช์นี้ (`git show
HEAD:hanzi_hunter_tower_v3_1_intro.html`) ได้ **131 ผ่าน / 13 ตกเท่ากันเป๊ะ** —
ทั้ง 13 เคสเป็นบั๊กที่มีอยู่แล้วในไฟล์ก่อนแพตช์นี้ ไม่เกี่ยวกับงานนี้เลย แบ่งเป็นสองกลุ่ม:

1. **ตัวนับ/สายอาชีพเปลี่ยนไม่ขึ้นตอนสลับคลาส** (8 เคส — `baPlSwitch`/counter/VIT
   recompute ไม่ตรงตามคาด) — บั๊กที่มีอยู่แล้วในระบบสลับคลาสของ v8.5 หรือชั้นที่ใหม่กว่า
   ไม่ได้อยู่ในขอบเขตของสเปกแพตช์นี้ (ซึ่งเน้นที่บันไดราคา/โบนัสของสกิล ไม่ใช่กลไก
   การสลับคลาส) และไม่มีโค้ดในแพตช์นี้แตะ `baPlSwitch()` หรือเส้นทางสลับคลาสเลย
2. **ปุ่ม "คลังการ์ดวิญญาณ (v9.7.1)" ไม่มีอยู่จริง** (5 เคส) — ยืนยันแล้วว่าเป็นผล
   จากการที่ CLAUDE.md บรรยายถึงแพตช์ v9.7.1/v9.7.2 ที่ไม่เคยถูกฝังลงไฟล์จริง
   (ดูหัวข้อ "หมายเหตุความถูกต้องของเอกสาร" ด้านบน)

**เคสที่ถูกพลิกโดยตั้งใจในแพตช์นี้** (precedent: v7.4 · v7.8 · v7.9 · v8.1-v8.9 พลิกเคส
ของชุดเทสต์เดิมทุกครั้งที่แพตช์ใหม่ตั้งใจเปลี่ยนบาลานซ์ที่ชุดเก่ายึดไว้)

| เคยยืนยัน (บันไดราคาเดิมของ v8.5) | ตอนนี้ (v9.12) |
|---|---|
| ช่อง 1/2 ราคา `[0,40,80,160,300]` | **`[0,50,120,250,500]`** |
| ช่อง 3 ราคา `[0,60,120,220,450]` | **`[0,50,120,250,500]`** |
| ช่อง 4 ราคา `[0,100,200,350,700]` | **`[0,50,120,250,500]`** |
| อัปเต็มทั้งสาย = 3,360 💎 | **3,680 💎** |
| หัก 💎 40 พอดี (ซื้อ Lv1→2) | **หัก 💎 50 พอดี** |
| ยอด 💎 ที่จ่ายทั้งหมด = 3,360 | **3,680** |

**ระหว่างพัฒนายังพบว่า `baBattleAudit().polarized.maxout` ค้างค่าเก่า (3,360) แม้จะ
mutate `BA_PL_COST` แล้ว** — เพราะ `BA_PL_MAXOUT` เป็นค่าที่คำนวณครั้งเดียวตอนแยกไฟล์
(ก่อนโค้ดของแพตช์นี้ทำงานในลำดับการโหลด) แก้ด้วยการทับค่าใน `baBattleAudit` wrapper
ของแพตช์นี้เอง (`r.polarized.maxout = BA_V912_MAXOUT`) — แก้เสร็จแล้วรัน
`test_polarized_class.js` ซ้ำได้ 131/13 เท่าเดิม ไม่มีเคสค้างจากปัญหานี้อีก

### ชุดเทสต์ที่เกี่ยวข้อง (ฟังก์ชันที่แพตช์นี้ห่อทับถูกใช้ร่วมกันหลายชั้น)

รันเพิ่มเป็นการตรวจสอบวงกว้าง เพราะแพตช์นี้ห่อ `hunterAtk` · `maxHpFor` · `critChance`
· `questionMs` · `maxMpOf` · `resolveAnswer` · `baBattleAudit` ซึ่งเป็นทางผ่านของ
หลายชั้นทั่วไฟล์ — **โบนัสทั้งหมดถูกกันไว้ด้วย `baV912ClassMaxed()` ซึ่งต้องการสกิล
ครบ Lv.5 ทั้ง 4 ช่องของสายนั้น** (ค่าเริ่มต้นของบัญชีใหม่คือ `[1,1,1,1]` ทุกสาย) ทำให้
ทุก wrapper เป็น **pure pass-through** สำหรับบัญชีที่ยังไม่ปลุกพลังสกิล — ฟิกซ์เจอร์
ทดสอบของชั้นอื่นแทบทั้งหมดจึงไม่มีทางชนกับโบนัสใหม่นี้โดยบังเอิญ

| ชุด | ผล |
|---|---|
| `verify_arena.js` | **144/0** — เรขาคณิตสนามรบ (ไม่แตะโดยแพตช์นี้) |
| `verify_monsters.js` | **80/0** |
| `test_lv_scaling.js` | **81/0** — สูตร `hunterAtk`/`maxHpFor` อื่น ๆ ที่ต่อกันเป็นชั้น ๆ |
| `test_codex_cores.js` | **93/0** |
| `test_skill_dispatch.js` | **94/0** — ระบบสกิล/ท่าโจมตีของสายอาชีพ |
| `test_skill_anim.js` | **170/3** — เท่ากับ baseline เป๊ะ (ยืนยันด้วย `git show HEAD:...` รันเทียบ ได้ 170/3 ชื่อเคสตรงกันทั้ง 3 ใบ — pre-existing ไม่เกี่ยวกับแพตช์นี้) |
| `test_shop_economy.js` | **139/0** — MP/ร้านค้า |

ทุกชุดผ่านครบ **ไม่มี regression สักเคสจากแพตช์นี้** (ชุดเดียวที่มีเคสตกคือ
`test_skill_anim.js` ซึ่งตกเท่ากันเป๊ะทั้งก่อนและหลังแพตช์นี้ — เป็นบั๊กที่มีอยู่แล้วใน
ระบบท่าโจมตี/สไปรต์ของสายอาชีพที่ไม่มีภาพครบ ไม่เกี่ยวกับบันไดราคา/โบนัสของแพตช์นี้เลย)

## สิ่งที่แพตช์นี้ไม่ได้ทำ

**ไม่แตะระบบ Farm Cap 450 💎/วัน ของ v9.10 (Diamond Economy Governor) เลยสักบรรทัด**
— ตรวจสอบด้วย grep ว่าไม่มีโค้ดในแพตช์นี้อ้างถึงกลไกการได้ 💎 จากเกมเพลย์เลยแม้แต่จุดเดียว
(โบนัสทั้งหมดเป็นเรื่องสถิติ/คูลดาวน์ ไม่ใช่เรื่องรายได้เพชร) เพดาน 450/วันจึงยังคุมอยู่
เหมือนเดิมทุกประการ — สเปกข้อ 1 ("Farm cap: 450 💎/day ceiling... bypass GM grants")
เป็นข้อกำหนด "อย่าไปยุ่ง" ไม่ใช่ข้อกำหนด "ต้องสร้างใหม่" จึงไม่มีโค้ดใหม่สำหรับเรื่องนี้

**ไม่เพิ่มฟิลด์บัญชีใหม่เลย** (`account.skills` เป็นฟิลด์เดิมของ v8.5) **ไม่เพิ่ม toast
· ไม่เพิ่ม gate · ไม่ลบปุ่มสักใบ · ไม่แทรกคอลัมน์ในตารางไหน · ไม่เพิ่มภาพหรือเสียงสักไบต์
· ไม่แตะ `Math.random` สักครั้ง** ไม่มีตรารุ่นระดับบัญชี และไม่ได้ห่อ `migrateAccount`
เพราะ `baPlEnsure()` ของ v8.5 เติม default ให้ `account.skills` ครบทุกคลาสอยู่แล้ว
